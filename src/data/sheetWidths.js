/**
 * ============================================================================
 * **紙の幅**(集中モード・レッスン表示・セッションの記録)
 *
 * もとは `LessonView.jsx` の中だけにあった。**セッションの記録でも
 * 同じ幅を選ぶ**ことになったので(第5.267節)、1か所に出した ——
 * **呼び名も値も2か所に書かない**(CLAUDE.md)。
 *
 * 「画面に合わせる」は**割合ではない。** 使える幅をそのまま使う
 * (紙の左右の余白 24px だけを残す)。%で足していくと、
 * どの端末でも「ちょうどいっぱい」にはならない。
 * ============================================================================
 */

/**
 * **教材の紙の幅**(これまでどおり)。
 * **一覧を減らさない。並べ替えも減らすに当たる**(共通ルール)。
 */
export const SHEET_WIDTHS = [
  { id: 'w100', label: '100%' },
  { id: 'w110', label: '110%' },
  { id: 'w120', label: '120%' },
  { id: 'w130', label: '130%' },
  { id: 'w140', label: '140%' },
  { id: 'w150', label: '150%' },
  { id: 'wfit', label: '画面いっぱい' },
]

/**
 * **セッションの記録の幅**(第5.267節・2026-09-26 利用者の指定)。
 *
 *   > セッション中にセッションの記録をトレーナーが画面共有しているときに、
 *   > これを画面いっぱい、または半分などに大きくして使用できるように
 *   > してほしいです。
 *
 * 教材の幅に「**半分**」を足しただけのもの。
 * **教材の側には足していない** —— 言われたのは記録の話である
 * (**言われた場所だけを直す**・CLAUDE.md)。
 *
 * **書き写さずに、`SHEET_WIDTHS` を広げる。** こうしておけば、
 * 幅を1つ足した日に、どちらにも同じものが出る。
 */
export const NOTE_WIDTHS = [
  { id: 'w50', label: '半分' },
  ...SHEET_WIDTHS,
]

/** 知らない値は、いちばん素直なものに落とす(**既定は 100%**) */
export const widthOf = (id, list = SHEET_WIDTHS) => (
  list.some((w) => w.id === id) ? id : 'w100'
)
