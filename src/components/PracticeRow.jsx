/**
 * **練習の1行の見出し**(第5.239節・2026-09 利用者の指定)。
 *
 * ════════════════════════════════════════════════════════════════
 *   > それぞれのステップでやることは違うけども、
 *   > **必要なアプリの挙動はほとんど同じ**だったりします。
 *
 * 【数えたこと】
 *
 *   6Steps の3つの画面(①ディクテーション / ②スラッシュリーディング /
 *   ④⑥1文ずつ)は、**行の見出しをまったく同じ形で書き写していた。**
 *
 *     番号 → 話す人の名前 → 右に道具の並び
 *
 *   中に並ぶ道具だけが違う。**入れ物は1つでよい。**
 *   置く場所の数だけ食い違う(CLAUDE.md)。
 *
 * 【なぜ右上にまとめてあるか】(2026-08 の指摘・第5.31.3節)
 *
 *   Listen / 解答を見る を本文の**下**に置いていたので、
 *   本文と解答がそのぶん下へ押し下げられていた。
 *   **話す人の名前と反対側(右上)**にまとめてある。
 *
 * 【`SoundPractice` は、まだここを使っていない】
 *
 *   あちらは音の練習(6Steps ではない)。**言われた場所だけを直す**
 *   (CLAUDE.md)ので、今回は手をつけていない。
 * ════════════════════════════════════════════════════════════════
 *
 * @param no      画面に出す番号(**ぜんぶの中での番号**)
 * @param speaker 話す人。**無ければ出さない**(記事には無い)
 * @param children 右に並べる道具
 */
export default function PracticeRow({ no, speaker = '', children }) {
  return (
    <div className="row-head">
      {/* **番号を出す**(2026-09 利用者の指定
          「どのページでも段落番号とか全て入れてください」)。
          紙とレッスンで「2番のところ」と同じ場所を指せる */}
      <span className="dictation-no">{no}</span>
      {speaker && <span className="passage-speaker" lang="en">{speaker}</span>}
      <span className="row-tools">{children}</span>
    </div>
  )
}
