# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**全プロジェクト共通のルールは [`.claude/rules/common.md`](.claude/rules/common.md) にある。**
言語・事実の扱い・検証の作法・記録の残し方・鍵の扱い・Git の決まりはそちら。
このファイルには、**このプロジェクト固有のこと**だけを書く。

作業ブランチ: `claude/project-spec-document-k5wmwy`

## 呼び方(利用者の会社の言葉に合わせる)

| 画面・文書に書く言葉 | データベース上の値 |
|---|---|
| **ゲスト** | `profiles.role = 'learner'` |
| **トレーナー** | `profiles.role = 'trainer'` |
| **管理者** | `profiles.role = 'owner'` — 全体の集計だけを見る。トレーナーの権限も兼ねる |

**「講師」「生徒」とは書かない。** 利用者のスクールでは
**トレーナー**(指導する人)/ **ゲスト**(教わる人)と呼ぶ(2026-08 に統一)。
**「配信する」とも書かない。**不特定多数へ発信する響きがあるため、
**「共有する」**と書く。実際、担当ゲストにだけ届く仕組みである。
画面に出すボタンは**「セッションで使う」**(2026-08 利用者の指定)。
コードの中の名前(`LessonView` など)は変えていない。
データベースの値(`learner` / `trainer` / `owner`)は英語のまま変えない。
`is_admin()` は `is_trainer()` の別名として残してあるだけ。新しく書くコードでは
`is_trainer()` / `is_owner()` を使う。
利用者に見えるところと、システム内部の識別子を混同しないこと。

## コマンド

| 目的 | コマンド |
|---|---|
| 開発サーバー | `npm run dev` |
| ビルド | `npm run build` |
| **書き間違いの検査** | `npm run lint` |
| GitHub Pages 用ビルド | `npm run build:pages` |
| 1ファイル版(ダウンロードして開ける版) | `npm run build:single` |
| **DB マイグレーションと RLS の検証** | `npm run test:db` |
| **スラッシュリーディングの区切りの検証** | `npm run test:chunk` |
| **声ごとの音量そろえの検証** | `npm run test:audio` |
| **音声を1本にまとめる検証** | `npm run test:mp3` |
| **会話の間(食い気味かどうか)の検証** | `npm run test:gap` |
| **止めた場所からの再生の検証** | `npm run test:play` |
| **紙の色(集中モード)の検証** | `npm run test:paper` |
| **帯の持ちものの検証** | `npm run test:bar` |
| **声と役の性別の検証** | `npm run test:voice` |
| **英文の「型」の見分けの検証** | `npm run test:frame` |
| **66 の型の Quick Response の検証** | `npm run test:shift` |
| **読み上げ用の英文への書き換えの検証** | `npm run test:speak` |
| **意味の近さで重複を弾く窓口の検証** | `npm run test:similar` |
| お手本音声の生成(Azure の鍵が必要) | `npm run audio` |
| **英文の「型」の資料を PDF にする**(トレーナー向け) | `node scripts/make-frames-pdf.mjs` |
| **英文の「型」のプレゼン資料を作る**(素人向け) | `npm run slides` |

自動テストは `npm run test:db` / `test:chunk` / `test:audio` / `test:mp3` /
`test:gap` / `test:play` / `test:paper` / `test:bar` / `test:voice` /
`test:frame` / `test:shift` / `test:speak` / `test:similar` の13個。
ユニットテストの枠組みは無く、どれも素の node / psql で走る。
**`test:similar` は、窓口(Deno)を素の node で走らせる**(第5.247節)——
Supabase も Deno も AI も偽物に差し替え、算段だけを確かめる。
**この環境では窓口を1度も動かせない**ので、走らせられる形を作った。
**`test:audio` は耳の代わりである**(こちらには音が聞こえない)。
UI を変えたら **`npm run lint` と `npm run build` の両方**を通し、
必要なら Playwright で実際に触って確かめる。

### `npm run build` が通っても安心してはいけない

**import を書き忘れても Vite のビルドは成功する。** 開いた瞬間に落ちる。
実際に `App.jsx` で `TrainerMaterials` の import が抜けたままビルドが通り、
画面を開くまで分からない状態になった(2026-08)。
`npm run lint` は、この「存在しない名前を使っている」を機械的に見つける。

**2つめの形がある(2026-09・第5.220節)。フックを `if (loading) return` の
後ろに足すと、読み込み中と読み込み後でフックの数が変わり、
画面がまるごと真っ白になる。** `lint` も `build` も通っていた。
**フックは、早い return より前に置く。**
`npm run lint` に `react-hooks/rules-of-hooks` を足して止めてある。
**名指しで外しているファイルは1つも無い**(`LessonView.jsx` の 10 か所は
第5.223節で直した)。`npm run test:play` が「誰も外していないか」を見張る ——
**赤くなったファイルを1行足して外せば緑に戻せてしまう**ためである。

**3つめの形がある(2026-09・第5.232節)。`materialKinds.js` に足した名前を、
`materials.js` の「出し直し」に足し忘れた。** 画面は `materials.js` から
取り込んでいるので**その名前は存在しない**のに、`lint` も `build` も通った。
`npm run test:play` が、画面が `materials.js` から取り込む名前を1つずつ、
**あちらが本当に出しているか**を見る。

**出したその日に、本物の入り口から1回開く。**
`npm run test:bar` が、メニューの行き先を1つずつ開いて落ちないかを見る
(**骨組みは部品しか描いていない** —— 画面の中身は通っていなかった)。

### ログインが要る画面を手元で確かめる方法

この環境からは Supabase に届かないため、ログインを通せない。
`.env` を一時的に退避すると Supabase 未設定の状態になり、
ログインなしで中の画面を開ける。**確認後に必ず戻すこと。**
トレーナー向けの画面など、利用者情報が要るものは、
一時的に仮の値を差し込んで見た目だけ確かめ、すぐ戻す。

### `npm run test:db` は SQL を触ったら必ず通す

`supabase/migrations/*.sql` を変更したら、Supabase に貼る前に必ず実行する。
手元の PostgreSQL(`service postgresql start` が要る場合がある)にまっさらな DB を作り、
マイグレーションを **2回** 実行してから、3つの検証ファイルを走らせる。

- `supabase/test/supabase_stub.sql` — `auth.users` / `auth.uid()` / `storage` /
  `authenticated` ロール / 既定の権限付与という、Supabase 環境の最小の再現
- `supabase/test/rls_test.sql` — トレーナー1人・ゲスト2人を作り、
  「誰に何が見え、何ができないか」を確かめる。**拒否されるべき操作が通ったら失敗する**
- `supabase/test/material_shape_test.sql` — 実物のドリルがそのまま入るか
- `supabase/test/dedup_test.sql` — 同じ英文が二度出ないか
- `supabase/test/vocab_test.sql` — 意味の控え・箱・句。**間隔の日数そのもの**も見る
- `supabase/test/verify_migrations.sql` — 利用者が Supabase に貼る確認 SQL。
  **これ自体が古びる**ので、ここで毎回まわして「まだです」が無いことを確かめる

#### 利用者が見る `check.sql` と、まとめた1つも、毎回まわす

**移行を足したのに `supabase/apply/check.sql` に項目を足し忘れると、
本当は足りないのに「全部 ✅」と出る。** いちばん悪い壊れ方である
(実際 0042〜0045 のあいだ、そうなっていた・2026-09)。

利用者にとっても、貼るファイルが増え続けるのは分かりにくい。
**「まだです」の行に書いてあるファイルを1つ貼れば済む**形にしておく。

- **まとめた1つ**は `supabase/apply/pending_matome.sql`。
  0041 以降を順に並べたもので、**何度貼っても安全**である
- `scripts/test-migration.sh` が2つ見張っている。
  ①そろった DB で `check.sql` が全部 ✅ になるか
  ②**0040 までの DB にまとめた1つを貼って**、全部 ✅ になるか
  (まとめたファイルが、本当に 0041 以降を全部含んでいるか)
- **移行を足したら、`check.sql` の行と `pending_matome.sql` の両方に足す。**
  足すまで赤いままなので、必ず1回は自分の目で数えることになる

この検証は実際に重大な穴(ゲストが自分をトレーナーに昇格できる)を見つけた実績がある。
省略しないこと。

#### 「頭から全部もう一度」も試す

`scripts/test-migration.sh` は **1ファイルを2回**だけでなく、
**0001 から最後までを、もう一度頭から**流す。利用者は
`supabase/apply/*.sql` を**まとめて貼る**ので、実際に起きるのはこちらである。

これを見ていなかったために、次の穴を2つ見落としていた(2026-08)。

- 0015 の `review_words` が、0018 で列を増やしたあとに再実行されると
  `cannot change return type of existing function` で止まる
- 0002 の `school_summary` も、0004 のあとで同じことが起きる

**返す列を変える関数は、先に `drop function if exists` を置く。**
あわせて「同じ名前の関数が2つ以上残っていないか」も見る。
残ると PostgREST が「どちらか分からない」と断り、画面から呼べなくなる。

#### 利用者に渡す「貼る SQL」も、そろった DB に貼ってみる

**順に全部流すだけでは足りない。** 利用者は
**そろっている DB に、あとから1つだけ貼る**ことがある。

実際、0027 は `review_words()` を **drop せずに**作り直していた。
書いた時点では返す列が同じだったので要らなかったが、
**あとから 0028 が返す列を増やした。** そのため 0027 だけを貼り直すと
`cannot change return type of existing function` で止まる(2026-09)。
全部を順に流す検証では、手前の 0024 が drop してくれるので**気づけない。**

**関数を作り直すファイルは、返す列を変えていなくても drop を置く。**
あとで誰かが列を足すかもしれない。
`scripts/test-migration.sh` が、`supabase/apply/pending_*.sql` を
**そろった DB の複製に1つずつ貼って**見張っている。

#### **空の表に貼るのは、試したことにならない**(2026-09 実機)

利用者が `pending_matome.sql` をもう一度貼ったところ、こう出て止まった。

    ERROR: 23514: check constraint "materials_kind_check"
    of relation "materials" is violated by some row

**そのとき検証は全部緑だった。** 理由は1つで、
**検証の DB には行が1つも無かった**からである。
`check (… in (…))` を書き直す SQL は、**すでにある行と食い違ったときにだけ**
止まる。空の表なら、どんなに狭い一覧でも通ってしまう。

中身はこうなっていた。まとめた1つの中で `materials_kind_check` が**2回**
書き直されており、**前のほうが狭かった。**

| どこ | 一覧 |
|---|---|
| 0043 の段 | pattern / reading / dialogue / meeting / speech / word / phrase / passage |
| 0047 の段 | 上に **`vocab`** を足したもの |

`kind = 'vocab'` の教材がある DB では、前の段で止まる。
**「何度貼っても安全である」という約束を、こちらで破っていた。**

- **値の一覧を書き直すファイルは、どれも同じ一覧を書く**
  (いちばん新しいものにそろえる)。
  「関数を作り直すファイルは、返す列を変えていなくても drop を置く」と
  **まったく同じ考え方**である。あとで誰かが値を足すかもしれない
- **まっさらな DB に順に流すぶんには、先の段で広い一覧を許していても
  何も起きない**(その値を作る仕組みが、まだ無い)。
  狭いままにしておく利点は1つも無く、**貼り直せなくなる害だけ**が残る
- 見張りは2つ。**片方だけでは足りない**
  - `scripts/check-constraint-lists.mjs` … 書いてある**文字**を突き合わせる。
    **まだ誰も使っていない値**の食い違いも見つかる
  - `scripts/test-migration.sh` … `supabase/test/seed_rows.sql` で
    **許されている値を1つずつ実際に入れてから**、①移行を頭から全部
    ②貼る SQL を1つずつ、もう一度流す
- **一覧は `seed_rows.sql` に書かない。** 表の制約(`pg_get_constraintdef`)
  から読み取る。そうすれば、種類を足したときも**書き足さずに**検証が付いてくる
- **探し方も緩くする。** はじめ `add constraint <名前> check` を
  文字列そのままで探していたので、**名前と `check` のあいだで改行している
  0007 を見逃していた**(行のある DB に流して、初めて赤くなった)
- 実際に `vocab` を1つ消して、**2つとも赤くなる**ことを確かめてある

**利用者の DB は無事だった。** Supabase の SQL Editor は失敗した貼り付けを
まるごと巻き戻すので、`check.sql` は 29 件すべて ✅ のままだった
(19・25・27 番の行が、制約そのものを見ている)。

#### 検証ファイルは1つずつ別の DB で走らせる

`material_shape_test.sql` が profiles を1つ作るため、同じ DB で続けて走らせると
`rls_test.sql` の数え上げがずれて落ちた(2026-08)。
`scripts/test-migration.sh` は、移行を流した DB を `template` として複製し、
**検証ファイルごとにまっさらな複製の上で走らせる。**
検証どうしが干渉すると、壊れていないものが赤くなり、
**本当に壊れているものを見落とす。**

#### 検証の出力をパイプに直接つながない

`psql ... | sed | grep` と書くと、`psql` が失敗しても最後の `grep` の結果で
成否が決まり、赤くならない。**出力を一度変数で受けてから流す。**

## このアプリが解く問題

パーソナル英語スクールの **レッスンと宿題の循環** を支える道具である。

```
週2回のレッスン → 宿題を出す → ゲストが取り組む
  → 次のレッスンで達成度を確認し、弱点を指摘する
  → その弱点に対応した宿題を出す   ← ここが要
```

**中心機能は学習記録でも発音採点でもなく、「トレーナーが弱点から教材を作り、
指定したゲストに配信する仕組み」である。** 弱点を指摘した直後に教材を出せないと、
練習量が確保できず克服につながらない。他の機能はこの循環の補助と位置づける。

対象はスクールの既存ゲストのみ(クローズド)。一般公開はしない。
詳細は `docs/PROJECT_SPEC.md`。**設計判断はすべてこの仕様書に記録すること。**

### トレーナーに渡す資料は `docs/` に、**作り方ごと**置く

2026-09 利用者の指定「①〜⑤の方法論をレクチャー風にまとめて、PDF に」。

`docs/sentence-frames.html`(英文の「型」)。**PDF はそこから作る**
(`node scripts/make-frames-pdf.mjs` → `docs/sentence-frames.pdf`)。

**資料は2つある。役目が違うので、2つとも要る**(2026-09 利用者の指定
「66 の型を素人の人でもわかりやすく説明したプレゼン資料に」)。

| | 誰に | 何を | 作り方 |
|---|---|---|---|
| `sentence-frames` | **トレーナー** | なぜその型が要るか・教える順番まで | 手書きの HTML |
| `frames-slides` | **素人・ゲスト・初めての人** | **1枚1つ**。横向き21枚 | **`npm run slides`(データから組む)** |

**プレゼンのほうは、66 型を書き写していない** —— `sentenceFrames.js` から
組み立てる。手で書いてあるのは**素人向けの言い換え**だけで、
それは `src/data/frameSlides.js` に置く(道具に置くと、
**読み込むだけで PDF が刷られて検証が汚れる**・実際にそうなった)。

- **PDF だけを渡して終わりにしない。** それだと直せる人がいなくなる。
  **元は HTML で置き、作り方をコマンドにしておく**
- 刷るのは **Chromium**(`page.pdf`)。`reportlab` を使わないのは、
  日本語のフォントを自分で組み込むことになり、表の組みも手で書くことに
  なるためである。**画面と同じ仕組みで刷れば、見たとおりに出る**
- ページ番号は **`footerTemplate`** にだけ書ける
  (`@page` の余白の箱と同じ話・CLAUDE.md「単語帳の紙」)

### 設計を縛っている最大の制約

**いま: トレーナー20人・1人あたりゲスト25人(ゲスト500人)。
伸ばしたあと: トレーナー約50人・ゲスト約1,500人**(第5.6.0節)。
**1,500人は目標であって、いまの数ではない。** 費用を見積もるときに
取り違えると、実態の3倍で怖がることになる。

ゲスト1人あたり**月4回**のレッスン。いまは**月2,000レッスン**。
レッスンごとに1本ずつ新規に作ると**月2,000本**になり、
これは第5.6.2節が見積もったライブラリ全体(333件)の6倍を毎月作る計算になる。
**「配る数」と「新しく作る数」を混同しない。** 費用が10倍違う
(第5.2.1節・第5.21.4節)。どちらの運用になるかは**導入期に実測する。**
制約は費用ではなくトレーナー1人あたりの作業時間である。
教材を毎回新規に作ると1人あたり週9時間かかり、成立しない。
**教材ライブラリの再利用は費用の節約策ではなく、この仕組みが回るための前提条件。**
**教材は既定で全トレーナーが共有する**(`materials.visibility = 'school'`)。
50人で共有すれば必要な教材は7週でそろう(第 5.6.2 節)。
その代わり、悪い教材1つが1,500人に届く。発行前の確認を省かない。

したがって教材まわりの UI は、**再利用を最短路にしなければならない**
(既定の動線はライブラリ検索、AI 生成は2番目。発行時に弱点タグを必須にする)。

## 構成

- **React 18 + Vite 5**。ルーティングなし。`src/App.jsx` が学習者画面と管理画面をタブで切り替える
- **GitHub Pages** に GitHub Actions で公開(`main` と `claude/**` から)。
  `BASE_PATH` でリポジトリ名のサブパスを指定する
- **Supabase**(Postgres + Auth + RLS + Storage)。接続情報は `.env` と
  GitHub Actions のシークレットから読む。**未設定でもアプリは落ちず、localStorage で動く**


### どの節にも効く決まり(**切り出す前に、223 節から抜き出したもの**)

#### 判断は1か所に持つ

- 判断は**関数1つ**に持たせ、**画面の中で `kind === '…'` や `role === '…'` と書かない**
  (`remakeModeOf()` / `canAskReview()` / `isDialogueKind()` / `showsBasics()` /
  `answerHasAudio()` / `noteIsAnswer()` / `soundPracticeOn()`)。
  **置く場所の数だけ食い違う。既定は「できない」「見せない」側**にする
- **数え方を2通り持たない。** 鳴らす側と落とす側、作る側と探す側、窓口と画面で
  同じものを書き写すと、**置き場所が食い違って二度課金される**
  (`speakChunks` / `wholeKeyOf` / `materialClipPieces` で3度踏んだ)
- **呼び名・色・幅・鍵の名前を2か所に書かない。** 必ず片方だけ古くなる
- **一覧(声・業種・趣味・場面・型・語)を勝手に減らさない。並べ替えも減らすに当たる**

#### 素の node で走らせられる形に切り出す

`src/lib/*.js` の多くは Supabase と `import.meta.env` を引き連れており、
**手元で一度も走らせられない。** 算段だけを何にも依存しない形へ出す
(`playMark.js` / `mp3Join.js` / `focusChunks.js` / `wholeAudio.js` /
`seamFind.js` / `posGroups.js` / `voiceOrder.js`)。
**描けないものは測れない**ので、画面の部品も「props で受け取るだけ」に切り出す
(`SpeechPractice` / `QrCard` / `ShelfAssign` / `VolumeRow`)。

#### 検証の書き方(**ここでいちばん多く転んだ**)

- **「名前が出てくるか」で見ない。** コメントにも説明にも同じ語が出るので、
  **コメントを落としてから、使っている形**(`= wholeKeyOf(…)`)で数える
- **「出る」と「出ない」の両方を見る。** 片方だけだと、
  **どこにも出さない形・全部に出す形**に書き換えても緑のまま
- **「無ければ素通り」する形の検証を書かない** ——
  空の一覧・1つの画面幅・間の無い時計・短い文字列・1つの品詞・
  空の表に貼る SQL。**いちばん危ない形を、検証の中に必ず1つ置く**
- **赤チェックは「その1本を外したら赤くなるか」。**
  ほかの見張りや、ほかの決まり(`!important` の打ち消し)に吸われると、
  **外れていても緑になる**
- **検証ごとに `ok()` の形が違う。** `test-bar.mjs` の `ok()` は
  **文字を出すだけ**で、`ok(条件, 名前)` ではない(`ng()` が失敗を数える)。
  `ok(条件, …)` と書くと **`✓ true` と出て、失敗しようがない**。
  **足したその日に、必ず1本外して赤くしてみる**(2026-09 に踏んだ)
- **値を書き写さない。性質で見る。** 定数を書き写すと、
  値を変えた日に期待値も一緒に動き、**仕組みを壊しても素通りする**
- **骨組み(`src/__screens.jsx`)は、本物と1文字も違えない**
- **赤チェックで `git checkout` を使わない**(まだコミットしていない
  ファイルを巻き添えに消す)。`cp` で写してから壊す

#### 費用

- **見えない費用は管理できない。** AI を呼ぶ前に、回数と語数と金額を画面に出す
- 音声も語の意味も**鍵が同じなら 0 円**。`CLIP_REV` を進めると全部が再課金
- **止まる条件を持たせる**(2回で止める・1画面で1回・やり直さない・
  同じ教材は二度やらない)
- **ファイルに書いてあるもの(訳・型・基礎単語)は引き直さない = 0円**

#### 利用者に見せるもの

- **成功と失敗を、同じ見た目で終わらせない**
- **失敗の知らせは、その操作をした場所に出す**(画面のいちばん下に出さない)
- **黙って消さない・黙って絞らない・黙って落ちない。**
  **0 と `null` を取り違えない**(数えられなかったら、その行ごと出さない)
- **効かない操作を見せない。行き止まりを作らない**
- **同じことをするものを2つ見せない**
- 画面にそのまま出る文字列に `**` を混ぜない(Markdown として読まれない)
- **色だけに頼らない**(うすい地色 + 同じ色の文字 + 枠線 + 太字)

#### 直し方

- **推測でいじらない。まず測る**(Playwright で描く・波形をほどく・
  窓口へ何が渡っているかを覗く・版を出す)
- **道が2つあるものは、いまどちらを通ったかを見えるようにしてから直す**
- **「何も変わらない」は、届いていないという意味である**
  (窓口が古い・その欄がそのモデルに無い・古い版が端末に残っている)。
  **値を上げ下げする前に、届いているかを確かめる**
- **検証が緑なのに実機で直らないときは、定数ではなく
  「検証が何を数えていないか」を疑う**
- **もらえる正解を捨てない。** 音声を作った仕組みが時刻を返しているなら、
  それが正解である(測り直さない)。**まず、返ってくるものを全部読む**
- **分かっていないことを、分かったように書かない**
- **言われた場所だけを直す。寄せたついでに直さない**

### 細かい経緯は `docs/notes/` にある

**1行も捨てていない。置き場所を移しただけ**である(2026-09)。
CLAUDE.md が 1.08MB になり、**毎ターン読み込まれて「prompt is too long」で
固まるようになった**ので、この節にあった 222 の小見出しをテーマごとに切り出した。
**決まりの重さは1つも変わっていない。**

- **見出しで探す**: `grep -n '^### ' docs/notes/*.md`
- **言葉で探す**: `grep -rn 'SentenceSkip' docs/notes/`
- **新しい決まりを足すときも `docs/notes/` の側に足す。**
  CLAUDE.md 本体に積み直すと、また同じことが起きる

| ファイル | 何が書いてあるか |
|---|---|
| [`01-教材の生成と演習.md`](docs/notes/01-教材の生成と演習.md) | 演習の種類を足す4か所・誤り訂正・ディスカッション・想定される質問・スピーチ練習・添削・被らない教材・空の応答・**かたまりの分類と練習(第5.230節)**・**Quick Response の独立した組(第5.235節)**・**意味の近さの判定が落ちていた / test:similar(第5.247節)**・**単語 / フレーズに「日本語 → 英語で言う」(第5.248節)** |
| [`02-音声・音量と間と声.md`](docs/notes/02-音声・音量と間と声.md) | 音量そろえ・会話の間・声の速さ・Speech練習の原稿・会議・役の性別・訛りの指定・作り直し・1本の MP3 |
| [`03-生成のきまり.md`](docs/notes/03-生成のきまり.md) | 0件で成功を返さない・道具の形で強制する・略語のピリオド・レベル14段・GSE・費用 |
| [`04-起動と窓口と通信.md`](docs/notes/04-起動と窓口と通信.md) | 起動画面・ホーム・SetupStatus・古い index.html・待ち時間の上限・止まる条件・CPU 2秒・150秒・**窓口は GitHub が配る(第5.233節)** |
| [`05-画面の骨組みと見た目.md`](docs/notes/05-画面の骨組みと見た目.md) | スクロール錠・下の帯・上に貼り付く帯・メニュー・字と角と間の段・すき間ゼロ・どの画面にも効く決まり・説明の文・**折り返す帯に入れるもの(第5.230節)**・**指の端末は幅で見分けない(第5.236節)**・**`.btn` を塗り直す箱は3色とも塗り直す(第5.244節)** |
| [`06-6Steps-と集中モード.md`](docs/notes/06-6Steps-と集中モード.md) | 取り組み方・集中モードの骨組みと紙・書き込み・メモ・上の帯・読み上げ・**6つを並べて見せる(第5.239節)** |
| [`07-文法解説と30日講座と単語帳の冊.md`](docs/notes/07-文法解説と30日講座と単語帳の冊.md) | 文法解説(0051)・30日講座(0052)・基礎単語・棚(0057/0058)・品詞・出しかた・**業種べつの単語帳をアサインの中へ / トレーナーには中身のある冊ぜんぶ(第5.246節)** |
| [`08-集中モードで読む.md`](docs/notes/08-集中モードで読む.md) | 1段落ずつ・入るまで割る・訳の入れ替え・練習の囲み・英文と訳の対 |
| [`09-読み上げの操作・錠剤とくり返し.md`](docs/notes/09-読み上げの操作・錠剤とくり返し.md) | 錠剤・段落ごとの Listen 廃止・くり返しの単位・ハイライトのずれ(36手の経緯)・QR に溜める・**数字は画面より長く読まれる(第5.231節)** |
| [`10-スラッシュとディクテーション.md`](docs/notes/10-スラッシュとディクテーション.md) | 区切りの決まり・カタマリの訳・書き取り・発音記号・test:bar・**本動詞を修飾語と取り違えない / 1段落おきの地色(第5.240節)**・**文法タグごとに区切りを確かめる(第5.241節)**・**区切ったらその場に訳 / ボタンの色は `btnTone.js` 1か所(第5.242節)** |
| [`11-教材を作る画面.md`](docs/notes/11-教材を作る画面.md) | 裏で走らせる・発行の直後・文型ドリルの問数・使う語の指定・演習の取捨 |
| [`12-レッスン表示と書き込みと印刷.md`](docs/notes/12-レッスン表示と書き込みと印刷.md) | ペン・メモ・操作盤の置き場所・紙の幅・A4・紙に出すもの・単語帳の紙・**紙の「覚えておきたい表現」は表現ごとに見出し(第5.243節)**・**印刷で縮むのは `size: A4` のせい(第5.248節)** |
| [`13-ゲストの画面.md`](docs/notes/13-ゲストの画面.md) | 一覧と見出し・まとめて消す(0041)・ファイル(0031)・貼り付く箱・宿題をさがす・**一覧は既定で畳む / その他の教材(第5.238節)** |
| [`14-教材の一覧とカード.md`](docs/notes/14-教材の一覧とカード.md) | カードの形・教材を消す(0044)・弱点タグ(0050)・リンクで渡す・さがす画面・**足りない演習だけを足す(第5.234節)**・**教材を先にえらぶ(第5.238節)**・**一括共有のチェックを排除(第5.248節)** |
| [`15-読み上げの声.md`](docs/notes/15-読み上げの声.md) | readAloud・標準の声に落とす・v3・声の名簿・教材ごとの声 |
| [`16-分野と場面と教材を作る欄.md`](docs/notes/16-分野と場面と教材を作る欄.md) | 業種と趣味・種類の2段・格闘技・場面と話題・欄の並び・**細かい指定が主になる(第5.232節)** |
| [`17-語のタップと手応え.md`](docs/notes/17-語のタップと手応え.md) | 長押しをやめた・なぞる・ふるえと音・やりかけの控え・語のそろえ方 |
| [`18-単語帳とQuickResponseの復習.md`](docs/notes/18-単語帳とQuickResponseの復習.md) | 答え合わせ・箱・出しかた・段の札・聞き流し・音量・手応え・手入力・単語帳から教材・**開いたらすぐ始まる(本棚 / 達成具合)**・**覚えておきたい表現集の冊(第5.237節)**・**QR の「ランダム」と「繰り返す」(第5.244節)**・**「達成具合」の廃止(第5.246節)** |
| [`19-記録と集計.md`](docs/notes/19-記録と集計.md) | 取り組みを裏で数える(0022)・集計(0023)・スピーチ練習・データの置き場 |
| [`20-ファイルに持った教材.md`](docs/notes/20-ファイルに持った教材.md) | Native Flow(Quick Response の冊)・コロケーション基本動詞(単語帳の4冊目)・0062・textNorm.js |
| [`21-英文の型の見分け.md`](docs/notes/21-英文の型の見分け.md) | 66 型を決まりで見分ける・当てられなければ黙る・目的語の切れ目・名詞のかたまり・型で絞る・test:frame |
| [`23-型シフト.md`](docs/notes/23-型シフト.md) | 66 型の Quick Response(日本語 → 英語 2,872 / 言い換え 2,585)・専用の画面もマイクも採点も廃止・**束で掛け算して型ごとの数を増やす**・型で絞る・ヒント・確かめてから出す・test:shift |
| [`22-パタプラ風の言う練習.md`](docs/notes/22-パタプラ風の言う練習.md) | 言う番から始める・答えを隠す・チャンクで積む・Type A → B・型でまとめる・qrOrder.js |
| [`25-読み上げ用の英文.md`](docs/notes/25-読み上げ用の英文.md) | 画面の英文と声にする英文を分ける・当てはめる順・分からないものは変えない・語の色はどちらの位置で数えるか・ここで転んだ6つ |
| [`24-名詞句.md`](docs/notes/24-名詞句.md) | 名詞句100・副詞句50・コロケーション100(単語帳の冊)・目的語もセット・骨を固定して肉だけ変える・66 型ぜんぶに骨・席の種類・主語の性格・確かめてから出す・nophrase |

## 利用者に作業を頼むときは `docs/APPLY.md` をそのまま使う

**「supabase/apply」は Supabase の中ではない。GitHub のリポジトリの中である。**
同じ名前のものが2か所にあると、利用者は必ず取り違える(2026-08 の指摘)。

- **場所は「どこのファイルか」から書く。** 「GitHub のリポジトリにある〜」
- **押せる URL を渡す。** リポジトリの中の道順を言葉で説明しない
  (`github.com/…/blob/…` の URL を `docs/APPLY.md` に一覧してある)。
  **`raw.githubusercontent.com` は使わない** —— リポジトリを非公開にした
  ので、あの URL は**ログインしていても 404 になる**(2026-09 に実測)
- **やり直しを頼むときは、ステップ1から最後まで書き直す。**
  「◯◯だけもう一度」と書かない。戻って読ませるのは間違いのもと
- 手順の型そのものは `.claude/rules/common.md`(全プロジェクト共通)にある

## この環境の制約

- **`supabase.co` への通信は遮断されている。** マイグレーションの実行も接続確認もできない。
  SQL は利用者が Supabase の SQL Editor に貼り付け、接続確認はブラウザで行う。
  だからこそ `npm run test:db` で手元で検証してから渡す
- **窓口(Edge Function)は、もう貼ってもらわない**(2026-09-22・第5.233節)。
  `.github/workflows/deploy-functions.yml` が、`supabase/functions/` を
  変えるたびに**自動で配る**。**届かないのはこの環境だけで、
  GitHub Actions からは届く。** 「窓口を置き直してください」と書かない
  (逃げ道の手順は `docs/APPLY.md` に残してある)。
  **SQL の自動適用はしない** —— 関数は配り直せるが、DDL は戻せない
- 利用者は **会社PC にアプリをインストールできない**。すべてブラウザで完結させる
- Chromium と Playwright は使える(`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`)。
  マイクは `--use-fake-device-for-media-stream` で代用する
- **`npm run test:bar` を途中で止めたら、残った vite を必ず殺す**(2026-09)。
  あれは**自分用の vite を2つ**立てる(`vite.bar.config.js` /
  `vite.shell.config.js`・`strictPort`)。止め方が悪いと**居座り**、
  次の回はポートを取れずに**古いサーバーを測る。**
  そうすると、**直していない場所が赤くなる**(実際に2回、そうなった ——
  早すぎる場所で落ちる / `書く欄` `文法解説の指定` が 5 本赤くなる)。
  **赤が出たら、まず `ps -eo pid,cmd | grep "[v]ite --config"` を見る。**
  居たら `kill <pid>`、`vite.*.config.js` と `__bar.html` `__shell.html` も消す
- **`pkill -f "vite"` のような指定で開発サーバーを止めない。**
  実行中のシェル自身が引っかかって落ち、そのコマンドの残りが実行されない
  (2回やった)。止めるなら `fuser -k 5173/tcp`。
  開発サーバーは放置しても害はないので、そもそも止めなくてよい
- 画面の確認は、`.env` を一時的に退避すると Supabase 未設定の状態になり、
  ログインを通さずに中の画面を見られる。確認後に必ず戻すこと

## 踏んだ落とし穴(繰り返さないこと)

`docs/PROJECT_SPEC.md` の 3.3.2〜3.3.4 に実機での検証結果がある。特に重いものだけ再掲する。

### iOS の録音 — 実機で何度も失敗した

**iOS では、すべてのブラウザの中身が Safari(WebKit)である。**
「iOS の Chrome を使えば回避できる」という手は存在しない。

- `MediaRecorder` は**録り直しで壊れる**。使わない
- `AudioContext` は **1つだけ作り、二度と `close()` しない**
- **マイクの取得と音声グラフの構築は1回だけ**。録音のたびに作り直すと数回で無音になる
- 音声認識の前に**マイクを解放する**。掴んだままだと `aborted` / `audio-capture` になる
- iOS は `isFinal` を返さないことがある。**確定が来なければ interim を採用する**
- 認識を止めるときは `abort()` ではなく **`stop()`**。`abort()` は結果を捨てる
- この作法は `src/lib/recognition.js` に集約してある。**音声認識を書くときは
  そこを使う。** 各画面で `SpeechRecognition` を直接触らない
- **iOS は高品質音声を Web Speech API に一切公開しない**(実機で生47件を確認、premium 0件)。
  端末の音声に頼らず、**事前生成した MP3 を配る**のが唯一の解

### 端末に何が入っているかを、当て推量で決めない

利用者の会社PC(Windows / Chrome)に入っている英語の声は**3つだけ**で、
**3つとも Google の「通信を使う声」**だった(2026-08 実機)。
Windows なら Microsoft の端末内蔵の声があるはず、という前提は成り立たない。

ここから2つ効いてくる。

- **オフラインでは読み上げが鳴らない。** 会社のネットワークが Google の
  音声サーバーを塞いでいると Listen が無音になる
- **`boundary` が1回も出ない。** 語ごとの色は合図だけに頼れない(上記)

`public/mic-test.html` は、声の一覧・通信かどうか・**合図の回数**を
機械的に出す。**実機の話は、ここの数字で決める。**

### 検証を頼む前に、版が分かるようにする

「直したはずの不具合が直っていない」の原因が、ブラウザに残った古い内容だったことが複数回あった。
`public/mic-test.html` には公開時に版が埋め込まれる(ワークフローの `__BUILD_STAMP__`)。
同種の作業では、**どの版を見ているか機械的に分かるようにしてから**確認を依頼する。

### RLS は「行」しか絞れない

`profiles` の更新を「自分の行なら可」にしたところ、ゲストが `role` を `admin` に
書き換えられた。**列の制限が必要なときは、列単位の `grant` を併用する。**
`profiles`(`display_name` / `industry` のみ)と `assignments`(`learner_done_at` のみ)が該当。

### SQL 関数はテーブルより後に定義する

SQL 関数は作成時に本文を検査される。まだ無いテーブルを参照していると
`relation does not exist` で失敗する。`0001_init.sql` では判定関数を
テーブルの後(第7節)にまとめてある。

## 鍵(このプロジェクト固有)

扱いの原則は共通ルールにある。このプロジェクトで出てくるのは次の3つ。

| 鍵 | 扱い |
|---|---|
| Supabase `sb_publishable_...` | 公開前提。`.env` と GitHub Secrets に置く |
| Supabase `sb_secret_...` / `service_role` | **扱わない。利用者にも求めない** |
| Azure Speech の鍵 | 利用者が `.env` / GitHub Secrets / **Supabase の Edge Functions → Secrets** に直接書く |

`anon` キーが公開されていても安全なのは RLS が守っているからである。
**RLS を無効化する変更は行わない。**

## 窓口ごとに、呼べる人がちがう。**まとめて語らない**

2026-09、安全性レビューに
「ログインした人なら誰でも、**教材生成**・読み上げ音声・語の意味を何回でも
呼べます」と書いた。**教材生成のところが誤りだった。**
利用者に「ゲストには教材作成の権限がないのに、なぜ費用がかさむのか」と
指摘されて気づいた。**コードを読まずに、まとめて書いていた。**

| 窓口 | 呼べる人 | 課金 |
|---|---|---|
| `generate-material`(教材・カタマリの訳・**文法解説**) | **`trainer` / `owner` かつ `status = 'active'` だけ** | Claude |
| `generate-material`(**添削** `mode: 'review_writing'`) | **`trainer` / `owner` だけ。** ゲストは Secrets `LEARNER_WRITING_REVIEW=on` のときだけ | Claude |
| `check-similar` | **`trainer` / `owner` だけ** | 無し |
| `create-user` | **`trainer` / `owner` だけ** | 無し |
| `speak` | **ログインしていれば誰でも**(役割を見ない) | ElevenLabs |
| `lookup-word` | **ログインしていれば誰でも**(役割を見ない) | Claude |

- **ゲストのアカウントで呼べるのは `speak` / `lookup-word` の2つだけ。**
  使いすぎを止める仕組みを入れるなら、まずこの2つである。
  **添削は一度ゲストにも開いたが、利用者の指定でその日のうちに閉じた**
  (道は残してあり、Secrets `LEARNER_WRITING_REVIEW=on` で開く)
- **`speak` と `lookup-word` は `status` も見ていない。**
  退会してプロフィールを消しても、`auth.users` の行が残っていれば呼べる
  (0041 は `auth.users` を消さない)
- **窓口の権限を語るときは、その窓口のコードを読んでから書く**

## リポジトリを非公開にすると、2つ壊れる(2026-09)

安全性レビューの2位「リポジトリが公開になっている」を受けて、
利用者が**非公開にした**(API で `"private": true` を確認)。
**そのとき何が起きるかを、こちらは十分に書いていなかった。**

| 何 | どうなるか |
|---|---|
| **`raw.githubusercontent.com`** | **404 になる**(実測)。貼る SQL・関数のソースを渡す道がここで切れる |
| **GitHub Pages** | **無料プランでは公開できない。** 非公開リポジトリから公開するには Pro 以上が要る |

- レビューには「**GitHub Pages は非公開リポジトリでも使えます**」と書いたが、
  **プランの条件を書いていなかった。**「〜のはずです」で終えない、の破り方である
- 渡し方は **`github.com/…/blob/…`** に変えた(`docs/APPLY.md`)。
  ログインしている本人なら開ける。右上のコピーの絵で全部が取れる
- **この環境からは `github.io` に届かない**(proxy が 403 を返す。
  他人の `pages.github.io` でも 403 なので、アプリが生きているかは
  **こちらでは確かめられない**)。**実機で見てもらう**

### **いまは「公開」に戻っている**(2026-09-19 に API で確かめた)

上の話は**そのとき非公開だった**ときのものである。**いまは違う。**

    y868hjwmz7-creator/English-AI-System   "private": false / "visibility": "public"

**GitHub Pages を無料プランで出すには、公開である必要がある**(上の表)ので、
そのために戻されたものと思われる。つまり**この2つは、いま起きていない** ——
`raw.githubusercontent.com` は使えるし、Pages も動く。

**だから、公開・非公開は「いまどちらか」を毎回確かめてから話す。**
記録を読んで「非公開だから 404 です」と言うと、**嘘になる。**

- **確かめ方**(0円・すぐ済む)。GitHub の API で `private` を見る
- **公開に戻っているあいだは、社外に出せないものを置かない。**
  利用者の会社(RIZAP ENGLISH)の教材 PDF などは、
  **リポジトリに入れるとインターネットから誰でも読める**
  (2026-09-19、5冊の PDF を受け取る話のときに、この順で気づいた)

### **公開できるブランチは1つだけ**(2026-09 実測)

`claude/laughing-euler-rl7rtk` へ push したところ、
**ビルドは成功、デプロイだけが2秒で失敗**していた(実行 596・597)。
同じコミットを `claude/project-spec-document-k5wmwy` へ送ると**成功する**(598)。

ワークフローの `branches:` は `claude/**` を許しているが、
**GitHub Pages の環境(`github-pages`)の側が、ブランチを絞っている。**
ワークフローの中を読んでも分からない ——
**設定が GitHub の画面側にあり、リポジトリの中に無い。**

- **公開したいものは `claude/project-spec-document-k5wmwy` へ push する**
  (CLAUDE.md 冒頭の作業ブランチと同じ)
- 「画面に出てこない」と言われたら、**まず Actions の deploy が
  成功しているかを見る。** 直したはずのものが出ないとき、
  原因はコードではなく**届いていないこと**である(CLAUDE.md「直し方」)
- 版は画面のいちばん下に出る(`2026-09-16 14:24 UTC / b270877` の形)。
  **どの版を見ているかは、必ずここで確かめてもらう**

## 音声と語の意味は、1回だけ課金される

利用者の確認(2026-09)。**この理解で合っている。**

| 何 | 鍵(これが同じなら課金されない) | 作られるとき |
|---|---|---|
| 読み上げ音声 | **(版, 段, 声の id, 英文の指紋)** | 発行の直後(裏の支度)/ 初めて Listen |
| 1本にまとめた音声 | **(版, 声の並び, 英文の並び)** | 同上 |
| 語・フレーズの意味 | **(そろえた語, その文)** —— **スクール全体で共有** | 支度のとき / 初めて触れたとき |

**そのあとは何度聴いても、何度見ても 0 円。**
画面は置き場所を計算して `<audio>` に渡すだけで、窓口を呼ばない。
語の意味も `word_glosses` を読むだけで、単語帳の「くわしく」も控えを読む。

**もう一度課金になるのは、この5つだけ。**

1. さがす画面の「**読み上げ音声を作り直す**」(押す前に本数と課金を出す)
2. **声を変えた**とき(別の声 = 別の置き場所)
3. **英文を1文字でも直した**とき(指紋が変わる)
4. `CLIP_REV` を進めたとき(**だから進めない**)
5. **同じ語でも、別の文で出てきた**とき(文によって意味が違うので別扱い)

**本文の音声は、いま2つの形を置いている**(1本にまとめたもの / 発言ごと)。
支度で作るのは前者だけだが、**段落ごとの Listen を押すとその段落ぶんが
別に作られる。** どちらか片方に決めれば、本文の音声代は半分になる
(利用者の指定で「しばらく置いておく」ことにしてある)。
