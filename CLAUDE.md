# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**全プロジェクト共通のルールは [`.claude/rules/common.md`](.claude/rules/common.md) にある。**
言語・事実の扱い・検証の作法・記録の残し方・鍵の扱い・Git の決まりはそちら。
このファイルには、**このプロジェクト固有のこと**だけを書く。

作業ブランチ: `claude/project-spec-document-k5wmwy`

## 呼び方(利用者の会社の言葉に合わせる)

| 画面・文書に書く言葉 | データベース上の値 |
|---|---|
| **ゲスト** | `profiles.role = 'learner'` |
| **トレーナー** | `profiles.role = 'trainer'` |
| **管理者** | `profiles.role = 'owner'` — 全体の集計だけを見る。トレーナーの権限も兼ねる |

**「講師」「生徒」とは書かない。** 利用者のスクールでは
**トレーナー**(指導する人)/ **ゲスト**(教わる人)と呼ぶ(2026-08 に統一)。
**「配信する」とも書かない。**不特定多数へ発信する響きがあるため、
**「共有する」**と書く。実際、担当ゲストにだけ届く仕組みである。
画面に出すボタンは**「セッションで使う」**(2026-08 利用者の指定)。
コードの中の名前(`LessonView` など)は変えていない。
データベースの値(`learner` / `trainer` / `owner`)は英語のまま変えない。
`is_admin()` は `is_trainer()` の別名として残してあるだけ。新しく書くコードでは
`is_trainer()` / `is_owner()` を使う。
利用者に見えるところと、システム内部の識別子を混同しないこと。

## コマンド

| 目的 | コマンド |
|---|---|
| 開発サーバー | `npm run dev` |
| ビルド | `npm run build` |
| **書き間違いの検査** | `npm run lint` |
| GitHub Pages 用ビルド | `npm run build:pages` |
| 1ファイル版(ダウンロードして開ける版) | `npm run build:single` |
| **DB マイグレーションと RLS の検証** | `npm run test:db` |
| **スラッシュリーディングの区切りの検証** | `npm run test:chunk` |
| **声ごとの音量そろえの検証** | `npm run test:audio` |
| **会話の間(食い気味かどうか)の検証** | `npm run test:gap` |
| **止めた場所からの再生の検証** | `npm run test:play` |
| **紙の色(集中モード)の検証** | `npm run test:paper` |
| お手本音声の生成(Azure の鍵が必要) | `npm run audio` |

自動テストは `npm run test:db` / `test:chunk` / `test:audio` / `test:gap` /
`test:play` / `test:paper` の6つ。
ユニットテストの枠組みは無く、どれも素の node / psql で走る。
**`test:audio` は耳の代わりである**(こちらには音が聞こえない)。
UI を変えたら **`npm run lint` と `npm run build` の両方**を通し、
必要なら Playwright で実際に触って確かめる。

### `npm run build` が通っても安心してはいけない

**import を書き忘れても Vite のビルドは成功する。** 開いた瞬間に落ちる。
実際に `App.jsx` で `TrainerMaterials` の import が抜けたままビルドが通り、
画面を開くまで分からない状態になった(2026-08)。
`npm run lint` は、この「存在しない名前を使っている」を機械的に見つける。

### ログインが要る画面を手元で確かめる方法

この環境からは Supabase に届かないため、ログインを通せない。
`.env` を一時的に退避すると Supabase 未設定の状態になり、
ログインなしで中の画面を開ける。**確認後に必ず戻すこと。**
トレーナー向けの画面など、利用者情報が要るものは、
一時的に仮の値を差し込んで見た目だけ確かめ、すぐ戻す。

### `npm run test:db` は SQL を触ったら必ず通す

`supabase/migrations/*.sql` を変更したら、Supabase に貼る前に必ず実行する。
手元の PostgreSQL(`service postgresql start` が要る場合がある)にまっさらな DB を作り、
マイグレーションを **2回** 実行してから、3つの検証ファイルを走らせる。

- `supabase/test/supabase_stub.sql` — `auth.users` / `auth.uid()` / `storage` /
  `authenticated` ロール / 既定の権限付与という、Supabase 環境の最小の再現
- `supabase/test/rls_test.sql` — トレーナー1人・ゲスト2人を作り、
  「誰に何が見え、何ができないか」を確かめる。**拒否されるべき操作が通ったら失敗する**
- `supabase/test/material_shape_test.sql` — 実物のドリルがそのまま入るか
- `supabase/test/dedup_test.sql` — 同じ英文が二度出ないか
- `supabase/test/vocab_test.sql` — 意味の控え・箱・句。**間隔の日数そのもの**も見る
- `supabase/test/verify_migrations.sql` — 利用者が Supabase に貼る確認 SQL。
  **これ自体が古びる**ので、ここで毎回まわして「まだです」が無いことを確かめる

この検証は実際に重大な穴(ゲストが自分をトレーナーに昇格できる)を見つけた実績がある。
省略しないこと。

#### 「頭から全部もう一度」も試す

`scripts/test-migration.sh` は **1ファイルを2回**だけでなく、
**0001 から最後までを、もう一度頭から**流す。利用者は
`supabase/apply/*.sql` を**まとめて貼る**ので、実際に起きるのはこちらである。

これを見ていなかったために、次の穴を2つ見落としていた(2026-08)。

- 0015 の `review_words` が、0018 で列を増やしたあとに再実行されると
  `cannot change return type of existing function` で止まる
- 0002 の `school_summary` も、0004 のあとで同じことが起きる

**返す列を変える関数は、先に `drop function if exists` を置く。**
あわせて「同じ名前の関数が2つ以上残っていないか」も見る。
残ると PostgREST が「どちらか分からない」と断り、画面から呼べなくなる。

#### 利用者に渡す「貼る SQL」も、そろった DB に貼ってみる

**順に全部流すだけでは足りない。** 利用者は
**そろっている DB に、あとから1つだけ貼る**ことがある。

実際、0027 は `review_words()` を **drop せずに**作り直していた。
書いた時点では返す列が同じだったので要らなかったが、
**あとから 0028 が返す列を増やした。** そのため 0027 だけを貼り直すと
`cannot change return type of existing function` で止まる(2026-09)。
全部を順に流す検証では、手前の 0024 が drop してくれるので**気づけない。**

**関数を作り直すファイルは、返す列を変えていなくても drop を置く。**
あとで誰かが列を足すかもしれない。
`scripts/test-migration.sh` が、`supabase/apply/pending_*.sql` を
**そろった DB の複製に1つずつ貼って**見張っている。

#### 検証ファイルは1つずつ別の DB で走らせる

`material_shape_test.sql` が profiles を1つ作るため、同じ DB で続けて走らせると
`rls_test.sql` の数え上げがずれて落ちた(2026-08)。
`scripts/test-migration.sh` は、移行を流した DB を `template` として複製し、
**検証ファイルごとにまっさらな複製の上で走らせる。**
検証どうしが干渉すると、壊れていないものが赤くなり、
**本当に壊れているものを見落とす。**

#### 検証の出力をパイプに直接つながない

`psql ... | sed | grep` と書くと、`psql` が失敗しても最後の `grep` の結果で
成否が決まり、赤くならない。**出力を一度変数で受けてから流す。**

## このアプリが解く問題

パーソナル英語スクールの **レッスンと宿題の循環** を支える道具である。

```
週2回のレッスン → 宿題を出す → ゲストが取り組む
  → 次のレッスンで達成度を確認し、弱点を指摘する
  → その弱点に対応した宿題を出す   ← ここが要
```

**中心機能は学習記録でも発音採点でもなく、「トレーナーが弱点から教材を作り、
指定したゲストに配信する仕組み」である。** 弱点を指摘した直後に教材を出せないと、
練習量が確保できず克服につながらない。他の機能はこの循環の補助と位置づける。

対象はスクールの既存ゲストのみ(クローズド)。一般公開はしない。
詳細は `docs/PROJECT_SPEC.md`。**設計判断はすべてこの仕様書に記録すること。**

### 設計を縛っている最大の制約

**いま: トレーナー20人・1人あたりゲスト25人(ゲスト500人)。
伸ばしたあと: トレーナー約50人・ゲスト約1,500人**(第5.6.0節)。
**1,500人は目標であって、いまの数ではない。** 費用を見積もるときに
取り違えると、実態の3倍で怖がることになる。

ゲスト1人あたり**月4回**のレッスン。いまは**月2,000レッスン**。
レッスンごとに1本ずつ新規に作ると**月2,000本**になり、
これは第5.6.2節が見積もったライブラリ全体(333件)の6倍を毎月作る計算になる。
**「配る数」と「新しく作る数」を混同しない。** 費用が10倍違う
(第5.2.1節・第5.21.4節)。どちらの運用になるかは**導入期に実測する。**
制約は費用ではなくトレーナー1人あたりの作業時間である。
教材を毎回新規に作ると1人あたり週9時間かかり、成立しない。
**教材ライブラリの再利用は費用の節約策ではなく、この仕組みが回るための前提条件。**
**教材は既定で全トレーナーが共有する**(`materials.visibility = 'school'`)。
50人で共有すれば必要な教材は7週でそろう(第 5.6.2 節)。
その代わり、悪い教材1つが1,500人に届く。発行前の確認を省かない。

したがって教材まわりの UI は、**再利用を最短路にしなければならない**
(既定の動線はライブラリ検索、AI 生成は2番目。発行時に弱点タグを必須にする)。

## 構成

- **React 18 + Vite 5**。ルーティングなし。`src/App.jsx` が学習者画面と管理画面をタブで切り替える
- **GitHub Pages** に GitHub Actions で公開(`main` と `claude/**` から)。
  `BASE_PATH` でリポジトリ名のサブパスを指定する
- **Supabase**(Postgres + Auth + RLS + Storage)。接続情報は `.env` と
  GitHub Actions のシークレットから読む。**未設定でもアプリは落ちず、localStorage で動く**

### Supabase 上で動く関数(Edge Function)4つ

ブラウザに置けないもの(鍵・重い処理)はここにある。配置は利用者が
Supabase の画面から行う。**1関数1ファイル**にしてあるのは、配置の手順を
増やさないため。

| 関数 | 役目 | 鍵 |
|---|---|---|
| `create-user` | アカウント発行 | `service_role`(関数の中だけ) |
| `generate-material` | 教材の下書き生成 | Claude API |
| `check-similar` | 意味が近すぎる英文を弾く | **不要**(`gte-small` が Supabase 内にある) |
| `speak` | 英文の読み上げ音声(MP3)を作って置く | Azure Speech |

同じ英文を二度出さない仕組みは3段(仕様書 第5.16.2節)。
**保証しているのは②と③で、①は誘導にすぎない。**
意味の近さの境目は `SIMILARITY_THRESHOLD`(`src/lib/materials.js`)1か所。
**実測していない初期値なので、外しすぎ・素通りしすぎが出たらここを直す。**

### 教材の種類と、その形

| 種類 | 中身 | 数え方 |
|---|---|---|
| 文型ドリル | 1つの文法を4演習 × 10問 | **問数**(40問。減らさない) |
| リーディング | 記事1本 + 内容理解5 + **ディスカッション5** + 語句8 | **段落数**(6段落 = 250〜350語) |
| ダイアローグ | 会話1本 + 内容理解4 + **ディスカッション5** + 語句6 | **発言数**(14発言) |
| 単語 / フレーズ | 20問 | 問数 |

**音読・オーバーラッピング・シャドーイング・リピーティングは、
演習の種類ではない。** 同じ本文に対する「取り組み方」であり、
ゲストの画面で切り替える(`PassagePractice.jsx`)。
これを演習として分けたために「長文」が16個の短文になった(2026-08)。
**種類を増やすときは「これは読み物か、問題か」を先に決めること。**

記事・会話では**弱点タグは任意**で、問数を弱点で分割しない。
分割すると話がつながらなくなる。探すのはジャンル・場面・見出しで行う。

### 穴埋めはやめた。いまは「誤り訂正」

`error_correction`(2026-09 利用者の指定)。**文型ドリルの2番目**である。

> そもそもこの穴埋めはいらないかもしれない。
> なぜなら、穴埋めは複数の回答が考えられる場合があり、すっきりしないからです。

きっかけは実機でこう出たこと。

    Before kickoff, could you （　　　） me where the away fans usually sit?
    与える語: tell   → tell

`could you` のうしろは原形なので**形を変える必要がなく**、
与える語がそのまま答えになっていた。**空欄の位置そのものが誤っている。**

- **4演習 × 10問 = 40問は変えない**(第5.13.3節)。差し替えただけ
- **誤りは1文に1か所。** 2か所あると、どちらを直すのか決まらない
- `answer` には**直した英文を1文まるごと**入れる。直した語だけにしない
- **`note` は必須。** なぜ間違いかが無いと、直す意味が薄い
- **音声は付けない**(`audioFrom: null`)。誤った文を手本として聞かせられず、
  直した文を読ませると答えが耳から入る。穴埋めのときと同じ扱い
- **`fill_blank` は消さない。** 一覧に残す(旧「長文」と同じ)。
  消すと、**すでに作った教材が開けなくなる**
- 与える語と解答が同じ問は `givesAwayAnswer()` が落とす。
  古い教材を作り直すときのために残してある

### 内容の理解には、設問と解答の訳が付く(0035)

2026-09 利用者の指定。

> 内容理解の設問と解答の訳を見れるようにしてください。音も聞けるように。
> そして解答や設問も単語の意味を調べて単語帳に追加できるようにしてください。

内容の理解は**設問も解答も英語**である。日本語が1つも無いと、
設問の意味が取れない人には**設問そのものが壁**になり、
本文を理解できていたのかどうかを確かめられない。

- 入れ物は `material_items.question_ja` / `answer_ja`(0035)。
  **`prompt_ja` を流用しない。** あれは本文・問題文の訳で、
  Quick Response・スラッシュリーディング・カタマリの訳がその対で動いている。
  **名前の違うものを、同じ欄に入れない**
- **解答はこれまで「ただの文字」だった。** だから語に触れても意味が出ず、
  単語帳にも入れられず、読み上げもできなかった。
  設問のほうは `EnglishText` だったので、**片方だけができていた**
- 出す場所は **`AnswerEn.jsx` 1つ。** 解答が出るのは3か所
  (教材の中身 / レッスン表示 / 今週の宿題)。**書き写さない**
- **設問の訳は伏せない。** 設問は「何を訊かれているか」であって答えではない
  (レッスン表示も、設問の日本語はもともと伏せていない)
- **解答の訳と Listen は、解答を開いたあとにだけ出る。**
  答えが先に耳から入ることはない
- **0035 を貼る前に作った教材には入っていない**(訳が出ないだけ)。
  だから `SPARE_FIELDS` に入れてある。必須にすると、
  窓口を配置し直す前の教材が**1問残らず落ちる**
- ディスカッションの設問には**足していない**(言われた場所だけを直す)。
  必要なら同じやり方で足せる

### ディスカッションには、正解が無い

`discussion`(2026-09 利用者の指定)。記事とダイアローグの**新しいページ**で、
既定は5問、「倍」で10問(`SCALABLE_SECTIONS`)。

> 内容理解５問に追加して、新しいページにディスカッションというものを
> 追加してれ。これも基本は５問、設定により１０問にできるようにしてくれ。

**内容の理解の置き換えではない。足すものである。** あちらは
「本文に何が書いてあったか」を確かめるもので**答えは本文の中にある**が、
こちらは本文をきっかけに**自分の考えを話す**もので**正解が無い。**

- **`answer` の欄そのものを出さない**(`SECTION_FIELDS.discussion`)。
  欄があると、AI は必ず何かを書く。**書けない欄は、最初から作らない**
- 代わりに `note` へ日本語の手がかり(話を広げる観点 + 使える表現)。
  ゲストにも見えてよいので、`hideAnswerFromLearner` は付けない
- **本文が要る**(`needsContext`)。本文が無いと問いが作れないので、
  内容の理解・語句と同じ扱いにする
- **Quick Response には出さない**(`PAIR_FIELDS` に入れない)。
  英語の設問で、対になる日本語が無い
- 声は標準の段(`voiceTierFor`)。まねる手本ではなく、設問である

### 演習の種類を足す場所は4つ。**表の制約を忘れない**

画面と窓口には足したのに **`material_sections_type_check`(表)に
足し忘れた。** 教材を**発行した瞬間に**こう出て止まる(2026-09 実機)。

    演習を登録できませんでした: new row for relation "material_sections"
    violates check constraint "material_sections_type_check"

`npm run lint` も `npm run build` も通る。**作ってみるまで分からない。**

| 足す場所 | 何を |
|---|---|
| `src/data/exerciseTypes.js` | `EXERCISE_TYPES` / `DEFAULT_SECTIONS` / `SCALABLE_SECTIONS` |
| 窓口 `generate-material` | `SECTION_INSTRUCTIONS` / **`SECTION_FIELDS`** |
| 窓口 `generate-material` | `needsContext`(本文が要る演習か) |
| **移行(SQL)** | **`material_sections_type_check` の一覧** |

**`scripts/check-exercise-types.mjs` が見張っている**
(`npm run test:db` の中で走る)。画面の `EXERCISE_TYPES` を読み、
表の制約に全部入っているかを機械的に確かめる。**この検証を外さない。**

### 空で返ってきたら、こちらで1回だけやり直す

道具(`emit_section`)は呼ばれているのに `items` が**空の配列**で返ることが
まれにある(`stop_reason: tool_use`)。形は `strict: true` で保証されているが、
**空の配列も「形としては正しい」**ので API は通してしまう。

> もう一度条件も変えずにそのままの状態で作成し直すと次は上手くいったりします。
> 安定させれないものでしょうか?(2026-08 利用者)

**利用者が押し直せば直るのだから、こちらで押し直す。**
費用は増えない(失敗した1回はどのみち課金されている)。

- **2回で止める。** 際限なく試すと、残高切れのときに待たされ続ける
- 2回目には「前回は空だった」と伝える。同じことを繰り返させない
- **断られた(`refusal`)・長すぎて切れた(`max_tokens`)はやり直さない。**
  同じことを頼んでも同じ結果になる

### 出来上がった問は、1問ずつ確かめる。**ただし落としすぎない**

`strict: true` は「その欄があること」までしか保証しない。
**空文字も「形としては正しい」**ので API は通してしまう。実際、
フレーズ20問のうち**1問目が空**(札だけで英文も訳も無い)のまま
「全 20 問」と出ていた(2026-09 実機)。
「中身が0件のまま『成功』を返さない」を、**1問ずつにも当てはめる。**

| 見るもの | どこ |
|---|---|
| **空の問**(必須の欄が空)| `isBlankItem()`(画面)+ 窓口でも落とす |
| **単語 / フレーズの取り違え** | `isWrongShape()`。**語数だけ**を見る |
| 答えが問題文に見えている | `givesAwayAnswer()` |

- **画面の側にも置く。** 窓口だけだと、利用者が配置し直すまで直らない
- **当てられることだけを見る。**「主語が付いている」「三人称単数」は
  語のリストでは当てられない(スラッシュリーディングと同じ)。
  確かなのは語数だけである
- **無くても成り立つ欄は必須にしない**(`SPARE_FIELDS`)。
  `phonetic`(0020 より前)・`answer_alt`・`question_ja` / `answer_ja`(0035)。
  ここを間違えると、**窓口を配置し直す前の教材が1問残らず落ちる**

#### 落とす仕組みには、必ず**安全弁**を付ける

**落とす検査を入れた日に、単語の教材が0問になった**(2026-09 実機)。
窓口にはまだ「単語は1語」と書いていなかったので、
`in light of` のような2語以上が返るのは当たり前で、
それを全部落とすと5回作り直しても1問も残らない。

- **空の問は絶対に落とす。** 中身が無いものは、どうやっても使えない
- **形のちがいは「使えるが望ましくない」だけ。**
  足りなければ**戻す**(`shaped`・`generateSectionUnique`)
- **0問の教材より、形が崩れた教材のほうがましである。**
  落とす仕組みが、教材そのものを作れなくしてはいけない

### 読み上げは、音が出るまで「用意しています…」と出す

**Listen の1度目が反応しない**という報告の正体(2026-09 実機)。

> Listen 全て(どこにあるものでも共通)において、1度目に押すと反応しない

その英文の MP3 がまだ無いと、窓口(`speak`)が作り終わるまで
**数秒間まったく音がしない。** ボタンは Stop に変わっているだけなので、
押しても何も起きなかったように見え、もう一度押す。
**ところがその2度目は「止める」なので、やはり鳴らない。** 鳴るのは3度目である。

- `readAloud` / `readAloudSequence` の **`onStart`** が、
  **実際に音が出た瞬間**に1回だけ知らせる(端末の声に落ちたときも呼ぶ)
- 文言は **`preparingLabel()`(`SpeakButton.jsx`)1か所。**
  2秒を過ぎたら経過秒数も出す。**3か所に書き分けない**
  (`SpeakButton` / `PassagePractice` / `LessonView` の「Listen (全体)」)

### 声ごとの音量は「全部の声の平均」にそろえる。**下げるだけ**

`src/lib/loudness.js`。**2026-09 に4度やり直している。経緯ごと残す。**

| | やり方 | 何が起きたか |
|---|---|---|
| ① | 決めた大きさ(0.15)に**全員を寄せる** | 小さい声が 2〜3 倍に持ち上がり、**息の音も破裂音も一緒に大きくなった** |
| ② | いちばん小さい声に合わせて下げ、**全体を 1.25 倍** | 上げるための面倒(頭打ち・割れの計算)が残った |
| ③ | 下げるだけ。**ただし Web Audio 経由** | **全部の声でバリバリ雑音**(下記。いちばん高くついた失敗) |
| ④ | **平均にそろえる。`<audio>` の `volume` だけ**(いま) | — |

#### **「平均に合わせる」と「最小に合わせる」は、同じ結果になる**

> コンプレッションやリミッターをかけるのではなく、ボリューム調整だけで、
> 全ての声の音量の平均値をとり、そこに全てを合わせてみてください(④の指定)

**先に算数の事実を押さえる。** 平均に合わせるには、平均より小さい声を
**上げる**必要がある。ところが `volume` は下げることしかできない。
そこで全体を「いちばん大きい倍率」で割って 1 以下に収めると、

    最終的な倍率 = いちばん小さい声 ÷ その声

**声どうしの比はまったく変わらない。** 変わるのは全体の音量だけである。
つまり②③④は**比としては同じもの**で、
**それでも差が残るなら、原因は「そろえ先」ではない。**

`gainFor()` は**指定どおり平均から出して、そのあと割っている。**
上げられるようになった日には、割るのをやめれば平均そのものに合う。

#### **RMS は「耳の感じる大きさ」ではない**

**④で実際に効いたのはこれである。**
波形をそのまま二乗した平均(素の RMS)は、**耳の感度を写していない。**
耳は低い音に鈍く、中〜高音に敏感なので、
**低音の多い声(男性に多い)は、数字上は大きいのにそう聞こえない。**
その声を下げすぎていた。

だから**測るときだけ**、放送の決まり(ITU-R BS.1770 の K特性)と同じ形に整える。

- 高い側を **+4dB**(1.5kHz からの棚)
- とても低いところを落とす(38Hz からの高域通過)

**Chromium で実測した効き**(同じ大きさの正弦波を通したときの変化)。

| 120Hz | 300Hz | 1000Hz | 1500Hz | 3000Hz |
|---|---|---|---|---|
| 105% | 101% | 108% | 126% | 154% |

**これは測るためだけの処理である。鳴らす音には一切かからない**(下記)。
**測り方を変えたら `MEASURE_REV` を必ず1つ進める。**
進めないと古い値と新しい値が混ざり、そろえ先がずれる。

#### **押し上げて帳尻を合わせない。そろえるのを壊す**

②③には「そろえ先の下限 0.05」があった。**これがそろえるのを壊していた。**
本当にいちばん小さい声が 0.04 だと、

    いちばん小さい声 … 0.05/0.04 は 1 を超えるので 1 倍のまま → 0.04
    ほかの声         … 0.05 まで下がる                       → 0.05

**いちばん小さい声だけが、いちばん小さいまま残る。**
人の声の平均は 0.03〜0.08 あたりなので、これは日常的に起きていた。
いまは**押し上げず、明らかに測り損ねたもの(0.01 未満)を外す。**
下げすぎの歯止めも 0.30 → **0.20**(3.3 倍ひらいた声が止まっていた)。

#### **音量を変えるために、音の通り道を変えない**

**③でいちばん高くついた失敗**(2026-09 実機)。

iPhone は `<audio>` の `volume` を無視する。そこでもそろえたくて、
`<audio>` を Web Audio(`MediaElementAudioSourceNode` → `GainNode`)に
つなぎ替えていた。すると、

> 小さな音量に合わせたはずなのに1回目の再生からバリバリ雑音だらけです。
> 1.25持ち上げると言う処理をやめたのに不思議です。
> ホワイトノイズが入るのであれば理解できるのですが。

**利用者の疑問が、そのまま答えだった。**
音を小さくする掛け算(0.4倍など)は**雑音を作れない。**
**原因は音量の値ではなく、通り道のほうにあった。**
Web Audio を通すと、`ctx.destination` の ±1.0 での切り落とし・
再生速度の処理・周波数の変換が**新しく**加わる。

**引き換えに iPhone ではそろわない。それでよい**
(そろわないことより、雑音が入ることのほうが悪い)。

**`npm run test:audio` が、`createMediaElementSource` /
`createGain` / `crossOrigin` を使っていないことを見張っている。**
**測る側で Web Audio を使うのは構わない**(`weighted()`)。
あれは鳴らす音に一切かからない。

#### **「プチッ」は、なだらかに上げ下げして消す**

2026-09 利用者の指摘。**2手かかっている。経緯ごと残す。**

> まだ会話の発言と発言の間にプチっと入りますね。入る時と入らない時が
> あります。…音の終わりをなだらかなフェードアウトにするとか、
> 何かできるはずです。ここは音に関することで、このアプリのキモです

**①の直し(差し替える前に `volume = 0`)では足りなかった。**
**0 に「跳ぶ」こと自体が段差**なので、鳴る場所を移しただけだった。
波形が途中の値のまま急に途切れれば、そこで「プチッ」と鳴る。

**②いまは、音量を時間をかけて上げ下げする**(`loudness.js` の `fadeGain`)。
動かすのは**もともと使っている `<audio>` の `volume` だけ**である
(**音の通り道は変えない** — ③でいちばん高くついた失敗)。

| | 値 | なぜ |
|---|---|---|
| 刻み | **10ms** | 画面の描き替え(約 16ms)では**粗すぎた**(下記) |
| 入り | **70ms** | 短すぎると1刻みめが持ち上がりすぎる(実測 50ms で 34%) |
| 終わり | **120ms** | ここが出どころ。長めに取る |
| 終わりの手前 | **25ms** | **ここで 0 に届かせる**(下記) |

**本物の `<audio>` で実測して、2つ直した。**

- **`requestAnimationFrame` では粗すぎた。** 1.5 秒の音を鳴らして測ると、
  鳴り終わりの最後のコマが「残り 41ms」で、**音量 0.205 のまま音が
  終わっていた**(半分の高さから急に切れていた)。`setInterval` で
  10ms ごとに動かすように変えた
- **10ms にしてもなお 0 に届かなかった**(最後の一刻みが「残り 33ms」で
  音量 0.11)。時計の揺らぎと、`ended` が長さぴったりでは来ないため。
  だから**終わりを 25ms 手前に見立てて**、そこで 0 に届かせる。
  **切っているのではない** — 最後まで鳴らしたうえで、そこだけ聞こえなくする

実測(音量 0.4・速さ 1.2 倍): 鳴り終わりは **10 段**で下がり、
1回の変わり幅は最大 **0.034**、音が終わった時点の音量は **0.024**
(直す前は **0.205**)。

**測るのは、鳴り終わってから**(2026-09)。測る側は Web Audio で MP3 を
ほどくので、**鳴っている最中に始めると音が一瞬途切れることがある。**
「入る時と入らない時がある」に合う — 測るのは**声ごとに1回だけ**なので、
その1本のときだけ起きる。**先に測らない**という決まりはそのままで、
変えたのは**いつ**だけである。

**`npm run test:audio` が曲線そのものを見張っている。**
こちらには音が聞こえないので、**耳の代わりに数字で見る。**
刻みの大きさ・終わりで 0 に届くか・入りの持ち上がり・`rAF` に頼って
いないか。実際に刻みを 8ms にして、赤くなることを確かめてある。

#### **差し替えの前に、止めて黙らせる**(①の直し。いまも残してある)

2026-09 利用者の指摘。

> 発言と発言の間、特に、ひとつの発言の終わりに小さく
> 「プチっ」というノイズが入ってます。

**`<audio>` は1つだけを使い回している**(iPhone の解錠をやり直さない
ため・CLAUDE.md)。次の発言に移るとき、**鳴り終わったままの `<audio>` に
新しい `src` を入れて `load()`** していた。これは
**再生の仕組みをいったん壊して作り直す操作**で、そのとき音の出口に
段差ができる。これが発言の切れ目で「プチッ」と鳴っていた。

**鳴らす音そのものは変えない。** することは2つだけ。

1. 先に **`pause()`**。鳴っている最中に作り直させない
2. **`volume` を 0 にしてから差し替える。** 段差ができても、
   音量が 0 なら**聞こえない。** 本当の音量は、読み込みが終わったあと
   `applyGain()` が入れ直す(**この順は変えない**)

**止めるとき(`stopClip`)も同じ。** 鳴っている途中で止めると、
そこに段差ができる。黙らせてから止める。

これは**すでに使っている `<audio>` の `volume`** であって `GainNode` では
ない。**通り道は1ミリも増えていない**(③の失敗をくり返さない)。

**`npm run test:audio` が順番を見張っている。** 入れ替わっても
`npm run lint` にも `npm run build` にも引っかからないためである
(実際に順を入れ替えて、赤くなることを確かめてある)。

#### 決まりごと

- **窓口では測れない。** Supabase の関数は CPU 2秒しか使えない。
  窓口側の音量指定も **ElevenLabs には無い**
- **1つの声につき1回だけ測る。** 端末に覚えるので、
  **その声の1本目だけはそろわない**
- **そろえ先は測るたびに動く。** より小さい声を測ると、ほかの声は次から下がる

#### **音は聞こえない。だから数字で確かめる**

> あなたはボリュームを感じることはできるのですか?(2026-09 利用者の問い)

**聞こえない。** ①の副作用も③の雑音も、見つけたのは利用者である。
だから**耳の代わりに `npm run test:audio` を置いた。**
**いちばん大事なのは「倍率がすべて 1 以下か」**である。

**この検証は、思い込みを3つ捕まえている。**
測った結果がモジュールに残って2つめ以降が素通りしていたこと、
「1人なら必ず 1.25 倍」だが割れる声があったこと、
**下限がそろえるのを壊していたこと**(0.04 と 0.10 がそろわなかった)。
**音まわりを触ったら、必ず通すこと。**

### 会話の「間」は、内容から決める。話す人が替わるときだけ入れる

`src/lib/turnGap.js`(2026-09 利用者の指摘)。

> 女性の発話が不自然なくらい早いタイミングで食い気味に入ってきます。
> 本当の会話っぽい間をいれれないものでしょうか?特に内容によって。。。
> 間がある時と食い気味な時を内容によって自動でコントロールできませんか?

**原因は ElevenLabs ではなく、こちらのコードだった。**
`readAloudSequence()` は鳴り終わった**その瞬間**に次を始めていた(間 0 ミリ秒)。
コメントにも「間をあけない」と書いてあり、読み込みで黙るのを嫌ったためだが、
**v3 の音声は前後の無音がほとんど無い**ので、息継ぎも無しに次の人が話し出す。

- **決まりで書く。AI に頼まない**(`chunker.js` と同じ考え方)。
  要るのは記号と閉じた語のリストだけで、1発言ごとに課金する理由がない
- **上から順に見て、当てはまった時点で決める。足し合わせない。**
  足すと「相づちなのに疑問文だから長い」という説明できない値になる

  **2026-09、利用者の指定で全部を半分にした。**

  > 会話の発言と発言の間、記事の段落と段落の間の間が少し長いです。
  > 意味や感情の流れにより間を調整するにしても、
  > 今の半分近くにして良いと思います

  **比はそのままで、値だけを半分にする。** 相づちは短く、問われたら長く、
  という関係は残したいので、5つの値と「長さに応じて足す分」を
  **そろって半分**にした。**片方だけ縮めると、そこだけ不自然に詰まる。**

  | 何 | 間 |
  |---|---|
  | 前が `—` `...` で終わる(言い終わっていない) | **60ms** 相手が引き取る |
  | 次が相づち・即答で始まる(`Right` `Exactly` `Of course`) | **60ms** |
  | 前が付加疑問(`…, right?`)で終わる | **60ms** 同意を求めているだけ |
  | 次が言いよどみで始まる(`Well` `Let me` `Actually`) | **450ms** |
  | 前がふつうの疑問文 | **450ms +** 長さに応じて足す |
  | それ以外 | **190ms**(前が長ければ少し足す) |
  | 同じ人が続けて話す(記事の段落の切れ目) | **300ms**(息継ぎ) |

- **どの継ぎ目にも入れる**(2026-09 利用者の指定「記事でも同じ仕様にしてください」)。
  はじめは話す人が替わるときだけにしていたが、記事も段落と段落が詰まって聞こえる
- **同じ人が続けて話すときは、受け答えの規則を当てない**(`sameVoice`)。
  相づちも付加疑問も**相手がいる話**であって、一人で読んでいるところには無い。
  当てると「段落が `Right of way…` で始まったので食い気味」という、
  意味のない間になる。要るのは**息継ぎ**(600ms)で、
  問いかけで終わっていれば一拍足す
- **速さで割る。** 120% で聞いている人には、間も 120% で来る
- **速くした声には、そのぶんの余白を必ず足す**(`speedPadMs`・2026-09 利用者の指定)。

  > スピードを速くしたスピーカーはそのぶん前後の間が短くなります。
  > 速くした分と同じだけ前後に余白を入れてください。
  > そしてその余白は内容とは別に必ず入れるようにしてください。

  **再生速度は、音声の中の無音まで一緒に縮める。** 1.2 倍にすると、
  その1本の前後にもともと入っていた息継ぎも 1/1.2 になるので、
  **その声のまわりだけ詰まって聞こえる。**

  縮んだ割合 `(1 - 1/R)` を、基準(`PAD_BASE` = 1000ms)に対して足し戻す。
  1.2 倍なら**片側 167ms**、あいだは前後で **334ms** 増える。

  - **内容から決める間とは足し算にする。** 詰まっているのは**音声そのもの**で、
    話の中身とは関係がない。「相づちだから短く」と打ち消し合わせない
  - **前と後ろの両方に足す。** 発言のあいだの無音は1つなので、
    実際には「前の声のうしろ」と「次の声の前」が合わさって1つの間になる
  - **速くしていない声は 0。** ほかの声のリズムは1ミリも変えない
  - **利用者が選んだ速さでは割らない。** 縮めているのは声ごとの補正
    (`voiceRateOf`)だけで、全体の速さはどの声にも同じにかかる
  - 遅くした声(1 未満)も 0。**伸ばした声は、もともと間も伸びている**
- **間は止められるようにする。** まとめて待つと Stop を押しても
  最大 1.4 秒動かない。50ms ずつ刻んで、そのつど生きているかを見る
- **上限は 0.7 秒**(2026-09 に 1.4 秒から半分)。
  それ以上は「待たされている」に変わる
- **`so` を言いよどみに入れない。** ただのつなぎ言葉として文頭に立つことが多く、
  間があるとは限らない(`HEAD_WORDS` に副詞を入れないのと同じ理由)
- **付加疑問は、閉じたリストで見分ける。**「? で終わって、最後のコンマから先が
  短い」だけで判定すると、`Would you take the escrow, the holdback, or juice?` を
  付加疑問と取り違えて食い気味になる。`npm run test:gap` が見張っている

**この検証は、書いたときの思い込みを2つ捕まえている。**
`Understood` が即答の語だと気づかずに「前が長い」の枝を試そうとしたこと、
`It depends.` が言いよどみなので上限の枝を通っていなかったこと。
**間を触ったら、必ず通すこと。**

### 声が遅いときは、名簿の `rate` で直す。窓口では直せない

`voiceRateOf()`(`src/data/clipVoices.js`・2026-09 利用者の指定)。

> Jofra と Henry のスピードのデフォルトを 120% にしてください。

**ElevenLabs の v3 は `speed` の指定に対応していない。** だから窓口では直せない。
鳴らすときの **`playbackRate`** で補正する(`audioClips.js`)。

- **MP3 を作り直さない。** 費用は1円もかからず、**`CLIP_REV` も進めない**
- **利用者が選んだ速さに掛ける。** 120% で聞いている人には、
  その声だけがさらに 1.2 倍になる(ほかの声はそのまま)
- **上限を置く**(`MAX_RATE` = 2.5)。掛け算が重なると聞き取れなくなる
- **語の色は狂わない。** 印は `el.duration`(素の長さ)から作り、
  位置は `el.currentTime` で見ている。どちらも再生速度の影響を受けない
- 指定の無い声は 1 倍のまま。**全員を速くしない**(`npm run test:gap` が見張っている)

### 「会議」は種類の1つ。中身は会話とまったく同じ(0037)

2026-09 利用者の指定「会議の教材が追加されていない」。

会議は**場面**(立ち上げ会議・アイデア出し・部署をまたぐ)と**人数**(3〜4人)
としては作ってあったが、**「トレーニングの種類」の一覧に出していなかった。**
だから会議を作るには「ダイアローグ(会話)」を選んで人数を3人にする必要があり、
**探すときも「会話」としてしか出てこなかった。**

- **足したのは `materials.kind` の値1つだけ**(0037)。
  **演習の種類は増やさない。** 増やすと `material_sections_type_check` も
  窓口も触ることになり、**すでに作った会話と別物**になる。
  中身(本文14発言 + 内容の理解5 + ディスカッション5 + 語句6)は同じ
- **窓口(`generate-material`)は置き直さなくてよい。** あちらは `kind` を
  読んでいない(演習の種類ごとに呼ばれる)。**貼るだけで使えるようになる**
- 人数は `materials.voice_ids` の**長さ**がそのまま持つ。**表も列も増えない**
- **`kind === 'dialogue'` と書かない。** 書くと会議で必ず抜ける。
  判断は **`isDialogueKind()`**(会話 + 会議)1か所。
  「記事 / 会話 / 会議」の呼び分けも **`bodyWord()`** 1か所
- **会議では2人を選ばせない**(`speakerCountsFor`)。
  2人ならそれはただの1対1の会話である。
  会議に切り替えたときは、人数を3人へ引き上げる。
  引き上げないと、**選択肢に無い値がプルダウンに残って空欄に見える**

### 会話には3〜4人を出せる(会議)

`SPEAKER_COUNTS`(`clipVoices.js`)+ 作る画面の「出てくる人数」。
**上限は4人**(2026-09 利用者の質問への答え)。

- 会話は 14 発言なので、3人なら1人 4〜5 回、4人なら 3〜4 回。
  **5人だと 2〜3 回しか話さず、その人らしさが出ないまま終わる**
- 声は訛り × 男女で選ぶので、**確実に聞き分けられるのは4人あたりまで**
- **入れ物は増やさない。** `materials.voice_ids` の**長さ**が人数である
- 窓口には人数だけでなく**1人あたり何回話すか**まで数えさせる。
  人数だけ言うと「ひとことで消える人」ができる

### 成功と失敗が、同じ見た目で終わってはいけない

生成が終わってもボタンが元に戻るだけで、**失敗したときと見分けが
つかなかった**(2026-08)。時間のかかる処理は、終わったときに
**何ができたのかを、押した場所のすぐ下に出す。**
問数だけでなく中身の一部(最初の1問)も出す。数字だけでは
本当に中身があるか分からない。ボタンの文言も「作り直す」に変える。

### 守らせたい形は、頼むのではなく道具の形で強制する

`generate-material` は `emit_section` という道具に JSON を書かせている。
**形は演習の種類ごとに作り、必要な欄を `required` にする**
(`SECTION_FIELDS`)。`strict: true` なので API が形を保証する。

以前は全種類で1つの形を使い、必須は `answer` だけだった。会話では
`answer` は空文字でよいため、**「話す人の名前だけの項目が2つ」**という
本文の無いものが通ってしまった(2026-08 実機)。音声もシャドーイングも
できない教材になる。「1項目 = 1つの発言」と**指示文には書いてあった。**

**演習の種類を足したら `SECTION_FIELDS` にも足すこと。**
使わない欄を出さないので、余計な出力に課金されない利点もある。

### 本文は「問」で数えない

記事は**段落**、会話は**発言**。`countUnit()` / `countLabel()`
(`src/data/exerciseTypes.js`)を使う。「会話(14 問)」と書くと
14個の設問があるように読める。

### 中身が0件のまま「成功」を返さない。転んだ段階の名前を言う

記事・会話は「本文 → 内容の理解 → 語句」と3回に分けて作る。
①の本文が空のまま②へ進み、②の入口の検査で
**「本文が空です。先に記事か会話を作ってください」**と出ていた。
押したのは「会話を作る」なので、利用者には何が起きたのか分からない
(2026-08)。

- **`items` が0件なら「成功」を返さない。** 終了理由を添えて失敗にする
- Claude の応答は `stop_reason` を必ず見る。`max_tokens` で切られると
  **JSON が途中までになり、`items` が丸ごと欠けた「空の成功」**になる
- 何段階かに分けて作るものは、**転んだ段階の名前を付けて**知らせる

### 失敗の知らせは、その操作をした場所に出す

生成に失敗したとき、知らせを画面のいちばん下に出していたためスマホでは
見えず、「何も起きずにボタンが戻る」ようにしか見えなかった(2026-08)。
**押したボタンのすぐ下に出し、そこまで画面を送る。**
時間のかかる処理は**経過秒数**も出す。動いていることが分からないと
固まったと思われる。

### レベルは14段階ぶん、別々に書く。束ねない

2026-09 利用者の指摘。

> 現状、中級から下のレベルは実際より難しく感じ、
> 中級より上は実際より簡単に感じる傾向があります。

原因は、窓口の「レベルの目安」が**14段階を6行に束ねていた**こと。
`Pre-Basic / Basic` が同じ1行、`C1 / C1+ / C2 / Proficiency` も同じ1行では、
**段を分けた意味がない。** しかも「中学1〜2年程度」のような学年のたとえは
測れないので、真ん中(A2〜B1あたり)へ寄る。

- **14段すべてに、別々の行を書く**(`generate-material` の「レベル(14段階)」)
- **測れる目安にする。** 1文の語数・使ってよい文法・語彙の範囲・1項目の長さ
- **Pre-Basic は「He is happy.」「I am eating lunch.」**(2026-09 利用者の指定)。
  これより長い言い方を思いついたら、それは Pre-Basic ではない
- **両側に逆向きの力がかかる**ので、打ち消す指示を書く。
  下では「もう一段やさしく」、上では「教科書的な平易さに戻らない」
- **作り終えたら語数を数えさせる。** 目安を書くだけでは守られない
- 依頼のたびに「**その段の行だけを見る**」と念を押す。表はキャッシュに
  載っているので、念押しは数十トークンで済む

土台は **CEFR-J**(日本の学習者向けに CEFR を細かく分けたもの)と
**English Grammar Profile / English Vocabulary Profile**(どの文法・語が
どの段で出るか)。**表そのものは窓口に置く。** 画面(`cefr.js`)は
名前だけを持つ。判断を2か所に置かない。

### レベルの選択肢には GSE の数字を出す

`cefrOption()`(`src/data/cefr.js`・2026-09 利用者の指定)。

> 各レベルの選択肢としての表記をよりシンプルで適切なものにしてください。
> 例：A1 (GSE 20-30)など

    A1 (GSE 22-25)      ← プルダウンの選択肢(`cefrOption`)
    A1(入門)            ← 札・見出し(`cefrLabel`。**変えていない**)

**選ぶときと、見るときでは要る情報が違う。**
選ぶときは VERSANT のスコアと突き合わせたいので数字が要る。
**VERSANT は Pearson の試験で、点数(10〜90)は GSE そのもの**なので、
「45点だから B1」と迷わず決められる。見るときは「中級」のほうが早い。

- **数字の出どころは Pearson の公表値**(de Jong & Benigno, 2017)。
  A1 = 22〜29 / A2 = 30〜42 / B1 = 43〜58 / B2 = 59〜75 /
  C1 = 76〜84 / C2 = 85〜90 / A1 未満 = 10〜21
- **「+」の4つは Pearson 自身の区切りと一致する**
  (A2+ = 36〜42 / B1+ = 51〜58 / B2+ = 67〜75)。
  残りは公表された帯を**こちらで半分に割ったもの。取り違えない**
- **札(14個並ぶ)には数字を入れない。** 触れたときの説明(`title`)に置く。
  札に入れると狭い画面で折り返して読めなくなる
- 窓口の「レベル(14段階)」の表にも同じ数字を入れてある。
  **画面と窓口で違う数字を持たない**

### 生成の費用は、ほぼ全額が「出力」側

**いま使っているのは Claude Sonnet 5**(入力 $2 / 出力 $10。100万トークン)。
利用者が実物で確かめて「十分」と判断済み(2026-08)。
**費用や品質を理由に勝手にモデルを変えない。**
`generate-material/index.ts` の `MODEL` 1行で切り替わる。
**変えたら画面側の単価(`PRICE_PER_MTOK`)も必ず一緒に変える。**
片方だけだと、表示される金額が実際と食い違う。
参考: Opus 5 は 入力 $5 / 出力 $25。**入力と出力で桁が1つ違う。**
しかも**考えている時間も出力として課金される。**
入力はキャッシュに載るのでほぼ無料(システム指示は約1,650トークン、
Opus 5 の最小長512を超えているので載る)。

**費用を増やす作りを持ち込まないこと。**
- 同じ内容を2つの欄に書かせない(`audio_text` を `prompt_en` と同じに
  させていて、英文を2回書かせていた)
- 弱点ごとに呼び分けない。1回にまとめる(弱点3つで3倍かかっていた)
- `output_config.effort` は `medium`

費用は画面に出す(`estimateCost`)。**見えない費用は管理できない。**

### 入り口の `index.html` は、古いものを使い回させない

中身(JS・CSS)はファイル名に印が付くので、新しくなれば必ず読み直される。
**`index.html` だけは名前が変わらない**ので、端末に残った古いものが
そのまま使われる。実際、スマホに古い版が残っていて、
**新しく足した「接続を確かめる」ボタンが出ていなかった**(2026-08 実機)。

`<meta http-equiv="Cache-Control" content="no-cache, must-revalidate">` を
置いてある。**それでも残ることがある**ので、URL のうしろに印を足す
(別の URL になるので、端末の控えを必ず素通りする)。

#### 確認を頼むときは、**押せる URL を毎回こちらで作る**

2026-09 利用者の指定。

> それをつけた url を毎回生成してください。

**「`?v=8` を付けてください」と書かない。** 付けるのは利用者の手間だし、
付け忘れれば古い版を見ることになる。**完成した URL をそのまま出す。**

    https://y868hjwmz7-creator.github.io/English-AI-System/?v=<コミット番号>

- **印は、そのビルドのコミット番号(短い方・7文字)にする。**
  番号を数えて付けると**付け間違える**うえ、
  **どの版を見ているかが URL から分からない**
- コミット番号なら、**必ず毎回ちがう値になり**、しかも
  「直したはずが直っていない」ときに、**古い版を見ているのか
  本当に直っていないのか**を切り分けられる
- 出すのは**ビルド(GitHub Actions)が終わってから。**
  終わる前に渡すと、押した人は前の版を見る

### 通信には、必ず「待ち時間の上限」を置く

**`fetch` は待ち時間の上限を持たない。** Supabase まで届かない状況
(電波が切れかけている・会社のネットワークが塞いでいる)では、
**エラーにならず、ただ返ってこない。**

実際、スマホから「押しても何も起きない・止まる」という報告が出た(2026-08)。
`signIn()` に `try` も上限も無かったので、ボタンが
**「ログインしています…」のまま戻らず、知らせも出なかった。**
**成功と失敗が、同じ見た目で終わってはいけない**のと同じ話である。

- 上限は **`supabase.js` の `withTimeout()` 1か所**に置く(15秒)。
  画面ごとに書くと必ずどこかが抜ける
- **例外を外に出さない。** 投げると呼んだ側の `await` がそこで止まる
- **通信が届いていない失敗は、例外ではなく `error` で返ってくる。**
  Chrome は `Failed to fetch`、**Safari は `Load failed`** と言う。
  見分けないと「つながりましたが断られました」と出て、
  **電波の話なのに設定の話だと思わせる**
- **経過秒数を出す。** 「ログインしています…」だけでは、動いているのか
  固まったのか分からない。5秒を過ぎたら「まだ返事がありません」も出す
- ログイン画面には**「接続を確かめる」**と**版**を出す。
  ログインできない人は、通信が届いていないのか ID が違うのかを
  切り分けられず、**どの版を見ているかも確かめられない**
  (版はこれまでフッター=ログインしたあとにしか無かった)

### 外部の窓口を自動で呼ぶものには、「止まる条件」を持たせる

教材を開いた時点で語の意味を先読みしている(`prefetchGlosses`)。
**Anthropic の残高が切れたとき**、開くたびに10語ずつ呼びに行き、
直らないことのために待たされ続けた(2026-08 実機)。

窓口の応答に `fatal` を付け、残高切れ・鍵ちがいのときは
**その画面のあいだ先読みをやめる。** 触れたときの1語引きは止めない。

断りの英語(`{"type":"error",...}`)をそのまま画面に出さない。
`humanApiError()` で日本語にし、**どこへ行って何をすればよいかまで書く。**

### ゲストには、仕組みの内側を見せない

「Claude の残高が足りません」「鍵が違います」は**ゲストに出さない。**
ゲストにできることは何も無く、スクールの内側の話でしかない。
窓口は `error`(誰に見せてもよい文)と `detail`(原因と直し方)の
**両方**を返し、どちらを出すかは画面側(`src/lib/viewer.js`)が決める。
**役割の判定を窓口と画面の2か所に置かない。** 必ず食い違う。
既定は「見せない」。役割が分からないうちは内側の話を出さない。

### 関数ごとに使うモデルが違う。取り違えない

| 関数 | モデル | なぜ |
|---|---|---|
| `generate-material` | `claude-sonnet-5` | 教材の質が要る |
| `lookup-word` | `claude-sonnet-5` | 質を優先。**遅さの原因は別にあった**(下記) |
| `speak` | Azure Speech(Claude ではない) | 音声合成。無料枠は毎月50万文字 |

`lookup-word` を一度 `claude-haiku-4-5` にしたが、
**利用者の実測で「質には満足だが、速くはなっていない」**(2026-08)。
遅さの正体はモデルではなく**関数の立ち上がり**(SDK の読み込み)だったため、
`claude-sonnet-5` に戻し、代わりに SDK を外して `fetch` で直接呼ぶようにした。
**モデルを速さの調整つまみとして使わない。**

**Haiku 4.5 は `output_config.effort` に対応していない。** 付けると失敗する。
指示のキャッシュも4,096トークン以上でないと載らない(黙って載らないだけ)。
`lookup-word` のモデルを変えるときは、`effort` の指定も一緒に見直すこと。

### Supabase の関数は CPU 2秒で止まる

150秒とは別に、**1回の呼び出しで使える CPU 時間が2秒**しかない。
英文を数値に変換する処理(`gte-small`)は CPU を使うため、まとめて行うと
`CPU Time exceeded` で関数ごと落ちる。**落ちると応答に CORS の印が付かず、
画面には「窓口につながりません」としか出ない**ため、未配置と区別がつかない
(2026-08 実機のログで確定)。

`check-similar` では、1回に変換するのを
**候補12件 + 追いつき5件まで**に制限してある。**この数を勝手に増やさないこと。**
0009 以降に作った教材の英文は作られた時点で変換・保存されるので、
追いつきが要るのは 0009 より前の教材だけである。

### Supabase の関数は150秒で切られる

Edge Function は150秒何も返さないと切断される。Claude の生成は
これを超えることがあるので、**答えが出るまで空白を5秒ごとに送り続ける**
(`generate-material`)。空白は JSON の前に付いても読み飛ばされる。
Anthropic 側も **streaming で受け取る**。`output_config.effort` は
`medium`。既定の `high` は40問で3分近くかかる。

### 画面の骨組みは「左のメニュー + 本体」

`AppNav.jsx`(メニュー)+ `AppTopbar`(上の帯)+ `.app-shell`(styles.css)。
**画面の一覧は `App.jsx` の `pages` 1か所。** メニューも帯の名前もここを見る。
**画面を足したら `pages` に足す。** 2か所に書くと並びと呼び名が食い違う。

| 画面の幅 | 形 |
|---|---|
| 1024px 以上(PC) | 押し出して並ぶ。☰ で**細くたたむと絵だけ**になる |
| 1024px 未満(スマホ・パッド) | 隠れていて、☰ で**かぶせて開く** |

- **判断は幅だけ**(`useWide()`・`src/lib/nav.js`)。UA も `pointer` も見ない
- **たたんだかどうかは覚える**(PC のみ。狭い画面では毎回閉じた状態から)
- かぶせて開いているときの閉じ方は**4つとも**用意する
  (外側・✕・Esc・項目を選ぶ)。1つしか無いと閉じ方を探すことになる
- **同じことをするボタンを2つ見せない。** PC の ☰ は帯にだけ置く
  (メニューの中にも置いたら、違いを探すことになった)
- メニューの絵は `Icons.jsx`。**同じ枠(20×20)・同じ線の太さ(1.5)**でそろえる
- **一度決める設定は、メニューの下に置く。** 配色・色づかいは
  上に出しっぱなしにすると、スマホで題名と同じ幅を食う

### どの画面にも効く決まりは、まとめて書く

`styles.css` の「どの画面にも効く決まり」の節に置く。
部品ごとに書いて回ると、新しい部品で必ず抜ける。

- **横には揺らさない**(2026-09 実機・利用者の指定)。

  > スマホやパッドでスクロールすると、横方向にゆらゆらと画面がズレる

  横にはみ出す中身は**その場で直すのが本筋**である(この回の原因は
  紙に敷いた板が自分の幅を測っていたことと、練習の入口が3つになって
  1行に収まらなくなったこと。どちらも実測して直した)。
  そのうえで `html, body { overscroll-behavior-x: none }` を置き、
  **端まで来たときの跳ね返りと、親へ送りが伝わるのを止める。**
  中身を切り落とさないので、横に送れる箱(タブ・表)はこれまでどおり送れる。

  **横のはみ出しは、実測でしか見つからない。** 見つけ方は
  「その箱の `scrollWidth > clientWidth` か」と
  「中の箱の右端が、外の箱の内側の右端より右にあるか」の2つ
- **選んでいる印を、べた塗りにしない。**「黒と濃い水色の塗りつぶしがダサい」
  (2026-08)。**うすい地色(`--accent-soft`)+ 同じ色の文字 + 枠線 + 太字**の
  4つで示す。色だけに頼らないので、プレインでも見分けられる
- **合っている=緑(`--ok`)/ まちがい=赤(`--ng`)。** ただし
  **地色と線(取り消し線・波線)も必ず併せる。** 緑と赤は見分けにくい人がいる
- **`:focus-visible` で輪を出す。** `:focus` だとマウスで押しただけでも出る
- **ボタンは 40px を割らない**(`.btn`)。小さくするものは `btn--small` で明示する
- **押し分けが要るボタンは3段で持つ。** 塗りつぶしを増やさない

  | 何 | 見た目 | 例 |
  |---|---|---|
  | 進む・正解 | `btn--primary`(青の塗りつぶし) | 覚えた / 言えた |
  | もう片方の答え | `btn--quiet`(**灰色の塗りつぶし**。`--border`) | まだ / 知らなかった |
  | 答えではない操作 | `btn--ghost`(枠線だけ) | 意味を見る / 英語を見る |

  **ボタンの色は専用の変数で持つ**(`--btn-bg` / `--quiet-bg` / `--on-accent`)。
  `--surface-0`(ページの地色)を使っていたので、暗い配色では**カードより
  暗くなり、黒い穴のように見えた**(2026-08「黒背景に黒ボタンは殺風景」)。
  **ボタンはカードより少しだけ明るく。** 青の塗りつぶしの文字も、
  暗い配色では白ではなく濃い色にする(うすい水色に白は薄い)

  **「まだ」を赤くしない**(2026-08「赤がいらないです」)。
  知らないことは失敗ではなく、語そのものに付く赤い線がその役目を担っている。
  `btn--ghost` を塗りつぶさないのは、暗い配色で**黒い箱に見える**ため
- **860px 以下では入力欄を 16px にする。** それ未満だと iOS が勝手に拡大する
- **札(chip)を `white-space: nowrap` にしない。** 弱点の名前は
  「間の取り方 + つなぎ言葉 + 決まり文句」のように長く、**札1つが
  囲みからはみ出す**(2026-09 実機・Safari)。`max-width: 100%` と
  `white-space: normal` にすれば ` + ` のところで折り返る。
  短い札(C1・食べ歩き)は、もともと1行なので見た目は変わらない
- **余白は「重なっているものをまとめて」見る。** 画面の余白(16px)→
  カード(20px)→ 見出しの囲み(12px)で**左右 48px ずつ**、
  iPhone(390px)では**画面の約1/4が余白**になっていた
  (2026-09 実機「文字が画面端から隙間が多すぎ」)。
  560px 以下では3つとも詰めて 32px にしてある。
  **1か所だけ詰めても効かない**
- **AI が付ける文字列(教材名・英語の見出し)には `overflow-wrap: break-word`。**
  長い1語が来ても囲みから出さない
- **実機で「文字が大きい」と言われたら、まず computed の px を測る。**
  こちらの指定と食い違うなら、**原因は端末側にある。**
  `-webkit-text-size-adjust: 100%` はすでに置いてあるので、
  疑うのは端末の「表示を大きく」の設定と、Safari のページズーム
  (サイトごとに残る。アドレスバー左の「ぁあ」で確かめられる)。

  **実際にそうだった**(2026-08)。写真では 12.8px の見出しが 16px 相当に
  見えており、こちらの指定より約 1.25 倍だった。
  利用者の端末が「表示が大きくなる設定」で、**アプリ側は正しかった。**
  一度は小さくして入れてしまい、そのあと戻した。

  **測らずに小さくして回らない。** 全部の画面が小さくなり、
  設定を戻した人には今度は小さすぎる
- **`prefers-reduced-motion` を尊重する。** 滑る動きが苦手な人がいる
- 影は `--shadow-1` / `--shadow-2`。**色と同じく変数で持ち、3か所すべてに置く**

### 画面まわりの決まり

- **「面の段差」で情報を分ける。明るい側も暗い側も同じ考え方にする。**
  地(`--surface-0`)→ カード(`--surface-1`)→ その中の面(`--surface-2`)に
  **目に見える差**を付ける。枠線と影も一段強くして、カードの輪郭を出す。

  | | 地 | カード | 中の面 | 枠線 |
  |---|---|---|---|---|
  | 明るい | `#eaecef` | `#ffffff` | `#f0f2f4` | `#d5d9de` |
  | 暗い | `#0c0c0b` | `#1c1c1a` | `#2b2b28` | `#46463f` |

  以前は明るい側が `#f7f6f3` → `#fcfcfb`(差 5)、暗い側が
  `#131312` → `#1a1a19`(差 7)で、**どこがカードか分からなかった**
  (2026-08「全体的に見にくい」)。
  **片方だけ直さない。** 同じ見づらさは両方の配色で起きる
- **明るい側の地色に、茶み(暖色)を入れない。**
  段差を付けたとき暖かいベージュ寄りにしたところ、
  **「この茶色の背景が好きになれません」**(2026-08 実機)。
  段の深さはそのままに、**灰色**へ寄せた(`#eceae4` → `#eaecef`、
  `#eeece6` → `#f0f2f4`)。
  **同じ色を使っているところは、まとめて直す。**
  紙(`.lesson-sheet`)の `--paper` / `--rule` / `--qa-bg` / `--qa-line` /
  `--qa-answer` / `--quiet-bg` と、吹き出しの `--pop-rule` が同じ系統だった。
  1か所だけ直すと、そこだけ色味が違って残る
- **配色は3通り**(自動 / 明るい / 暗い)。`src/lib/theme.js` が
  `<html>` の `data-theme` を切り替える。色の変数を足すときは
  **`styles.css` の3か所すべてに書く。**
  `:root` / `@media (prefers-color-scheme: dark)` / `:root[data-theme="dark"]`。
  **1か所でも抜けると、そこを選んだときだけ色が食い違う。**
  実際、暗い側を media にだけ2回書き、`[data-theme="dark"]` に書き忘れて
  吹き出しの帯が明るいまま残った(2026-08)。**確認は明るい・暗いの両方で行う**
- **色を使うかどうかは選べる**(カラー / プレイン)。`src/lib/palette.js` が
  `<html>` の `data-palette` を切り替える。プレインは
  `:root[data-palette="plain"]` で**変数だけを差し替えて**色を消している。
  したがって**色は必ず変数で持つこと。** 値を直接書くと、プレインを
  選んだときだけその色が残る(紙の中だけは例外。常に白なので直接書く)。
  **ボタンやタブの青は消さない。** あれは「押せる」という合図である
- **読み上げ中の色は「文」単位。** フルストップからフルストップまでを
  まとめて光らせる(2026-08 利用者の指定)。語ごとに動かしていたが、
  合図(`boundary`)を出さない端末では見積もりに頼るしかなく、
  **1語ずれると目に見えて気持ちが悪い。** 文の単位なら、多少ずれても
  「いまこの文を読んでいる」は正しいままである。
  **精度を上げるより、外れても困らない見せ方を選ぶ。**
  文の切り方は `splitSentences()`(`src/lib/wordTiming.js`)1か所
  - **色は文ぜんぶを囲む箱1つ**(`.etext-sent`)に付ける。語や空白に
    別々に付けると帯が階段状になる。`<button>` は `display: inline` を
    指定しても効かず、Chromium では `inline-block` になるためである
    (実測で語の箱 31.6px / 空白の箱 21.0px)。`.etext-run` と同じ考え方
  - 箱は**ただのインライン**にする。`display` を変えると折り返し方が変わる
  - 位置の元になる合図は2段。① ブラウザの `boundary` があればそれに従う
    ② **来ない端末では時間から見積もる**(`attachWordTracking`)。
    利用者の会社PC(Windows / Chrome)の英語の声は3つとも Google の
    「通信を使う声」で、**3つとも合図を出さない**(mic-test.html で 0 回を実測)。
    MP3 なら `audio.duration` で長さが分かるので、そのぶん正確になる
- **狭い画面では、めったに触らない操作をしまう。** レッスン表示の操作欄が
  スマホで4段になり、**画面の4割**を占めていた(2026-08 実機)。
  いつも要るのは「閉じる・ページ送り・解答」の3つだけ。
  速さ・配色・文字の大きさ・印刷は「表示」の札に畳む(860px 以下)。
  **パソコンでは畳まない。** 札も出さない
  - **端末の種類を当て推量しない。** UA も `pointer` も見ず、
    **見えている幅だけ**で決める。横向きにすれば広い扱いになるのも素直
  - **いつも要る操作は、1つの囲みにまとめて折り返させない**
    (`.lesson-bar-main` に `flex-wrap: nowrap`)。帯ぜんぶを折り返させて
    いたので、iPhone(390px)で「表示」だけが2段目に落ち、
    **帯の高さが2倍**になっていた(2026-08 実機)。紙がそのぶん狭くなる。
    詰め方は**言葉を短く → 余白を詰める → 絵だけにする**の順で、
    320px でも1行に収まることを実測してある
  - **一度決める設定は覚える。** 文字の大きさを覚えていなかったので、
    開くたびに操作欄を触ることになっていた(それ自体が原因の一つ)
  - Esc は**開いているものから閉じる。** いきなり画面ごと閉じない
- **レッスンで使う表示**(`LessonView.jsx`)は画面共有用。
  **暗い配色を選んでいても紙は白のまま。** 共有先で見やすいことが優先。
  したがって**紙の中の文字色は決め打ちにする。** `var(--text-primary)` の
  ままだと、暗い配色のとき白い文字が白い紙に載って消える(2026-08 実機)。
  **文字色だけではない。** `<select>`・入力欄・札・ボタンも同じで、
  端末の色のままだと白い紙に黒い箱が乗る(2026-08 実機)。
  **部品ごとに打ち消して回らない。** `.lesson-sheet` で
  `--surface-*` / `--border` / `--text-*` / `--btn-bg` ごと差し替え、
  **紙の中を「明るい配色の島」にしてある。**
  そうすれば、紙の中に何を置いても自動的に明るい側の色になる。
  **色の変数を足したら、紙にも足す。** 名前が1つ違うだけで暗い色が残る
  (`--speak-bg` は紙にあったが、6Steps が使うのは `--speaking-bg` で、
  シャドーイング中の段落が**真っ黒**になっていた・2026-08 実機)。
  確かめ方は「暗い側にあって紙に無い変数」を洗い出すこと
- **色が付くときだけ余白を足さない。行が横に動く**(2026-09 実機・利用者の指摘)。

  > 普通のモードで再生されている時と通常時で数px横に表示がズレる

  狭い画面の紙で、**読み上げ中の行にだけ `padding-left` を足していた**
  (色の帯が番号の丸にかぶらないように)。そのぶん行が右へ動いていた
  (実測 6px)。**はじめからその余白で置く。**
  同じ注意は `.passage-part` の側にコメントで書いてあったのに、
  こちらで破っていた。**片方に書いた注意は、もう片方でも確かめる**
- **内側に敷いた帯は、角を丸めると両端が細る**(2026-09 利用者の指定
  「この左側の色の線は真っ直ぐにして欲しいです」)。
  `border-radius` は `box-shadow: inset` にも効く。
  左だけ角を落とさない(`border-radius: 0 6px 6px 0`)。
  **文字が太くなるのは構わない**(利用者の判断)
- **会話は、番号と話す人の名前で1行を使う。** 本文は次の行から左端にそろえる
  (`.lesson-speaker + .lesson-en { clear: left }`)。スマホでは番号を
  `float: left` で浮かせているが、丸は話す人の行より**数 px だけ背が高い。**
  そのはみ出しに1行目が回り込み、**発言ごとに1行目だけ字下げ**されていた
  (2026-08 実機)。記事は話す人がいないので、これまでどおり番号の横から
- **一覧は「押せる」ことが見て分かる形にする。** 宿題が下の小さなボタンで
  しか開けず、ただの一覧に見えていた(2026-08)。行そのものを押せるようにし、
  ▸ / ▾ の印を出す

### 指導ポイントの見た目は、指示があるまで変えない

`TeachingNote.jsx` と `.teaching-*`(2026-08 利用者の指定)。

> 教材の指導ポイント内のデザイン、とても良い感じです。
> 今後ここは指示がない限り絶対に変えないでください。

**似た作りの場所をまとめて直すときも、ここは対象から外す。**

### ③ オーバーラッピングは「2〜3語うしろから追いかける」

**「同時に重ねて読む」ではない**(2026-08 利用者の指定)。

> オーバーラッピングは音と同時に喋るではなく、音から2〜3単語遅れて、
> 耳に入り聞いて確認してから真似るのが正しいやり方です。

同時に声を出すと、自分の声でお手本が聞こえなくなる。
少し遅れて追いかけるから、耳で確かめてからまねられる。
⑤ シャドーイングとの違いは、**本文を見るか隠すか**だけである。

### 6Steps は「同じ本文への取り組み方」。演習として分けない

`src/lib/sixSteps.js` に6つの定義がある。①ディクテーション ②スラッシュリーディング
③オーバーラッピング ④意味音読 ⑤シャドーイング ⑥リピーティング。

**別々の教材ではない。** 分けたために「長文」が16個の短文になった
失敗がすでに一度ある。切り替えは `PassagePractice.jsx` の中に置く。

- **単位が2通りある。** ①②④⑥ は**1文ずつ**、③⑤ は**本文まるごと**。
  1文ずつのステップは `sentencesOf()` で段落・発言をほどく
  (切り方は読み上げと同じ `splitSentences()`。**2か所に持たない**)
- **⑤ シャドーイングは本文を隠す。** ③との違いはそこだけである。
  隠しても**行の場所は残す**(消すと段落がいくつあるか分からなくなる)
- 日本語は**段落ぶんしか無い。** 文ごとの訳は付けられないので、
  段落の訳に「段落の訳」と札を付けて出す。**無いものをあるように見せない**
- ステップを足したら `SIX_STEPS` に足す。**画面に書き足さない**

### 練習の行は「6Steps → Quick Response → 集中モード」。**どの画面でも同じ**

2026-09 利用者の指定。

> トップの画面でのボタンの並びを左から「6Steps」「Quick Response」
> 「集中モード」にして、ディクテーション画面でもスラッシュリーディング
> 画面でも同じにしてください。
> そして、スラッシュリーディングやディクテーション、その他のトレーニングの
> タブの左の 6Steps の文字を削除してください

- **並びを変えない。** 6Steps を開いても、閉じても、同じ3つが同じ順で出る。
  **場所が動くボタンは、探すことになる**
- **集中モードは、6Steps の最中も同じ行に出す。** そのときは
  **いまの取り組み方**の集中モード(1文ずつ / 1発言ずつ)に入る。
  開いているかどうかは**レッスン表示の側が持ち**、
  `PassagePractice` に渡す(`focus` / `onFocusChange`)。
  **中にも同じボタンを置かない** — 1つの画面に同じものが2つ出る
- **プルダウンの左の「6Steps」は出さない。** 上のボタンの行に「6Steps」が
  あり、押して開いた先なので、ここでもう一度名乗る必要がない。
  **読み上げのための名前(`aria-label`)は残す**

### 単位の言葉は、教材の形から決める(段落 / 発言)

2026-09 実機・利用者の指定。

> これは、会話だから「段落ごと」ではなく「発話ごと」で分けてほしいです。
> 会議も同じく。記事だと段落ごとです。

記事は**段落**、会話・会議は**発言**で数えるのは、もともとの決まり
(`countUnit()`)。ところが ② スラッシュリーディングの「単位」だけ
**「段落ごと」と決め打ち**にしていたので、会話でも「段落ごと」と出ていた。

- `slashUnitsFor(isDialogue)`(`sixSteps.js`)1か所で決める
- **会議も本文の演習は `dialogue`** なので、これ1つで両方に効く

| 教材 | 選択肢 |
|---|---|
| 記事 | 段落ごと / 文章全体 |
| 会話・会議 | **発言ごと / 会話全体** |

- **残っていた2か所も直した**(2026-09 利用者の指定「『発言』にしましょう」)。
  「段落の訳」の札(`slash-ja-label`・3か所)と、
  ③⑤ の「段落で区切らない」。
  **言葉は `bodyUnitWord(isDialogue)`(`sixSteps.js`)1か所で決める。**
  出す場所は4つあるので、書き写すと必ずどこかが「段落」のまま残る
  (実際、`slashUnitsFor()` で選択肢だけ直したときに残っていた)

### 6Steps にも集中モードがある。**取り組み方ごとに1つずつ**(`StepFocus.jsx`)

2026-09 利用者の指定。

> 6steps全てに集中モードを作ってください。今は形式的にはボタンがあっても、
> 押すとトップの単語チェックのページの集中モードに飛ばされてしまいます。
> ディクテーションやスラッシュリーディングでこれをできるようにしてください

`PassagePractice` の中の「集中モード」は、押すと `FocusReader`
(**本文を読んで語を調べる**画面)を開いていた。書き取りの途中でも、
区切りを入れる途中でも、**同じ「語を調べる画面」に飛ばされる。**
いま取り組んでいることが、そこには無い。

- **骨組みは `.focus` をそのまま使う**(`FocusReader` と同じもの)。
  足すのは「上の帯の右に 6Steps のプルダウンを置く」1点だけ
- **中身は作らない。** ふだんの画面(`PassagePractice`)の中身を
  **そのまま渡す。** 書き写すと片方だけ古くなる(単語帳で踏んだ失敗)
- **1つだけ渡す。** 部品(`StepDictation` / `SlashReading` / `StepSentence`)は
  一覧を受け取る作りなので、**長さ1の一覧**を渡すだけで1つずつになる。
  描き方を変えていないので、見た目も操作もふだんと同じ

  | 取り組み方 | 単位 | 何を渡すか |
  |---|---|---|
  | ① ディクテーション | 文 | `groupSentences()` の1かたまりに入っている文 |
  | ② スラッシュリーディング | 段落 / 発言 / 文章 | `slashBlocks` の1つ |
  | ③⑤ 本文まるごと | 段落 / 発言 | `section.items` の1つ |
  | ④⑥ 1文ずつ | 文 | `sentences` の1つ |

- **数え方を2通り持たない。** ①は `StepDictation` と同じ `groupSentences()` を
  通してから数える(難易度を変えると数が変わる)
- **取り組み方・単位・難易度を変えたら、先頭へ戻す。** 個数が変わるので、
  前の番号をそのまま使うと別の場所に飛ぶ
- **6Steps はこの画面の中で切り替えられる。** いちいち出て、選び直して、
  また入る…では続かない
- **プルダウンは縮める。** `<select>` は**いちばん長い選択肢**の幅になるので
  (「② スラッシュリーディング」)、iPhone(390px)で 20px はみ出し、
  **画面が横に送れる状態**になっていた(実測 410 > 390)。
  `min-width: 0` で縮め、入らないぶんは「…」で切る。
  **番号(①〜⑥)は必ず見える**ので、どれを選んでいるかは分かる
- **レッスン表示では、ボタンは上の行にある1つだけ**(2026-09 利用者の指定で
  並びをそろえた)。6Steps を開いていなければ**本文を読んで語を調べる**
  集中モード、開いていれば**いまの取り組み方**の集中モードに入る。
  **役目は違うが、押す場所は1つ**である
- **紙の幅を引き継ぐ**(`focusWidth`)。120% で読んでいた人が、
  入った瞬間に 100% に戻っては落ち着かない。
  数字は `FocusReader` と同じ `.focus--w*`(**2か所に持たない**)
- **1枚のカードにする**(単語帳の集中モードと同じ考え方)。
  地の上に面(`--surface-1`)を置くと、
  どこからどこまでが「いま向き合うもの」かが分かる。
  1つしか出さないので**下の区切り線は引かない**(続きがあるように見える)。
  カードにする前は「1文しか無いのに画面いっぱいへ散らばって」見えていた
  (2026-09 実機「UIとデザインが崩れています」)
- **幅は、もとの紙と同じ。`margin: 0 auto` を必ず残す**(2026-09 利用者の指定)。

  > 画面幅を広げると、コンテンツがどんどん左に寄ってしまうのを改善して
  > ください。これは全ての集中モードで統一にしてください。
  > 元々の幅に合わせてください。

  一度 `min(100%, 48rem)` で頭打ちにしたが、**紙より細くなる。**
  上限は `.focus--w*`(紙の幅)だけが決める。
  **実測: 1430px でも 2200px でも、左右の余白が同じ**(199 / 584)
- **中は「濃く、色でメリハリ」**(2026-09 実機
  「全体的にコンテンツの文字などが薄すぎて少し見づらいです」)。
  1つだけに向き合う場所なので、紙の一覧より一段はっきりさせる。
  話す人は**紙と同じように色で分け**(`--voice-a` / `--voice-b`)、
  番号は**うすい青の丸**(`--accent-bg` + `--accent`)、
  「速さ」などの見出しはうすい灰色をやめる。
  **色はすべて変数から取る**(プレインを選べば、そのまま灰色になる)
- **番号は、どのページでも出す**(2026-09 利用者の指定
  「どのページでも段落番号とか全て入れてください」)。
  ②スラッシュリーディングと③⑤の本文にも足した。
  **`startNo` を外から渡す** — 集中モードでは1つしか渡さないので、
  そのままだと何番目でも「1」になる
- 集中モードでは、**見出し・やり方・6Steps の欄・音声認識の断り書きは出さない。**
  何度も読むものではないので、向き合っている場所をそのぶん狭めない。
  **速さ・本文を出す(⑤)は残す** — その取り組み方の最中に要る操作である

### 集中モードは「黒い地の上に、白い紙」。**普通の画面と同じ形**

2026-09 利用者の指定。**2手かかっている。経緯ごと残す。**

> 濃さについては、普通の画面と同じように、左右の空白部分は黒にしてください。

集中モード(`FocusReader` / `StepFocus`)は**レッスン表示の紙の中**から
呼ばれる。紙は `--surface-*` などを差し替えて**明るい配色の島**にしてある
ので(`.lesson-sheet`)、そのまま描くと、**画面ぜんぶを覆うこの画面まで
紙の白**になっていた。暗い配色を選んでいても、左右の余白が真っ白になる。

はじめは `createPortal` で body の直下に出しただけにした。地は黒くなったが、
**中身まで暗くなった。** これは言われたことではなかった。

> 集中モード、全てを黒にしてほしかった訳ではないです。
> 6Step モードではない普通のモード時は、真ん中に紙があり、
> コンテンツは基本白ベース、幅により余る左右のスペースが黒。
> このようにして欲しいのです。

**「普通の画面と同じ」とは、地が黒いことではなく、
黒い地の上に白い紙が1枚あることだった。**

- **`createPortal` で body の直下に出す**(地はアプリの配色に戻る)。
  吹き出し(`.etext-pop`)を紙の外に出しているのと同じ理由である。
  こうすると**どこから入っても集中モードは同じ見た目**になる
- **中身は `.focus-paper` に載せる。** 紙の変数の島(`.lesson-sheet` の
  `--paper` / `--ink` / `--surface-*` …)を**選択子で分け合う。**
  `.lesson-sheet, .focus-paper { … }` と書けば、色は1か所のままである。
  **書き写すと、片方だけ古くなる**
- **置き方は分ける。** 送る箱なのは `.lesson-sheet` だけで、
  集中モードは外側(`.focus-body`)が送る
- **紙にしか無い色は、アプリ側にも置く。** 番号の丸(`--accent-bg`)と
  話す人(`--voice-a` / `--voice-b`)は `.lesson-sheet` の中にしか
  無かった。`.num-badge` のように**紙の外でも使う部品**があるので、
  **`styles.css` の3か所すべてに書く**(`:root` / media / `[data-theme]`)。
  プレインの側にも足す
- **`var(--qa-line, var(--border))` のように控えを書いてある変数は、
  そのままでよい。** 紙の外でも成り立つように作ってある
- **「紙の上か」を見ている場所も、一緒に数える。** 語の意味の吹き出しは
  `anchorEl.closest('.lesson-sheet')` で明るい色に切り替えていたので、
  紙の外へ出した瞬間に**白い紙の上へ暗い吹き出しが載る**ようになった。
  `.focus-paper` も同じ扱いにする(`GlossPopover`)。
  **紙を増やしたら、「紙かどうか」を尋ねている場所を探すこと**
- **白い紙があると、中央にあることが目で分かる**(2026-09 実機
  「集中モードを押すとコンテンツが左に寄っていて、中央に配置されません」)。
  実測では**前から中央にあった**(1440px で左右 204px ずつ)。
  ところが**黒い地の上に文字だけが載っていた**ので、
  短い1文では左に寄っているようにしか見えなかった。
  **「中央かどうか」は、囲みが無いと見えない**

### 紙の周囲は、配色によらず黒。**暗い配色では紙も黒**(2026-09 利用者の指定)

> 配色が「暗い」「明るい」どちらが選択されているかにかかわらず、
> 紙の周囲は黒で統一でお願いします。
> つまり、配色が「暗い」の時は紙も黒です。

**上の節(「黒い地の上に、白い紙」)を、利用者の指定で一段進めたもの。**
以前は地が `var(--surface-0)` だったので、**明るい配色では周囲まで明るい灰色**
だった。「黒い地の上に紙が1枚」という形は、配色によらず同じであってほしい。

| 配色 | 地 | 紙 |
|---|---|---|
| 明るい | **黒** | 白(これまでどおり) |
| 暗い | **黒** | **暗い**(カードと同じ色) |

- 地は **`.focus--sheet`** に**決め打ち**(`.lesson-bar` と同じ作法)。
  `FocusFrame` が付けるので、**紙のある集中モードだけ**に効く。
  **単語帳の集中モード(`.wbfocus`)には効かせない** — あちらには紙が無く、
  言われたのは「紙の周囲」の話である
- **レッスン表示の紙(`.lesson-sheet`)は1ドットも変えていない。**
  あちらは画面共有用で、「暗い配色を選んでいても紙は白のまま」という
  **別の決まり**がある(共有先で見やすいことが優先)
- **紙を完全な黒にはしない**(地 `#0c0c0b` に対しカード `#1c1c1a`)。
  段差が無いと**紙がどこからどこまでか見えなくなる**
  (「白い紙があると、中央にあることが目で分かる」の裏返し)

#### 暗くする方法 — **色の値を書き写さない**

紙は `--surface-0` などアプリと**同じ名前**を差し替えた島である。
暗い配色で戻そうとして `--surface-0: var(--surface-0)` と書くと、
**自分を参照する輪**になって黙って効かなくなる。

だから**紙の外(`.focus`)でアプリの色を別名(`--app-…`)に控え、**
`.focus-paper` はそこから取る。**暗い側の色を1つも書き写していない**ので、
配色を直せば集中モードの紙も一緒に直る。

- 島に変数を足したら、**暗いときの戻し(2か所)にも足す。**
  `:root[data-theme="dark"]` と `@media (prefers-color-scheme: dark)` の両方
- **`npm run test:paper` が見張っている。** 抜け・食い違い・値の書き写し・
  別名の取り違え・地が黒いかを、機械的に確かめる

#### 紙にかかる指定で、**色を決め打ちしない**

**この検証で実際に3つ見つけた**(2026-09)。

| どこ | 何が起きていたか |
|---|---|
| `.lesson-sheet .btn, .focus-paper .btn` | `background: #fff`。**白い錠剤が黒い紙に載る** |
| `.focus-paper select / input / textarea` | 同上 |
| `.qr--paper` の5色 | `--qr-dim: #55554e`。**黒い紙に黒い「英語を見る」** |

どれも「紙は暗い配色でも白のまま」だった頃の名残である。
**`var(--btn-bg)` / `var(--ink-soft)` のように紙の変数から取れば、
レッスン表示の紙(島の値=明るい)と集中モードの紙(配色に従う)の
両方が1つの指定で正しくなる。**

**語の意味の吹き出しも同じ**(`GlossPopover`)。
`.focus-paper` の上では **`etext-pop--paper` を付けない** —
何も足さなければアプリの配色に従い、紙とそろう。
**レッスン表示の紙の上では、これまでどおり明るい色に決め打ちする。**

### 集中モードは「いま取り組んでいること」を1つずつ出す。**別の練習へ移さない**

2026-09 実機・利用者の指摘。**同じ取り違えを2度した。**

> Quick Response で集中モードを押すと違うトレーニングになってしまいます。

6Steps でまったく同じことを言われている(「押すとトップの単語チェックの
ページの集中モードに飛ばされてしまいます」)。あのときは 6Steps だけを直し、
**Quick Response を数え忘れていた。**

**集中モードは「取り組み方」ではない。** いま開いているものを
**1つずつ画面に固定する見せ方**である。だから
**練習を開いているあいだは、その練習の集中モードに入る。**

| いま開いているもの | 集中モードで出るもの |
|---|---|
| 何も開いていない | 本文を読んで語を調べる(`FocusReader`) |
| 6Steps | いまの取り組み方を1つずつ(`StepFocus`) |
| Quick Response | **その1問**(`QuickResponse` の `focus`) |

- **開いているかどうかは、ボタンのある `LessonView` が持つ。**
  中にも同じボタンを置かない(1つの画面に2つ出る)
- **Quick Response には下の帯(◀ 前 / 次 ▶)を出さない。**
  「まだ / 言えた」で進むので、進め方が2つになる
- **練習をやめたら閉じる。** 開きっぱなしだと、次に開いた瞬間に
  いきなり集中モードで始まって驚く

### 集中モードの骨組みは `FocusFrame.jsx` 1つ。**3か所に書き写さない**

集中モードは3つある(`FocusReader` / `StepFocus` / Quick Response)。
`.focus` の骨組み(portal・上の帯・白い紙・下の帯・出るボタン・
書き込む / メモ・うしろを動かさない)は**まったく同じ**なので、
`FocusFrame` 1つに置く。**書き写すと、必ずどこかだけ古くなる**
(単語帳で `LearnerWordbook` を別に持って踏んだ失敗)。

- 渡すのは **`top`(帯の中身)・`topEnd`(右端)・`bar`(下の帯)・中身**だけ
- **`bar` を渡さなければ、下の帯は出ない**(Quick Response)。
  効かない場所を作らない
- **キーボードは各画面が持つ。** Esc も矢印も画面ごとに違う
  (まとめを先に閉じる / 文字を打っている最中は横取りしない / 矢印で送らない)。
  **同じに見えて同じではないものを、無理に1つにしない**
- `.focus-paper` は**フレームが必ず1枚だけ**巻く。
  中身の側にも付けると、余白が二重になる
- **速さ・文字・幅・印刷(`settings`)も渡す**(下の節)。
  **書き込む / メモと同じかたまり**に入り、右へ寄る

### 集中モードにも「書き込む」と「メモ」を置く(`FocusBoard.jsx`)

2026-09 利用者の指定「集中モードでも上部のバーに『書き込む』や『メモ』を
配置してください」。

集中モードは**1つだけに向き合う場所**である。線を引きたくなるのも、
気づいたことを残したくなるのも、まさにその最中なのに、
この2つは**レッスン表示の帯にしか無かった。**

- **状態も見た目も `useFocusBoard()` 1か所。** 集中モードは2つある
  (`FocusReader` / `StepFocus`)ので、書き写すと片方だけ古くなる
  (単語帳で `LearnerWordbook` を別に持って踏んだ失敗)
- **色・道具・太さは `src/data/inkTools.js`。**
  レッスン表示と集中モードで同じものを出す
- **板(`InkLayer`)は送る箱(`.focus-body`)の中に敷く。**
  外に置くと、送ったときに線だけが取り残される
- **書き込みのあいだは、上の帯をまるごと入れ替える**(レッスン表示と同じ)。
  2段にすると読むところがそのぶん狭くなる
- **メモは本文の横に並べる**(`.focus-main`)。狭い画面では下に積む。
  出すのは**担当トレーナー(と管理者)だけ** — 判定は `viewer.js` を通す
- **狭い画面では絵だけにする**(`.wide-text`)。ボタンが2つ増えたので
  iPhone(390px)で帯が横にはみ出していた(実測 400 > 390)。
  ペンとメモは**絵が別物**なので取り違えない。
  **読み上げのための名前(`aria-label`)を必ず添える**
- **パソコンでは、送るボタンを Listen のとなりへ寄せる**(利用者の指定
  「PCでは『次』『戻る』ボタンをもっと Listen ボタンの近くに配置して」)。
  スマホでは**親指が届く両端**が正しいが、パソコンでは左右の隅まで離れ、
  まん中の Listen とのあいだが数百 px 空く。
  1024px 以上でだけ `.focus-bar` をまん中にまとめる(**判断は幅だけ**)。
  実測(1440px): 前 632〜704 / 次 736〜808。390px では前 12・次 378 のまま

### 集中モードの上の帯には、レッスン表示と同じものを置く

#### 速さ・文字・幅・印刷は、どの集中モードでも上の帯に出す

2026-09 利用者の指定。

> 全ての集中モードにおいて、音声の速さ、画面の幅、印刷、文字の大きさの
> UIはこの写真のように黒のデザインで上部バーに配置しておいてください。
> 「書き込む」も同じように他のUIと同じく右に寄せて

- **中身は `LessonView` が作る**(`focusSettings`)。3つに書き写さない。
  押したときの動き(速さを変えたら読み上げを止める・選んだ値は覚える)も
  レッスン表示の帯と**同じものを使い回す**ので、判断が2か所に分かれない
- **書き込む / メモと同じかたまり**(`.lesson-settings`)に入れて右へ寄せる。
  狭い画面では「表示」に畳む(レッスン表示とまったく同じ作法)
- **色は決め打ち**(`.lesson-bar` と同じ #24241f / #f2f2ee / #55554e)。
  明るい配色を選んでいても、この帯だけはいつも暗い
- **印刷は紙(`#lesson-sheet`)を刷る。** 集中モードで見えているのは
  1つだけだが、**紙は教材まるごとの控え**である

#### 帯は1行に収める。**flex は「縮める前に折り返す」**

`.focus-top` に `flex-wrap: wrap` を置いたまま項目を足したので、
iPhone(390px)で 6Steps のプルダウンが2段目へ落ち、
**帯の高さが2倍(95px)**になっていた。320px では3段(137px)。

**縮められるのに折り返す**のは、flex が行を割るとき
「中身の幅」で判断するからである。`.lesson-bar-main` と同じく
**いつも要るものを1つの囲み(`.focus-top-main`)にまとめ、
`flex: 1 1 0`(基準を 0)にする。** `auto` のままでは効かない。
縮むのはプルダウンだけ(`min-width: 0` + `text-overflow`)。

実測: 1440 / 390 / 320px とも **53px の1行。** 横のはみ出しはどこにも無い。

#### 「読んで調べる」集中モードからも 6Steps へ移れる

2026-09 利用者の指定。

> 黒がメインの画面にも6stepsに行くためのタブを上部バーに実装してください。

`StepFocus` と**同じ見た目のプルダウン**を `FocusReader` の上の帯に置く。
先頭は「読んで調べる」= いまの画面そのものである。
調べ終えたらそのまま取り組みたくなるので、**いったん出て 6Steps を開いて
また入る、では続かない**(`StepFocus` に中の切り替えを置いたのと同じ理由)。

- 行き先は `LessonView` が `startStep` で `PassagePractice` へ渡す。
  **効くのは値が変わったときの1回だけ**(`askedStep`)。
  そのあとは中のプルダウンで自由に切り替えられる
- **紙に戻る場所(`backTo`)は控えたまま。** 6Steps を閉じれば、
  最初に集中モードへ入る前の場所へ戻れる

### 「ここから」は置かない。**Listen があるところでは全部**

2026-09 利用者の指定。

> 「ここから」ボタンは Listen があるところでは全て排除してください。
> 全体で共通です。(ゲストエンドでも)

Listen のとなりに「ここから」を並べると、**どちらも鳴らすボタンに見えて
選ばせることになる。** 通しで聴く道は残してある。

- 右下に貼り付く**「Listen (全体)」**(`.sheet-floats`)
- **通し表示(◯◯で区切らない)では、段落そのものを押せばそこから鳴る**
  (`playAll(item.id)`。通し表示では `.passage-actions` を出していない)

出していたのは `PassagePractice` の1か所だけなので、
**そこを消せばゲストの画面にも同じように効く**(部品が1つだから)。

### 集中モード — 1段落ずつ調べるときは、「送る必要」をなくす

`FocusReader.jsx`(2026-09 利用者の指定)。
**画面に出す名前は「集中モード」**(利用者が選んだ)。
コードの中の名前(`FocusReader` / `.focus-*`)は変えていない。
**単位(段落 / 発言)は名前に入れない。** 名前は1つにしておき、
いま何番目かは中の「3 / 6 段落」が言う。

> 段落ごとに画面を固定し、ブラウザの機能をシャットアウトできるように
> したいです。そのモードの時は段落を進める、戻るのボタンを配置して、
> ドラッグなどをしてもブラウザの影響を受けないように。

**「送らせない」のではなく「送る必要をなくす」。**
語のタップと画面送りが喧嘩していたのは、**画面が送れる状態だったから**である。
iOS はいったん送り始めると `touchmove` を取り消せない。
**時間や判定で戦うかぎり、この穴は塞げない**(3度踏んでいる)。
**1段落だけを画面ぴったりに出せば、送るものが無くなる。**

| 邪魔なもの | 止まるか | どうやって |
|---|---|---|
| ページ全体が動く | **止まる** | `100dvh` で画面ぴったり。送るものを無くす |
| 引っぱって更新 | **止まる** | `overscroll-behavior: contain` |
| アドレスバーの出入り | **止まる** | 送らなければ動かない |
| 二度叩いての拡大 | **止まる** | `touch-action: manipulation` |
| **指2本での拡大** | **止まらない** | iOS は `user-scalable=no` を**無視する** |
| **端からの戻るスワイプ** | **止まらない** | Safari の仕様。JS から触れない |

止まらない2つは**避ける**。押すものを端から離し、
**ホーム画面に追加**してもらえばアドレスバーごと消える(`manifest.json` +
`apple-mobile-web-app-capable`。**両方要る。片方では片方の端末で効かない**)。

- **`100vh` を使わない。** アドレスバーが隠れている前提の値なので、下が切れる
- **入り口は、レッスン表示の「練習の行」に置く**(`.practice-row`。
  6Steps・Quick Response と横に並べる)。

  はじめは `PassagePractice` の中にだけ置いていた。ところが
  **レッスン表示では `PassagePractice` は「6Steps」を押したときにしか
  描かれない**ので、集中モードは 6Steps の**中に埋もれていた。**
  ゲストの「今週の宿題」に至っては、`PassagePractice` は
  **印刷する一瞬しか描かれない。** だから利用者からは
  **「どこにも集中モードがありません」**に見えた(2026-09 実機)。

  語を調べるのは 6Steps に入る**前**の段階である。
  **入り口を、その中に置かない。**
  `PassagePractice` の側は `showFocus={false}` で出さない
  (**同じことをするボタンを、1つの画面に2つ見せない**)。
- **入口が3つになったら、狭い画面で1行に入るか実測する。**
  紙の上の `.practice-row` は `flex-wrap: nowrap` なので、
  **入らないぶんは横にはみ出し、紙が横に送れる状態になる**
  (下の「横方向のゆらゆら」)。
  600px 以下では**絵を落として**言葉だけにしてある
  (この3つは名前でしか見分けられないので、削るのは絵のほう)。
  実測 320px → 272px / 390px → 342px。どちらも1行 44px
- **入り口は、右下にも貼り付ける**(`.focus-float`・2026-09 利用者の指定)。

  > このボタンは「教材を作る」のように常に右下にも固定してください。

  ボタンの行は**紙と一緒に送られて消える。** 記事は6段落あるので、
  読んでいる途中で「この語を調べたい」と思ったときにはもう画面の外にいる。
  置き方は**さがす画面の `.finder-float` にそろえる。**
  ただし**通しの練習(6Steps / Quick Response)のあいだは出さない**
  (あちらも下にボタンがあり、重なる)。
- **出る場所も、同じ右下にそろえる**(`.focus-exit`・利用者の指定
  「すぐに元に戻れるボタンも作ってください」)。
  左上の「✕ 閉じる」は残してあるが、**スマホで親指がいちばん届かないのは左上**
  である。入った場所を押せば戻れる。
  **帯の上に浮かせる**(`bottom: 100%`)。帯そのものに置くと「次 ▶」と
  重なるか、320px で1行に収まらない(実測)
- **広い画面では、本文を横のまん中に寄せる**(利用者の指定
  「パソコンではもう少し文章を真ん中に寄せてください」)。
  上限が無かったので、1440px では 19px の行が画面いっぱいに伸び、
  **左に貼り付いて見えていた**(実測 左 16px / 右 778px)。
  `.focus-body > *` に `max-width: 34em` と左右 `auto` の余白
  (Quick Response の日本語と同じ読み幅)。
  **`max-width` なので狭い画面は何も変わらない**(実測 390px は前と同じ)。
  **文字そのものは左そろえのまま。** 寄せるのは「かたまり」である。
  なお `.focus-en, .focus-ja` に `margin: 0` と書くと**あとに来るこちらが勝って
  寄せが効かない。** 上下だけ 0 にする
- **いま紙のまん中に出ている発言から始める**(2026-09 利用者の指定)。

  > KENJI が大体画面の中心に来ている時は集中モードを押したら
  > ②KENJI の集中モードに入り、解除したら同じ場所に戻るようにしてください。
  > 集中モードに入る前のピッタリの場所です

  集中モードは「どこまで見たか」を覚えているが、**紙を読んでいる途中で
  押したときは、そちらのほうが強い。** 目はいまその発言の上にあるので、
  別のところから始まると探し直すことになる。

  - 選び方は**画面のまん中からの近さ**(`centeredFocusIndex`)。
    **まん中にかかっていれば距離 0** なので、画面をまたぐ長い発言でも当たる
  - 目印は **`data-focus`**(`<li>`)。**`data-key` では足りない。**
    あれは演習をまたいで一意なだけで、集中モードは
    **本文が空の項目を外した番号**で数えている
  - **控え(どこまで見たか)は書き換えない。** 覗いただけで動くと、
    次に開いたときに話が飛ぶ。◀ ▶ を1度でも押したら控えの側に戻す
  - **見えていないときは、これまでどおり覚えている場所から。**
    ほかのページを開いていると本文の `<li>` は高さ 0 なので、
    そのときは `null` を返す
- **終えたら、入る前とぴったり同じ場所へ戻す**(利用者の指定)。
  紙は `overflow-y: auto` の箱なので `scrollTop` を控えておくだけだが、
  練習中は `.lesson-sheet.is-running` で**並べ方が変わる**ため、
  そのままでは送りの量が落ちる。
  - 戻すのは **`useLayoutEffect`**(描き直しのあと・目に映る前)。
    `useEffect` だと、いったん頭に戻ったのが見えてしまう
  - 並べ方が落ち着くのが次の1コマ先のことがあるので、**そこでも念のため戻す**
  - **中で ◀ ▶ を押していても、戻るのは入る前の場所。**
    「入る前のピッタリの場所」という指定である
  - 実測(390px / 1440px): 送り 277 → 277・1071 → 1071・1812 → 1812。
    **6Steps と Quick Response には効かせていない**(言われた場所だけを直す)
- **くり返し(オートリピート)を Listen の横に置く**(2026-09 利用者の指定
  「これは集中モードで、全てのデバイスで同じにしてください」)。
  まねて言うには、同じ発言を何度も聴く。押し直していては口が追いつかない
- **「この段落で ◯語 入れました」は出さない**(2026-09 利用者の指定
  「やはりいらないです」)。数えるだけの行で、読むものが1行増える害のほうが
  大きかった。**まとめの1枚は残してある** — あちらは教材ぜんぶの控えである
- **本文の隣に、紙と同じ丸の番号を出す**(2026-09 利用者の指定
  「集中モード内も①や②のような番号を入れてください」)。
  レッスンは紙を見ながら話すので、「2番のところ」と言えば
  **どちらを見ていても同じ場所を指せる。**
  上の帯の「3 / 14 発言」は**どこまで来たか**の目安であって、役目が違う。
  - 見た目は `.lesson-items > li::before` にそろえる(うすい面の丸)
  - **数字は自分で丸に入れる。** ①②③ の文字は端末ごとに形も大きさも違い、
    50 を超えると尽きる(絵文字を使わないのと同じ理由)
  - **話す人がいなくても出す**(記事の段落にも紙は番号を振っている)
  - 英語と訳のどちらでも出す。**箱も帯も 1px も動かない**
    (実測 `.focus-body` 57→777 / `.focus-bar` 777→844 が両方で同じ)。
    本文そのものは上下のまん中に置いてあるので、
    行数が変われば上下する。**これは前からの決まりで、変えていない**
- **紙の幅をそのまま引き継ぐ**(2026-09 利用者の指定
  「PCで集中モードに入った時は、それまでの画面幅を引き継いでください」)。
  紙で 130% にして読んでいた人が、入った瞬間に別の幅に変わっては落ち着かない。
  数字は `.lesson-sheet--w*` と**同じもの**を使う(`.focus--w*`)。
  **狭い画面では効かない**(`max-width` なので、画面のほうが狭ければそのまま)。
  実測: 紙 860 / 946 / 1032 / 1118 / 1204 / 1290 / 1416px に対し、
  集中モードの本文も同じ幅になる
- **横スワイプで送らない。** 2語以上をなぞって調べる操作とぶつかり、
  端では「戻る」とも誤爆する。**ボタンだけにする**
- 吹き出し(`.etext-pop` は z-index 200)**より下**に置く(120)。
  語の意味がこの画面の裏に隠れては意味がない

#### 語を調べられるのは、広い画面と集中モードだけ

2026-09 利用者の指定。**方針の変更である。**

> スマホ、パッドのみ集中モード以外の時は単語を調べる機能を除外してください。
> これでスクロールが快適になるはずです

**語のタップと画面送りは、同じ指の動きから始まる。** 長押しをやめ、
「動かずに離れたらタップ」まで詰めてもなお喧嘩は残った(3度踏んでいる)。
**1段落だけを画面に固定する集中モードでは、送るものが無い**ので、
この喧嘩そのものが起きない。だから**調べるのは集中モードで**、
ふだんの読みは送りに徹する。

- 判断は `EnglishText` の **`tappable`** 1か所。既定は `'auto'`(広い画面だけ)、
  集中モードだけが `'always'` を渡す。**画面ごとに書き分けない**
- **判断は幅だけ**(`useWide()` = 1024px)。UA も `pointer` も見ない。
  **パッドを横向きにすれば押せるようになる**のも素直である
- **押せないときは `<button>` を残さない。** `.etext-word` は
  `user-select: none` を持っているので、押せないのに残すと
  **選んでコピーすることまでできない**、いちばん困る形になる。ただの文字に戻す
- **色(知っていた / 知らなかった)は消えない。** 印は `.etext-run` が
  持っているので、中を `<span>` にしても残る
- 要点フレーズの札(`PhraseChips`)は**そのまま。** あれは独立したボタンで、
  本文の上に重なっていないので送りの邪魔をしない(言われた場所だけを直す)
- **効く範囲は英文が出る画面すべて**(`EnglishText` を使う10か所)。
  狭い画面では、**紙・宿題・単語帳の例文でも語を押せなくなる。**
  記事・会話は集中モードで調べられるが、**ドリル・単語の問題文には
  集中モードが無い**(本文が無いので作れない)。狭い画面でそれらの語を
  調べる道は、いまは無い

#### 訳は「並べる」のではなく「入れ替える」

2026-09 利用者の判断「画面を切り替えれるようにするのが良い」。

**①並べるとこのモードが壊れる**(箱が2倍になり、送らないと読めない)。
**②訳が隣にあると、英語を読まずに済んでしまう**(記事の紙で同じ判断をしている)。

- **箱の高さは中身で変わらない**(残りいっぱいを取る)。
  実測で、切り替えても**帯も本文も 1px も動かない**
- **段落を送ったら、必ず英語に戻す。** 訳のまま送ると次も日本語で始まり、
  **英語を一度も読まないまま最後まで行けてしまう**
- **戻る道は同じボタン**(「訳を見る」⇄「英語に戻す」)
- **訳が無い段落では出さない**(効かない操作を見せない)
- **音も訳も両方出す**(2026-09 利用者の指定)

#### `wordStatuses` は Map である。`statuses[w]` と書かない

`useWordStatuses` が返すのは **Map**。オブジェクトのつもりで `statuses[w]` と
書くと**常に `undefined` になり、黙って 0 が並ぶ。**
実際に間違え、**Playwright で描かせて初めて気づいた**(2026-09)。
`npm run lint` も `npm run build` も通っていた。

### ③⑤ は「同じ本文を、どう見ながら読むか」を選べる

`PASSAGE_VIEWS`(素の文章 / 区切りを出す / **区切り + 訳**)と「段落で区切らない」。
区切りは②と同じ決まりで出す(`SlashedText`)。**2か所に持たない。**

- **通し表示では、行ごとの操作も語の意味も出さない。** 押したところから
  読み上げが始まるので、語の吹き出しとぶつかる。読むことに集中させる
- **通し表示では、話す人の名前も出さない**(2026-09 利用者の指定)。

  > 本文全文表示させた時に話しての名前が文章の間に入ってしまっています。
  > これは排除してください。段落ごと、発現ごとの場合は今まで通りでOKです

  通しは**1本の文章として続けて読む**ための見せ方なので、発言を横につなぐ。
  すると名前が**文の途中**に紛れ込み、英文の一部のように見えていた。
  誰の発言かは**声が分けている。** 段落ごと・発言ごとでは、これまでどおり出す
- **要点の札(`PhraseChips`)は ③⑤ に出さない。** 声に出す練習なので、
  読むものが増えると気が散る(2026-08 の指摘)
- **通しの読み上げは、押した段落から始められる**(`playAll(id)`)

### 練習の囲みは、どのトレーニングでも同じ形にする

**1問 = 1枚のカード**(`.qa-row`)、**答えは灰色の囲み**(`.answer-box`)。
細い線1本で区切っていたので、どこからどこまでが1問か分からなかった
(2026-08 の指摘)。**部品ごとに書くと、必ずどこかだけ違う見た目になる。**
紙の上の色は `--qa-bg` / `--qa-line` / `--qa-answer` で差し替える。

### 英文と訳は「1文ずつ」対にする

教材の1項目は、記事なら**段落**、会話なら**発言**である。そのままでは
Quick Response の1問が5行になり、6Steps でも1文に段落まるごとの訳が付く。
`alignedSentences()`(`src/lib/sentencePair.js`)が英語と日本語を
それぞれ文で切って対にする。**数が合わないときは切らない。**
長い対より、**まちがった対のほうが害が大きい**(違う英語が答えとして出る)。
合わなかったときだけ `jaIsWhole` を立て、画面が「段落の訳」の札を出す。

**日本語のほうが文が多いときは、長さの比でまとめて数をそろえる**
(2026-09 実機「ディクテーション内での訳が、1文ずつになっていません」)。
英語1文がコロンのところで2文に訳されることがあり、実測した段落は
**英語4文・日本語5文**で、段落の訳が4回並んでいた。
日本語と英語の長さの比は同じ段落の中ではほぼ一定なので、
「英語の何%まで来たか」で当てられる。
**外れそうなときはやらない**(`TOLERANCE` を超えたら「段落の訳」に落ちる)。
あわせて窓口にも「**prompt_ja の文の数は prompt_en と同じにする**」を足した。

### 練習の画面は、文の長さが変わってもボタンが動かないようにする

「文章の長さにかかわらず、上下にスクロールして微調整せずに、
常に安定して全体像がまん中あたりに収まるように」(2026-08 利用者の指定)。

- **Quick Response は、入るなら並べる。入らないときだけ入れ替える**
  (2026-08 利用者の指定)。**入るかどうかは実測する**
  (`scrollHeight > clientHeight`)。文の長さは教材で桁が違うので、
  文字数で当て推量しない。枠の高さは決め打ちなので**ボタンは動かない**
  - **答えはうすい色の囲み**(`.answer-box`)に入れる。
    ほかのトレーニングの解答と同じ形にそろえる
  - **上下のまん中に置く。文字が増えたら下へ伸ばし、それでもまん中を保つ**
    (2026-08 の指定で改めた)。枠は `min-height`(下限 6rem)。
  - **ただし、伸ばしていいのは「残りの高さ」までである**(2026-09 実機
    「quick response で英語を見ると、その後閉じられません」)。

    上限がまったく無かったので、長い英文を開くと枠がそのぶん下へ伸び、
    **「英語を隠す」「まだ」「言えた」の行を画面の外へ押し出していた。**
    押した本人からは「開いたら閉じられない」に見える。
    しかも枠は伸びる一方なので `scrollHeight > clientHeight` が
    **一度も成り立たず**、入れ替えの判断(`is-tight`)も死んでいた。

    紙の上では**残りの高さを計算しない。**
    枠を `flex: 1 1 auto`、ボタンの行を `flex: 0 0 auto` にすれば、
    残りにひとりでに収まる。**ただし途中の箱(`.qr` / `.qr-card`)に
    `min-height: 0` を置く。** flex の箱は既定では中身より小さくならないので、
    置かないと長い英文で紙より背が高くなる(実測 320×568 で 404 → 659px)。
    **`min-height: 0` を置くのは途中の箱だけ。** いちばん中の出題の枠には
    下限(5rem)を残す(0 まで縮むと問題が真っ白になる)。
    紙の外で使うときのために `max-height: calc(100dvh - 15rem)` も置いてある
- 出題の枠は **`min-height`**(`clamp(6rem, 26vh, 15rem)`)。
  **下限を必ず置く。** `min-height: 0` にすると窓が低いときに
  **高さ 0 になって問題が真っ白になる**(2026-08 実機)。
  上限は置かない — 文字が増えたら下へ伸ばす
- **広い画面では、1行ぶん上に置く**(2026-08 利用者の指定)。
  まん中に置く決まりはそのままで、**下に余白を足してまん中を持ち上げる**
  (足す量は上げたい量の2倍。まん中なので半分ずつ効く)。
  **スマホには足さない**(「良好な表示位置なので変えないでください」)
- **Quick Response は、いつでも上下のまん中**(2026-08 の指定で改めた)。
  紙の残りいっぱいを取り、その中でまん中に置く。
  以前は「上から積む」だったが、パソコンの大きな画面で
  **上半分に寄って、下が大きく空いていた**
- **`min-height: 0` にしない。** 窓が低いと高さ 0 まで縮み、
  **問題が真っ白になった**(2026-08 実機)。下限(`7rem`)を必ず置く
- `justify-content` は **`safe center`**。`center` だけだと、はみ出したときに
  中身が**上へ逃げて届かなくなる**
- それでも入りきらないときは、**こちらで送る。** ただし
  `scrollIntoView` は**紙まで一緒に送ってしまう**ので、`scrollTop` で
  **その枠だけ**を動かす。**送りの面倒は1つの effect にまとめる**
  (「開いたら送る」と「問が変わったら戻す」を別々に書いたら、
  次の問の日本語が枠の外から始まった)
- **6Steps の切り替えは、プルダウン**(2026-09 利用者の指定「6Steps は
  プルダウンにしてください」)。以前は札を6つ横に並べ、どの幅でも6つとも
  見えるように折り返させていたが、狭い画面では2段になり、
  紙で文字を大きくすると帯だけで場所を食っていた。
  **番号(①〜⑥)は選択肢に出す**(何番目かが道しるべになる)
- **行の操作は右上へ。** 話す人の名前と反対側に置くと、本文と解答を
  そのぶん上に寄せられる(`.row-head` / `.row-tools`)
- **音声認識の断り書きは、話すボタンがあるステップにだけ出す。**
  ② スラッシュリーディングには関係がない
- **答えはボタンの上に出す。** 下に出していたので画面の下端へ押し出された
- 紙(レッスン表示)では `.lesson-sheet.is-running` で**縦いっぱいの1枚**にする
- 練習中は**効かないボタンを出さない**(ページ送り・解答)。迷わせるだけ
- **紙の上の札は小さくする**(`0.8em`)。始めたあと出しっぱなしになるため。
  ただし**狭い画面(600px 以下)では px で固定し、折り返させない。**
  紙の文字を「特大」にすると 0.8em でも 18px になり、
  「6Steps」「Quick Response」が縦に積まれて 105px を食っていた
  (2026-08 実機)。**操作は、読むものの大きさに合わせなくてよい**

### くり返しのボタンは `RepeatToggle.jsx` 1つ

2026-09 利用者の指定。

> Listenボタンの横にはオートリピートのボタンをつけてください。
> 意味音読、オーバーラッピング、シャドーイングも段落内の再生ボタンの横にも
> リピートボタンを配置してください。

まねて言う練習(③オーバーラッピング・④意味音読・⑤シャドーイング・
⑥リピーティング)も、Quick Response の答え合わせも、
**同じ英文を続けて何度も聴く。** そのたびに押し直すのでは、
口が追いつく前に止まってしまう。

ディクテーション(①)の各文には前からあったので、**その形をそのまま
部品にした。** 見た目も文言も変えていない。

- **覚えない。** 鳴りっぱなしになる指定なので、次に開いたときは必ず「1回」に戻す
- **押している印は、色と文字の両方で出す**(色だけに頼らない)
- **止まる条件を持たせる。** 0.3秒に満たずに終わったら失敗とみなしてやめる
  (`SpeakButton` の `repeat`。③⑤ は `PassagePractice` の `speakOnce` が同じ判定)
- ③⑤ は**段落ごと**、①④⑥ は**文ごと**に持つ(その練習の単位に合わせる)
- **② スラッシュリーディングには置かない。** 区切りを入れる練習で、
  まねて言うところではない(効かない操作を見せない)

### Quick Response では、答えのすぐ横に Listen を置かない

2026-09 利用者の指定「正解の英文のすぐ横の listen ボタン、排除しましょう。
これはスマホでも同じです」。

下のボタンの行にも Listen がある。**同じことをするボタンを2つ見せない。**
英文の途中に押すものが挟まると読むときに目が引っかかるうえ、
**長い英文では位置が毎回変わる。**

**番号は丸で出す**(2026-09 利用者の指定「クイックレスポンスの文章にも
番号をつけてください。おしゃれにね」)。上の「2 / 25」は
**どこまで来たか**の目安で、役目が違う。
丸は **`.num-badge` 1か所**(集中モード・紙と同じ見た目)。

### 「まだ」を押した文は、復習に溜める(0040)。**方針の変更**

2026-09 利用者の指定。

> 教材の中で取り組んだ Quick Response の中で「まだ」を押したものは、
> Quick Response という復習用の機能を独立して作り、
> ひとつのアカウントにつきひとつ持たせてください。
> UI は通常の Quick Response の画面と同じです。
> 単語と同じく、「テキスト」「日付」「業界」「シチュエーション」などから
> 絞り込んで練習できるようにしてください。
> 「まだ」「おぼえかけ」の仕組みは同じです。

**もとは「記録は残さない」だった**(単語帳の箱と2か所で動くのを避けるため)。
けれども教材の Quick Response は**その教材の通し**なので、
言えなかった文は、その教材を開き直さないと二度と出てこない。
**溜める先は単語帳とは別のもの**(あちらは語、こちらは文)なので、
同じものが2か所で動くことにはならない。

- **鍵は「そろえた英文」**(`en_norm`)。**`material_items` の id にしない。**
  Quick Response の1問は「1項目を文でほどいたもの」なので、
  **文の切り方の決まりを直すと番号がずれる。**
  英文そのものなら、決まりが変わってもずれない。
  おまけに**教材をまたいで1つにまとまる**(利用者の指定)
- **そろえ方は `normEn()`(`materials.js`)と `norm_en()`(0008)をそのまま使う。**
  すでにある規則を使い回す。**同じことをする規則を2つ持たない**
  (語のそろえ方を3か所でそろえるのに苦労したのと同じ話)
- **溜めるのは「文章」だけ**(`group === 'sentence'`)。
  単語・フレーズは**単語帳**が持つ(利用者の指定)
- **間隔の決まりは単語帳とまったく同じ**(`mark_qr()`・0040)。
  卒業25回・休み30日・箱は「覚えかけ」なら 3 で頭打ち。
  **数字がずれていないことを `vocab_test.sql` が見張っている**
- **「言えた」だけでは溜めない**(`p_only_existing`)。言えた文は復習に要らない。
  ただし**すでに溜まっている文**を教材の中で言えたなら、
  それは「言える」を押したのと同じなので箱を1つ上げる(利用者の確認)
- **誰の記録になるかは `learnerId` が決める**(0025 とまったく同じ)。
  トレーナーがゲストのページで押せばゲストの、
  自分の「教材」で押せばトレーナー自身の記録になる。
  判定は `mark_qr()` の中(`teaches()`)だけ。**画面に持たせない**
- **ボタンの言葉だけが違う**(利用者の指定)。
  教材の中は「まだ / **言えた**」(その場で言えたか)、
  復習の画面は「まだ / **言える**」(これから言えるか)。
  単語帳が「まだ / 覚えかけ」と訊くのと同じ関係である
- **消す道を必ず用意する**(「もう出さない」/ `drop_qr()`)。
  溜まる一方だと、押し間違えた1問がずっと出続ける
- **0040 を貼る前でも壊れない。** 一度断られたら覚えておき、
  そのあとは呼びに行かない(`qrReviewSupported()`)。
  教材の Quick Response はこれまでどおり動き、溜まらないだけである

#### Quick Response は、押したら**そのまま集中モードで始まる**

2026-09 利用者の指定「Quick Response も、自動的に集中モードで開始される
仕様にしてください」。

**単語帳の復習と同じ考え方である**(「入口をもう1つ挟まない」)。
「Quick Response」を押した人は**もう答える気で来ている**のに、これまでは
そのあと「集中モード」をもう一度押す必要があった。
**1問ずつ画面に固定するのが、この練習の既定の形**である。

- **集中モードのボタンは残す。** 出たければそちらで閉じられる
  (閉じても Quick Response は紙の上で続く。**行き止まりを作らない**)
- 開いているかどうかは、これまでどおり **`LessonView` が持つ**(`qrFocus`)

#### 1問ぶんの見た目は `QrCard.jsx` 1つ。**2か所に書き写さない**

Quick Response は2か所にある(教材の中 / 復習)。出題の枠・答えの囲み・
「英語を見る / Listen / くり返し」の行・答えの2つは**まったく同じ**なので、
`QrCard` 1つに置く。**書き写すと必ず片方だけ古くなる**
(単語帳で `LearnerWordbook` を別に持って踏んだ失敗)。

- 渡すのは **`pair` / `no` / `onAnswer` / ボタンの言葉 / `extra`** だけ
- **切り出しただけであることを、撮り比べで確かめた。**
  390px / 1280px とも、答えを開く前後とも**1ドットも変わっていない**
- **`.qr-peek` は折り返しを許す。** 復習では「もう出さない」が1つ増えるので、
  iPhone(390px)で4つが1行に入らず、**文言が切れて読めなかった**(実測)。
  教材の中は3つのままなので、そちらの見た目は変わらない

#### 絞り込みは `WordbookFilter` を使い回す。**教材名だけ呼ぶ側で決める**

単語帳では教材名のプルダウンを出さない(2026-09 利用者の指定)。
Quick Response の復習では**出す**(利用者の指定「教材の名前で絞る」)。
**似た部品を2つ作らず、`showMaterial` で切り替える。**

- 並べ方は**混ぜる / 教材の順**の2つ(利用者の指定)。覚える
- **既定は「今日出すぶん」。** 箱(間隔)を置いている意味がそこにある

### やり方の説明は、1行に1つ

`SIX_STEPS` の `how` は**配列**。1つの段落に流すと、改行も区切りも無い棒に
なって読めない(指導ポイントで一度学んだこと)。`<details>` で畳み、開閉を覚える。

**見た目は指導ポイント(`.teaching-note`)にそろえてある**(2026-08 利用者の指定)。
題は「やり方」、色は 6Steps の緑(`--accent`)のまま。

- **ちがうのは「中身が手順」だという1点。** 指導ポイントは並びに意味の無い
  箇条書きなので丸い点、こちらは**番号の丸**にしてある
- `aim`(何ができるようになるか)は**「ねらい」の札**にして中身のいちばん上へ。
  題が「やり方」になったので、見出しには置けない。
  **札は塗りつぶさない**(2026-08「塗りつぶしが濃すぎます」)。
  まわりがうすい面なので、細い枠と文字だけで足りる
- **CSS の値は書き写してある。** 指導ポイントは「指示があるまで変えない」と
  決まっているので、規則を共有するとこちらを直したついでにあちらが動く。
  **あえて分けてある。** 直すときは片方だけを直してよい

### スラッシュリーディングの区切りは、決まりで出す。AI に頼まない

利用者が挙げた決まりは、**どれも閉じた語のリストで判定できる**
(`src/lib/chunker.js`)。前置詞も助動詞も数が決まっている。
1文ごとに課金する理由がない。**決まりで書けるものを AI に頼まない。**

- **まちがいは、その場に吹き出しで出す。** 下にまとめて並べていたので、
  どの区切りの話なのか分からなかった(2026-08 の指摘)。
  短い一言(`short`)を赤い吹き出しでスラッシュの横に出し、
  詳しい言い方(`text`)は `title` に持たせる
- **指摘は、次の操作で片づく。** 読んだあと自分で消して回らせない。
  ①隣の正しいところを押す ②吹き出しを押す ③よそを押す — **3つとも**
  で、まちがいのスラッシュごと消える。
  **ただし、いま押したものは残す。** 消すと吹き出しが出た瞬間に消える
- **やり方の説明を、本文の上に置かない**(2026-09 利用者の指定
  「青くハイライトした文言は不必要です。消してください」)。
  同じことは上の「やり方」(`SIX_STEPS` の `how`)に畳んで置いてある。
  **同じ説明を2か所に置かない。** 読むものが増えると本文が下へ押し出される
- **模範の区切りは出さない。比べさせない**(2026-08 利用者の判断)。
  > そもそもが区切り方を比べる自体が難しいです。視覚からパッと入って
  > 来ません。比べる気も起こりません。そして、区切り方は、ルールとして
  > 決めたこと以外、正解はないからです。

  以前は模範を出して見比べさせ、合っている数・模範には無い数・あと何か所、
  まで数えていた。**これは採点である。** 区切り方に正解は無いので、
  案と違うだけのものを並べると**決まりに反している1本が埋もれる。**
  言うのは「**決まりに反している**」ことだけ。それ以外は何も言わない
- **画面は3段**(利用者が書いた設計そのまま)。
  ① 自分で区切る → 決まりに反するものだけ 💬 が出る
  ② 「この区切りで訳を出す」を押す
  ③ **自分の区切りが入った英文**と、それに対応する訳が一緒に出る。
  出したあとは、上の英文(訳なし)が**練習用**、下の対が**確認用**
- **②は「段落ごと」と「まとめて」の両方から押せる**(2026-08 利用者の指定)。
  > これは段落ごとでも全体を一度にでもどちらでもできるようにして欲しいです。

  段落の数だけボタンを押させない。上の帯に**「すべての区切りで訳を出す」**を
  置く(2段落以上あるときだけ)。1つずつ確かめる道も残す。
  **単位(段落ごと / 文章全体)を切り替える話とは別物。** あちらは
  区切りを入れる単位、こちらは訳を出す操作である
- **「通しで見る」は、いつでも押せる**(2026-09 利用者の指摘)。

  > 段落ごとに分けた英文と訳は、すべてつながって見れる場所が欲しいです。

  はじめは**全段落の訳を出したあとだけ**出していた(虫食いの一枚を
  見せないため)。ところが**そこへ行き着く道が見えなかった。**
  いまは**押したときに、こちらで全段落の訳を出してから切り替える。**
  虫食いにならず、しかも1回で着く。
- **全部の段落が終わったら「通しで見る」**(2026-08 利用者の指定)。
  > 全ての段落が終わった後には、各段落の区切った文と訳を全て繋いである
  > ものを用意してあげて参照できるようにしたいな。
  > ボタンを押せば切り替わるような仕組みが良い

  段落ごとの箱を外し、**自分の区切りと訳だけをつないだ1枚**にする。
  - **終わる前は出さない。** 押しても虫食いになるだけ
    (「効かないボタンを出さない」)。終わったかどうかは
    「全段落の訳を出したか」で見る
  - **通しでは語を押せなくしておく。** 参照するためのものなので、
    区切りを触れると練習用と見分けが付かなくなる
  - **段落の切れ目は残す**(点線)。消すとどこからどこまでが1段落か
    分からなくなる(⑤ シャドーイングで本文を隠すときと同じ考え方)
  - 対の描画は `MinePairs` 1か所。**段落の箱と通しで書き写さない**
- **NG の判定は `slashProblem()` 1か所。** 模範を作る `add()` も同じものを
  通す。2か所に書くと**模範が自分の決まりを破る**(`test:chunk` が落ちる)。
  NG は「**うしろの語にかかる語で、カタマリを終えない**」の言い換えである。

  | 何 | 言い方 |
  |---|---|
  | 前置詞のあと(前置詞と冠詞のあいだを含む) | `of` の前で区切る |
  | 冠詞のあと | `the` は名詞と離さない |
  | 所有格(`'s`)のあと | 次の名詞と離さない |
  | 助動詞のまとまりの途中(`have to` / `will` など) | `have to` は切らない |
  | **句動詞の途中**(`Get / up at 6am.`) | `Get up` は切らない |
  | **形容詞・分詞とうしろの名詞のあいだ** | `new` は名詞と離さない |
  | 接続詞・関係詞のあと(`and` `which` `because` …) | 次のまとまりの先頭に置く |

- **注意に使う語のリストは、模範を作るリストと別にしてある**(2026-08 実機)。
  取り違えようのある語を前置詞・冠詞と決めつけて、**誤判定を2つ出した。**

  | 出した誤判定 | 本当は |
  |---|---|
  | `Apps like this / let …` に「this は名詞と離さない」 | `this` は**代名詞** |
  | `pull in / more than …` に「in の前で区切る」 | `in` は**句動詞の副詞** |

  だから注意する側(`SURE_PREPS` / `SURE_DETERMINERS` / `SURE_MODALS` /
  `HEAD_WORDS`)には、**ほかの品詞にならない語だけ**を入れる。
  `in` `on` `up` `over` `by` `about`(句動詞の副詞)、`like`(動詞)、
  `this` `that` `some`(代名詞)、`his` `her`(目的語)、
  `have` `do` `is`(本動詞)、`while`(名詞)は**入れない。**
  見逃しても害は小さいが、**間違った注意は何も言われないより困る。**
- **句動詞は「動詞 + 副詞」の対で持つ**(`PHRASAL_PAIRS`・2026-08 利用者の指定)。
  > Get up at 6am. だとしたら、Get up / at 6am です。この場合の up は
  > 前置詞ではなく、副詞であり、get とセットとして考えるべきです。

  **動詞のリスト × 副詞のリストで掛け合わせない。** 同じ語が
  句動詞の副詞にも前置詞にもなる(`pull in` / `run in the park`、
  `get up` / `go up the stairs`)。掛け合わせると**正しい区切りを咎める。**
  対そのものを並べれば取り違えない。**ここは模範の側にも効かせてある**
  (`Get / up` という不自然なカタマリを作らないため)
- **形容詞は「述語かどうか」で見分ける**(2026-08 利用者の指定)。
  > 形容詞と名詞の間に区切りを入れても注意されません。ただ、
  > I am happy / because … みたいに後ろが名詞ではない場合は OK であるべきです。

  咎めないのは3つのどれかに当てはまるとき。
  ①つなぎの動詞が2語前までにある(`is happy /` `feels very safe /`)
  ②うしろが機能語(前置詞・接続詞・助動詞・`than` `as` `to`)
  ③文の切れ目・読点のあと。
  `clean` `open` `light` `right` `hard` `fast` `late` `high` は
  **動詞・名詞・副詞にもなる**ので `ADJECTIVES` に入れない
- **うしろから修飾する分詞は、切っても切らなくてもよい**(利用者の指定)。
  > I saw some people / dancing in the park. でも OK だし、
  > I saw / some people dancing in the park. でも OK です。

  だから分詞を見るのは**冠詞や形容詞のうしろに立つとき**だけにする
  (`a broken / window` は咎め、`some people / dancing` は咎めない)。
  不規則な分詞(`broken` `written` `lost`)は形からは分からないので並べてある
- **前置詞を取る動詞は `PHRASAL_PAIRS` に入れない**(`work on` `look after`
  `stand by` `go over`)。あれは前置詞句なので `work / on the report` と
  切ってよい。入れると正しい区切りを咎める
- **数字や記号が混じった語を、語のリストに当てない**(`bare()`)。
  `6am` が `am`(be動詞)に化けて、`at / 6am` という**自分の決まりに
  反する区切りを模範が作っていた**(2026-08 実測)。`5,000` も同じ
- **模範を作る側(`idealSlashes` の `add()`)は、広いリストのままにする。**
  狭めると模範の区切りが**増え**、教材に控えたカタマリごとの訳(0021)と
  数が合わなくなって、**訳が丸ごと出なくなる。**
  注意より常に厳しくしておけば、模範が決まりを破ることはない
  (`npm run test:chunk` の1本目が見張っている)

- **文の切れ目(`.` `?` `!`)と読点のあとは、いつでも正しい**(利用者の指定)。
  > 「以前」という意味の副詞として before が使用され、文の最後に置かれ、
  > ピリオドが続き、そのピリオドとその後の文の始まりの間に区切りを
  > 置くことは何ら問題はありません。

  `before` `after` `since` `over` は前置詞にも副詞にもなる。
  **どちらかは語のリストでは当てられない。** けれども文が終わっていれば
  前置詞ではありえない(前置詞ならうしろに名詞が要る)ので、
  記号だけで確実に分かる。**咎めない。**
  同じ理由で、`once` `when` `so` などの**副詞にもなる語は
  `HEAD_WORDS` に入れない**(あやふやなことを言わない)
- **1つも反していなければ、褒める**(2026-08 利用者の指定)。
  > 違反している区切りはありません、ではなくて、結果的に違反の数がゼロなら
  > fantastic! などポジティブな声かけに、変えましょう

  「◯◯はありません」は**無いことの報告**であって、褒め言葉ではない。
  声かけは**段落ごとに決める**(`praiseFor()`)。押すたびに入れ替わると、
  目が言葉のほうへ行って気が散る
- **理由の一覧は出さない**(長い文で12行並び、「見づらいだけ」と言われた。
  理由は1本ずつ `title` に持たせる)
- **押すのは「語」。語と語のあいだではない。**
  あいだに押せる帯を置いたら「最初から全部にスラッシュが入っているように
  見える」「壊滅的に使いづらい」と言われた(2026-08)。
  語なら大きくて指で狙え、押すまで**素の英文のまま**である
- **空白は囲みの外に置く。** `white-space: nowrap` の中に入れると
  改行できる場所が無くなり、長い文が画面からはみ出す(実測)
- 前置詞・接続詞・助動詞のまとまり・冠詞のリストを触ったら、
  **必ず `npm run test:chunk` を通す**(模範が自分の決まりに違反していないか、
  NG に注意が出るか、文末の副詞を誤って咎めていないかを見る)
- **上級ほど区切りが減る**(利用者の指定)。強さだけで決めると初級と中級が
  ほとんど同じになったので、**カタマリの最小語数**でも絞っている
- **「動詞の前で切る」は、当てられるぶんだけ入れてある。**
  一般の動詞は語のリストでは当てられない(`run` は名詞にも動詞にもなる)。
  **あやふやなことを言わない。**「たぶん違う」は、何も言われないより困る。
  ただし**助動詞と be動詞は数が決まっていて、しかも必ず動詞**なので、
  その前では切る(強さ1。**初級でしか出ない**)。
  `How long / is the ride?` — 利用者の紙の教材と同じ形になる
- **「動詞のあとで切る」は、控えの側にだけ入れてある**(2026-08 利用者の指定)。

  > 動詞の後も初心者には区切ってもらいたいポイントです。

  `The office bought / a new coffee machine` `the trend has / a darker side`。
  **注意する側(`slashProblem`)には足していない。** 控えは切れ目が増えるだけで
  害が小さいが、注意は取り違えるとそのまま**間違った注意**になる。
  **動詞を数え上げない**(2026-09 利用者の指定で改めた)。

  > 動詞の目的語を動詞とは分けて考えるのが初心者です。
  > どうにかしてあらゆる他動詞について一括でこのルールを変えれないですか?

  はじめは `COMMON_VERBS` という一覧に載っている動詞のあとだけ切っていたが、
  **載っていない他動詞では切れなかった。** 一般の動詞は語のリストでは
  当てられないので、数え上げるかぎり穴は残る。
  いまは `objectHere()` が**名詞のかたまりの始まり**の側から見る。
  前の語が冠詞・形容詞・所有格・前置詞・接続詞・副詞のどれでもなければ、
  そこが動詞と目的語の切れ目である(**一覧は消した**)。
  取り違えを避ける条件は4つ。
  ①前が冠詞・形容詞・所有格・前置詞なら**名詞**である(`the plan` `a new place`)
  ②**前の語が名詞のかたまりの中身**なら動詞ではない
  (`We need the plan / a week` の `plan`。形容詞をまたいで冠詞まで戻って見る)
  ③句動詞の副詞が続くときは `add()` が断る(`talking / up a stock` を防ぐ)
  ④うしろが**名詞のはじまり**のときだけ切る。冠詞・代名詞・数に加えて、
  **`anything` のような不定代名詞**も名詞のはじまりとして数える
  (2026-09 実機。`Before you buy / anything fancy,` が切れていなかった)。
  **`-ing` で終わるだけの語**(`anything` `morning`)を分詞と間違えないよう、
  `NOT_PARTICIPLES` で除いてある。
  **`have` `do` は本動詞にもなる**ので `insideAux()` を緩めてある
  (うしろが名詞のはじまりなら助動詞ではない)。ただし**文頭の
  `Do the students …?` は疑問文の助動詞**なので、そこでは切らない。
  **冠詞の付かない目的語(`waste money`)は、いまも切れない。**
  名詞のはじまりの印が無く、当てようがないためである
- **単位は「段落ごと」か「文章全体」。** 1文ずつでは細かすぎる
  (2026-08 の指摘)。`blocksOf()` で切り替える
- **自分の区切りに合わせた訳は、控えを組み替えて作る**(`chunkPairsAtMarks()`)。
  訳を分けられるのは**控えの境目(初級の区切り)だけ**なので、
  そこと重なった区切りで分ける。重ならない区切りは**英語には出し、
  訳はそのカタマリぶんをまとめて置く。**
  **押すたびに AI に訳し直させない。** 無料で、待ち時間も無い
- **カタマリごとの訳は、教材に控える**(`material_items.chunks`・0021)。
  切る場所は決まりで分かるが、**そのカタマリを日本語で何と言うか**は
  決まりでは書けない。だから**そこだけ**を AI に頼み、
  作るときに1回だけ作って控える(仕様書 第5.29.3節)。

      どれくらいの長さですか / 乗っているのは
      How long              / is the ride?

  - **1つのカタマリに訳がいくつも入ったときは、行を分ける**(2026-09 実機)。
    自分の区切りが控えの境目と重ならないと、その区切りは**英語にだけ出て、
    訳はそのカタマリぶんがまとまって**置かれる(下の決まり)。
    ところが**つないで1本の文字列にしていた**ので、

        今日時間を作ってくれて話しておきたかったスケジュールについてそして料金について

    と、**日本語として読めない棒**になっていた(利用者の写真)。
    `chunkPairsAtMarks()` がつなぐ前の姿(`jaParts`)も返し、画面が
    **行を分けて**出す。**区切り記号は足さない**(訳の側にスラッシュは
    出さない・2026-08 の指定)。行が分かれているだけで、別の意味だと分かる
  - **並びは「英語が上・訳が下」。訳はそのカタマリの真ん中に置く**
    (2026-08 利用者の指定)。訳の側にスラッシュは出さない
    (区切りは上の英語が示している)
  - **囲みも線も置かない**(2026-08 利用者の指定。帯 → 線 と2度直して、
    どちらも「ダサい」と言われた)。**字づかいだけで層を分ける。**
    大きさ 0.72 倍・落ち着いたセージ(`--chunk-fg`)・わずかな字間・
    行間を広く。どのカタマリの訳かは**真下にあること**で分かる(近接の原理)
  - **狭い画面(860px 以下)では、訳を左寄せにする**(2026-08 利用者の指定)。
    > このように毎回改行するのであれば日本語は左寄せでお願いします。

    まん中に置くのは、カタマリが**横に並んでいる**ときの話である。
    スマホでは1カタマリで1行を使い切るので、まん中だと訳が広い行に浮き、
    **上の英語のどこに対応するのか分からなくなる。**
    **判断は幅だけ**(`useWide()` と同じ 860px)。文字数から当て推量しない
  - **カタマリの訳だけを出す形は、どの画面幅でも作らない**
    (2026-08 利用者の判断。一度作って取り下げた)。
    > 訳と自分の区切りが一致しなければ意味がないし、スマホでは
    > スクロールして訳を見ても同時に英文が見えないからです
    > pcでは訳だけ表示するメリットはありそうですが、それでも自分の
    > 区切り方と模範解答の、区切り方が異なることも多いので、
    > それでは意味をなしません

    **これは狭さの話ではない。** カタマリの訳が持っている情報は
    「この日本語は、**英語のどこからどこまで**に当たるか」であって、
    訳の文言そのものではない。**英語が隣に無いと、その情報が消える。**
    しかも自分の区切りは模範と違っていることが多いので、
    訳だけを並べても自分の切り方には重ねられない。
    だから **PC でも訳だけにはしない。** 英語と訳は必ず対で出す。

    出し入れは「カタマリの訳を出す」(訳を全部消す)と
    「解答を出す / 隠す」(箱ごと閉じる)で足りる。
    **同じことをする操作を3つ置かない**
  - **「区切りの訳を作る」ボタンは置かない**(2026-08 利用者の指定)。
    > 教材を作った時点で区切りの訳がバックグラウンドで生成される
    > デザインの方が良いです。

    作るときに `MaterialForm` が一緒に作る。足りない教材は、さがす画面で
    **開いたときに裏で1回だけ**作る(`needsChunkJa`)。
    **止まる条件を持たせる**(1教材につきその画面で1回。失敗しても繰り返さない)
  - **前の名詞を説明する語句は、名詞と1つの単位で訳させる**
    (2026-09 利用者の指定)。

    > the boy running in the park の訳が「男の子は走っている / が…」
    > 本来は、「走っている男の子」であるべきです

    英語は名詞のうしろから説明するが、日本語は**前から**説明する。
    別々のカタマリで訳させると「男の子は」「走っている」となり、
    つなぐと**述語のように**読める。`baseChunks()` が
    `postModifier()` の切れ目だけを落として1つにまとめる。
    **英語の区切り(スラッシュ)は変えていない。** 変えたのは訳の単位だけ
  - **並べているもの(`and` / `or`)の前で切る**(2026-09 利用者の指定)。

    > A pair of shoes / and a water bottle を /靴一足と水筒/ と
    > なってしまいます。靴一足 / と水筒一本 となるように

    強さ1(初級だけ)。`but` `because` のような「ここから意味が変わる」
    切れ目とは違い、並べているだけである。
    **`and` のあとで切るのは、これまでどおり咎める**
  - **訳をつなぐときは、同じことを二度書かない**(`joinJa`)。
    窓口が隣のカタマリの意味まで書くことがあり、
    「無視するのは難しい無視するのは」と出ていた(2026-09 実機)。
    **窓口の指示を直しても、すでに作った訳は直らない。** 出すときにも見る
  - **中身の空いたカタマリがあれば、作り直す**(`needsChunkJa` ④・2026-09 実機)。
    窓口の指示を直しても、**すでに作った訳は直らない**(訳は教材に控えてある)。
    ところが「主語のカタマリに全部書いてしまう」形の訳には、
    **必ず空のカタマリが残る**(書く分が無くなるため)。
    **英語があるのに訳が空、というのはどんな場合でも正しくない**ので、
    これを見つけたら作り直す。窓口を配置し直したあとなら、
    次にその教材を使ったときに**ひとりでに直る**
  - **足りなかった段落は、こちらでもう一度頼む**(`generateChunkJa`)。
    窓口は「カタマリの数が合った段落だけ」を返し、合わなかった数を
    `skipped` で知らせていたが、**画面がそれを見ていなかった。**
    6段落のうち何段落かだけ訳が入り、残りは
    「訳を出す(段落まるごと)」に落ちていた(2026-09 実機)。
    **2回で止める。** 頼み直すのは足りない段落だけなので、
    うまくいく回の費用は変わらない
  - **主語だけ・目的語だけのカタマリは、その分だけを訳させる**
    (2026-09 利用者の指定)。日本語は「主語 → 目的語 → 動詞」の順なので、
    主語のカタマリに動詞と目的語の意味まで書いてしまう。
    書いてしまうと**次のカタマリの訳が空になり**、どの英語がどの日本語に
    当たるのか分からなくなる(実機で
    「小さなクラウド障害が先週大きな問題を引き起こした」が
    主語のカタマリに丸ごと入っていた)
  - **控えは、いまの決まりより粗ければ作り直す**(`needsChunkJa` ③)。
    控えが粗いと、ゲストがそこで切っても訳を分けられず、
    2つぶんの訳がまとまって出て**右へずれて見える**(2026-08 実機)。
    粗いままでも**訳は出る**ので、作り直しは見た目をそろえるためである
  - **控えの単位は、細かいほどよい。** 訳を分けられるのは控えの境目だけなので、
    細かいほど「どこで切っても訳が真下に来る」。前置詞・接続詞に加えて、
    次も切れ目にしてある(強さ1)。**足りない型に気づいたら、ここに足す。**

    | 型 | 例 |
    |---|---|
    | 副詞(`-ly` と、よく出る形) | `… a week / suddenly started` |
    | 主語の代名詞 | `Last spring / she noticed` |
    | **前置詞 + 代名詞のあと** | `Apps like this / let a trader` |
    | **句動詞のあと** | `Some streams pull in / more than …` |
    | **前の名詞を説明する分詞** | `Phone scams / targeting elderly people` / `The money / raised by the fund` |
    | **比べる `than` の前** | `it feels safer / than reading a textbook.` |
    | **動詞のあと** | `The office bought / a new coffee machine` |
    | **文末の時・場所の副詞句の前**(2026-09) | `for making time / today.` / `a table / backstage.` |

    **文末の「いつ・どこで」は、切り離す**(2026-09 利用者の指定
    「前からの訳において、初心者がやるように細かく分けるとまだ
    対応しきれていない部分があります」)。
    `today` `tonight` `tomorrow morning` `next month` `backstage` は、
    直前の名詞句とは別の情報なのに、くっついたまま1つのカタマリに
    なっていた。だから訳も「来月のイベントの前に」と1本になり、
    **「いつ」がどの英語に当たるのかが見えなかった。**
    `TAIL_ADVERBS` / `TIME_HEADS` / `TIME_NOUNS`(閉じたリスト)で見分ける。
    **前が前置詞・冠詞・接続詞のときと、そこで文が終わるときは切らない**
    (`before / next month` のような、ありえない切り方になる)。

    #### 目的語をはさむ句動詞は、**まとめて1つ**にする(2026-09)

    `set that up` `write that down` `working this out` を
    `set / that up` と切っていた。`up` `down` `out` を前置詞と
    取り違えているためで、**「動詞 + 目的語 + 副詞」で1つの意味**である。
    `splitPhrasalAt()` / `insideSplitPhrasal()` が、
    **その副詞でカタマリが終わるか、うしろが前置詞・接続詞のとき**だけ
    分離した句動詞と見なす(`run in the park` を巻き込まないため)。

    **注意する側(`slashProblem()`)には足していない。**
    控えは切れ目が減るだけで害が小さいが、注意は取り違えるとそのまま
    **間違った注意**になる(`PHRASAL_PAIRS` と同じ考え方)。

    #### 数詞と `one` は割らない(2026-09)

    - `Ninety / thousand` … `NUMBER_WORDS` に `eleven`〜`nineteen` /
      `forty` / `sixty`〜`ninety` が**抜けていた。** 数どうしは切らない
    - `to this / one` … `one` `ones` は**代名詞**なので、
      指示語のうしろで切ると訳が「4つ」のように化ける。
      冠詞・指示語のうしろの `one` では切らない
    - `since / you mentioned` … `since` は接続詞にもなる。
      うしろが主語の代名詞なら、そこは**節の始まり**であって前置詞句ではない
    - `and we'd / like your set` … `like` は**動詞にもなる**(`verbBefore()`)。
      主語の代名詞・助動詞・短縮形のうしろでは前置詞として扱わない
    - `Got / it,` … 節を閉じる**裸の目的語の代名詞**の前では切らない

    **分詞は `-ing` だけではない**(2026-08 利用者の指定)。

    > 前の名詞を説明する分詞も初心者は分けて考えます。

    `-ed` と不規則な分詞も同じように切る(`postModifier()`)。
    ただし**前の名詞の説明ではないとき**は切らない。
    助動詞のうしろ(`was raised` `has been caught`)、
    冠詞・形容詞のうしろ(`a broken window`)、
    前置詞のうしろ(`by reading books`)、副詞のうしろ(`quietly selling`)。

    ただし切らないものが2つある。
    **`more than` `less than`** は数量のひとかたまり(`more / than` にしない)。
    **うしろに `-ing` を取る動詞**(`stop` `start` `keep` `enjoy` …)は、
    そのあいだで切らない。`She stopped / guessing` と切ると、訳が
    「彼女はやめた」「推測すること」に割れて意味をなさない。
    活用は語幹に戻して当てる(`stopped` → `stop`。
    **`ped` を `p` に置き換えると `stopp` になる。** 子音の重なりで戻す)

    `like this` は**前の名詞にかかる2語のかたまり**なので、
    `Apps / like this` と切ると「アプリが」「このようなものは」に割れる
    (2026-08 実機)。**前置詞の前ではなく、代名詞のうしろで切る**
  - **控えには「カタマリの英語」も一緒に残す**(`chunks.parts`・2026-08)。
    切れ目を**表示のたびに計算し直していた**ので、区切りの決まりを直すと
    数が変わり、**すでに作った訳が丸ごと出なくなった**(実機)。
    控えたときの切れ目そのものを残せば、あとで決まりを変えてもずれない
  - **「作り直しが要るか」は `chunkPairsOf()` で見る。`storedChunks()` の
    有無で見ない。** あれは英文が同じかしか見ないので、数がずれた教材まで
    「もう作ってある」と判定し、**訳が出ないのに作り直せない**行き止まりになる
  - **数が合わないときは、黙って英語だけを出さない。**
    「作り直してください」とどこで押すかまで書く
  - **控えるのは初級の区切りぶんだけ。** 中級・上級の区切りは初級の
    **一部**なので、隣どうしをつないで作る(`src/lib/chunkJa.js`)。
    3レベルぶん作らせると費用も3倍になる。
    **この「一部である」前提そのものを `npm run test:chunk` が見張っている**
  - **数が合わなければ出さない。** 英文を直した教材・決まりを直したあとでは
    数が変わる。**ずれた対は、無いより悪い**(`sentencePair.js` と同じ)
  - 窓口は増やさない。`generate-material` に `mode: 'chunk_ja'` を足してある。
    **切り終わったカタマリを渡す。** 区切りの決まりを窓口に持たせない
  - すでにある教材には、さがす画面の**「区切りの訳を作る」**で後から足せる
- **文の切れ目(`.` `?` `!`)でも切る。** 切らないと段落の中で文がつながる
  (`the ride? Can you tell me` が1カタマリになっていた)。
  文の切り方は読み上げ・QR と同じ `splitEnSentences()`。**2か所に持たない**

### ディクテーションの操作は、文ごとに持つ

2026-09 利用者の指定。

> ディクテーションのデフォルト画面で、stopボタンがあるのですが、これは
> 必要ありませんので消してください。そしてその横の再生スピード調整タブも、
> 削除しする代わりに各文につけてください。また、同じく各文にリピート再生と
> 一度の再生を切り替えるボタンも。

書き取りは**1文ずつ**の練習である。鳴らすのは各文の Listen なので、
**帯の Stop は、押しても何も鳴っていない**ことがほとんどだった
(Listen は鳴っているあいだ Stop に変わる)。速さも「どの文も同じ」になる。

- **Stop は、どの取り組み方でも帯に置かない**(2026-09 利用者の指定
  「STOPボタンが必要ないです。すべてのページで同じにしてください」)。
  **鳴らすボタンがそのまま Stop に変わる**(各文の Listen も
  「Listen (全体)」も同じ作法)。止める場所を探させない
- **通しで鳴らしているあいだは、その段落の Listen も Stop になる**
  (2026-09 利用者の指定)。

  > そもそも全体を再生を押した後は、再生中の段落の listen ボタンは
  > Stop ボタンになっているべきです

  **鳴らすボタンがそのまま Stop に変わる**のがこのアプリの作法だが、
  通しの読み上げは `SpeakButton` の**外側**で鳴らしているので、
  その段落が鳴っていることをボタンが知らなかった。
  目はいま光っている段落にあるのに、そこには「Listen」と書いてあり、
  押すと**二重に鳴り出す。** 押したら通しごと止める。
  - **見た目(`speakingId`)と、押したときの判定(`speakingRef`)を
    そろえる。** 片方だけ動かすと「Stop と書いてあるのに止まらない」になる
- **通しで鳴らしているかは、専用の状態で持つ**(`playingAll`)。
  「いま読んでいる文」の印(`speakingId`)は最初の合図が来るまで空なので、
  それだけで見ていると**押しても Stop に変わらない時間**ができる
- **速さは、①ディクテーションでは出さない**(文ごとに置いてある)。
  ③⑤(本文まるごと)には残す
- **帯が空になったら、場所も取らない**(`.passage-tools:empty`)。
  空のまま置くと、上下の余白だけが残る
- 文ごとに **Listen / くり返す・1回 / 速さ / 解答を見る** を置く
- **速さを変えるのは、その文だけ。** 開いたときの速さ(`rateId`)を
  一緒に動かすと「1つ変えたら全部変わった」ように見える(実測)。
  ただし**最後に選んだ速さは覚える**(次に開いたときの初めの速さ)
- **くり返しは覚えない。** 鳴りっぱなしになる指定なので、
  次に開いたときは必ず1回に戻す
- くり返しは `SpeakButton` の `repeat`。**止まる条件を持たせる**
  (0.3秒に満たずに終わったら失敗とみなしてやめる。
  画面から消えるときも止める。**放っておくと鳴り続ける**)

### 短すぎるものは、次とまとめる

「Hi!」だけをディクテーションの1問にしても、書き取る意味がない(2026-08 実機)。
`groupSentences()` が **22 文字に満たないかたまりを次と合わせる。**
難易度(`DICTATION_LEVELS`)を上げると、一度に覚える文が 1 → 2 → 3 と増える。

### 単語・フレーズには発音記号を付ける

`material_items.phonetic`(0020)。**教材を作る時点で一緒に書かせる。**
開くたびに引くと、1教材20項目 × ゲスト500人ぶんの呼び出しになる
(要点フレーズ `phrases` と同じ考え方)。費用も桁が違う。

| やり方 | 1本あたり |
|---|---|
| 教材を作るときに一緒に | 約10トークン × 項目数。**0.3 円ほど** |
| 開くたびに引く | 約200トークン × 項目数。**6 円ほど** |

- **控えに入れるのは記号だけ。** `/ /` は画面(`Phonetic.jsx`)が付ける
- `SECTION_FIELDS` で `vocabulary` / `phrase` の**必須**にしてある。
  発音の練習に使うので、「あったり無かったり」では困る
- **0020 を貼る前に作った教材には入っていない。** その項目は
  何も出ないだけ(`runTolerant()` が列を外して読む)

### 教材の生成は、画面から切り離して走らせる

`src/lib/generateJob.js`(2026-09 利用者の指定)。

> 教材の作成中に別のところに飛んでもバックグラウンドで作業が続くように
> してください。止まるのはキャンセルボタンを作成し、それを押したときのみに
> してください。バックグラウンドでの作成が終わったら音やポップアップでの
> 通知してください。

生成は `MaterialForm` の中で `await` していた。別の画面へ移ると
**部品ごと消える**ので、返ってきた下書きの行き場が無くなり、
待った時間(記事なら3分近い)がまるごと無駄になっていた。
**通信は続いているのに、受け取る人がいなくなる。**

- 仕事の状態は**書類(モジュール)に1つだけ**置く。ここは画面が消えても残る
- **1度に1つだけ。** 2本走らせると費用が2倍になり、
  どちらの下書きを画面に入れるのかも決められない
- **止まるのはキャンセルを押したときだけ。** 画面を離れても閉じても止まらない。
  **通信そのものは取り消さない**(送った1回はどのみち課金されている)。
  段と段のあいだで `cancelled()` を見る
- 下書きには**そのときの入力も一緒に控える**(`formSnapshot`)。
  戻ってきたときに種類やレベルが初期値に戻っていると、
  記事の下書きが「文型ドリル」として保存されてしまう
- 終わったら **`App.jsx` が音(`done`)とお知らせを出す。1回だけ**
  (`markJobSeen`)

#### 進み具合の帯と、青い小さな丸(2026-09 利用者の指定)

> 作成中の進行度合いを示すバーを作成して、どのページにいても見えるように、
> そして、完成した際も音に加えて視覚的にもわかるサインをください。
> 例えばサイドバーとトップバーの教材の横に青い小さな丸が点灯する、など

**音は聞こえないことがある**(音を切っている・レッスン中・席を外している)。
だから**目でも分かる印**を、同じ場所に残しておく。

| 何 | どこ | いつ |
|---|---|---|
| 帯(`JobBar.jsx`) | 上の帯のすぐ下に**貼り付ける** | 作っているあいだだけ |
| 青い丸(`.nav-dot`) | 左メニューの「教材」・上の帯の名前の横 | 作っている最中と、**下書きを受け取るまで** |

- **帯は `position: sticky`。** 下へ送って見えなくなるなら、置いた意味がない
- **1行に収める。折り返させない**(`.lesson-bar-main` と同じ)。
  実測で iPhone(390px)の幅では「作るのをやめる」が2段目に落ち、
  **帯の高さが2倍**になっていた。名前を縮め(`text-overflow`)、
  狭い画面では「教材の画面へ」を外す(メニューの青い丸が同じ役目をする)。
  320px でも 55px の1行に収まることを実測してある
- **名前を二度書かない。** 帯が「記事を作っています…」と出しているので、
  そこへ `記事(1/3)` を続けると「記事…記事」になる(実測)。
  文言は **`jobProgressLabel()` 1か所**(作る画面のボタンと同じもの)
- **進み具合は「終わった段 + 0.5」で出す**(`jobRatio`)。
  `done` は終わった段の数なので、そのままだと最初の段のあいだ 0 のままで、
  **動いていないように見える**
- **経過秒数も出す。** 何%かだけでは、止まったのかどうか分からない
  (通信の待ち時間と同じ作法)
- **丸が消えるのは、下書きを受け取ったとき**(`takeJobResult`)。
  `markJobSeen`(お知らせを出した印)とは**別物**である。
  お知らせで消すと「印を見て開いたのに何も無い」が起きる
- **作っている最中はゆっくり息をし、できあがったら点きっぱなし。**
  `prefers-reduced-motion` の人には動かない(どの画面にも効く決まり)
- **絵の側にも丸を出す**(`.nav-dot--on-icon`)。細くたたむと名前が消えるので、
  名前の横だけに置くと見えなくなる。**たたんでいないときは出さない**
  (同じ印を2つ見せない)
- **上の帯には、いま見ている画面の印だけ**を出す。
  「単語帳」の横に青い丸が出ても、何の印か分からない
- **色だけに頼らない。** 読み上げには `visually-hidden` で言葉を添える

### 文型ドリルは 4演習 × 10問。20問も選べる

2026-09 利用者の指定。

> 文型トレーニングでは、すべての設問を基本10問にしてください。
> 教材ひとつで40問。そして各ページの問題数を10、20の2つで調整できる
> ようにしてください。これは作成時に選べるように。また、絞り込みでも指定できるように

- **既定は10問のまま**(4演習 × 10問 = 40問)。第5.13.3節の決まりは変えない
- 4つの演習も `SCALABLE_SECTIONS` に入れた。作る画面で**演習ごとに
  「標準(10問)/ 倍(20問)/ 3倍(30問)」**を選べる
- **上限は30問**(2026-09 利用者の指定)。

  > 30問までにしてください。なぜなら弱点を3つまで指定できるからです

  弱点は3つまで選べ、窓口は問を弱点へ**できるだけ均等に**配る。
  1つの弱点に10問ずつ当てるには30問が要る(20問だと6〜7問になる)。
  **3倍が出るのは文型ドリルだけ**(`amountsFor()`)。記事・会話の設問は
  これまでどおり標準か倍である
- **上限は2か所にある。** 画面(`MAX_ITEMS`・`exerciseTypes.js`)と
  窓口(`generate-material` の丸め)。**片方だけ変えない。**
  窓口を配置し直すまでは、30問を選んでも20問までしか作られない
- **さがす画面にも「1つの演習の問数」を足した**(すべて / 10問 / 20問 / 30問)。
  **手元で絞る**(数は取ってきた教材から数えられる。表も列も増やさない)。
  数え方は `drillBucket()`(`exerciseTypes.js`)1か所。
  **本文(記事・会話)の段落数・発言数は数えない**
- **その問数で絞っていたら、作る画面もその問数で始まる**(`initial.amounts`)。
  絞り込みの項目を足したら `initial` にも足す。
  **その演習で選べない増やし方は渡さない**(記事・会話の設問に3倍を渡さない)

**10問に足りないまま出来上がることがある。** 重複を落として作り直すのを
**3回から5回**に増やした(実機で穴埋めが6問だった)。
作り直しは**足りない分だけ**を頼むので、うまくいく回の費用は変わらない。
それでも足りなければ、これまでどおり画面に「◯問足りません」と出す。

### どの演習を入れるかは、都度チェックで選ぶ

2026-09 利用者の指定。

> どの問題が何問必要なのかを都度選択できる設計にしてください。
> 今は数だけ変更できる問題を、チェックによって入れるか入れないかも
> 決めれるように。

`include`(`MaterialForm`)+ `isIncluded()` / `sectionsFor(kind, amounts, include)`。

- **外したものだけを持つ**(`{ listening: false }`)。既定は入れる。
  空のオブジェクトが「全部入れる」なので、演習を足してもここを触らずに済む
- **外せるのは `SCALABLE_SECTIONS` だけ。** 本文(記事・会話)は外せない。
  内容の理解・ディスカッション・語句は本文から作るので、
  本文が無くなると何も作れない
- **最後の1つは外せない**(チェックを押せなくする)。作るものが無くなる
- **並べるのは `defaultSectionsFor(kind)`。** `sectionsFor()` は外したものが
  消えているので、そちらを並べると**一度外した演習が画面から消えて戻せない**
- **外した演習に問数の切り替えを出さない**(効かない操作を見せない)。
  ただし**行そのものは薄くして残す。** 消すと何を外したのか分からなくなる
- 控え(`formSnapshot`)にも入れる。戻ってきたときに初期値へ戻っていると、
  外したはずの演習が「作った」ことになる

### 内容理解・語句の数は「標準 / 倍」で選ぶ

`SCALABLE_SECTIONS` と `sectionsFor()`(`src/data/exerciseTypes.js`)。
2026-09 利用者の指定「ディフォルトの数またはその倍という感じの2パターン」。

- **本文は増やさない。** 記事・会話の count は段落数・発言数なので、
  増やすと読み物の長さそのものが変わる
- **上限は20。** 窓口(`generate-material`)が 1〜20 に丸めるので、
  超えると画面に出した数と実際の数が食い違う
- **細かい数は選ばせない。** 2つで足りる

### 練習は「中身を見る」タブと同じ形にしない

タブ(丸い札)は**教材の中身を見る**もの。Quick Response のように
**教材1本を通しで練習する**ものは行為が違うので、
タブの上に**絵の付いたボタン**の行(`.practice-row`)を置く。
押しているあいだはタブの中身を出さない(注意が2つに割れる)。

- **材料は作らない。教材にある英語と日本語の対をそのまま使う**
  (`src/lib/quickResponse.js`)。AI に作り直させると課金され、しかも
  **宿題でやった文とは別の文**になる。それでは復習にならない。
  したがって **SQL も Edge Function も生成の費用も要らない**
- 対の場所は種類ごとに違う。**英文和訳は和訳が `answer` に入っている。**
  穴埋め・リスニング・内容の理解は対が無いので**出さない**
- **英語を出さなくても、答えの音は聞ける**(2026-09 利用者の指定・**方針の変更**)。

  > 英語を表示させなくても答えの音声が聞けるようにしたいです。

  もとは「答えを見る前に音を鳴らさない(鳴らせば答えが聞こえてしまう)」
  だった。けれども Quick Response は**口に出して言う**練習なので、
  自分で言ってから**耳で答え合わせをする**ほうが素直である。
  読んで確かめるより、聞いて確かめるほうが力が付く。
  「英語を見る」と Listen は**同じ段**に置く(どちらも答えそのものではない)。
  **単語帳の「日本語 → 英語」は変えていない**(言われた場所だけを直す)
- **取り組み方の切り替えは、プルダウンではなく2つの札**
  (2026-09 利用者の指摘「タブの文字をもう少し小さく」)。

  **`<select>` のままでは小さくできない。** 860px 以下では 16px を割ると
  iOS が勝手に画面を拡大するので、**下限が 16px だった。**
  札(`<button>`)は入力欄ではないので、**この縛りが無い**(12px にできた)。
  取り組み方は2つしかないので、札を並べても折り返さない
  (**6Steps のプルダウンは6つあるので、あちらはそのまま**)。

  - **名前は切らない。**「文…」では何の札か分からない(実測)。
    縮めるのではなく、`.qr-head` の側で行を分ける
  - 実測(紙の上): iPhone(390px)は **36px の1行。** 320px では2行になるが、
    名前は読める。**切って1行にするより、1行増えるほうがましである**
- 言葉づかいも単語帳にそろえる(**「まだ」/「言えた」**)。
  「言えなかった」はスマホで2行に割れた
- **順番は教材のまま。** 記事と会話には話の流れがあり、混ぜると場面が飛ぶ
  (単語帳は逆に毎回混ぜる。あちらは並び順で覚えてしまうため)
- 記録は残さない。単語帳の「箱」と2か所で動くことになる
- **取り組み方は2通り**(2026-09 利用者の指定)。
  **文章**(本文・和訳)と**フレーズ・単語**(出てきた語句)を
  プルダウンで切り替える。混ぜて1本にすると、長い文と1語が交互に来て
  頭の切り替えが追いつかない。**片方しか無い教材では出さない。**
  何問目まで進んだかは**取り組み方ごとに覚える**(鍵が別)
- **紙には両方まとめて出す**(`QuickResponseSheet`・いちばん後ろのページ)。
  練習の画面そのものは紙に出さない(ボタンも進み具合も要らない)

### 紙(レッスン表示)にボタンを置いたら、色を塗り直す

`.lesson-sheet .btn` が**紙の上のボタンを全部白い枠にそろえる**(0,2,0)。
`.btn--primary` / `.btn--quiet`(0,1,0)は負けるので、
**押し分けが要るボタンは紙用に塗り直す。**
塗り直さないと「まだ」と「言えた」が同じ見た目になる。
**色だけに頼らない。** プレインでも分かるよう、塗りつぶしと枠線で分ける。

### セッションの記録は、日付ごとに1枚(0032)

`lesson_notes`(表)+ `src/lib/lessonNotes.js` + `LessonNotes.jsx`
(2026-09 利用者の指定)。

> トレーニング中、または個々のゲストの情報内でセッションに関する記録や
> メモをするためのフリーボード、例えばワードのようなものを呼び出せると
> 嬉しいですね。それはカレンダーと同期して呼び出せるものだと嬉しいです。

- **ゲスト × 日付で1行。** レッスンはその日に1回なので、これがいちばん
  素直な形になる(`vocab_days` / `practice_days` と同じ)
- **書けるのは担当トレーナー(と管理者)だけ。ゲスト本人は読める**
  (2026-09 利用者の判断)。両方が書けると、レッスン中に同じ1枚を
  二人で上書きする。判定は 0032 の RLS と `viewer.js` で、**画面に作らない**
- **書式は持たない。** 改行だけがある白い紙。書式を保つ仕組みそのものが
  壊れどころになる
- **「保存」を押させない。** 1.2 秒だまったら送る(`useProgress` と同じ間合い)。
  **閉じるとき・日を変えるときは、書きかけを送り切る。**
  待ち時間の途中で閉じただけで消えるなら、押させないようにした意味がない
- **読み終わる前に送らない**(空でサーバーを上書きする)
- **空にしたら行ごと消す。** カレンダーは「書いてある日」に印を付けるので、
  空の行が残ると中身の無い日に印が付く
- カレンダーは `CalendarPopover`。ただし**書いていない日も押せる**
  (`anyDay`)。これから書く日を選べないと、その日のメモが作れない
- 呼べるのは3か所。ゲストのカードのプルダウン「セッションの記録」、
  レッスン表示の「メモ」(**相手がいるときだけ**出す)、
  ゲストの「今週の宿題」の畳んだカード(読むだけ)
- **ペン(書き込み)とは別物。** あちらは閉じれば消える板書、
  こちらは日付ごとに残る記録である

### 紙への書き込み(ペン)は、紙の「中」に敷く

`InkLayer.jsx` を `.lesson-sheet` の**中**に `position: absolute` で置く
(2026-09 利用者の指定)。

> ペンツールから画面に書き込んだ際、画面をスクロールすると書き込んだ内容
> だけが同じ場所にとどまり続けてしまうのですが、これを解消できませんか?

会議アプリ(Zoom・Teams)のペンは**画面のガラス面**に描いている。
こちらがページを送ったことを知らないので線は置いていかれる。
**向こうの仕組みなので、こちらからは直せない。**
紙の中に敷けば、線は紙と一緒に動いて英文にくっついてくる。

- **大きさは中身の丈に合わせる**(`scrollWidth` / `scrollHeight`)。
  文字の大きさやページで丈が変わるので、`ResizeObserver` と
  `MutationObserver` で見張って測り直す
- **測るあいだ、自分を数に入れない**(2026-09 実機
  「スマホやパッドでスクロールすると、横方向にゆらゆらと画面がズレる」)。

  板は紙の**中**にあるので、`scrollWidth` には**板自身の幅も入る。** すると

      測った幅 → 板の幅 → 次に測る幅 …

  で**一度広がった幅が二度と縮まない。** 紙の中身が一瞬でも横にはみ出せば
  ——練習の入口が1行に収まらない、長い1語が来る、文字を「特大」にする——
  その幅が板に焼き付き、**紙がずっと横に送れる状態のまま残る。**
  上下に送るたびに左右へ揺れるのは、これである。

  **測るあいだだけ板を消す**(`display: none` → 測る → 戻す)。
  `MutationObserver` は属性を見ていないので、測り直しは繰り返されない。
  実測: 924px にはみ出させたあと取り除くと、366px に**戻る**
- **押した場所は紙の中の座標に直す**(画面の座標 + `scrollLeft` /
  `scrollTop`)。送った状態で描いた線がずれる
- **ふだんは `pointer-events: none`。** 外すのはペンを持っているあいだだけ。
  そうしないと語に触れる・なぞる・解答を開くが全部できなくなる
- ペン中は `touch-action: none`。指で送れると線が途切れる
- **保存しない。ページごとに持つ。** レッスン中の板書であって教材ではない。
  残したいことはメモに書く
- **紙には出さない**(`no-print`)
- **ボタンは「表示」の中に置く**(2026-09 実機・利用者の指定)。
  > 書き込む、の機能が画面に収まってません。文字の大きさや明暗の
  > 切り替えの機能と同じところに入れてください。

  いつも要る1行は**「閉じる・ページ送り・解答・表示」の4つだけ**である。
  5つめを足したので、iPhone(390px)で**画面の外へはみ出した。**
  書き込みは始めと終わりに1回ずつ触るものなので、
  一度決める設定と同じところでよい。**「メモ」も同じ理由でここに置く**

### 書き込みのあいだは、帯をまるごと入れ替える

2026-09 利用者の指定。

> 「書き込み」を開いたときは、2列目に機能が表示されるのではなく、
> 1列目にもともとのツールバーを一時的に消して表示させてください。

一度は2段目に出したが、**2段にすると紙がそのぶん狭くなる。**
書いているあいだはページ送りも速さも触らないので、入れ替えてしまってよい。

- **戻る道は必ず先頭に置く**(「書き込みを終える」)。
  無いと、閉じ方を探すことになる
- **吹き出しにしない。** 外側を押すと閉じるので、紙に書いた瞬間に消える。
  ペンの道具は、書いているあいだ何度も触る
- 実測: ふだんも書き込み中も **50px の1行**。どちらもはみ出さない

### 書き込みの道具は3つ。**色は共有する**

`INK_TOOLS`(ペン / ハイライト / 消しゴム・2026-09 利用者の指定)。

- **ハイライトは、ペンと同じ色を使う。** 別の色の組を持つと
  「いま何色か」が道具で変わり、覚えていられない。
  変えるのは**太さ(5倍)と透け方(0.38)**だけ(`InkLayer`)
- **消しゴムは、触れた線を1本まるごと消す。** 点で削る仕組みは、
  線を切り分けて持ち直すことになり、「ひとつ戻す」と噛み合わない
- **消しゴムのときは色を出さない**(効かない操作を見せない)
- 線の太さは **2.1px**(3px の 70%・利用者の指定)。
  3px は画面共有では太く、英文の上に引くと字がつぶれていた
- 色の丸には**薄い輪**を付ける。付けないと、黒が暗い帯の上で消える

### メモの幅は、境目をつまんで変えられる

2026-09 利用者の指定「メモを開いたときには、サイドバーの幅を変えれるように」。

- 幅は **px で持つ**(`eas.lessonNotesW`・240〜720px)。
  割合にすると、画面が狭いときに紙が読めない幅まで細る
- **覚える。** 開き直しても同じ幅になる
- **キーボードでも動かせる**(← → で 20px ずつ)。
  つまんで動かす道しか無いと、届かない人がいる
- **狭い画面では出さない。** 縦に積むので、幅をつまむ意味が無い

### 読んでいるところは、紙の**まん中**に置く

2026-09 利用者の指定。

> 全体を再生中に、今再生している段落や発言がハイライトされるのですが、
> その段落や発言のボックスが画面中央に表示されるようにしてください。
> 今も追ってはくれるのですが、下の方に表示されてしまいます。

以前は `scrollIntoView({ block: 'nearest' })` だった。**あれは
「見えるところまで、いちばん少なく動かす」**ので、下から入ってきた発言は
**下端に貼り付いたまま**になる。そこは右下の操作盤と重なるうえ、
次の発言が見えないので話の流れが追えない。

- **`scrollIntoView` は使わない。** あれは紙だけでなく外側まで送る。
  紙は `overflow-y: auto` の箱なので、**その箱の `scrollTop` だけ**を動かす
  (Quick Response の枠で同じ失敗をしている)
- **画面より背の高い発言は、上をそろえる。** まん中に置くと頭が切れて、
  読み始めが見えなくなる
- **`prefers-reduced-motion` を尊重する**(滑らせない)
- 端(いちばん上・いちばん下)は、**紙にそれ以上の余地が無い**ので
  まん中には来ない。これは動かしようがない

実測(ずれ 0px で中央):iPhone 390px / PC 1440px / iPad 820px のいずれでも、
送れる範囲にある発言は**きっちりまん中**に来る。

**`PassagePractice`(6Steps の ③⑤ の通し再生)にも同じ作りがある。**
そちらは**まだ `nearest` のまま**にしてある(言われた場所だけを直す)。

### 通しの読み上げと集中モードは、右下に貼り付ける

2026-09 利用者の指定。

> 全体を再生を一度押すと、どこにも再生を止めるボタンがないので、
> 右下の集中モードの横あたりに再生中ならstop、停止中ならlistenが
> 出てる仕様にしてください。

「Listen (全体)」は**本文のいちばん上**にある。押したあと読み進めると、
**止めるボタンごと画面の外へ出ていく。** 鳴らすボタンがそのまま Stop に
変わる作法は合っていても、**その1つが見えないところにあっては止められない。**

- 置き方は `.sheet-floats`(さがす画面の `.finder-float` と同じ考え方)。
  **1つの行にまとめて、折り返させない。** 別々に `fixed` で置くと、
  片方が消えたときにもう片方が飛ぶ
- **本文のページを開いているときだけ**通しの読み上げを出す。
  ほかのページには通しで鳴らすものが無い(効かない操作を見せない)
- 「同じことをするボタンを2つ見せない」の**例外ではない。**
  これは「上のと同じもの」ではなく、**送っていったときの居場所**である

### 止めたら、次はそこから鳴らす(`playMark.js`)

2026-09 利用者の指定。

> 全文を聞いている途中にストップを押し、もう一度再生を押すと、
> また元に戻ってしまいます。止めた場所から再び再生する機能がほしいです。
> これは段落ごとの再生ボタンでも同じ仕様にしてください。

記事は6段落あるので、4段落目で止めて押し直すと**また1段落目の頭から**
だった。聞き直したいのは止めたところであって、頭ではない。

- **覚えるのは `playMark.js` 1か所。** 画面ごとに持つと、紙・宿題・
  レッスン表示で別々にずれる。画面は **`resumeKey` を渡すだけ**
- **`audioClips.js` には持たせない。** あちらは「いま鳴っている MP3」しか
  知らず、それが何番目の段落なのかを知らない。`stopClip()` は
  **秒数を返すだけ**にしてある
- **目印の付け方は2通りだけ。** 段落ごとの Listen は
  `one|<声>|<英文>`(`SpeakButton` 1か所に書けば、紙・宿題・単語帳・
  ディクテーションのどこでも効く)、通しは `all|<教材>|<演習>`
- **取り出したら消す。** 残すと、再開して最後まで聴いたあとに
  もう一度押したとき、**また終わりぎわから鳴る**
- **最後まで鳴りきったら捨てる**(`finished()`)。
  「止めた」と「終わった」を見分けるため、通しには
  **`onDone`(鳴りきったときだけ)**を足してある。
  `onIndex(null)` は止めたときにも来るので、それだけでは区別できない
- **0.3 秒に満たなければ覚えない。** 鳴り出す前に止めたか、
  **端末の声**(途中から鳴らす手段が無く、秒数は 0 で返る)である。
  中途半端に覚えるより、頭から鳴るほうが説明できる
- **一覧を切り取って渡さない**(`startIndex` を使う)。
  切ると番号がずれ、控えが**別の段落を指す**
- **送り戻し(◀◀ ▶▶)では控えを当てない**(`resume: false`)。
  行き先をこちらが決めているのに控えが勝つと、
  **押したのに動かない**ように見える

#### **`npm run test:play` が見張っている**

この決まりは、間違えても `npm run lint` にも `npm run build` にも
引っかからない。しかも間違い方が「押すたびに終わりぎわから鳴る」
「いつまでも頭から鳴る」のように、**押してみるまで分からない。**
だから**何にも依存しない形**(`src/lib/playMark.js`)に切り出して、
素の node で確かめられるようにしてある。**この検証を外さない。**

`readAloud.js` の中に書くと Supabase と `import.meta.env` を引き連れ、
**手元では一度も走らせられない。**

### 読み上げの操作盤は `PlayerBar.jsx` 1つ。置き場所は覚える

2026-09 利用者の指定。

> そうすると音声プレーヤーのUIを入れるのも良いですね。
> (上部バーもしくはフロート(切り替えられると最高))

「止めた場所から」が効くようになると、**いまどこを鳴らしているのか**が
要る情報になる。5段落目から再開したのか頭からなのかが分からないと、
押した結果を確かめられない。段落の送り戻しも同じ理由で要る。

| 置き場所 | いつ向くか |
|---|---|
| **上の帯の中**(`bar`・**既定**) | いつも同じところにある。画面共有でも相手に見える |
| **右下**(`float`) | 紙を読みながら。親指の届くところに置きたいとき |

**既定は上の帯**(2026-09 利用者の指定で右下から改めた)。

> スマホ、パッド上のフロート再生UIはディフォルトでは上部バーに
> 配置してください。幅が入らない場合はフロートUIを起動するスイッチのみを
> 配置してください。これはデバイスの画面幅により最適化される仕様に

**幅で3通りに分かれる。判断は幅だけ**(`fitsInBar` = `useWide(1380)`)。

| 幅 | 上の帯 | 右下 |
|---|---|---|
| **1380px 以上** | 操作盤ぜんぶ | 出ない(▼ で移せる) |
| **1380px 未満** | **スイッチだけ**(絵1つ) | スイッチを押したときだけ開く |

- **スイッチは、いつも見える行に置く**(`.player-launch`)。
  「表示」の中に畳むと、鳴らすまでに2回押すことになる
  (あそこは「一度決める設定」のための場所である)
- **鳴っているあいだは、押している印を出す。** 開いていなくても
  「いま鳴っている」ことがこの1つで分かる
- **狭い窓では、覚えている値によらず上の帯にする**(`spot`)。
  狭い画面には置き場所の切り替えを出していないので、右下のまま
  覚えていると**戻す道が無くなる**(行き止まりを作らない)
- **右下を出す条件に `!fitsInBar` を必ず添える。** 添えないと、
  右下を開いたまま窓を広げたときに**帯と右下の2つ**が出る(実測)
- **覚える**(`eas.playerPlace`)。一度決めれば毎回選ぶものではない
- **選んだほうだけを出す。** 同じことをするものを2つ見せない
- **`bar` は、上の帯の「中」に入れる**(2026-09 利用者の指定)。

  > 上部のバーに配置している際もフロート時と同じ幅、同じUIにして、
  > 2行にならないようにしてください。つまり、「書き込む」など
  > 他のUIの左側に1行に並んで収まるようにしてください

  はじめは**帯の下にもう1本、横いっぱいの行**を足していた。
  けれども上の帯はもともと1行に収まっているので、
  **そこへ入れれば行は増えない。**
  置き場所は `.lesson-settings` の**先頭**(「書き込む」のすぐ左)。
  **中身は右下とまったく同じ**にする(利用者の指定「同じ幅、同じUI」)
- **入るかどうかは、実測で決める。** 帯にはもともと
  閉じる・ページ送り・解答・書き込む・メモ・速さ・文字・幅・印刷が
  並んでいて、そこへ操作盤(約 380px)を足すと狭い窓では折り返す。

  | 何が出ているか | 1行に収まる幅 |
  |---|---|
  | メモあり(担当ゲストと開いているとき) | **1380px 以上** |
  | メモなし(教材の画面から開いたとき)   | 1300px 以上 |

  **広いほうに合わせる**(`useWide(1380)`)。狭いほうに合わせると、
  ゲストと開いたときだけ2行になる。これより狭い窓では**右下に出し、
  切り替えのボタンも出さない**(押しても右下のままなら、効かない操作)
- **色は帯に合わせて決め打ち**(`.lesson-bar .btn` と同じ作法)。
  変数のままだと、**明るい配色のときだけ黒い帯に黒い文字**が乗る
  (実測 rgb(74,78,84) on rgb(36,36,31))
- **帯の中では縮ませない**(`flex: 0 0 40px`)。`flex: 0 1 40px` に
  していたら、**幅に余裕がある 1440px でも進み具合の帯が 0px に潰れて**
  いた(実測)。flex の箱の中の箱は、`flex-basis` を
  「いちばん広いときの幅」として数えてくれないことがある。
  **入る幅は `flex-basis` ではなく、縮まない指定で押さえる**
- **右下では、自分で貼り付かない。** 貼り付く役は `.sheet-floats`(親)が
  持っている。ここでも `position: fixed` にすると同じ隅に2枚重なり、
  **「集中モード」が下敷きになって押せなくなる**(実測)
- **狭い画面(560px 以下)では、右下の行を折り返させる。**
  320px では操作盤の中身がはみ出して「集中モード」の下に潜り込み、
  **置き場所の切り替え(▲)が押せなくなっていた**(実測)。
  「別々に `fixed` で置かない」決まりはそのままである
  (**同じ囲みの中で折り返すぶんには、2つは一緒に動く**)
- **出す数字は「段落」まで。** 1つの段落の中で何秒めか、までは出さない
  (**数えていないものを、数えているように見せない**)
- **三角は文字(◀◀ ▶▶)で描く**(絵文字は端末ごとに形が違う)
- 実測: 1920 / 1600 / 1440 / 1380 / 1280 / 1024 / 390 / 320px の
  どれでも、**帯は 50px の1行**のまま。横のはみ出しもどこにも無い
  (メモを出した状態でも確かめてある)

### 取り組み方の説明は畳んでおく

2026-09 利用者の指定。

> 文章の前の指導のような内容、開いたり閉じたりできるようにしてください。
> 初めは閉じてて欲しいです。

> このページは、まず聞いたり読んでみたり、集中モードで単語やフレーズに
> マークしたり、内容を確認したり様々なことを総合的に行うページです。

いろいろなことを続けて行う場所なので、そのたびに同じ説明を読むことはない。
けれども**消してはいけない。** 初めて開いた人には要る。

- **開け閉めは覚えない**(「初めは閉じてて欲しい」)。
  6Steps の「やり方」は覚えているが、あちらは**ステップごとに中身が違う**
  ので開いたまま次へ進みたい。こちらは1つの演習に1つである
- **`<details>` は使わない。** 閉じている中身は、いまのブラウザでは
  **紙にも出せない**(`content-visibility` で消える)。
  `.lesson-page` と同じく**描いてから隠す**
- **紙には開いて出す。** 紙で解く人には説明が要る

### 見出しには、小さな訳を添える(0036)

2026-09 利用者の指定「1番上のタイトルに、小さな訳をつけてください」。

教材名(「意外な話」)は**短い呼び名**であって、英語の見出しの訳ではない。
そのため見出しだけが、**日本語のないまま**置かれていた。
本文を読む前にいちばん先に目が行くところである。

- 入れ物は `materials.headline_ja`(0036)。**作るときに1回だけ控える。**
  開くたびに訳させると課金が続く(発音記号・要点フレーズ・
  カタマリの訳と同じ考え方)。1教材に1つなので十数トークンで済む
- **0036 を貼る前に作った教材には入っていない**(訳が出ないだけ)。
  読むときは `opt('headline_ja')`、入れるときは列が無ければ送らない
  (`voice_ids` と同じ作法)
- **添え物なので、見出しより先に目が行ってはいけない。**
  英語のすぐ下に一段小さく、静かに置く

### レッスン表示の帯から外したもの

2026-09 利用者の指定「『全ての解答を出す』と『カラー/プレイン』を
上部のバーから削除してください」。

- **すべての解答を出す** … 解答は**1問ずつ**開く(その場に「解答を見る」がある)。
  まとめて出す道が無くなったので、控えも1つでよい
  (`closedItems` と `showAnswers` は一緒に落とした)
- **カラー / プレイン** … 左のメニューの下で選べる。**同じ設定を2か所に置かない**

### 段が多い設定は「◀ いま ▶」にする(`Stepper.jsx`)

2026-09 利用者の指定。

> 上部バーの機能について、画面幅、文字の大きさ、そして読み上げの速さ、
> 全てを画面幅と文字の大きさのUIに統一し、そして、現在の設定の左右に
> 三角を置くデザインにしてください。そうすればスペースを有効に使えます。◀︎標準▶︎

**選択肢を全部並べる形は、段の数だけ横に伸びる。** 読み上げの速さは13段
(70〜130%)、紙の幅は7段(100〜150% + 画面いっぱい)になったので、
札を横に並べる形もプルダウンも成り立たない。
三角なら**いまの1つ分**しか場所を取らず、段が増えても太らない。

- **3つとも同じ形にする**(速さ / 文字 / 幅)。片方だけ変えない
- **見出しは残す。** 同じ形になったので、見出しが無いとどれがどれか
  分からない。しかも**2つは「%」**で、値だけでは見分けられない
- **並びは小さい順。** ◀ で下がり、▶ で上がる。
  数直線と同じ向きにしないと、押すたびに迷う
- **端まで来たら、そのボタンを押せなくする。** ぐるっと回すと
  「いま端にいる」ことが分からない
- **いまの値の幅をそろえる**(`min-width` + `tabular-nums`)。
  そろえないと、押すたびに三角と隣のボタンが左右に動く。
  紙の幅だけは「画面いっぱい」が長いので、そのぶんを先に取ってある
- **三角は文字(◀ ▶)で描く。** 絵文字は端末ごとに形も大きさも違う
- **色は変数で持つ。** 帯の上だけ `.lesson-bar .stepper*` で暗い色に
  差し替える(`.lesson-bar .btn` と同じ作法)

実測: 1024px 以上で帯は **50px の1行**(前は選択肢を並べていたぶん折り返していた)。
390px でも、はみ出しはどこにも無い。

### 読み上げの速さは 70〜130%、5% 刻み

2026-09 利用者の指定。`SPEECH_RATES`(`src/lib/speechRate.js`)1か所。

- もとは5段階(80〜120%)で**言葉の名前**(速い / 普通 …)が付いていた。
  13段になると名前は付けられないので、**数字だけ**にする。
  どのみち「速い(120%)」と二重に言っていた
- **並びは遅い順。** ◀ で遅く、▶ で速くなる
- 覚えている値(`eas.speechRate`)は 80 / 90 / 100 / 110 / 120 のどれかで、
  **新しい一覧にも全部ある。** だから貼り直しも作り直しも要らない
- ここを変えると、**プルダウンで速さを選ぶ4か所**(本文の練習・今週の宿題・
  ディクテーションの各文・発音練習)にもそのまま効く。
  `{r.label}({r.id}%)` と書いてあったところは `{r.label}` にした

### 紙の幅は、パソコンでだけ変えられる(100〜150% + 画面いっぱい)

2026-09 利用者の指定。

> PCで表示する際に、紙の幅を変えれるようにしてください。110% / 120% /
> 130% 三段階で。何故かというと、ctrl＋上下だとツールバーまで拡大して
> 2列になってしまうからです。

**ブラウザの拡大は画面ぜんぶを大きくする。** 上の操作欄まで太って2段になり、
紙がそのぶん狭くなる。ここで変えるのは**紙の幅だけ**なので帯は1段のまま。

- 基準は 860px。110% = 946 / 120% = 1032 / 130% = 1118 /
  140% = 1204 / 150% = 1290px(2026-09 に 150% まで伸ばした)
- **いちばん上は「画面いっぱい」**(2026-09 利用者の指定
  「その上の最大値はそのデバイスの画面幅に合わせる設定に」)。
  **これは割合ではない。** 使える幅をそのまま使い、左右の余白 24px だけを残す。
  %で足していっても、どの端末でも「ちょうどいっぱい」にはならない
  (実測 1440px の窓で 1416px)
- **覚える**(`eas.lessonWidth`)。一度決めれば毎回選ぶものではない
  (文字の大きさと同じ)
- **狭い画面(860px 以下)では出さない。** 紙がもともと画面いっぱいで、
  広げる余地が無い(効かない操作を見せない)
- **文字の大きさとは別物。** あちらは字を大きくするだけで、
  紙が広がらないと行がすぐ折り返す

### 印刷は「隠す」ではなく「消す」。位置も動かさない

2026-09 実機「とりあえず印刷しようとするとデザインがずれまくってる」。
2ページ目から見出しの上に本文が重なり、**句読点だけが宙に浮き**、
6段落の記事が**11ページ**になっていた。原因は印刷のやり方そのもの。

| やっていたこと | なぜ壊れるか |
|---|---|
| 他を `visibility: hidden` で隠す | **場所を空けたまま**なので、隠したものの高さが紙に残る |
| 出す部分を `position: absolute` で左上へ | **位置を動かした箱は、ページの区切りをまたげない** |
| 紙(`.lesson-sheet`)は `overflow-y: auto` | **中で送る箱**をそのまま刷ると、見えているぶんしか出ない |
| 画面で選んだ文字の大きさのまま | `.lesson-sheet--xl` は 23px。**それで11ページ** |

いまは `printElement()` が **body から出したい部分までの「道筋」**に
`print-path` の印を付ける。

- 道筋から外れたものは **`display: none` で消す**(場所ごと無くす)
- 道筋そのものは**「ただの入れ物」に戻す**
  (`position` / `overflow` / 幅・高さ / 余白 / 地の色を外す)
- 出す部分も `position: static` ・ `overflow: visible` ・ 高さは中身なり。
  **文字の大きさは紙の実寸(11pt)に固定する**

こうすると中身は**ふつうの流れ**のまま並ぶので、
**ページの区切りはブラウザが正しく決められる。**

### 記事・会話の紙は「書き込むための用紙」。画面の控えではない

2026-09 利用者の指定。

> スラッシュリーディングを手書きするための少し隙間を多くとった
> 本文（訳なし）のページ、内容理解（今まで通り）、ディスカッション、
> クイックレスポンス（今まで通り）… 上記の構成にしてください

```
1枚目 本文(訳なし・行間を広く)   ← ここにスラッシュを手で書く
2枚目 内容の理解
3枚目 ディスカッション
4枚目 Quick Response
```

- **「本文に出てきた語句」は紙に出さない**(利用者の指定)。
  語の意味は画面で触れば出るので、紙に写す意味がない
- **本文の訳を出さない。** 訳があると、英語を読む前に目がそちらへ行く。
  日本語が要るときは、いちばん後ろの Quick Response の紙にある
- **隙間は縦と横の両方**。行間 2.6 と語間 0.3em。
  縦だけ広げても、語がくっついていると斜線が引けない
- **1つの演習で1枚**(`break-before: page`)。
  「中途半端なところから次の項目を入れない」(利用者の指定)
- 選り分けの目印は **`data-type`**(`MaterialBody` と `LessonView` の
  演習の箱)。**画面には指定を1つも足していない。**
  演習の種類を足したら、紙に出すかどうかもここで決める

### 紙の灰色は、画面の値をそのまま持ってこない

2026-09 実機・利用者の指摘。

> 記事を印刷したときにアイコンやグレーの部分の色が薄いが、どうにか
> できないか？全体的にもう少し濃くできるとうれしいな。

画面の薄い灰色が読めるのは、**光る面の上だから**である。紙に載せると
同じ数字でもずっと薄くなり、家庭用のプリンタは薄い灰色を網点で刷るので、
**`0.5pt` の線と `#ccc` の灰色はほとんど消える。**
番号の丸(`::before`)が「アイコンが薄い」の正体だった。

`@media print` の中では、**紙で読める濃さに引き直す。** 目安は3段。

| 何 | 紙での色 |
|---|---|
| 本文・見出し・英文 | `#000` |
| 添え物(訳・補足・日付) | `#333` 〜 `#3a3a3a` |
| 線・枠 | `#555` 〜 `#999`。**太さも 0.8pt 以上** |

- **これより薄い値を `@media print` に足さない**
- `print-color-adjust: exact` を `.print-target` に置く。
  既定ではブラウザがインクを節約して色を落とす。
  地は透明にしてあるので、全面が塗られることはない
- **絵(SVG)は線が細く、紙では消えかける。** 黒くし、線を太くする
  (`.print-target svg * { stroke-width: 2 }`)

### 紙に出るのは「いま描いてあるもの」だけ

印刷は**別窓を開かず、いまの画面に印を付けて刷る**(`src/lib/print.js`)。
だから **画面に描いていないものは、紙にも出ない。**
これで2つの穴を同時に開けていた(2026-09 実機「PDFを出力すると
中身が白紙になります」)。

- **`button` をまとめて消していた。** 英文は1語ずつ `<button>` で
  描いてあるので(`EnglishText.jsx`)、**紙では英文が丸ごと消えていた。**
  消すのは `button:not(.etext-word)` にする。
  **語を `<button>` で描いていることを、印刷の指定は知らない。**
  部品の作りを変えたら、`@media print` の側も数えること
- **開いていない演習を、描かずにいた。** タブを1つも押さずに印刷すると
  見出しだけの紙になる。**画面では隠す(`is-closed`)が、描いておく。**
  紙用の指定が `display: block` に戻し、**教材まるごと**を刷る
- **レッスン表示も、紙には全ページを出す**(2026-09 利用者の指定
  「内容確認と出てきた語句のページも正しく印刷できるように」)。
  画面は ◀ 1 / 3 ▶ で1ページずつ見るが、**紙は教材まるごとの控え**である。
  ページを送りながら3回印刷させない(`.lesson-page.is-closed`)。
  ただし**通しの練習(6Steps / Quick Response)は紙に出さない。**
  あれは画面でやることで、中身はページがすべて持っている
- ただし**「隠して描いておく」が使えない場所がある。**
  ゲストの「今週の宿題」の演習には `PassagePractice` が入っており、
  あれは `usePracticeLog('six_steps')` で**取り組みの時間を数えている。**
  隠したまま置くと、やってもいない練習が記録される。
  だから**印刷する一瞬だけ**全部を描き、終わったら戻す
  (`printId` → `useEffect` で `printElement`。
  ボタンの中で直接呼ぶと、描き替わる前の画面が紙になる)

### 見張り(`useEffect`)に、自分が書き換えるものを入れない

単語帳の出題は、10語を組む `useEffect` の見張りに **`rows`(単語帳ぜんぶ)**を
入れていた。ところが答えると、その `answer()` が答えた語を `rows` から外す。
**1語答えるたびに10語が組み直されていた**(2026-09 実機)。

- 「1 / 10 語」から先へ進まない
- この10語の結果(`doneRef`)が毎回空になり、**終わっても結果が出ない**
- 並びも毎回シャッフルされ、**押しても進んでいないように見える**

組み直すのは「読み直したとき」と「絞り込みを変えたとき」だけにする。
いまの一覧は控え(`rowsRef`)から読む。
**「何をしたら組み直すのか」を、書き換えるものの側から決めない。**

あわせて、**時間のかかる呼び出しは `try` で囲む。**
`setWordStatus` が投げると、`setBusy(null)` に届かず
**知らせも出ず、ボタンも押せないまま**になる。
これも利用者には「押しても何も起きない」に見える。

### 早く帰る条件を足すときは、その下を必ず見る

`if (e.key !== 'Escape') return` を先頭に置いたため、
**同じ関数の下にあった矢印キーのページ送りが動かなくなっていた**
(2026-08、`LessonView`)。ビルドも lint も通る。
**条件を足したら、その関数の最後まで読むこと。**

### 教材名にゲスト名を入れない

**教材は既定で全トレーナーが共有する。** 名前が入っていると
「その人専用」に見えて再利用されなくなる(2026-08 利用者の指定)。
誰に出したかは `assignments` に残っているので、さがす画面の
**「ゲスト」の絞り込み**で引ける。**表も列も増やさない。**

### ゲストの見出しは「左が誰か・右がどのくらいか」

2026-09 利用者の指定。

> 青でハイライトした部分を右に寄せて下さい。
> そして空いたスペースには、ゲストが選んだアイコンを入れれるとよいです。

| 左(`.learner-who`) | 右(`.learner-side`) |
|---|---|
| アイコン・名前・状態 | タブ・TOEIC / VERSANT / レベル |

スコアは名前の真下(左)にあったが、右へ寄せてタブの下にそろえた。
**アイコンは名前の行とスコアの行にまたがる高さ**なので、左右が同じ背丈になり、
行が余らない。**狭い画面(640px 以下)では右寄せをやめる。**
右に寄せるのは「横に並んでいる」ときの話である(カタマリの訳と同じ)。

### ゲストの一覧は「名前 → スコア → 最後 → 押すもの」

2026-09 利用者の指定「このゲストのボックス内の情報と見え方を
プロの仕事で整理してください」。

```
名前(20px 太字)            TOEIC  VERSANT  レベル   ← 上の行:左が誰か・右がどのくらいか
受講中
● 2日前 最後      [取り組みを見る] [この人を開く]   ← 下の行:左が中身・右が押すもの
```

- **アイコンは、画面のどこにも出さない**(利用者の指定
  「アイコンはいらない」)。ゲストの一覧から消したあと、
  **左メニューの自分の欄に残っていて2度言わせた。**
  「いらない」は**画面ぜんぶの話**である。1か所消して終わりにしない。
  入れ物(`profiles.avatar`・0029)と `Avatar.jsx` は残してあるので戻せる
- **囲みの中に、囲みを重ねない。** カードの中に札の箱、その隣にボタンの箱…と
  入れ子が続くと、線ばかりが目に入る。
  最後に取り組んだ日は**点と文字だけ**。上下は**細い線1本**で分ける
- **押すものは1本の行にまとめる。** 以前は取り組みが右、開くが左と
  離れた2行に散っていた
- **名前はカードでいちばん大きい**(20px)。17px ではスコアの数字(16px)と
  変わらず、どこを見ればよいか分からなかった。
  青い下線はやめ、**見出しの顔**にする(開く道はボタンにもある)
- **広い画面では、スコアの幅を決める**(300px)。中身なりにしていたので
  点数の桁でカードごとに左端がずれ、一覧がぎざぎざに見えた
  (実測 x=1013 / 1044 / 1080)
- **狭い画面では、押すものが横いっぱいを分け合う**

### 数を並べただけでは、何も伝わらない

2026-09 実機。

> 日数とか取り組みの表示がたくさんある割にパッと見て何もわからないです。
> それなら「取り組みをみる」とか、何かいい名前のボタンを押してより
> わかりやすい吹き出しなどで表示される方が良いです

取り組みの札を5つ並べていた(最後 / 2週間 / 回数 / 時間 / 種類)。
スマホでは**3行を食う**うえ、「2週間」が何の2週間なのか分からない。
**数が多いほど、どれも読まれない。**

- **いつも見えるのは1つだけ**にする(ここでは「最後はいつか」)
- 残りは**「取り組みを見る」**を押したときに、吹き出しで出す
- 吹き出しには場所があるので、**言葉で説明した名前**を使う
  (`practiceStats` の `items[].full`。札用の短い `label` と別に持つ)
- **リマインドも吹き出しの中へ。** 取り組みを見て決めることだから

**もう一度押したら閉じる。** 押したボタンの上は「外側」ではない。
そこで閉じてしまうと、そのあとボタンの `onClick` が
「閉じているから開く」と判断して**開き直し、消えなくなる**(2026-09 実機)。
`Popover` は `anchorEl` の中を押したときは閉じない。

**吹き出しは `Popover.jsx` を使う。** 出す場所の決め方(下 → 上 →
押し上げる)と閉じ方(外側・Esc)は**どの吹き出しでも同じ**である。
カレンダー(`CalendarPopover`)も同じものを使っている。

### 一覧から開く道は、1つにする

「過去の宿題を見る」「レベル・スコアを記録する」と2つ並べていたが、
開いた先の切り替えは**プルダウン**なので、
**押したものと出てくるものが結び付かなかった**(2026-09 実機
「『過去の宿題をみる』がプルダウンなのも分からない」)。

一覧には「**この人を開く**」だけを置き、
中で何を見るかは開いてからプルダウンで選ぶ。

### 見出しの中で、押すものを右上の隅に置かない

2026-09 利用者の指定「プルダウンの機能が右にあるのも使いにくい」。

**右上の隅は、いちばん指が届きにくい。** しかもスコアの真上にあったので、
押すたびに数字の列をまたいでいた。
**押すものは、見出しの下に1行取って左端から始める**(`.learner-tabrow`)。
狭い画面では横いっぱいにする。

**スコアは右のままでよい。** あれは読むもので、押すものではない。
「右へ寄せる」(2026-09 の指定)は、**読むものについての話**である。

### 名前と札は、同じ行に。隙間だけを広めに取る

2026-09 利用者の指定。

> この受講中の、アイコンは名前の右に少しだけ隙間を空けて配置してください。
> やはり下にあるとバランスが悪いです。

一度は縦に積んだが、**札のためだけに1行増える**ぶんカードの背が高くなり、
落ち着かなかった。横に戻し、**隙間(12px)だけを広めに取る。**
2026-08 の「ゲスト名と『受講中』が近すぎます」への答えは、この隙間である。
長い名前で札が次の行へ落ちても崩れないよう、折り返しは許す。

### アイコンは「選んだ印」だけを残す。絵も写真も持たない

`profiles.avatar`(0029)+ `src/data/avatars.js` + `Avatar.jsx`。

- **選ぶのは本人だけ。** 置き場所は**左のメニューの下、自分の名前の隣**の
  1か所(`AvatarPicker`)。トレーナーの画面では**出すだけ**で、
  人のアイコンは変えられない
- **写真は上げさせない。** 置き場所(Storage)が要るうえ、
  **顔写真は個人情報**である。選ぶだけなら、その心配がない
- **選んでいない人も、空の丸にしない。** 名前の頭文字を出す
  (`initialOf`)。空だと、選び忘れなのかまだ読めていないのかが分からない。
  丸の色は**名前から決める**ので、同じ人はいつも同じ色になる
  (色をこちらで持たない。表も列も増やさない)
- **絵文字を使うのは、ここだけの例外。** ふだん絵文字を使わないのは
  「隣の文字と大きさがそろわない」ためだが、アイコンは丸の中に1つ置く
  だけで、そろえる相手がいない
- **印そのものを保存する**(id と絵の対応表を持たない)。
  表を書き換えたときに、すでに選んだ人の絵が変わってしまう
- `role` は**これまでどおり誰にも書き換えさせない。** 0029 が開けるのは
  `avatar` の1列だけで、0001 の考え方は変えていない
  (`rls_test.sql` が「他人のアイコンは変えられない」を見張っている)

### ゲストのファイルは、非公開の置き場に入れる(0031)

`learner_files`(表)+ `learner-files`(Storage・**非公開**)+
`src/lib/learnerFiles.js` + `LearnerFiles.jsx`。

- **見えるのは本人と、いま担当しているトレーナー(と管理者)だけ。**
  判定は 0001 の `teaches()`。**新しい判定を作らない**
- **中身**(バイト列)は置き場、**何があるか**(名前・大きさ・置いた人・
  メモ)は表。置き場だけでは「誰のものか」を SQL で絞れない
- **道は必ず `<ゲストの id>/…` で始める。** 表の側にも置き場の側にも
  同じ決まりを書く。**片方だけでは守れない**
- **URL は押した瞬間に作る**(5分だけ有効)。一覧に持たせると、
  画面を開いたままにしているうちに期限が切れる
- **書き換えのポリシーは作らない。** 直したいときは消して入れ直す
- **中身 → 控え の順**に入れる。控えの書き込みに失敗したら、
  **置いた中身を消す。** どちらかだけが残るのがいちばん困る
- **読み上げ音声(`tts`)は公開、こちらは非公開。取り違えない**
- 第 3.2 節の「**録音音声は端末から出さない**」は変えていない。
  ここに入るのは利用者が自分で選んで置いたファイルだけである

### ゲストを1人開いたら、そのゲストだけを映す

**レッスンは、ゲストと画面を共有しながら行う**(2026-08 実機)。

> Airi の情報内にいるのにスクロールしていくとテスト太郎の情報が出てきます。
> これではレッスン中、画面共有の際にトラブルになります。

`TrainerLearners` は全ゲストのカードを並べ、開いた人のカードの**中だけ**を
広げていた。だから下へ送ると次のゲストの名前・スコア・取り組みが出る。

- 開いているあいだは、**そのゲストのカード1枚だけ**を描く
- **人数(「12 人 / 受講中 9 人」)も出さない。** それも他のゲストの情報である
- **「← ゲストの一覧に戻る」**を上に置く。戻り方が名前の押し直ししか
  無いと、探すことになる
- 開いたら**画面のいちばん上へ送る**

これは見た目の整えではなく、**情報が漏れない作り**である。
仕様書 5.5 の「他のゲストの名前は出ません」は教材を作る欄の話で、
**一覧の側で破れていた。** 画面に何かを並べるときは、
**それが画面共有に映ってよいものか**を必ず数える。

### 教材の中身は `MaterialBody.jsx` 1つ

トレーナーの「教材」と、ゲストの情報の中の「過去の宿題」で**同じものを開く**
(2026-08 利用者の指定「宿題の表示をトレーナーの教材の表示と統一」)。
**同じ見た目を2か所に書き写さない。**

- 宿題の一覧(`loadLearnerAssignments`)は**中身を持っていない**(id と数だけ)。
  開いた教材だけ `loadMaterial()` で読み、**同じ教材を二度読まない**
- 読んでいるあいだは「開いています…」に変える
- 練習の記録を消すボタンは `showReset` を渡したときだけ(トレーナーの教材のみ)

### 教材のカードに、開く・閉じるを置かない

2026-09 利用者の指定。

> わざわざ開いて「印刷・PDFで保存」のボタンを出す必要がない、
> 閉じる・開く機能を排除、そして「この教材をゲストと共有する」ボタンを
> 印刷ボタンの右側へ移してください

開いても出るのは印刷のボタンだけになっていたので、
**開くこと自体が回り道**だった。グレーの囲みは**ただの見出し**に戻す。

```
グレーの囲み(見出し。押せない)
何が何問あるか
[印刷 / PDFで保存] [練習の記録を消す]            ← 手元で扱う操作。1行にまとめる
[この教材をゲストと共有する]                      ← 人に渡す操作。教材の画面だけ
[セッションで使う(大きく表示)]
```

**「練習の記録を消す」は、印刷のとなり**(2026-09 利用者の指定
「練習の記録を消す、を印刷ボタンの右に置いてください」)。
前の2つは**同じ教材を手元で扱う**操作(紙にする / この端末の書きかけを消す)で、
共有だけが**人に渡す**操作である。

**`.btn-row` のままでは「右」にならない。** あれは折り返すので、
狭い画面では縦に積まれる。その1行だけ `.card-tools` にして
**折り返させず、入りきらないときはボタンのほうを縮める**
(`.btn--small` の `flex-shrink: 0` を戻す)。
**縮んでも文言は削らない** — 何のボタンか分からなくなる。
実測: 390px 以上は1行 34px、320px でも横に並ぶ(文字が2行になる)。

- **中身は、紙に出す一瞬だけ描く**(`printId` → `.print-holder`)。
  35件ぶんをいつも描くと、語は1つずつボタンで描いているので画面が重い。
  描かないと**見出しだけの紙**が出る(2026-08 に一度やった)
- `.print-holder` は画面では `display: none`。
  `@media print` が `.print-path` を `display: block !important` に
  戻すので、紙にはちゃんと出る
- **`MaterialBody` にボタンを置かない。** 置くと「開かないと押せないもの」に
  戻る。印刷・共有・記録を消すは**呼ぶ側のカード**にある
- 開閉が無くなったので、**語の先読みと区切りの訳の作り直しは
  「セッションで使う」で拾う**(以前は「開いたとき」だった)

### 教材のカードは、3つの画面で同じ形にする

トレーナーの「教材」/ ゲストのカードの「過去の宿題」/ ゲストの「今週の宿題」。
**1画面だけ直さない。** 同じ指定を2度言わせた(2026-09)。

> 教材モードの教材の表示と同じだと言っても伝わらないのですかね?

```
グレーの囲み(押すと開閉。右端に ▸ / ▾)
何が何問あるか
[印刷 / PDFで保存]
[セッションで使う(大きく表示)]        ← ゲストでは「大きく表示する」
```

- **「中身を見る・印刷する」の行は置かない。** 閉じる言葉も置かない。
  開けるのも閉じるのも同じ囲みなので、**同じことをするものを2つ見せない**
- **`.material-detail` に囲みを付けない。** 中に残るのはボタンだけなので、
  枠だけが空っぽで残ると中身が抜けているように見える

### 教材のカードには、中身を出さない(2026-09 利用者の指定)

> 赤で囲った部分は必要ないです。
> 教材のあるところ全てで適用してください。(ゲストエンドでも同じ)

カードを開いても出るのは**印刷 / 大きく表示する**だけ。
**指導ポイント・Quick Response・演習のタブと設問の一覧は出さない。**
中身に取り組むのは「セッションで使う(大きく表示)」、
持ち出すのは「印刷 / PDFで保存」である。

- **描くのはやめない。隠すだけ**(`exercise-view is-closed`)。
  描かないと、紙に**見出しだけ**が出る(2026-08 に一度やった)
- ただし**ゲストの「今週の宿題」は、印刷する一瞬だけ描く。**
  `PassagePractice` が入っており、隠したまま置くと
  **やってもいない練習の時間を数える**
- `TeachingNote` の**見た目は変えていない。** 出す場所だけの話

### 「やった」は申告させない。取り組んだら自動で残す

2026-09 利用者の指定「やった もいらないです / やればトレーナー側で
わかる仕組みにしてください」。

**申告は二重に当てにならない。** 押し忘れれば「まだ」のまま、
中身を見ずに押しても「やった」になる。
いまは**「大きく表示する」で開いたときと、印刷したとき**に
`assignments.learner_done_at` を**1度だけ**書く。

- すでに日付が入っていれば触らない(**最初に手を付けた日**を残す)
- **取り消す道は作らない。** 申告ではなく、起きたことの記録である
- **黙って記録しない。** ゲストには「8/30 に取り組みました」と静かに出す
- トレーナー側には**いつ取り組んだか**も出す。「やった」だけでは
  今日なのか3週間前なのかが分からない
- 入れ物は増やさない。**SQL は要らない**

### 教材の画面は「探す / 条件で絞り込む / 作る」の3つ

2026-09 利用者の指定。

> 教材をさがす、作るの欄で条件を入れると教材が絞り込まれますが、
> UIがわかりにくいです。「探す🔍」「条件で絞り込む」「作る」の3つを
> ベースにしつつ、絞り込み、検索結果内にも「ないので作る」のボタンを
> 配置してください

以前は「教材をさがす・作る」という**1つの箱に条件の欄だけ**が入り、
名前で引く入力欄はその**外**にあった。同じ「さがす」が2か所に分かれ、
「作る」は名前だけあってボタンが無い。**何をする欄なのかが読み取れない。**

```
教材をさがす                    [＋ 教材を作る]   ← ③ いつも見える
🔍 [教材名・見出しでさがす ] [新しい順 ▾] 35 件   ← ① 畳まない
▸ 条件で絞り込む (3)                              ← ② 畳める
```

- **③「作る」は、いつも見えるところに置く。** 名前だけでは押せない
- **②は畳めるが、いくつ条件が付いているかは畳んだままでも出す**
  (`.finder-badge`)。出さないと**なぜ1件しか出ないのか分からない**
- **条件を外す道を、条件が付いているときだけ出す**
  (「絞り込みをぜんぶ外す」)。1つずつ「すべて」に戻して回らせない
- **一覧のいちばん下にも「無いので作る」を置く**(`.finder-more`)。
  無いと分かるのは下まで見たときなので、画面の上まで戻らせない。
  0件のときは、その1枚だけを出して青くする(`.finder-empty`)
- **一覧の途中では、画面の隅に小さく出す**(`.finder-float`・2026-09 実機)。
  36件もあると、上の箱もいちばん下も画面の外にいる時間が長い。
  **その2つのどちらも見えていないあいだだけ**出す
  (`IntersectionObserver`。同じことをするボタンを2つ見せない)。
  **青の塗りつぶしにしない。** 既定の動線は探すことである
- **「条件で絞り込む」は、はじめは閉じておく**(2026-09 利用者の指定)。
  欄が5つ縦に並ぶので、開いたままだと教材が1件も見えない。
  開け閉めは覚える。**鍵の名前を変えた**(`eas.finderFilters`)ので、
  前に「開いている」を覚えた端末も、いったん閉じた状態から始まる
- **条件が付いていれば「この条件で教材を作る」**と書く。
  押したあと何が引き継がれるかが、押す前に分かる

### さがす画面は「見比べる」ための画面。1件を長くしない

行そのものを押すと中身が開く(▸ / ▾)。カードにいつも出しておくのは
**「セッションで使う(大きく表示)」と「ゲストと共有する」の2つだけ。**
すぐしたいのは中身を眺めることではなく、使うか配るかである。
**青の塗りつぶしは「セッションで使う」。** 共有は小さく灰色にする
(2026-08 利用者の指定。さがした直後にするのは、まず開いて見ることである)。

**弱点の札だけを目立たせる**(`mtitle-chip--main`)。何の弱点の教材かは
選ぶときにいちばん見る情報なので、レベルや業界と同じ見た目だと埋もれる。
**どれが弱点かは呼び出し側から渡す**(`weakness`)。札の並び順から当てない。
見出しそのものが弱点のとき(文型ドリル)は、札で繰り返さない。

**日付は右上、その下にカテゴリー名**(2026-08 利用者の指定)。
横に並べると、どちらが日付でどちらが種類なのか読み取りにくい。
日付は `MaterialTitle` にも出せるので、**2か所に出さない**
(さがす画面では `hideDate` を渡す)。

**弱点タグを2回出さない。** 弱点は教材名の中にすでに入っており、
`MaterialTitle` が出す(ドリルは見出しそのもの、記事はグレーの札)。
別に札の行を並べない。手で名前を付けた教材のためには、
`fallbackTags` の先頭に弱点を渡す。
**この決まりは、ゲストの「過去の宿題」にも同じように効かせる。**

### 押せる場所は、押す前から押せると分かるようにする

`:hover` だけで「押せる」を示さない(2026-08 利用者の指定)。

> タップした時にタイトル周りが薄いグレーで囲まれますが、
> タップする前からその仕様にしてください。
> 上部のグレーの部分を押して開くことは押してみるまで分かりません。

**触る端末には「カーソルを載せる」が無い。** `:hover` で色を付けると、
**押したあとに初めて色が付き、しかもそのまま残る。**
押せる場所を探すのに、片っ端から押してみることになる。

- **囲みは、はじめから出しておく**(`.material-open` の `--surface-2`)
- **囲みだけでは足りない。** 押すと何が出るのかは言葉でしか書けないので、
  **「▸ 中身を見る・印刷する」**と書いた行を囲みの中に置く(開くと「閉じる」)
- カーソルでは**色を変えず、輪郭を出す**(`box-shadow: inset`)。
  色をもう一段足すと、明るい側と暗い側で「濃くなる / 薄くなる」が逆になる

さがす画面のカードの中身は **見出し → 札(レベル・業界)→
カテゴリー名(左)と日付(右)→ 中身を見る**の順(2026-08 利用者の指定)。
日付とカテゴリーを右上に縦に積んでいたが、読み取りにくかった。

### 一覧は「1件 = 1枚のカード」。線1本で区切らない

細い線で区切って余白も詰めると、**どこからどこまでが1件か分からない**
(2026-08「宿題と宿題の区切りがパッと見て分からない」)。
枠線 + 角丸 + 地色のカードにし、**カードどうしのあいだを空ける**
(1問ごとのカード `.qa-row` と同じ考え方)。中の要素にも息をつかせる。

### いつも 0 になる数字を、画面に出さない

「学習した日 0 日 / 合計 0 時間」を出し続けていた。ゲストが自分で
入力する仕組みをやめた(0022)ので、**もう誰も入力しない数字**である。
**数えていないものを、数えているように見せない。**
いまは `practice_days` を裏で数えたものを出す(`practiceLine()`)。


中身の抜き書き(記事の1段落目)を並べていたので、**1件で画面ぜんぶを
食っていた**(2026-08「教材の内容の要約のような部分、いらないです」)。
出すのは**何の演習が何問あるか**だけ(`.material-parts`・1行に畳む)。
中身は「中身を見る」で開ける。**一覧に足すものは、1行に収まるかで決める。**

### さがす画面の指定は、作る画面へ「全部」引き継ぐ

`TrainerMaterials`(さがす)→ `MaterialForm`(作る)の `initial` に
**さがす画面にある指定を全部渡す。** 一部だけ渡して2度同じ失敗をした。

1度目は弱点タグ、2度目は種類(ダイアローグを選んだのに文型に戻る)(2026-08)。
どれが残ってどれが消えるのかは利用者には見分けられず、気まぐれに見える。

**絞り込みの項目を足したら、`initial` にも必ず足すこと。**

### 読み上げは `readAloud.js` を呼ぶ。端末の声を直接使わない

**iPhone では、どのブラウザを使っても良い声を出せない。**
iOS はすべてのブラウザの中身が Safari(WebKit)であり、
高品質音声を Web Speech API に**一切公開しない**(実機で生47件・premium 0件)。
2026-08、知り合いの iPhone + Chrome で「最低な声」が出た、という報告で確定した。

そこで教材の英文は **こちらで作った MP3 を配る。**

```
tts/<話者>/<英文の SHA-256>.mp3   ← 場所は計算で決まる。目録の表は持たない
```

- 画面はまずこの場所を `<audio>` に渡す。**あればそれで終わり**(CDN と端末の控え)
- 無ければ窓口 `speak` が Azure に作らせて置き、場所を返す
- それも駄目なら**端末の声に落ちる。画面には何も出さない**

**画面はどちらを鳴らすか決めない。** 英文が出る場所は4つある。
4か所に分岐を書けば必ず食い違う。`src/lib/readAloud.js` の
`readAloud()` / `readAloudSequence()` / `stopReading()` だけを呼ぶこと。
`speech.js` の `speak()` は受け皿であって、画面から直接呼ぶものではない。

**速さでファイルを増やさない。** MP3 は自然な速さで1本だけ作り、
5段階の速さは `playbackRate` で変える。作り分けると費用も容量も5倍になる。

**語の重みの配り方は `wordTiming.js` 1つに置く。** 端末の声と MP3 で
別々に書くと、同じ本文なのに色の進み方が変わる。

**`<audio>` は1つだけ作り、作り直さない。** 最初に画面へ触れた瞬間に
無音を鳴らして解錠する。iOS は「押した流れの中で始まった再生」しか許さず、
MP3 を取りに行く待ちで流れが切れるためである
(`AudioContext` を1つだけ作るのと同じ理由)。

**会話の話者は `castClipSpeakers()` で決める。端末の声から換算しない。**
英語の声が1つも無い端末では、換算すると2人とも同じ話者になる。

`tts` バケツに**録音は入らない。** 入るのは教材の英文の読み上げだけ。
だから public にしてある。**書き込めるのは窓口だけ**(0016 はそれ以外の
書き込みポリシーを1つも作らない)。ここを緩めない。

**音声を作れなかったら、係の人に知らせる**(2026-09 実機)。
`speak` を配置していなかったあいだ、画面は**黙って端末の声に落ちて**いた。
理由(`lastClipDetail`)は集めていたのに、**どこからも読んでいなかった**ので、
「良い声にならない」ことに何日も気づけなかった。
`onClipTrouble()` で `App.jsx` に伝え、**トレーナーと管理者にだけ**静かに出す。
**ゲストには出さない**(仕組みの内側の話で、できることが何も無い)。
役割が分からないうちも出さない(既定は「見せない」)。

**良い声を用意できなかったときは、良い段の場所に置かない**(2026-09 実機)。
以前は頼まれた段のまま置いていたので、`ELEVENLABS_API_KEY` を入れる前に
一度でも鳴らした英文は、**標準の声の MP3 が良い段の場所に居座り、
あとから鍵を入れてもその英文だけ永久に標準の声のまま**だった。
`speak` は**実際に作った段**(`madeTier`)で置く。落ちているあいだは
画面が毎回窓口を呼ぶが、**作り直しはしない**ので課金は増えない。

**すでに置かれてしまった MP3 は、放っておいても直らない**(2026-09 実機)。

> Mika のひとつ目の発言だけ、明らかに ElevenLabs ではない酷い音声に
> なってしまいます。他でも起こりそうなので原因究明して改善してください。

**1つの発言だけがおかしい、という形で出る。** 直す前の窓口は、良い声で
作れなかったときも**良い段の場所に標準の声で置いていた。** 画面はその場所を
見に行き、**あればそれで終わり**にする(速いから)。だから窓口を直しても、
**一度でも鳴らした英文だけは永久に標準の声のまま**になる。
鍵を入れる前に Listen を1回押した発言 —— たいてい**1つめ** —— がこれになる。

- **これから作るものは、`madeTier` で直っている。**
  ただし**利用者が `speak` を配置し直すまでは増え続ける**
- **すでに置かれたものは、こちらから作り直させる。**
  `speak` に `force`(あっても上書き)、画面に
  **「読み上げ音声を作り直す」**(`remakeClips.js` + さがす画面のカード)
- **自動では作り直さない。** 作り直しはそのまま ElevenLabs への課金になる。
  押す前に**本数と、課金になること**を書く(**見えない費用は管理できない**)
- **2段で押させる**(1回目は「本当に作り直す」に変わる)。
  押し間違いがそのまま課金になる
- **同じ URL のまま中身だけが変わる。** 置くときに1年もつ指定を付けてあるので、
  そのままでは古い音が鳴る。**うしろに印(`?v=`)を足して別の URL にする**
- **「もうあるので作りませんでした」で返ってきたら、失敗として扱う。**
  それは**窓口がまだ古い**(`force` を知らない)という意味である。
  成功と失敗を同じ見た目で終わらせない
- 作り直すのは**良い段の英文だけ。** 標準の段(ドリル・単語)は
  Google / Azure の無料枠で作られていて、**段を取り違えようがない**

**声は2段。判断は `voiceTierFor()`(`src/lib/voiceTier.js`)1か所。**
窓口は渡された段に従うだけ。**判断を2か所に置かない。**

| 段 | 音声 | どこで |
|---|---|---|
| `premium` | ElevenLabs | 記事・会話の本文 / リスニング / **弱点が「発音」「リズム」** |
| `standard` | Google Chirp 3: HD と Azure Neural | ドリル・単語・フレーズ・吹き出し |

**Azure の無料枠(F0)は HD 音声を含まない。** DragonHD を指定すると失敗する。
Azure の役目は標準の段なので、HD でない Neural を使う。

**標準の段は話者ごとに会社を固定する**(`SPEAKER_PROVIDER`)。
女性=Google / 男性=Azure。両方の無料枠を使って毎月150万文字まで無料になる。
**1本の教材の途中で会社を切り替えない。** 役の声が変わって聞こえる。

| Secrets | 使われる声 | 無料枠 |
|---|---|---|
| `GOOGLE_TTS_API_KEY` | Google Chirp 3: HD | 毎月100万文字 |
| `AZURE_SPEECH_KEY` + `AZURE_SPEECH_REGION` | Azure Neural(HD 不可) | 毎月50万文字 |
| `ELEVENLABS_API_KEY` | ElevenLabs | 無し(従量) |

### 声の名簿は `src/data/clipVoices.js` 1か所。**利用者が編集する**

**2026-09、利用者が選んだ10人が入った**(米2・英4・豪4)。
これで `hasPremiumVoices()` が true になり、**良い声の段が動き出す。**
`ELEVENLABS_API_KEY` を Supabase の Secrets に入れるまでは、
`speak` が標準の声に落として作る(壊れないが、良い声にもならない)。

- **`use` は全員 `both` から始める。** ElevenLabs 側の分類を
  こちらで確かめていないので、決めつけない。決めつけて `conversation` を
  付けると、記事の選択肢からその声が消え、**探しても見つからなくなる**
- **足すだけなら `CLIP_REV` を進めない。** 置き場所は
  `<版>/<段>/<声の id>/<指紋>.mp3` で、**声の id が道に入っている。**
  進めるとすでに作ったすべての音声が作り直し(= 再課金)になる。
  進めるのは**既存の行の `elevenId` を差し替えたとき**だけ


1行1声。`id` / `accent` / `gender` / `use` / `label` / `elevenId`。
**Voice ID もここに書く。Secrets に分けない。**
名前と性別と向きは画面が要るので、名簿はどのみちコード側に要る。
id だけ Secrets に置くと**30行を2か所でそろえる**ことになり、必ずずれる。
Voice ID は鍵ではない(それだけでは何もできない)。**API キーだけが鍵。**

- **`id` は一度決めたら変えない。** 変えると置き場所が変わり、作り直しになる
- 空でも壊れない。標準の声(Google / Azure)で読み上げる
- `use` は **`narration` / `conversation` / `both`**。
  ElevenLabs の分類をそのまま持ち込んだもの(2026-08 利用者の指定)。
  **記事の朗読に感情豊かな声を当てると芝居がかって聞きづらく、
  会話に淡々とした声を当てると人と話している感じがしない。**
  会話 → 会話向き、記事・ドリル・単語 → ナレーション向き。
  判断は `voicePurposeFor()` 1か所

### 声は教材ごとに選ぶ。`materials.voice_ids` は**並び**

`voice_ids text[]`(0017)。会話は2人以上が話すので1つでは足りない。
**先頭から順に、出てくる話す人へ当てる**(`castClipSpeakers`)。
名前から性別を推測して当て直さない。選んだとおりにならないと説明できない。

- 画面は**訛りだけ選べばよい。** 指名しなければ、その訛りから
  **男女交互におまかせ**で選ぶ(`pickVoices`)
- **おまかせは作るときに1回だけ決めて保存する。** 開くたびに選び直すと、
  同じ教材なのに毎回ちがう声になり、そのたびに課金される
- **DB でも窓口でも選択肢を縛らない。** 声を足すたびに移行を貼ったり
  関数を配置し直したりするのでは、利用者が声を選び直せない
- **標準の段(Google / Azure)にはこの訛りが無い。** `baseOf()` で
  代役(`us-female` など)に落とす。置き場所も
  **良い声は名簿の id / 標準は代役の名前**にしてあるので、
  同じ訛り・同じ性別の声どうしはドリルの音声を共有できる
- **訛りを増やすと音声も増える。** 鍵は(段, 声, 英文)なので、同じ英文を
  3つの訛りで作れば3倍かかる。再生のたびに選ばせない

**`CLIP_REV` は2か所にある。** `src/lib/audioClips.js` と
`supabase/functions/speak/index.ts`。**声を変えたら、両方を1つ進める。**
進めないと前の声の MP3 が返り続け、片方だけ進めると画面が見に行く場所と
窓口が置く場所が食い違い、**毎回作り直して毎回課金される。**

### 読み上げのボタンは「Listen」/「Stop」。絵は `Icons.jsx` から

読み上げの操作は、どの演習でも **`Listen` / `Stop` の対**(通しは `Listen (全体)`)。
**片方だけ英語にしない。** 対になる操作は、同じ言葉づかいにする。
以前は「お手本」「聞く」「お手本を聞く」と場所ごとに違った(2026-08 に統一)。
**日本語で書く決まりの例外。利用者の指定である。**

絵文字(`🔊` `🎤`)は使わない。端末ごとに形も色も違い、文字と大きさが揃わない。
`src/components/Icons.jsx` の絵を使う。大きさは `1em`。**px で固定しない**
(レッスン表示は文字を3段階に変えられる)。

### 会話は、話す人ごとに声を変える

`src/lib/voiceCast.js` の `castVoices()` を使う。同じ声だと、
どちらが話しているのか耳で分からず、役を追えない。

**通しの読み上げは `speakSequence()`(`src/lib/speech.js`)を使う。**
`speak()` は前の読み上げを止めるので、続けて呼んでも最後の1本しか鳴らない。

### タブは `Tabs.jsx` を使う。折り返させない

スマホの幅でタブが2行に折り返し、「教」「材」のように縦に割れて
読めなくなった(2026-08)。**折り返さず横に流し、選んでいるタブを
見える位置へ寄せる。** 画面の切り替えは下線、教材の中の切り替えは丸い札で、
階層を見分けられるようにしてある。

### 同じ分野の中の「種類」は、2段の欄で選ぶ

`parent` / `short` / `kindsOf()`(`src/data/industries.js`・2026-09 利用者の指定)。

> 各趣味、業種は、同じ業種内に種類がある場合はさらにメニューが展開して
> 選べるようにしてください。

**入れ物は1つのまま**(`materials.industry`)。保存されるのは
**選んだ種類の id**(`consult_con` など)で、**階層は保存しない。**
だから SQL も要らないし、種類をあとから組み替えても既存の教材は壊れない。

- 1つめの欄には**親だけ**を出す(`industriesIn`)。
  種類があるときだけ2つめの欄が出る(**効かない操作を見せない**)
- 種類の先頭は**「全般」(親そのもの)。** 決めきれない人が行き止まりに
  ならないようにする。**全般では、種類ぜんぶの場面・話題を集めて出す**
  (`ownOf`)。親に場面を持たせていないので、そうしないと共通しか出ない
- さがす画面で「全般」を選んだら、**その中の種類の教材もまとめて出す**
  (`searchMaterials`)。コンサルを選んで建設・車が出ないのでは意味がない
- **`label` は、それだけで通じる名前にする**(「コンサル(建設)」)。
  教材の名前や絞り込みに出るため。2つめの欄に出す短い名前が `short`
- **さがす画面と作る画面で、同じ形にする**(片方だけ2段にしない)

### 一度入れた業種・趣味を、勝手に減らさない

2026-09 利用者の指定。**プロジェクトを超えた決まり**である。

> 一度入れた業種や趣味は勝手に減らさないでください。
> 一度追加した要素は指示がない限りは勝手に変更を加えないでください。
> これはプロジェクトを超えたルールです。業種や趣味については追加を
> お願いしたことはありますが、減らしてくださいとは一度も頼んでいません。

**「並べ替え」も「減らす」に当たる。** 2026-09 に「種類があるときは2段に」
という指定を受けて、**趣味にも当ててしまった。** ところが「スポーツ」
「食べること」「音楽」という親は**こちらが勝手に作ったもの**で、
その下にゴルフ・料理・食べ歩き・ワイン・DJ を押し込んだ結果、
1つめの欄に出る趣味が **15 → 6 に減って見えた。**

- **2段にしてよいのは「同じ分野の中の種類」だけ**
  (コンサル(建設 / 車)のように、もともと1つの分野が分かれる場合)
- **無い親を作って、既にあるものをその下に入れない**
- 消さざるをえないときも、**`id` は残す。**
  その id で作った教材の名前が出なくなる

### 種類を持てるのは2段まで

`parent` は1つしか持てない。3段にしたくなったら、
**中の1つを1つめの欄へ上げる**(2026-09、格闘技を MMA / ボクシングの
親にするために、スポーツの種類から分野へ上げた)。
上げても**見えなくなるわけではない。** むしろ上に出る。

### 格闘技は、それだけで1つの分野。**種類はその中に並べる**

2026-09 利用者の指定。**3手かかっている。経緯ごと残す。**

| | 言われたこと | どうしたか |
|---|---|---|
| ① | — | 格闘技はスポーツの種類だった(MMA もボクシングも無い) |
| ② | 「格闘技には、MMAとボクシングを」 | **種類は2段まで**なので、格闘技を1つめの欄へ上げ、下に2つを並べた |
| ③ | 「スポーツの中に MMA・ボクシング・柔術を追加してください」 | 3つをスポーツへ移した |
| ④ | **「スポーツの中に MMA とボクシングは必要ない。格闘技の中に、さまざまな格闘技を入れ…ムエタイもほしい」** | 格闘技の下に戻し、種類を9つに増やした |

**③はこちらの読み違いだった。** 利用者が言っていたのは
「**格闘技の一覧を充実させたい**」であって、
「スポーツの中に混ぜたい」ではなかった。
**言われた言葉のとおりに置く前に、その人が何をしたいのかを考える。**

いまの格闘技の種類(9つ + 全般)。

    全般 / MMA / ボクシング / キックボクシング / ムエタイ /
    柔術 / 柔道 / レスリング / 空手 / テコンドー

- **1つずつ並べる。まとめない。** 同じ「格闘技」でも、ジムで交わす言葉は
  別物である。ボクシング(手だけ)/ ムエタイ(肘膝と首相撲)/
  柔術(寝技と帯)/ レスリング(タックル)/ 柔道(投げと一本)。
  場面・話題もそれぞれに用意してある
- **`id` は一度も変えていない。** 変えると、その id で作った教材の
  名前が出なくなる。②③④で動かしたのは `parent` だけである
- **「全般」を選べば、種類ぜんぶの場面・話題が並ぶ**(`ownOf`)

### 業界は `src/data/industries.js` 1か所。**趣味・娯楽も同じ列に入れる**

**表も列も増やさない。** `materials.industry` の1列のまま、`group` で
「お仕事」と「趣味・娯楽」に分けて見せるだけ(`<optgroup>`)。
`group` が無いものは、これまでどおり「お仕事」。

> 業界を選ばない場合に選べるようにしたいのが、「趣味・娯楽」です。
> (2026-08 利用者の指定)

**生成の窓口には、id ではなく日本語の名前を渡す**(`industryText`)。
以前は `it の場面に寄せること` と入っており、**AI には何のことか伝わらない。**
趣味を足したことで `listening` `watching` のような語が増え、
「聞き取りの場面」と読まれる危険が出た。
**データベースに入れるのは、これまでどおり id。**

### 場面・話題は「分野ごとの特化 + 共通」で出す

`src/data/genres.js` 1か所(2026-08 利用者の指定)。

> どの業種でもあるような共通のシチュエーションはそれぞれに入れておいて
> ください。なぜなら例えば朝のミーティングでも業界によって使われる単語や
> フレーズが異なるからです。

`scenesFor(industry)` / `genresFor(industry)` が
**その分野に特化したもの + どこにでもあるもの**を並べて返す。

- **共通の側は、仕事と趣味で別に持つ。** 仕事の「真面目な会議」を
  ゴルフに出しても、その人が英語を使う場面にはならない
- **登録の無い分野は、共通だけになる。** 分野を足して場面・話題を
  足し忘れても壊れない
- **id は分野の名前で始める**(`sur_preop` / `wing_grape`)。
  `materials.scene` / `materials.genre` に保存されるので**変えない**
- **分野を変えたら、場面・話題もその分野のものに入れ替える。**
  入れ替えないと、外科医の教材に「打ち合わせ前の雑談」が残る。
  いまの選択がその分野にもあれば、そのままにする
- 名前引き(`sceneLabel` / `genreLabel`)は全部から探す。
  **これまでに作った教材の名前が出なくならないように**

### 教材を作る欄の並びは、利用者が決めている

**レベル → 業界・趣味 → トレーニングの種類 → 話題 / シチュエーション → ゲスト**
(2026-08 利用者の指定)。`MaterialForm.jsx` 1か所なので、
トレーナーの「教材」からでも、ゲストのカードの「この人に教材を作る」からでも
同じ並びになる。**並びを変えるときは、この指定を確かめること。**

**「読み上げの声」は、種類・場面のすぐ下に置く**(2026-09 利用者の指定)。

> 話者(国・訛り)の指定が、記事・ダイアローグ、会議を選んだ時点で
> 表示されるよう改良してください

以前は「ゲスト」より**下**にあったので、記事や会話を選んでもそこから
見えず、**送らないと気づけなかった。** 種類を選んだ流れのまま、
誰が読むかまで決められるようにする。
**上の5つの並びは崩していない** — 声をゲストの前に入れただけである。

**訛りの欄と「話す人」の欄は、下端でそろえる**(`.voice-row` の
`align-items: flex-end`・2026-09 実機「話者を指定するタブが縦にずれて
表示されます」)。訛りには**見出しを出していない**(2026-08 利用者の指定)
ので、見出しの1行(16px + 隙間 4px)だけ背が低い。
既定の `stretch` では入力欄がそれぞれの箱の**上**に置かれ、
**訛りだけが 20px 高い位置**に出ていた。
下でそろえれば、見出しの有る無しにかかわらず1直線に並ぶ。
**見出しを足して直さない** — 出さないと決めた見出しである。

**業界と趣味は、プルダウンを2つに分けて同じ行に左右**に並べる。
入れ物は `materials.industry` の1列のままで、**表も列も増やさない。**
教材が持つ場面は1つなので、片方を選ぶともう片方は空に戻る。

### ゲストのカードから作るときも、まったく同じ画面にする

**片方にだけ注意書きを足さない**(2026-08 利用者の指定)。

> 教材作成のデザインはゲストモード内でも教材モードと全く同じにしてください。
> ゲストモード内は余計な文言が多すぎて無駄が多いので注意書きも
> 教材モードと同じくほとんどない状態にしてください。

外したのは2つ。どちらもゲストのカードからのときだけ出ていた。

- 「◯◯さんだけに共有されます」— **選べるゲストがその人1人しか出ていない。**
  画面を見れば分かることを、文で言わない
- 「1人に絞ったので…教材名にもお名前が入ります」— **もう本当ではない**
  (教材名にゲスト名を入れない)。同じ英文を二度出さない仕組みは、
  選んだ人数によらず働く

**古い注意書きは、消し忘れると嘘になる。** 仕様を変えたら、
それを説明していた文も一緒に探すこと。

### 画面に出す長い日本語は `TeachingNote.jsx` を通す

指導ポイントを1つの `<p>` に流したところ、改行も区切りも無い棒になり
読めなかった(2026-08 の指摘)。行で区切り、【弱点名】を見出しにし、
**日本語の中の英語を太字にする。** 古い教材(改行の無い長文)は
句点で切って並べるので、作り直さなくてよい。

**`\n` という「文字」が混じることがある**(2026-08 実機)。
画面に `\na few/a little は…` と出ていた。これは本物の改行ではなく、
**バックスラッシュと n の2文字**である(AI が JSON の中で改行を書こうとして、
そのまま文字になったもの)。**すでに作った教材にも入っている**ので、
作り直させずに、`toLines()` が出すときに本物の改行として扱う。

### 開く手段を、環境の判定1つに賭けない

語の意味の吹き出しを「カーソルが使える端末ならカーソル、そうでなければ長押し」で
出し分けたところ、`(hover: hover) and (pointer: fine)` が false になる端末で
**何をしても開かなくなった**(2026-08)。

**どの操作で来たかは `pointerType` で見る。** 環境の当て推量に賭けない。
語の意味は **パソコン=クリック / 触る端末=タップ(指を離したときに開く)**。
カーソルを置いただけでは開かない。

**長押し(450ms)はやめた**(2026-08 実機・3度目の指摘)。

> タッチで使用するデバイスだと相変わらずスクロールしようと
> タッチすると単語の意味が出てしまいます。

長押しは**押しているあいだに開く。** 指を置いて少し迷ってから画面を送ると、
**送り始める前に 450ms が過ぎて開く。** 動いたら取り消す見張りを足しても、
**動く前に開いてしまうので間に合わない。**
時間で判定するかぎり、この穴は塞げない。

**「指を離したときに決める」。** 画面を送るときの指は**必ず動く**ので、
「動かずに離れた」ことがそのままタップの証拠になる。
押している長さは見ない(迷って長く触れても、動かなければタップ)。

**ダブルタップにはしない**(利用者が案として挙げたが採らなかった)。
iOS では拡大の操作とぶつかり、**2語以上をなぞって調べる道**とも
両立しにくい。1回叩くだけで開くほうが、調べる回数の多い読解では速い。

**送りを止めるための1回は、タップとみなさない。**
画面が流れているときに叩くのは「止めたい」であって「調べたい」ではない。
`lastScrollAt`(書類ぜんぶに1つだけ置く)を見て、
**直前 400ms 以内に画面が動いていたら開かない。**

タップのあとには `click` が続けて起きる。**この1回を捨てないと、
開いた直後に閉じる。**

### なぞっているあいだは、画面を動かさない

**最初の一動きで決め、決めたら最後まで貫く**(2026-08 利用者の指定)。

> 2語以上を選択しようと横に移動や、下の行にドラッグしようとすると、
> スクロールが始まってしまい上手く出来ません。

- 語の上で指を置いたら `'undecided'`。**最初の 10px は画面を動かさない**
  (`preventDefault`)。判断を保留するため
- 10px を超えたら、**横が縦より大きければ `'drag'`、そうでなければ `'scroll'`**
- **`'drag'` のあいだは縦横とも動かさない。** そうしないと
  「横に動かしてから下の行へ」ができない
- 見張りは **`{ passive: false }` で自分で付ける。**
  React の `onTouchMove` は passive なので `preventDefault()` が効かない
- **あとから止めることはできない。** iOS はいったん画面送りを始めると
  `touchmove` を取り消せなくなる。だから最初の一動きで決める

**タップの判定は「動いたら・画面が送られたら、やめる」。**
iOS は画面が動き始めると、その要素へ pointer の知らせを送らなくなるので、
語に来る `pointermove` / `pointercancel` だけでは気づけない
(2026-08 実機「スクロールしようとすると単語の意味が出て来てしまいます」)。
縦に送るとき指は**同じ語の上に留まる**ので、語が変わったかでも足りない。
**書類ぜんぶに2つ見張りを置く。**

- 指が **10px** より動いたか(`pointermove` / `touchmove`)
- **画面が動いたか**(`scroll`。**伝わらないので capture で拾う。**
  紙は `overflow-y: auto` の要素なので、window だけでは届かない)

画面が動いたあとの指離しは、**なぞりと見なさない。**
`touch-action: pan-y` は**使えない**(インラインの箱には効かない)。

### 触る端末では、押した手応えを返す

**指は画面を隠す。** マウスと違い、押した場所が見えない
(2026-08 利用者の要望)。`src/lib/haptics.js` 1か所に置き、
`installTapFeedback()` を **App で1回だけ**呼ぶ。画面ごとに書くと必ず抜ける。

| 端末 | 手 |
|---|---|
| Android | `navigator.vibrate`(タップ 8ms / 長押し 18ms) |
| iPhone / iPad | `vibrate` は**無い。** Safari 17.4 以降の switch 付きチェックボックスを1つ隠し持ち、切り替えて振らせる |
| それ以外 | 何もしない。**押した印(CSS)だけが残る** |

- **音も鳴らす**(2026-09 利用者の指定。「鳴らさない」から改めた)。

  > ボタンなどを押したときは音が鳴るように、そして正解したらピンポン
  > となるようにしてほしいです。体で感じれる仕様です
  > ぶるっとしたり、おとがなったり、そういうのを実装してください

  **ふるえと音は、同じところから出す**(`tapFeedback()`)。
  別々に書くと、片方だけ鳴るボタンが必ずできる。
  音は `src/lib/sfx.js` が**その場で短い波を作る。**
  ファイルを持たない(読み込み待ちも容量も増えない)。

  | 合図 | いつ |
  |---|---|
  | `tap` | 押した。**いちばん短く、いちばん小さく**(何度も鳴る) |
  | `correct` | 言えた・覚えた。**ピンポン**(低い → 高い) |
  | `miss` | まだ。**責めない。** 低く1つだけ |
  | `done` | 裏の仕事が終わった。3つ上がる |

  - **`AudioContext` は1つだけ作り、二度と閉じない**(`speech.js` と同じ作法)。
    最初に画面へ触れた瞬間に起こす(iOS は触っていないところで
    始めた音を鳴らさない)
  - **切れるようにしてある**(左のメニューの下)。レッスン中に邪魔なとき用。
    もとの「鳴らさない」は、それが理由だった
  - 答え合わせの合図(`answerFeedback`)も**触って押したときだけ。**
    マウスの人には色と文字がすでに見えている
- **語は長押しが効いた瞬間だけ。** 触れた瞬間に振ると「押せた」と誤解させる。
  押しているあいだは `.etext-word.is-holding` で目にも見せる
- **画面を送ったときは返さない。** 指を置いた場所がボタンでも、
  そこから送り始めたなら押していない。`pointerdown` で返すと送るたびに
  振れるので、**指を離したときに、押しっぱなしだった場合だけ**返す
- **当てにしすぎない。** 効かない端末では静かに何もしない

あわせて、**開いたものを離れた瞬間に閉じない。** 中のボタンを押せなくなる。
閉じるのは ✕ / 外側を押す / Esc / 同じものをもう一度押したときだけ。

### トレーニングのやりかけは、覚えておく

`src/lib/progress.js` 1か所(2026-08 利用者の指定)。

> 各種トレーニングをやり途中で他のページに行ってから戻ると
> 途中まで区切ったスラッシュリーディングや書き途中だった
> ディクテーションが消えてしまいます。

- 鍵は **`eas.prog.<教材id>.<演習id>.<何の途中か>`。**
  教材の id を頭に置いてあるので、**教材ごとにまとめて消せる**
- `useProgress()` は `useState` と同じ形。**鍵が変わったら読み直す**
  (読み直しは**レンダーの中**で行う。`useEffect` だと1回ぶん古い値を
  新しい鍵で書いてしまう)
- 覚えるのは ①書きかけ ②区切り ③開いた行 ④どのステップか
  ⑤ Quick Response の何問目か。**トレーナー側・ゲスト側で同じ**
- **控えていた場所が範囲の外になっていることがある**(教材を直したあと)。
  そのまま使うと問が空になるので、必ず中に収める

**途中経過は、ゲストとトレーナーで分かち合う**(0025・2026-08 利用者の指定)。

> スラッシュリーディングの区切りやディクテーションで書き込んだことや、
> 取り組んだ回数なども同じ扱いとしてゲスト側に共有され保存される。

- 置き場所は **端末(localStorage)+ `material_progress`(0025)の両方。**
  端末の控えを先に出すので待たされないし、Supabase が無くても動く
- `useProgress(key, initial, learnerId)` の**3つめを渡したときだけ**
  サーバーにも置く。渡さなければ、これまでどおり端末の中だけ
- **あとから書いたほうが上書きしてよい**(利用者の指定)。
  > レッスン中にトレーナーが書いたものが、ゲストの書きかけを
  > 消してよいですか → 大丈夫です。添削できるからその方が良いです。

  だから「どちらが新しいか」を争わせない。読むときはサーバーを採り、
  書くときは 1.2 秒だまってからそのまま上書きする
- **読み終わる前に送らない。** 端末の控えでサーバーを消してしまう

**教材ごとのリセットは、トレーナーの「教材の中身」に置く**(利用者の指定)。
押し間違いを防ぐため2段(1回目で「本当に消す」に変わる)。

**消えるのは localStorage の途中経過だけ。** 単語帳(`word_reviews`)・
宿題・取り組みの記録は Supabase にあるので触れようがない。
**消せるものと消せないものを、置き場所で分けてある。**

### 画面に何かを足したら、トレーナー側とゲスト側の両方を数える

語の意味を出す仕組みを**ゲストの画面にしか入れず**、利用者(トレーナー)からは
「動いていない」ようにしか見えなかった(2026-08)。英文が出る場所は4つある。

| 画面 | 部品 |
|---|---|
| ゲスト「今週の宿題」 | `LearnerHomework.jsx` |
| ゲスト「本文の練習」 | `PassagePractice.jsx` |
| トレーナー「レッスンで使う(大きく表示)」 | `LessonView.jsx` |
| トレーナー「教材の中身」 | `TrainerMaterials.jsx` |

**4つ全部を確かめる。** 利用者はふだんトレーナーとして使っている。

### 語のそろえ方は3か所にある。ずらさない

`norm_word()` は **SQL(0011)・Edge Function(lookup-word)・画面(`src/lib/vocab.js`)**
の3か所に同じ規則で書いてある。**ずれると意味の控えを引き当てられず、
同じ語に何度も課金される。** `don't` と `well-known` は1語。
**原形には戻さない**(戻すには辞書が要り、間違えると別の語の意味が出る)。

語の意味の控え(`word_glosses`)に**書き込めるのは Edge Function だけ**。
画面からは読むだけ。

**「知っていた / 知らなかった」は、どの画面で押したかで持ち主が変わる**
(0025・2026-08 利用者の指定)。

| どこで押したか | 誰の記録になるか |
|---|---|
| ゲスト自身の「今週の宿題」 | そのゲスト |
| トレーナーの**ゲストのカードの中**(過去の宿題・レッスン表示) | **そのゲスト** |
| トレーナーの「教材」画面 | **トレーナー自身**(利用者の確認) |

> セッションで一緒に取り組んでいるので学習時間に入ります

レッスンは一緒に進めるので、そこで付けた語も、書いた区切りも、
取り組んだ時間も**ゲストの学習そのもの**である。

- **表の RLS は緩めない。** `mark_word()` / `log_practice()` を
  `security definer` にして、**関数の中だけ**で `teaches()` を確かめる。
  窓口を1つに絞るほうが、表そのものを開けるより穴が小さい
- 誰の記録にするかは `markIn(mark, materialId, forLearner)` で1回かぶせる。
  **画面ごとに書き写さない**
- ゲストのカードの中では、**映す記録もそのゲストのもの**にする
  (`useWordStatuses(openId)`)。押した色と、実際に残る先をそろえる
- `rls_test.sql` が「担当していないゲストには書けない」を見張っている。
  **この検証を外さない**

処理は `src/lib/useWordStatuses.js` に1つだけ置く。画面ごとに書き写さない。

**単語帳の「今日の復習」は、意味を隠してから出す。**
記憶は**思い出そうとしたときに**強くなる。読み返した回数ではない。
**答えを見る前に必ず1回考える。** 一覧の形(語と意味が同時に並ぶ)は、
見返す用のタブにだけ残してある。決まりは4つ(2026-08 利用者の指定)。

- **「覚えた / まだ」は最初から押せる。** 分かっているものを開かせない。
  **答えの2つは、となりどうしに置く**(2026-08 の指定で改めた)。
  「意味を見る」は答えではないので**1段上**。3つを1列に並べると、
  狭い画面で [まだ][意味を見る] / [覚えた] と折り返し、
  **答えの2つが上下に分かれていた**(実機)
- **毎回順番を混ぜる。** 並び順で覚えると、意味を思い出さずに答えられる
- **出会った文を、訳なしで先に出す**(`seen_in`)。文の音も聴ける。
  「意味を見る」で語の意味と**文の意味**(`seen_in_ja`)が出る
- **日本語 → 英語の向きも要る。** 読んで分かる力と、話すときに出てくる力は
  別物である。この向きのときは、**出会った文の中の答えを伏せる。**
  ただし**語そのものの音は聴ける**(2026-09 利用者の指定・**方針の変更**)。
  もとは「鳴らせば答えが聞こえてしまう」ので伏せていたが、この向きは
  **口に出して言う**練習なので、自分で言ってから**耳で答え合わせをする**
  ほうが素直である(Quick Response で同じ判断をしている)

### 答え合わせは「その場」で返す。下に出さない

2026-09 利用者の指定。

> 解答が下に出るので非常に見にくいです。上の太字の単語が日本語に変わるとか、
> 真ん中に吹き出しで答えが出る、またはピンポン!て音でわかりやすく、
> 選択したボタンが正解なら緑、不正解なら赤などになる方がわかりやすいです。

4択で答えると、下に答えの欄が開いていた。**目はいちばん上の太字にある**ので、
下に出しても見ない(しかもスマホでは画面の外に出る)。

- **押したボタン**に色を付ける。正解=緑(`--ok`)/ まちがい=赤(`--ng`)。
  **色だけに頼らない。** ✓ / ✗ の印と、地色・枠線・取り消し線も併せる
- **上の太字が、英語から日本語の意味に変わる。**
  まちがえたときは**「正解は」と添える**(✗ と意味を並べただけだと、
  その意味がまちがいのように読める)
- **音を鳴らす**(`answerFeedback`)。正解はピンポン、まちがいは低く1つ
- **下の答えの欄は開かない**(4択のみ)。同じことが2か所に出る

### 4択の並びは、1問のあいだ変えない

`makeChoices()` は**呼ぶたびに混ぜる。** ところがレンダーのたびに呼んでいたので、
**答えた瞬間の描き直しで並びが変わり**、正解が別の場所へ動いてから
緑になっていた(2026-09 実機)。

> 正解の時に、元々の正解の選択肢のあった場所から対角線上に移動してから
> 緑になったり、ランダムに移動して緑に光るから変な感じがして落ち着きません

`useMemo` で、**語(と出題の形)が変わったときだけ**作り直す。
**`rows` は見ない。** 答えると `rows` から1語外れるので、
見ていると同じことが起きる(出題を組み直さないのと同じ理由)。

**「混ぜるもの」を、描くたびに呼ばない。** 同じことが起きる場所は
ほかにもある(`buildSession` は `deal` のときだけ呼んでいる)。

### 押した跡を、次の問題に持ち越さない

4択の `<li>` の `key` を番号にしていたため、React が同じボタンを使い回し、
**押した跡(黒い枠)が残ったまま**次の問題に出ていた(2026-09 実機)。

- `key` は **`語:選択肢の文`**。同じ文が次の問でも出ることがあるので、
  **文だけでは足りない**
- 押した瞬間に `blur()`。さらに**語が変わったときにも、
  4択のボタンに残った焦点を外す**(入力欄からは奪わない)

### 単語帳の復習も、集中モードで出す

2026-09 利用者の指定「単語帳モード、集中モードにしよう」。

> この集中モード、単語帳にも採用したい。最高ですね。
> そもそも集中モードというものをおかずに集中モードのように
> 表示されるのが理想です。スマホの画面にいかに全てを詰め込むのかが

単語帳は上に**札・プルダウン・絞り込み**が積み重なっていて、スマホでは
**カードが画面の下のほうから始まっていた。** 出題を画面ぴったりの1枚にすれば
送るものが無くなり、**1語だけに向き合える。**

- **骨組みは `.focus` をそのまま使う**(`FocusReader` と同じもの)。
  `.wbfocus` で足すのは、上の帯が進み具合の帯を持つという1点だけ。
  **同じ見た目を2か所に書き写さない**
- **入口をもう1つ挟まない**(利用者の言葉「集中モードというものをおかずに」)。
  復習の札を押した人は、もう答える気で来ている。**開いた瞬間から出す**
- **戻る道は上の帯の左端**(「✕ とじる」)。出したあとは
  一覧に「今日の復習をつづける」を出す。**行き止まりを作らない**
- **`margin-bottom: 28vh` は外す。** あれは「ページを送ってカードを
  まん中へ持ってくるための送りしろ」で、**送るものが無いこの画面**では
  ただの空白になり、カードが下へずれて見える
- **出題の枠の高さ(`.wordcard-q`)は決め打ちのまま。**
  語や文の長さで枠が伸びると、そのぶん答えのボタンが動く
- **画面を送る `scrollIntoView` は、集中モードでは呼ばない**
  (送るものが無いので、呼ぶと何も起きないか、かえって狂う)
- うしろの画面は `document.body.style.overflow = 'hidden'` で止める

### 復習のカードは、画面のまん中に固定する

2026-09 利用者の指定。

> 単語は、「覚える」や「まだ」「覚えかけ」などを押し出したら画面に固定になり、
> 単語や解答の長さに関わらず、しっかり中央に居座るようにしてください。
> (特にスマホで)そして、4択の回答は中央に表示してください。
> 下に表示されて切れてしまっている時が多いです。

- **出題の枠は高さそのものを決める**(`.wordcard-q` の `height`)。
  `min-height` だけだと長い語・長い文で伸び、**そのぶん答えが下へ動く**
  (実測 112px → 224px で4択が画面の外へ出た)。
  はみ出したぶんは**枠の中だけ**で送る
- **1語ごとに、カードを画面のまん中へ寄せる**(`scrollIntoView`。
  `block: 'center'`・動きは付けない)。単語帳は上に札・プルダウン・
  絞り込みが積まれているので、スマホではカードが下から始まる
- **送りしろは `.wordcard` の下に置く**(`margin-bottom`)。
  ページの終わりに余白が無いと、それ以上送れず**まん中まで来ない。**
  **`.wb-run` に付けない。** あれは進み具合の帯だけの入れ物で、
  カードはその外にある(付けたら帯とカードのあいだが 200px 空いた)
- **出題の形(おまかせ)は、進み具合の行の右に置く**(2026-09 利用者の指定)。
  画面のはるか上にあったので、訊き方を変えるたびに上まで送り戻していた。
  **変えたくなる場所のとなりに置く**
- **その見た目は、読むものの邪魔をしない。** 灰色の塗りつぶしの丸だったので、
  この行でいちばん大事な「6 / 10 語」より目立っていた。
  1回選んだらしばらく触らないものなので、**ふだんは文字と小さな矢印だけ。**
  触れたときにだけ、うすい面を出す。
  **矢印は自分で描く**(端末ごとに形も大きさも違う)
- **10語やり終えた結果も、同じ場所に置く**(利用者の指定)。
  最後の1語に答えた時点で、画面はその語のカードに合わせて送ってある。
  ところが結果の箱は**それより短い**ので、そのままでは
  画面のずっと下に出たり、上に空きができたりする。
  **語のカードと同じ決まりで置き直す**(狭い画面は上、広い画面はまん中)
- **狭い画面では、進み具合の行を画面の上にそろえる**(利用者の指定)。
  まん中に寄せると上に空きができるぶん、4択の下が画面の外へ出やすい。
  上にそろえれば**進み具合・出題の形・カードが1画面に収まる。**
  貼り付いている帯のぶんは `scroll-margin-top` で空ける。
  広い画面は空きが多いので、これまでどおりまん中
- **「教材」の絞り込みは出さない**(2026-09 利用者の指定)。
  教材名は長く、狭い画面ではこれだけで1行を食っていた。
  **仕組み(`applyWordbookFilter`)は残してある**ので、あとから戻せる

### 「覚えた」はやめた。**続けて押した回数**で卒業を決める(0038)

2026-09 利用者の指定。

> あと、「覚えた」はなくしましょう。
> 20-30回くらい連続で覚えかけを押すとしばらくは出てこない仕様にしましょう

**自分で「覚えた」と申告する道は、当てにならない。** 1回思い出せただけで
押してしまうし、押した瞬間に箱が上がって**30日先へ飛ぶ。** そして忘れる。

いま押せるのは **「まだ」/「覚えかけ」の2つだけ。**

| 押したもの | 数え | 箱 | 次に出す |
|---|---|---|---|
| まだ | **0 に戻す** | 0 | 翌日 |
| 覚えかけ(24回目まで) | +1 | +1(上限 3) | 1 / 2 / 4 日 |
| 覚えかけ(**25回目から**) | +1 | 6 | **30日**(しばらく出てこない) |

- **回数は `word_reviews.learn_streak`**(0038)。**箱では代われない。**
  覚えかけの箱は 3 で頭打ちなので(0027)、「25回続いた」を数えられない
- **卒業までおよそ3か月**(最長4日おき × 25回)。途中で1度でも「まだ」を
  押せば 0 に戻る。**早くしたいときは、0038 の `c_graduate` 1か所を変える**
- **卒業しても `status` は `learning` のまま。** `known` にすると復習から
  **永久に**消えるので、利用者の言う「**しばらく**は出てこない」にならない
- **`known`(覚えた)そのものは残してある。** 本文の中で語に触れて押す
  「知っていた」がこの状態を使っており、すでに付いている語も1件も変わらない。
  **消すのと、見せるのは別のことである**
- **4択・つづりで当たったときも `learning` にする。** ここだけ `known` を
  書いていると、**機械が採点した語だけが卒業の数えから外れる**
- **`vocab_days.correct` は「思い出せたか」で数える**(0038)。
  `known` だけを正解に数えていたので、そのままだと**いつも 0** になる
- **一覧(見返す用)の「覚えた」は、積んだ語にだけ出す**(0039・
  2026-09 利用者の指定「ある程度の回数『覚えかけ』を押さないと
  『覚えた』は出ない仕様にしましょう」)。

  はじめは一覧のボタンをそのまま残していた(言われたのは復習のカード
  だったため)。ところが**そこから1回で卒業できる**ので、
  カードから外した意味がなくなっていた。
  かといって丸ごと外すこともできない — 本文の中で押す「知っていた」が
  同じ状態を使っており、**すでに `known` の語もある。**

  - 判断は **`canMarkKnown()`(`vocab.js`)1か所。**
    目安の回数は **`KNOWN_AFTER`(10)**。25回で自動的に卒業するので、
    その手前の目安である。**変えるならこの1行**
  - **0039 が要る。** `review_words()` が `learn_streak` を返していな
    かったので、画面からは何回積んだのか分からなかった
  - **0039 を貼る前は、これまでどおり出す**(回数が返ってこないため)。
    **貼る前でも動く道を残す**(CLAUDE.md)
  - **足りない語には「覚えかけ 3 / 10 回」と出す。**
    ボタンが消えるだけだと「消えた」のか「まだ」なのか分からない
- **0027 を貼っていない Supabase では「覚えかけ」が使えない**ので、
  そのときだけ「覚えた」を出す(`learningSupported()`)。
  外すと「まだ」しか押せず、**行き止まりになる**

### 復習のカードは、種類 → 語 → 見る・聴く → 答え の順(2026-09 利用者の指定)

> 言い回し、名詞というのは上に持ってこう。そしてもっとデザイン性を持たせて。
> そして、英語をみる、英語を聴くの２つを日本語の問題の下に。
> そして英語を見るにすると日本語と入れ替えで同じ場所に表示してください。
> その際も聴くボタンをすぐ下に。

```
言い回し ・ 名詞          ← 種類と品詞。**語の上**(眉として読める)
屋上会場・屋上で…         ← 問題。押すと英語と**入れ替わる**
────────────────
[英語を見る] [英語を聴く]  ← 答えではない操作。問題のすぐ下に2つ
[まだ]       [覚えかけ]    ← 答え(2つ)
```

- **種類と品詞は、小さな札2つにする**(2026-09 利用者の指摘
  「単語・名詞の部分にもう少しデザイン性を持たせて。なんか地味でダサいです」)。
  細い字と中黒(`単語 ・ 名詞`)だけでは地味すぎた。
  種類は**うすい地色 + 同じ色の文字**(`--accent-soft` + `--accent`。
  「選んでいる印」と同じ組み合わせ)、品詞は**細い枠線だけ。**
  **塗りを2つ並べない。** 大きさは 11px のままなので、
  26px の語より目立つことはない(2026-08 の「札にすると語より目立つ」への答え)
- **答えは「足す」のではなく「入れ替える」**(集中モードの訳と同じ考え方)。
  下に足すとカードが伸び、**いま押そうとしているボタンがずれる**
- **入れ替えるのは4択以外の3つとも**(2026-09 利用者の指定
  「日本語→英語と同じ要領です」)。はじめは「日本語 → 英語」だけに
  していたが、「思い出す」で意味がボタンの下に出ていた。

  > この意味を元々の日本語の下、または日本語も含めた意味に入れ替えて
  > 同じ場所に表示してください。下に出るのはみづらいです

  **目は出題(いちばん上の太字)にある。** そこから離れた場所に出しても
  見ない。**同じものは同じ動きにする**(4択だけは、選択肢そのものが
  下にあるので入れ替えない。答えたら太字が意味に変わる)
- **答えを、ボタンの下に出さない。** 残りの「うしろ側」(くわしく・文の訳)も、
  高さの決まった問題の枠(`.wordcard-q`)の中に入れる。
  枠の外に置くと 49px 伸び、カードは上下のまん中に置いてあるので
  **その半分だけ全体が上へ動く**(実測 24px)。
  枠の中なら、はみ出したぶんはその中で送れるだけで**1px も動かない**
  (実測 390px・1440px・320px とも、押す前と後で枠・操作・答えの位置が同じ)
- **枠の中に入れたときは、線を引かない。** すぐ下の操作の行にも線があり、
  **細い線が2本**並んで見える
- **つづりを書く形も、同じ場所で合否を返す。**「正しくは …」を
  入力欄の下に出していたが、**同じ答えが2か所**に出るので外した。
  ✗ と綴りを並べただけだと**その綴りがまちがいのように読める**ので、
  4択と同じく「正解は」を添える
- **Listen は、入れ替わる形では下の行に置く。** 語のとなりに置いたままだと、
  **意味を見たとたんに Listen が消える。**
  4択は入れ替わらないので、これまでどおり語のとなり
- **その1つだけは「英語を聴く」と書く**(2026-09 利用者の指定)。
  読み上げのボタンは、どの画面でも `Listen` / `Stop` の対にする決まり
  だが、ここは**となりが「英語を見る」**で、しかも英語が伏せてある。
  「Listen」だけでは**何の音か**が分からない。
  **鳴っているあいだ `Stop` に変わるのは、ほかと同じ**
- 読み上げのボタンの文言は **`Listen` のまま**(どの画面でも同じ・利用者の指定)。
  鳴っているあいだ `Stop` に変わる

### 出題の形は「箱」で決める。判断は `formForBox()` 1か所

同じ語でも、覚えの深さで訊き方を変える(`src/lib/wordQuiz.js`、2026-08)。
箱(0〜6)は**その語をどれだけ覚えているか**そのものなので、これを使う。

| 箱 | 形 |
|---|---|
| 0〜1 | 4択 |
| 2〜3 | 思い出す |
| 4〜5 | 日本語 → 英語 |
| 6 | つづりを書く |

**答えを出しておいて、答えさせない。** つづりの形で英語をそのまま
見せていたので、写すだけで正解できた(2026-08)。
**出すのは意味だけ**にする。日本語 → 英語も同じ。

**4択のまちがいは、自分の単語帳のほかの語から作る。**
AI に作らせると1問ごとに課金される。自分がつまずいた語どうしなので、
適当な語を並べるより難しい。3語そろわなければ**4択を作らず「思い出す」に落とす**
(選択肢2つでは、まぐれで当たる)。

**1回は10語**(`SESSION_SIZE`)。「今日出す」が48語あると始める気にならない。

### 仕組みの内側の数字を、画面に出さない

**「箱 0 / 次は今日」を出していた**(2026-08 利用者の指定で外した)。

> 単語帳内の「箱0 / 次は今日」はバックグラウンドのデータとして
> 表には出さないでください。

箱(0〜6)と次に出す日は、**間隔をあけて出題するために内側で使う数字**である。
ゲストにできることは何も無く、覚える助けにもならない。
`mark_word()` は変えていない。**残すのと、見せるのは別のことである。**

同じ考え方で外したもの・外さないもの。

| 何 | どうする |
|---|---|
| 箱の番号・次に出す日 | **出さない**(内側の数字) |
| 「今日出す 6 / 覚えかけ 49 / 覚えた 61」 | 出す(何をすればよいか分かる) |
| 「1週つづけて / 今週1日」 | 出す(続ける励みになる) |
| 生成の失敗の `終了理由: tool_use` | **出さない**(仕組みの内側の言葉) |

### 続いているのは「日」ではなく「週」で数える

日ごとの連続記録は**1日休んだ瞬間に途切れ、それがやめる理由になる。**
週2回のレッスンに合わせた宿題なので、毎日やる前提が合っていない。
`vocab_days`(1日1行)に残し、`vocab_week()`(SQL)が週で数える。
**数え方を画面に持たない。**

### 単語帳には、手でも入れられる

`WordbookAdd.jsx`(2026-09 利用者の指定「単語帳に手打ちで入力できる
機能をつけてくれ」)。教材の外で出会った語も、その場で入れられる。

- **入れ物は増やさない。** 入り口が違うだけで、入る先はこれまでと同じ
  `word_reviews`。`mark_word()` をそのまま呼ぶので **SQL は要らない**
- **意味は、入れたときに1回だけ引く**(`lookupWord`)。
  単語帳の画面は**控えを読むだけ**で AI に尋ね直さないので、
  引かずに入れると「意味の控えがありません」のまま残る。
  1語あたり約0.1円、控えはスクール全体で共有される。
  **開くたびには引かない**(発音記号・要点フレーズと同じ)
- **引けなくても語は入る。** 意味はあとで教材の中で触れたときに付く
- 空白を含めば `phrase`、含まなければ `word`。**判定は1か所**
- 入るのは **「まだ」**。**入れた語は、その日の復習に出る**(0030)。
  `mark_word()` は「まだ」を翌日に回す決まりだったので、
  **初めて入る語まで翌日**になり、入れた日には1回も出てこなかった
  (2026-09 実機「登録しても反映されません」)。
  **間隔の決まりは変えていない。** 2回目からはこれまでどおり
- **トレーナーも、ゲストの単語帳に手で入れられる**(2026-09 利用者の指定・
  **方針の変更**)。レッスンは画面を共有しながら進めるので、その場で
  出てきた語を入れられないと、紙にメモして入れ直すことになる。
  入る先は**そのゲストの記録**(`mark_word` の `p_learner`・0025)。
  担当していないゲストには SQL 側が書かせない
  (`rls_test.sql` が見張っている)。
  **どちらの単語帳に入るのかは、ボタンの文言に出す**
  (「Airi さんの単語帳に入れる」)。黙っていると自分のに入れたつもりになる
- **「覚えた / まだ」を代わりに付ける道は、いまも作っていない。**
  あれはゲスト本人の申告である。足せるのは「語そのもの」だけ

### 単語帳は `Wordbook.jsx` 1つ。**2つ持たない**

2026-09 利用者の指定。**3度言われて、ようやく1つにした。**

> 教材モード内の単語帳と、ゲストモード内の単語帳のUIがまだ異なっています。
> ゲストモード内のものを教材モード内のものと同じにしてください。

> 全然別物ですよね。「おまかせ」タブがない、単語ごとの表示の仕方も
> 全く違う。。。何をしているのですが

トレーナー用に `LearnerWordbook.jsx` という**別の部品**を持っていた。
中身をそろえたつもりでも、直すたびに片方が古くなり、
**「おまかせ」も出題も、こちらには無いまま**だった。
だから**部品ごと消して、`Wordbook.jsx` に一本化した。**

    <Wordbook />                                  自分の単語帳
    <Wordbook learnerId={l.id} learnerName={…} onMakeMaterial={…} />
                                                  ゲストの単語帳

**同じ部品に「誰の単語帳か」を渡すだけ**にすれば、片方だけ古くなることが
起こりえない。**似ているものを2つ持たない。そろえるのではなく、1つにする。**

- 読み書きは `readWordbook()`(`src/lib/vocab.js`)**1か所**を通す。
  画面ごとに書き分けると、0027 の `todo` の落とし穴を片方だけ踏む
- **中身をそろえても、囲みが違えば別の画面に見える**(2026-09 実機)。
  自分の単語帳は `<section className="card">` の中にあるのに、
  ゲストの単語帳は地の上に直に置いていた。**外側まで数える**
- **「覚えかけ」は 0027 を貼るまで使えない**(2026-09 実機)。
  古い `mark_word()` が「状態は known か unknown です」と断り、
  画面には赤い知らせが出るだけだった。
  **一度断られたら、その画面のあいだはボタンごと出さない**
  (`learningSupported()`・`vocab.js` に1つだけ置く)。
  **勝手に「まだ」として記録しない。** 押した言葉と残る記録が
  食い違うほうが困る。知らせ方は役割で変える
  (トレーナーには貼るファイル名まで、ゲストには内側の話を出さない)
- ちがうのは**選んで教材を作れる**(`onMakeMaterial` を渡したときだけ)
  1点だけ。**復習の出題も、トレーナー側で出す。**
  記録は `learnerId` の人のものになる(0025)。レッスンは一緒に進めるので、
  そこで答えたものはゲストの学習そのものである

### 単語帳から、その語で教材を作れる ← ここが循環の要

`Wordbook`(`learnerId` を渡した側)で語を選ぶ → `MaterialForm` の
`initial.mustUse` → 窓口へは**既存の `reviewWords`** として渡す。
**新しい欄を作らない。**

- **同じ「必ず入れる語」でも、演習の種類で言い方を変える。**
  単語・フレーズは「先頭から順にこの語で作る」、文型ドリルは
  「問題文の中で使う」、記事・会話は「本文の中で使う」
- 名指しの語と、自動で拾った語(`reviewPool`)を**二重に出さない。**
  そろえた形(`normWord`)で重複を落とす
- `wordbook_views`(0019)に「見たこと」だけを残す。ゲストの画面に
  **「担当トレーナーが 8/29 にこの単語帳を見ました」**と出る。
  **人が見ていると分かることが、どんなバッジより効く**

**`shortDate()` に時刻付きの値を渡さない。** `viewed_at` をそのまま渡して
`8/NaN` と出した(2026-08)。**日付だけに切ってから渡す。**

**語の意味の先読みは、英文が出る4つの画面すべてに入れる**(2026-08 実機)。

> 単語の意味ですが、やはり初めて調べた時に3-5秒ほどかかるので
> レッスンの時間が無駄になります。

先読み(`prefetchGlosses`)は `LessonView` と `PassagePractice` にしか
入っておらず、**トレーナーの「教材の中身」とゲストの「今週の宿題」には
無かった。** 英文が出る場所は4つある(上の表)。**4つ全部に入れる。**

あわせて先読みそのものも3つ直した。

| 何 | 前 | いま |
|---|---|---|
| 先読みする語の数 | 24語 | **120語**(記事1本を通しでまかなえる) |
| 引き方 | 10語ずつ**順番に** | 10語ずつ**3本同時**(うしろの段落が間に合わない) |
| 先読み中の語に触れたとき | **もう1本呼んでいた** | 走っている問い合わせを待つ(`inFlight`) |

24語では、記事の**後ろのほうの段落がほとんど先読みできていなかった。**
費用は1語あたり約0.1円なので120語で**12円ほど、しかも一度きり**で、
控えはスクール全体で共有される(2人目からは無料)。
**レッスン中に3〜5秒待つほうが高くつく。**

**吹き出しに意味を並べない。** 読んでいる途中に出すものなので、
**いま要るのは1つ。** ほかの意味は畳んでおく(「ほかの意味(N)」)。
生成も1〜3件に絞ってある。**深く調べるのは単語帳の「くわしく」。**
場面が違う(読んでいる最中 / 机に向かっている)。

**出会った文を一緒に控える**(`word_reviews.seen_in`、0018)。
人は文脈ごと覚える。**最初の1文だけを残し、あとから上書きしない。**
上書きすると、覚えかけた手がかりが毎回入れ替わる。

**単語帳は「入った日」と「教材名」で絞れる**(0024・2026-08 利用者の指定)。

- **`updated_at` は使えない。** あれは最後に答えた日で、復習のたびに動く。
  日付で絞る役には立たないので `added_at` を足した(**あとから動かさない**)
- `material_id` の列は 0011 からあったが、**画面から渡していなかった**ので
  ずっと空だった。`markIn()`(`useWordStatuses.js`)で教材が分かる場所に
  1回だけかぶせる。**同じ包み方を6か所に書き写さない**
- 古い語の教材名は**空のまま**(利用者の判断「これからの分だけでよい」)。
  出会った文から推測すると、外れたときに**間違った教材名**が出る
- 絞り込みは**手元で行う**(`WordbookFilter.jsx`)。`review_words()` は
  200件まで返しているので、選ぶたびに聞き直さない
- **カレンダーは、語が入っている日にだけ印を付け、無い日は押せない**
- 部品は**ゲストとトレーナーの単語帳で共通。** 書き写すと片方だけ古くなる

**次に出す日を画面で計算しない。** `mark_word()`(SQL)が決める。
箱(0〜6)が上がるたびに 1 → 2 → 4 → 7 → 14 → 30 日と延び、
分からなかったら 0 まで戻す。**間隔の決まりを2か所に持たない。**
端末の日付や時差で食い違う。数字は `vocab_test.sql` で確かめてある。
**卒業までの回数(25)と、休ませる日数(30)も同じ関数の中にある**(0038)。

句・イディオム・句動詞も**同じ表に入る**。鍵は「そろえた形」なので
`look forward to` はそのまま鍵になる。`kind` で見分けるだけ。
**表も鍵も増やさない。**

要点フレーズ(`material_items.phrases`)は**教材を作る時点で拾う**。
開くたびに AI に拾わせない。費用が毎回かかり、何が出るかも分からない。
`generate-material` の `PHRASE_TYPES` にある演習だけが対象。

意味の吹き出しは `GlossPopover.jsx` 1つ。語(`EnglishText`)と
句(`PhraseChips`)の両方から使う。**同じ見た目を2か所に書き写さない。**

**吹き出しの中は、上下に送れるようにする。**
吹き出しは `position: fixed` なので**画面を送っても付いてくる。**
中身が画面より高いと、はみ出した部分に永久に手が届かなかった
(「文が1番下までくると、外の画面を送ることさえできない」2026-08)。
入る高さを毎回測って上限にし、`.etext-pop-body` の中だけを送らせる。
**「知っていた / 知らなかった」はその外に置く。** 意味がどれだけ長くても、
ボタンは必ず見えている。

**吹き出しは `createPortal` で body の直下に出す。**
紙(`.lesson-sheet`)は `overflow-y: auto` で、CSS の決まりにより
**横方向も切る。** 中に置くと端の語で切れ、紙が横にずれる(2026-08 実機)。
body の直下に出るので `.lesson-sheet .etext-pop` では色が届かない。
`.etext-pop--paper` を付けて当てる。「外側を押したら閉じる」も、
**吹き出しの中は外側ではない**と判定する。

**語をなぞると、まとめて1つの言い回しとして引ける**(`data-widx` +
`elementFromPoint`)。ブラウザの範囲選択は使わない。語には
`user-select: none` が要る(iPhone の長押しメニューを止めるため)。

**2語以上のまとまりは、囲みを1つにする**(`.etext-run`)。
語ごとに色を付けると語間で線が切れ、空白に色を足しても
`<button>` と `<span>` で下端が 7px ずれて継ぎ目が残った(実測)。
色は **1語 = 赤 / 2語以上 = 紫**(2026-08 利用者の指定)。

**吹き出しの中の色は `--pop-bg` / `--pop-fg` / `--pop-dim` / `--pop-faint` /
`--pop-rule` の5つだけで決める。** 紙の上ではこの5つを差し替える。
個別に上書きして回ると必ず取りこぼす(「ボタンだけ黒い」「1つめの意味が
白いまま」を実際に出した)。

**まとまりは、見た目も操作もまとまりとして扱う。** 塊の中のどの語を
押しても塊の意味を引き、カーソルも塊ごと反応する。1語も長さ1の
まとまりとして描く(語に直接色を付けると帯の高さが揃わない)。
**同じ見た目を紙用に別に書かない。** 色は変数で持ち、紙が差し替える。
別に書いたせいで、あとに置いた紙用の指定が勝ち、まとまりの中で
1語だけ反応する不具合になった(2026-08)。

**新しい列を「取り出す欄」に足すと、貼る前は教材が1件も開けなくなる。**
`materials.voice_ids` と `material_items.phrases` を足して、実際に
「教材が見れなくなった」を起こした(2026-08 実機)。PostgREST は
知らない列が1つでもあると**問い合わせ全体を断る**(42703)。
`src/lib/materials.js` の `runTolerant()` が、断られた列だけを外して
読み直す。**足りない列は2つ以上あることがあるので、繰り返す。**
挿入するときも、無いと分かっている列は送らない(送ると挿入ごと失敗する)。

**DB の関数を使うように変えるときは、貼る前でも動く道を残す。**
`setWordStatus()` を `mark_word()` に切り替えたとき、SQL を貼るまで
「押しても色が付かない」状態になった(2026-08 実機)。
関数が無いという断りのときだけ、前の書き方に戻している。
詳しくは仕様書 第5.23節。

### 取り組みは、こちらが裏で数える(0022)

**ゲストに「何分やった」を入力させない。** 入力そのものが手間で、
しかも入れ忘れる(2026-08 の設計変更。「学習の記録」の画面は外した)。

- 数えるのは `usePracticeLog(kind)` **1つ**。画面で1回だけ呼ぶ
- **見えている時間だけ**数える。裏に回した端末の時間は数えない
- **1分ごとに送る。** 1回ごとでは通信も行も増えすぎる
- **ゲストのぶんだけ**(`viewerRoleOf() === 'learner'`)
- 1人 × 1日 × 1種類 で1行(`practice_days`)。`vocab_days` と同じ考え方
- 数え方は DB(`learner_practice()`)に置く。**画面に持たない**

**リマインドは、トレーナーが押したときだけ飛ぶ**(利用者の指定)。
自動では送らない。だからゲストの画面に「トレーナーから」と出しても
**嘘にならない。** ゲストが変えられるのは `seen_at` だけ(列単位の grant)。

### 集計(管理者)は「教材の種類と内容」で数える。学習時間で数えない

`AdminDashboard.jsx` + `src/lib/schoolStats.js` + **0023**。
前は `study_logs`(ゲストが手で入力した学習時間)を並べていたが、
その入力欄は 0022 で無くなっている。**入らなくなった数字のグラフを
置き続けると、いつまでも 0 が並ぶ。**

- 数えるのは **種類**(`materials.kind`)・**内容**(弱点タグ / CEFR)・
  **届き方**(`assignments`)・**取り組み**(`practice_days`)
- **数え方は DB に置く。** 合計も割合も画面で足し直さない
- **いちばん見せたいのは「ライブラリの穴」。** 教材が0件の弱点も、
  ゲストがいるのに教材が0本のレベルも**必ず行として出す。**
  隠すと、次に何を作ればよいかが分からない
- 5つの関数はどれも `security definer`。**`is_owner()` の判定を外さない。**
  外すと全校のデータが誰にでも見える(`rls_test.sql` が見張っている)
- `learner_practice()`(0022)は**担当ゲストだけ**のトレーナー用。
  `school_practice()`(0023)は**全校**の管理者用。**取り違えない**
- **Supabase が無いときに見本の数字を出さない。** 実際と食い違ったまま
  判断されてしまう

### 発音練習は独立した機能。ただし**点数は付けない**

`pronunciation.js` の点数は**録音を解析していない仮の数値**である。
これを独立した機能の看板にすると、**測れていないものを測れているように
見せる**ことになる。いまは「聴いて、まねて、言う」までにしてあり、
画面にもそう書いてある。採点を足すのは、やり方が決まってから。
**録音を外に送るのは方針の転換**にあたる(仕様書 3.2)。

### データの置き場

| 何を | どこに | なぜ |
|---|---|---|
| 学習記録・教材・配信・スコア | Supabase | 端末をまたいで共有する必要がある |
| **録音した音声そのもの** | **端末内(IndexedDB)** | 生体情報に近い。サーバーに上げない(`docs/PROJECT_SPEC.md` 3.2) |
| お手本音声(MP3) | Supabase Storage | 年 0.3〜1.4GB。Git リポジトリには置けない |
| 設定・アプリ状態 | localStorage | `src/lib/store.js` |

**録音音声をサーバーに送る変更は、方針の転換にあたる。** 勝手に行わず必ず確認する。

## 利用者に作業を頼むときは `docs/APPLY.md` をそのまま使う

**「supabase/apply」は Supabase の中ではない。GitHub のリポジトリの中である。**
同じ名前のものが2か所にあると、利用者は必ず取り違える(2026-08 の指摘)。

- **場所は「どこのファイルか」から書く。** 「GitHub のリポジトリにある〜」
- **押せる URL を渡す。** リポジトリの中の道順を言葉で説明しない
  (`raw.githubusercontent.com` の URL を `docs/APPLY.md` に一覧してある)
- **やり直しを頼むときは、ステップ1から最後まで書き直す。**
  「◯◯だけもう一度」と書かない。戻って読ませるのは間違いのもと
- 手順の型そのものは `.claude/rules/common.md`(全プロジェクト共通)にある

## この環境の制約

- **`supabase.co` への通信は遮断されている。** マイグレーションの実行も接続確認もできない。
  SQL は利用者が Supabase の SQL Editor に貼り付け、接続確認はブラウザで行う。
  だからこそ `npm run test:db` で手元で検証してから渡す
- 利用者は **会社PC にアプリをインストールできない**。すべてブラウザで完結させる
- Chromium と Playwright は使える(`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`)。
  マイクは `--use-fake-device-for-media-stream` で代用する
- **`pkill -f "vite"` のような指定で開発サーバーを止めない。**
  実行中のシェル自身が引っかかって落ち、そのコマンドの残りが実行されない
  (2回やった)。止めるなら `fuser -k 5173/tcp`。
  開発サーバーは放置しても害はないので、そもそも止めなくてよい
- 画面の確認は、`.env` を一時的に退避すると Supabase 未設定の状態になり、
  ログインを通さずに中の画面を見られる。確認後に必ず戻すこと

## 踏んだ落とし穴(繰り返さないこと)

`docs/PROJECT_SPEC.md` の 3.3.2〜3.3.4 に実機での検証結果がある。特に重いものだけ再掲する。

### iOS の録音 — 実機で何度も失敗した

**iOS では、すべてのブラウザの中身が Safari(WebKit)である。**
「iOS の Chrome を使えば回避できる」という手は存在しない。

- `MediaRecorder` は**録り直しで壊れる**。使わない
- `AudioContext` は **1つだけ作り、二度と `close()` しない**
- **マイクの取得と音声グラフの構築は1回だけ**。録音のたびに作り直すと数回で無音になる
- 音声認識の前に**マイクを解放する**。掴んだままだと `aborted` / `audio-capture` になる
- iOS は `isFinal` を返さないことがある。**確定が来なければ interim を採用する**
- 認識を止めるときは `abort()` ではなく **`stop()`**。`abort()` は結果を捨てる
- この作法は `src/lib/recognition.js` に集約してある。**音声認識を書くときは
  そこを使う。** 各画面で `SpeechRecognition` を直接触らない
- **iOS は高品質音声を Web Speech API に一切公開しない**(実機で生47件を確認、premium 0件)。
  端末の音声に頼らず、**事前生成した MP3 を配る**のが唯一の解

### 端末に何が入っているかを、当て推量で決めない

利用者の会社PC(Windows / Chrome)に入っている英語の声は**3つだけ**で、
**3つとも Google の「通信を使う声」**だった(2026-08 実機)。
Windows なら Microsoft の端末内蔵の声があるはず、という前提は成り立たない。

ここから2つ効いてくる。

- **オフラインでは読み上げが鳴らない。** 会社のネットワークが Google の
  音声サーバーを塞いでいると Listen が無音になる
- **`boundary` が1回も出ない。** 語ごとの色は合図だけに頼れない(上記)

`public/mic-test.html` は、声の一覧・通信かどうか・**合図の回数**を
機械的に出す。**実機の話は、ここの数字で決める。**

### 検証を頼む前に、版が分かるようにする

「直したはずの不具合が直っていない」の原因が、ブラウザに残った古い内容だったことが複数回あった。
`public/mic-test.html` には公開時に版が埋め込まれる(ワークフローの `__BUILD_STAMP__`)。
同種の作業では、**どの版を見ているか機械的に分かるようにしてから**確認を依頼する。

### RLS は「行」しか絞れない

`profiles` の更新を「自分の行なら可」にしたところ、ゲストが `role` を `admin` に
書き換えられた。**列の制限が必要なときは、列単位の `grant` を併用する。**
`profiles`(`display_name` / `industry` のみ)と `assignments`(`learner_done_at` のみ)が該当。

### SQL 関数はテーブルより後に定義する

SQL 関数は作成時に本文を検査される。まだ無いテーブルを参照していると
`relation does not exist` で失敗する。`0001_init.sql` では判定関数を
テーブルの後(第7節)にまとめてある。

## 鍵(このプロジェクト固有)

扱いの原則は共通ルールにある。このプロジェクトで出てくるのは次の3つ。

| 鍵 | 扱い |
|---|---|
| Supabase `sb_publishable_...` | 公開前提。`.env` と GitHub Secrets に置く |
| Supabase `sb_secret_...` / `service_role` | **扱わない。利用者にも求めない** |
| Azure Speech の鍵 | 利用者が `.env` / GitHub Secrets / **Supabase の Edge Functions → Secrets** に直接書く |

`anon` キーが公開されていても安全なのは RLS が守っているからである。
**RLS を無効化する変更は行わない。**
