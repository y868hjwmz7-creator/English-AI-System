# 教材の一覧とカード

**[CLAUDE.md](../../CLAUDE.md) の「構成」から切り出したもの。決まりの重さは1つも変わっていない。**

カードの形・教材を消す(0044)・弱点タグ(0050)・リンクで渡す・さがす画面

見出しで探す: `grep -n '^### ' docs/notes/*.md`　言葉で探す: `grep -rn 'さがす言葉' docs/notes/`

---
### 教材の中身は `MaterialBody.jsx` 1つ

トレーナーの「教材」と、ゲストの情報の中の「過去の宿題」で**同じものを開く**
(2026-08 利用者の指定「宿題の表示をトレーナーの教材の表示と統一」)。
**同じ見た目を2か所に書き写さない。**

- 宿題の一覧(`loadLearnerAssignments`)は**中身を持っていない**(id と数だけ)。
  開いた教材だけ `loadMaterial()` で読み、**同じ教材を二度読まない**
- 読んでいるあいだは「開いています…」に変える
- 練習の記録を消すボタンは `showReset` を渡したときだけ(トレーナーの教材のみ)

### 教材のカードに、開く・閉じるを置かない

2026-09 利用者の指定。

> わざわざ開いて「印刷・PDFで保存」のボタンを出す必要がない、
> 閉じる・開く機能を排除、そして「この教材をゲストと共有する」ボタンを
> 印刷ボタンの右側へ移してください

開いても出るのは印刷のボタンだけになっていたので、
**開くこと自体が回り道**だった。グレーの囲みは**ただの見出し**に戻す。

```
グレーの囲み(見出し。押せない)
何が何問あるか
[印刷 / PDFで保存] [練習の記録を消す]            ← 手元で扱う操作。1行にまとめる
[この教材をゲストと共有する]                      ← 人に渡す操作。教材の画面だけ
[セッションで使う(大きく表示)]
```

**「練習の記録を消す」は、印刷のとなり**(2026-09 利用者の指定
「練習の記録を消す、を印刷ボタンの右に置いてください」)。
前の2つは**同じ教材を手元で扱う**操作(紙にする / この端末の書きかけを消す)で、
共有だけが**人に渡す**操作である。

**`.btn-row` のままでは「右」にならない。** あれは折り返すので、
狭い画面では縦に積まれる。その1行だけ `.card-tools` にして
**折り返させず、入りきらないときはボタンのほうを縮める**
(`.btn--small` の `flex-shrink: 0` を戻す)。
**縮んでも文言は削らない** — 何のボタンか分からなくなる。
実測: 390px 以上は1行 34px、320px でも横に並ぶ(文字が2行になる)。

- **中身は、紙に出す一瞬だけ描く**(`printId` → `.print-holder`)。
  35件ぶんをいつも描くと、語は1つずつボタンで描いているので画面が重い。
  描かないと**見出しだけの紙**が出る(2026-08 に一度やった)
- `.print-holder` は画面では `display: none`。
  `@media print` が `.print-path` を `display: block !important` に
  戻すので、紙にはちゃんと出る
- **`MaterialBody` にボタンを置かない。** 置くと「開かないと押せないもの」に
  戻る。印刷・共有・記録を消すは**呼ぶ側のカード**にある
- 開閉が無くなったので、**語の先読みと区切りの訳の作り直しは
  「セッションで使う」で拾う**(以前は「開いたとき」だった)

### 押すものが4つ並んだら、**役目で行を分ける**(`IconButton.jsx`)

2026-09 実機・利用者の指摘。

> 印刷/PDF・練習の記録を消す・音声ダウンロード・読み上げ音声を作り直す
> この4つはアイコン化して省スペースしてください。無理やり並べすぎていて
> プロの仕事とは思えません。…今後、トレーナーは通勤中に電車で教材を
> 準備したりするのです。**見やすくコンパクトに、移遷しやすく、を徹底して
> ください。** アイコン化して、触れると説明が出る仕様でよくないですか?

`.card-tools` は **`flex-wrap: nowrap` で、`.btn--small` は縮まない**。
だから入りきらないぶんは**文字が1字ずつ縦に積まれる。** iPhone(390px)では
「印 / 刷 / P / D / F …」という**棒が4本**並び、カード1枚で画面の高さを
丸ごと食っていた。**教材を見比べるための画面が、1件も見比べられない。**

- **絵は `Icons.jsx` に足す**(`EraserIcon` / `DownloadIcon` / `RefreshIcon`)。
  **絵文字を使わない**(端末ごとに形が違う)
- **消しゴムをゴミ箱にしない。** すぐ下に「教材を消す」があり、
  **消える相手がまったく違う**(あちらは教材そのもの、こちらはこの端末に
  残っている書きかけ)。同じ絵にすると取り違える
- **押せる大きさ(40px)は割らない。** `.btn--small`(34px)を下敷きに
  しているので、**`.btn.iconbtn` と2つ重ねて**書く ——
  `.btn--small` はこの行より**あと**に書いてあり、
  `.iconbtn` 1つでは負ける(実測して気づいた)

#### 触る端末には「カーソルを載せる」が無い。**長押しで名前を出す**

`title` だけでは、何のボタンか一生分からない。

- **長押しで「実行」しない。** 2026-08 に、長押しで語の意味を開いて
  3度踏んだ穴がある(送ろうとして指を置いただけで開く)。ここで出るのは
  **消せる案内1つ**なので、間違って出ても害が無い。しかも `EnglishText` と
  まったく同じ作法で、**動いた・画面が送られた時点で取り消す**
- **長押しのあとの指離しは、押したことにしない**(`skip`)。
  名前を読もうとしただけで印刷が始まっては困る
- **カーソルのときは出さない**(`pointerType === 'mouse'` で帰る)。
  あちらには `title` がある。**同じことをするものを2つ見せない**
- **読み上げ機には、いつも名前を渡す**(`aria-label`)

#### **言葉が要る状態では、言葉を出す**

絵だけでは言えないものが3つある。**そのときだけ**横に言葉が出て、
ボタンがその幅に広がる(`text`)。ふだんは絵だけに戻るので、
幅が広がるのは状態のあいだだけである。

| 何 | なぜ絵では言えないか |
|---|---|
| **本当に消す**(2段め) | 元に戻せない。「いま押したら本当に消える」が絵にならない |
| **集めています… 3 / 14** | **進み具合は必ず数で出す。** 絵だけでは止まって見える |
| **作っています… 3 / 14** | 同上(しかもこちらは課金が走っている) |

**「読み上げ音声を作り直す」の「閉じる」だけは言葉にしない。**
押すと**すぐ下に欄が開く**ので、開いていることは目に見えている。
うすい青の印(`iconbtn--on`)だけで足りる。

#### **絵だけでは言えないものがある。だから4つとも絵にはしなかった**(2手め)

2026-09 実機・利用者の指定。**上の直しを、その日のうちに一段進めた。**

> 「音声を作り直す」「学習の記録を消す」を教材を消すの左側に並べて、
> 「印刷 / PDF」と「音声ダウンロード」アイコンを今の位置に並べてください。
> ちなみに、印刷 / PDFマークについては「🖨️」だけでは PDF が出せることが
> わからないので、「印刷 / PDF」として、音声ダウンロードもそのまま
> 「音声ダウンロード」としましょう。
> **もともとスペースの問題だったのでこれで解決です。**

**利用者の言うとおりだった。** プリンタの絵からは「PDF でも出せる」が
読み取れず、下向きの矢印からは「**何を**落とすのか」が分からない。
長押しすれば名前は出るが、**それは知らないと押せない。**

**詰まっていたのは「4つ並んでいた」ことのほうで、絵にすることは
目的ではなかった。** 役目で2つの行に分ければ、2つずつになる。

| 行 | 何 | 見た目 |
|---|---|---|
| `.card-tools`(いまの位置) | 印刷 / PDF ・ 音声ダウンロード | **言葉つき** |
| `.material-foot`(いちばん下) | 読み上げ音声を作り直す ・ 練習の記録を消す ・ **教材を消す** | **絵のまま** |

- **分け方は「どれくらい押すか」ではなく「後戻りが利くか」。**
  下の3つは、作り直し(そのまま課金)・記録を消す・教材を消すで、
  **どれも元に戻らない。** ふだん使う2つから離す理由がそろっている
- **下の2つは絵のまま。**「読み上げ音声を作り直す」は11文字あり、
  「教材を消す」と並べると iPhone では1行に入らない。
  長押しで名前が出る(`IconButton`)
- **`MaterialDelete` は自分の行(`.btn-row.material-danger`)を持ったまま
  中に入る。** 外から包むと、**消せない人のカードに線だけが残る**
  (あの部品は消せない人には何も描かない)。だから中の余白と線は
  `.material-foot` の側で打ち消す
- **説明と知らせは、次の行いっぱいに落とす**(`flex: 1 0 100%`)。
  そのままだとボタンの横に並び、細い柱に押し込まれて読めなくなる
- **作り直しの欄と、記録を消すときの説明も一緒に下げた。**
  **失敗の知らせは、その操作をした場所に出す**(CLAUDE.md)

#### 検証は「1行に収まったか」だけを見ない

**それだけだと、絵を小さくして押せなくしても緑のまま**になる。
`npm run test:bar` は 390 / 375 / 360 / 320px で見る。

①**上の行に言葉が出ているか**(絵だけに戻すと、利用者の指定が消える)
②**下の3つが同じ1行にいて、「教材を消す」が右端か**
③**40px を割っていないか** ④**`aria-label` が渡っているか**
⑤**長押しで名前が出るか** ⑥**ふつうのタップは効くか**
(ここが壊れると**どのボタンも押せなくなる**)
⑦**言葉が要る状態で言葉が出るか** ⑧**「教材をシェア」があるか**。
あわせて **`TrainerMaterials` が本当に同じ形で書いているか**も見る
——検証の入り口(`__screens.jsx`)だけ直しても、利用者の画面は変わらない。

**同じ1行かを、上端で比べない。** 絵のボタンは 40px、「教材を消す」は
34px なので、まん中でそろえてある行では**上端が 3px ずれる**(実測)。
見るのは**まん中**である。

実際に ①長押しを外す ②画面の1つを元のボタンに戻す
③`.material-foot` の並べ方を壊す ④`askOpenId` を渡すのをやめる、の
4つで、赤くなることを確かめてある。

#### **直したのは「教材」の画面だけ**

同じ形の行は `TrainerLearners`(過去の宿題)と `LearnerHomework`
(今週の宿題)にもあるが、**あちらは印刷が1つあるだけ**で、
詰まっていない。**言われた場所だけを直す**(共通ルール)。
4つ並ぶようになったら、そのときここと同じ形にする。

#### **渡す道をボタン1つにまとめ、3つ並べた**(3手め・2026-09 利用者の指定)

> 「教材をシェア」と「教材をゲストと共有」はボタンをひとつにして
> その中でゲストと共有なのか普通の共有なのかを選べるようにしてください。
> 省スペースです。そして、スマホの表示で、「印刷/PDF」「音声ダウンロード」
> 「教材をシェア」を適宜言葉を減らしてアイコンを活かすことで
> ３つ並ぶようにしてください。直感でわかれば良いのです。

**2手めと逆のことを言われている。方針の変更である。経緯ごと残す。**
あのとき言葉を残したのは**2つだったから**で、
3つになれば入る幅が変わる。**数が変われば、答えも変わる。**

| | 上の行 | 人に渡す行 |
|---|---|---|
| 2手め | 印刷 / PDF ・ 音声ダウンロード | この教材をゲストと共有する ・ 教材をシェア |
| **3手め** | **🖨 PDF ・ ⬇ 音声 ・ ⤳ 共有** | **無し**(共有に吸収した) |

##### **絵だけには戻していない。削ったのは添えの部分だけ**

    🖨 PDF   … 絵が「印刷」、字が「PDF も出せる」  ← 2手めの心配はここで消える
    ⬇ 音声   … 絵が「落とす」、字が「何を」
    ⤳ 共有   … 絵が「渡す」、字が「何をするか」

**何のボタンかを言う語は、1つずつ残っている。**
落としたのは「印刷 /」「ダウンロード」「教材を…シェア」という添えだけである。

- **集めているあいだの文言も短くする**(`dlShort`)。
  「集めています… 3 / 14」では3つ並ばない。
  **削るのは動詞だけで、数は1文字も削らない**(進み具合は必ず数で出す)。
  動いていることは、**数が増えることと、押せなくなること**が言う
- **`label()` は残す。** ゲストの「今週の宿題」はボタンが少なく、
  あちらは言葉のまま入る(**押す場所ごとに要る幅が違う**)
- **`ShareIcon` を足した**(点と点をつなぐ形)。`LinkIcon`(鎖)では
  「リンクを渡す」しか言えず、**ゲストに宿題として出す道**を言えない。
  **足す前に、同じ絵がもう無いかを探してある**

##### 中で選ぶ。**ここでは「どちらにしますか」を先に訊く**

`MaterialShare` には**逆の決まりが書いてあった** ——
「2つとも出しておけば、選ぶことがそのまま操作になる」。
あれは**メール / リンクという小さな2つ**の話で、**いまも守っている**
(リンクを選んだ中では、①②が並んで出る)。

**ゲストを選ぶ欄は大きい**(担当25人ぶんのチェックが並ぶ)ので、
両方を開くと箱が画面2枚ぶんになる。利用者が挙げた目的は**省スペース**で、
言葉も「**選べるように**」である。**利用者の言葉が勝つ。**

- 札は **`.chip` / `.chip--on` を使い回す**(ここで新しい配色を作らない)。
  **色だけに頼らない**(うすい地色 + 同じ色の文字 + 枠線 + 太字)
- **既定は「ゲストと共有」。** このアプリの中心は
  「弱点から教材を作り、**指定したゲストに配る**」循環である(冒頭)
- **開くたびに既定へ戻す。** 前に「リンクを渡す」を見ていた人が
  次の教材でもそこから始まると、**ゲストに配る道が隠れて見える**
- **並べない。入れ替える**(訳の出し方と同じ決まり)
- **開け閉めは呼ぶ側(`TrainerMaterials`)が持つ**
  (`open` / `onOpen` / `onClose`)。中に閉じ込めると、
  **共有し終わったときに閉じる合図をもう1本渡す**ことになる
- **「やめる」は箱が持つ。** どちらの道でも同じ場所にある
- 開いている印は **`.btn.share-open.is-on`。`.btn.is-on` と広げない** ——
  `is-on` は読み上げの操作盤(`player-play`)など**あちこちで別の意味**に
  使っており、広げるとそこまで青くなる(**寄せたついでに直さない**)

##### 幅は実測して決めた。**いちばん狭い端末では余白だけを詰める**

| | 3つぶんの幅 | 入る幅(320px) |
|---|---|---|
| ふつう | 221px | 262px |
| 集めている最中 | 232px | 262px |
| **320px + 文字 1.25 倍 + 集めている最中** | **265px** | **262px** ← 3px 足りない |

その1つだけ、**ボタンの中の字が2行に折り返していた**(高さ 34 → 55px)。
360px 以下で**余白だけ**を詰める(12px → 8px・隙間 6px → 4px)。
**言葉も、集めた本数も1文字も削らない。** `.btn--small` の 34px は
そのままなので、押せる大きさは変わらない。

##### **開いた箱は、行の外へ落とす**(撮って初めて気づいた)

いちばん危なかったのはここである。`MaterialShare` は
**ボタンと箱を続けて返す**ので、ボタンを `.card-tools` に置くと
**箱も同じ行の一員**になる。行は `nowrap` だったので箱がそこへ割り込み、
**PDF / 音声 / 共有が縦棒3本に潰れた。**

**高さも「はみ出し」も正常のまま**だったので、
**測っても気づけなかった** —— `SHOT` で撮って初めて分かった。

- `flex-basis: 100%` は**折り返す行でしか効かない。** だから
  `.card-tools` の `nowrap` をやめ、`.share-box` に `flex: 1 0 100%` を置く
- **3つは実測で 320px でも 221〜253px に収まる**ので折り返さない。
  もし入らなくなったら縦に積まれるが、そこは検証が
  「3つが1行か」で赤くする ——**潰れて気づけないより、赤くなるほうがよい**
- 検証にも足した ——**開いたとき、箱がボタンの下にいるか**と、
  **いちばん細いボタンが 50px を割っていないか**
  (実際に戻すと **44px / 34px** で赤くなる)

##### 検証は「1つになったか」だけを見ない

**それだと、片方の道を消しても緑のまま**になる。
`npm run test:bar` は ①ボタンが1つか(**前の2つが残っていないか**)
②開くと2つとも選べるか ③**既定はゲストと共有か**
④切り替えると**中身が本当に入れ替わるか**
⑤3つが1行か(**文字 1.25 倍でも**)⑥集めた本数が消えていないか
⑦**開いても3つの行が崩れていないか**、を見る。

**「あるか」だけでも足りない** —— 共有だけ別の行へ戻しても
「`<MaterialShare>` がある」は満たされるので、
**3つとも同じ `.card-tools` の中にいるか**まで数える。

### 弱点タグは**画面と表の2か所**にある。片方だけに足さない(0050)

2026-09 利用者の指定。

> ついでに苦手タグの文法項目にまだないもので
> 一般的なものがあれば追加してください

文法の見出しは16件しか無く、**受動態・助動詞・動名詞・文型といった、
どの文法書にも見出しがあるものが選べなかった。**15件足した
(文型 / 受動態 / 助動詞 / 動名詞 / 関係副詞 / 代名詞 / 使役・知覚 /
話法 / 否定 / 強調・倒置 / 形容詞と副詞 / 未来の言い方 / there の文 /
命令文・依頼 / 時・条件の節)。

- **既存の16件は1文字も触っていない。** 並びも動かしていない
  (「一度入れたものを勝手に減らさない」— 並べ替えも減らすに当たる)
- **見出し(`category`)は増やしていない。** 言われたのは文法の項目である

#### **`material_tags.tag_id` は表を参照している。だから SQL が要る**

    tag_id text not null references public.weakness_tags(id)

つまり**画面(`weaknessTags.js`)に足しただけでは足りない。**
そのタグを付けて教材を**発行した瞬間に**外部キー違反で止まる。
`npm run lint` も `npm run build` も通る ——
**演習の種類・教材の種類で2度踏んだ穴と、まったく同じ形**である。

- `scripts/check-exercise-types.mjs` が**3つめとして**見張る
  (`npm run test:db` の中)。表のタグ一覧を渡して、
  **画面にあって表に無いもの**を赤くする
- **表のほうが多いぶんには何も言わない。** 使わなくなったタグを
  表から消さない(消すと過去の教材が行方不明になる)ので、
  そちらは正しい状態である

#### この見張りは、**作った日にもとからの抜けを1つ捕まえた**

いちばん最初に赤くなったのは、足した15件ではなく **`all-vowels`
(母音全般)**だった。`all-consonants`(子音全般)は 0001 で入っている
のに、**母音のほうはどの移行にも無い。** 画面には最初からあるので、
**そのタグを付けて発行するとずっと止まっていた**ことになる。

**誰も踏まなかったのは、これが `drill`(網羅型)で弱点の選択肢に
出ないタグだったからである。** 見張りが無ければ、踏むまで分からなかった。
0050 で一緒に足した(**これがそのまま red-check になっている** ——
実際に赤くなり、足したら緑になった)。

#### 「無生物主語」と「名詞構文」を足した(0060・2026-09 利用者の指定)

> 「無生物主語」と「名詞構文」のタグをつけ足してください。

英文を組み立てる「型」のうち、**この2つだけ行き先が無かった。**

    無生物主語 … Cutting corners costs more in the long run.
                 This tool allows you to share files instantly.
    名詞構文   … We decided to postpone → Our decision to postpone

**どちらも日本語から訳しても絶対に出てこない**ので、
レッスンで指摘する値打ちがいちばん大きい。それなのに指摘しても、
**貼る先のタグが無かった。**

- **いまあるタグでは足りない。** `sentence-pattern`(文型 SVOC)は
  **並びの決まり**、`gerund`(動名詞)は **-ing を名詞にする形**そのもので、
  どちらも「**人以外を主語の席に立てる**」「**文を名詞のかたまりにする**」
  という**組み立ての型**は指せない。**似たタグを2つ作らない**ので、
  重ならないところだけを足してある
- **足し方は 0050・0052 とまったく同じ。** 画面(`weaknessTags.js`)と
  表(`weakness_tags`・0060)の**両方**に足す。片方だけだと、
  そのタグを付けて**発行した瞬間に外部キー違反で止まる**
- **既存の64件は1文字も触っていない**(並びも動かしていない)

#### 「丁寧な言い回し」を足した(0061・2026-09 利用者の指定)

> 弱点タグの「表現」内に「丁寧な言い回し」を追加してください。
> つまり、ビジネスで使用する丁寧な言い回しを網羅するためのタグとします。
> 例えば、I was wondering if you could 〜、Do you mind 〜ing? ,
> Do you mind if I 〜 ? Could you possiblly do 〜? などなどです

**仕事の英語でいちばん事故が起きるのは、言えないことではなく
言い方がぶっきらぼうになること**である。

    Send me the file.                    ← 通じるが、命令に聞こえる
    Could you send me the file?          ← ふつう
    I was wondering if you could send 〜  ← 相手に断る余地を残す

- **`fixed-phrase`(決まり文句)とは軸が違う。** あちらは
  「挨拶・依頼・断り・相づちの**定型を知っているか**」、こちらは
  「**その場に合う丁寧さで言えるか**」。`Do you mind if I 〜?` を
  知っていても、使いどころを外せば同じことになる。
  **似たタグを2つ作らない**ので、重ならないところだけを足してある
- `sort_order` は **335** —— `collocation`(330)のすぐ後ろ、
  `hesitation`(340・なめらかさ)の手前で、**表現の並びの中**に入る
- **足し方は 0050・0052・0060 とまったく同じ。** 画面
  (`weaknessTags.js`)と表(`weakness_tags`・0061)の**両方**に足す。
  片方だけだと、そのタグで**発行した瞬間に外部キー違反で止まる**
- **巻末のレクチャーとつながっている** —— `sentenceFrames.js` の
  ③「やわらげる・提案する(仕事で効く)」がそのままこの型である。
  **紙で見つけて、このタグで練習に回せる**
- 既存のタグは1文字も触っていない(並びも動かしていない)

##### 表も列も作らない移行の「印」は、**その行が在るか**で見る

`SetupStatus` の印(`NEWEST_MARK`)は `table`(表の有無)か
`rpc`(関数の有無)しか見られなかった。ところが 0060 は
**弱点タグを2行足すだけ**で、表も列も関数も1つも増えない。

**`weakness_tags` は 0001 からある。** だから表の有無で見ると、
**貼る前でも「もう入っています」と出る** ——
CLAUDE.md が「いちばん悪い壊れ方」と呼んでいるものである
(0056 で「表を作らない移行」に気づいて `rpc` を足したのと同じ形の穴が、
もう一段下に残っていた)。

- **`row`(その行が在るか)**を足した。0件は「まだです」と読む
- **0件が「まだです」を意味する表だけを、この印に選ぶ。**
  `weakness_tags` は 0001 で「タグは全員が読める」(`using (true)`)なので
  当てはまる。**RLS に断られて0件になる表を、印にしてはいけない**
- `check.sql` も同じ形で見る(0050 の行がすでにそうしている)
- `npm run test:play` が、**その移行が本当にその行を入れているか**と、
  **画面が0件を「まだです」と読んでいるか**を見張る。
  実際に「表があれば ok」に戻して、赤くなることを確かめてある

### 教材へのリンクを、トレーナー間で渡せる(`materialLink.js`)

2026-09 利用者の指定。

> 「教材をシェア」ボタンをつけてトレーナー間でシェアできるように
> してください。これで教材へのリンクをシェアできるようにします。

教材は既定で**全トレーナーの共有物**(`materials.visibility = 'school'`)
だが、**その1本を名指しで指す道が無かった。**「あの記事の教材」と伝えても、
受け取った人はさがす画面で同じものを見つけ直すことになる。

#### **道(パス)は増やさない。印を1つ付けるだけ**

このアプリには**ルーティングが無い**(`App.jsx` がタブで切り替える・
CLAUDE.md「構成」)。ルーターを入れると、GitHub Pages の設定
(`BASE_PATH`・404 の扱い)まで触ることになる。**そこまでの話ではない。**

    https://…/English-AI-System/?m=<教材の id>

- 入り口の URL はこれまでどおり1つ。**GitHub Pages を触らない**
- **`?v=<コミット番号>` と一緒に付いていてもよい**
  (`urlWithoutMaterial()` は `m` だけを外す)
- **SQL も窓口の置き直しも要らない**(表も列も増やさない)

#### 渡し方は2つ。**並べて出す**(`MaterialShare.jsx`)

2026-09 利用者の指定。**その日のうちに一段進めた。**

> シェアする際はメールアドレスを入れる、またはリンクを生成して
> 好きなところに貼り付けれるように、2つから選べると良いですね

はじめは押した瞬間に端末の共有シートを開いていたが、
**何が起きるのか押す前に分からなかった。** いまは欄を開いて2つを見せる。

| | 何を | どうなるか |
|---|---|---|
| ① | **メールで送る** | 宛先を書いて押すと、**利用者自身のメールソフト**が開く |
| ② | **リンクをコピー** | リンクをそのまま欄に出す。好きなところに貼れる |

- **「どちらにしますか」を先に訊かない。** 2つとも出しておけば、
  選ぶことがそのまま操作になる(押す回数が1つ増えない)
- **共有シートを3つめとして足さない。** 利用者が挙げたのは2つである
  (`writingTones.js` の「4つ以外を足さない」と同じ決まり)。
  `npm run test:play` が `navigator.share` の混入を見張る
- **こちらからメールは送らない。** 送るには外の送信サービスとその鍵が要り、
  **鍵は扱わない**という決まりを、この機能のために曲げない
  (`erase_learner()` が `auth.users` を消さなかったのと同じ考え方)。
  開くのは `mailto:` で、宛先・件名・リンクは入れてある。
  **そのことを画面にも書く** —— 送ったつもりで送れていない、が
  いちばん困る
- **リンクは、いつも見えるところに出す**(読み取り専用の欄)。
  コピーを断る端末(古い Safari・http)でも手で選んで写せる。
  **行き止まりを作らない**
- **宛先の形は、画面で見分けない。** `mailtoFor()` が形の違う宛先には
  `null` を返すので、それをそのまま「押せない」の合図に使う
  (**判断を2か所に置かない**)。**選ばせてから断らない**
- **形は厳しく見ない**(`isEmailLike`)。メールアドレスの本当の決まりは
  とても広く、**正しい住所を弾くほうが害が大きい。**
  見るのは「`@` が1つあって、その前後に字がある」だけである
- **改行は `\r\n`。** `\n` だけでは行が変わらないメールソフトがある
- **「やめる」を、走らせるボタンのとなりに置く**(CLAUDE.md)。
  上のボタンをもう一度押しても閉じるが、画面を送るとそこは見えなくなる
- **部品は `MaterialShare.jsx` 1つ。** さがす画面の中に書くと、
  Supabase を引き連れるので**描いて確かめられない**
  (`VoiceRemake` / `MaterialDelete` と同じ考え方)。
  `npm run test:bar` が 390 / 320px で**実際に開いて**、
  2つとも出るか・宛先が空なら押せないか・はみ出さないかを見る

##### **説明の中で同じ言葉を使う部品は、「名前が出てくるか」で見られない**

**この回で実際に踏んだ。** `test:play` を
`ms.includes('② リンクをコピー')` と書いていたので、
**見出しの行を丸ごと消しても緑のまま**だった —— この部品は
**冒頭の説明の中でも同じ言葉を使っている**からである。

- 見るのは**書いてある形**まで
  (`<p className="field-label">② リンクをコピー`)。
  説明の文には引っかからない
- **画面に本当に出ているか**は、`npm run test:bar` が描いて確かめる。
  こちらは中身の言葉を読んでいるので、消せば必ず赤くなる
  (実際に消して確かめてある)
- 「無ければ素通り」する形の検証を書かない、と**まったく同じ話**である

#### 受け取る側 — **取り出したら、印を消す**

- 読むのは**開いた瞬間の1回だけ**(`App.jsx` の `useState` の初期値)。
  そのあと `history.replaceState` で印を外す。残しておくと、
  別の教材を発行して一覧を読み直すたびに**リンクの教材へ引き戻される**
  (`playMark.js` の「取り出したら消す」とまったく同じ作法)
- **絞り込みを外してからさがす。** 外さないと、受け取った人がたまたま
  別の条件で絞っていたときに**その教材が一覧に出てこない**
- 送って光らせるのは、**発行した直後とまったく同じ道**(`justId` /
  `data-mid`)。**数え方を2通り持たない**
- **見つからなければ黙らない。** 「消されたか、共有されていない可能性が
  あります」と出す。**原因は決めつけない** —— 消されたのか、共有されて
  いないのか、リンクが古いのかは、こちらには分からない
- **ゲストには効かない。** `TrainerMaterials` はゲストには描かれないので、
  条件を1つも足さずに、ひとりでにそうなる。
  **見えるかどうかは RLS がどのみち決める**

#### **id の形を、受け取った側で確かめる**

`materialIdFromUrl()` は **UUID の形以外を受け取らない。**
受け取った id は `document.querySelector('[data-mid="…"]')` にそのまま
入るので、引用符などが混じると**選択子ごと壊れる。**
「窓口が言ったことを、そのまま画面に流さない」と同じ考え方である。

#### 「ゲストと共有する」とは**別物**。ただし**ボタンは1つ**にまとめた

| 中の道 | 渡す相手 | 何が起きるか |
|---|---|---|
| ① ゲストと共有 | **ゲスト** | 宿題として届く(`assignments`) |
| ② リンクを渡す | **トレーナー** | リンクを渡すだけ。何も配らない |

**もとは別々のボタンだった**(「この教材をゲストと共有する」と
「教材をシェア」)。2026-09 に利用者の指定で1つにまとめてある ——
**渡す相手が違うだけで、していることは同じ**だからである。
詳しくは「渡す道をボタン1つにまとめ、3つ並べた」の節。

- **片方の道を、ついでに消さない。**「1つになったか」だけを見る検証では
  **片方を消しても緑のまま**になるので、`npm run test:bar` は
  **中で2つとも選べるか**まで数える
- **絵は `ShareIcon`(点と点をつなぐ)。** 鎖(`LinkIcon`)では
  「リンクを渡す」しか言えず、**ゲストに宿題として出す道**を言えない

#### 算段は `materialLink.js` 1か所

`App.jsx` にも `TrainerMaterials.jsx` にも URL の形を書かない。
あの2つは Supabase と `import.meta.env` を引き連れていて、
**素の node で一度も走らせられない**(`playMark.js` / `mp3Join.js` と
同じ考え方)。`npm run test:play` が、印の読み書きと
**画面が本当に呼んでいるか**を見張る
(実際に `askOpenId` を渡すのをやめて、赤くなることを確かめてある)。

### さがす帯も畳める。**件数は札にして、題の反対側へ**

2026-09 実機・利用者の指定。

> 宿題を探すも折りたたみ式にしてください。そして検索バーの下の「3件」は
> 丸などで囲って何か配色してください。そして位置は宿題を探すの文字の反対側、
> 検索バーの右端の上にしてください

ゲストのページには、**畳める箱と畳めない帯が縦に並んでいた**
(宿題をさがす → 宿題をしぼる)。しかも件数は検索バーの**下**に
うすい文字で置いてあり、いちばん知りたい数がいちばん目立たなかった。

- **形は「宿題をしぼる」とまったく同じ**(`card material-search`)。
  すぐ下に並ぶので、**形が違うと2つの別物に見える**
- **札は `finder-badge` を使い回す**(「条件で絞り込む (3)」と同じ)。
  うすい地色 + 同じ色の文字 + 枠線。**ここで新しい配色を作らない**
- **札は畳んだままでも見える。** だから**開かなくても何件あるか分かる**
  (「なぜ1件しか出ないのか分からない」を防ぐ、絞り込みの札と同じ考え方)
- **同じ数を2か所に出さない。** 畳める形のときは、行の中の
  `.searchbar-count` を出さない
- **既定は閉じている**(`eas.pastSearch`)。開いたままだと検索の欄と
  絞り込みの札で画面が埋まり、**宿題が1件も見えない**。
  **教材の欄(`eas.materialSearch`)とは別に覚える** ——
  別の画面の別の欄なので、片方を閉じてもう片方まで閉じては困る

#### そのあと、**さがすとしぼるを1つにまとめた**(2026-09 実機・利用者の指定)

> ゲストの１人のページ内で「教材をさがす」と「教材を絞る」を１つにまとめて、
> そして「日付・並び順」「分野: すべて」「苦手項目で絞る」をその下においてくれ

**上の直しで、同じ見た目の畳める箱が2つ縦に並んだ。** しかも
**そのあいだに絞り込みの行が挟まっていた**ので、
どちらを開けばよいのか押すまで分からない。

    ▸ 宿題をさがす          [3 件]     ▸ 宿題をさがす・しぼる   [3 件]
    [日付・並び順][分野][苦手項目]  →   [日付・並び順][分野][苦手項目]
    ▸ 宿題をしぼる

- **中身は「名前で引く」と「取り組みで絞る」で、どちらも一覧を狭める
  という1つのこと**である。分けておく理由が無い
- **`SearchBar` に `children` を1つ足しただけ。** 部品を増やさない。
  **畳めるときだけ効く**ので、教材の画面(`collapsible` を渡さない)は
  1ドットも変わらない
- **控えも1つになった。** 使わなくなった `eas.pastFilter` は
  `slashLevel.js` ごと消す —— **値を偽にするだけにしない**
  (残すと、次に見た人が「まだ使うのかもしれない」と読む)
- **教材の画面(トレーナーの「教材」)はまとめていない。**
  あちらは「探す / 条件で絞り込む / 作る」の3つで、まとめてよいものが違う
  (**言われた場所だけを直す**)
- `npm run test:bar` が4つ見る ——
  ①畳める箱が1つか ②取り組みの札がその中にいるか
  ③**絞り込みの行が箱の下か**(ソースの並び順)
  ④**教材の側が変わっていないか**。実際に
  ①まとめるのをやめる ②絞り込みを上に戻す、の2つで赤くなることを
  確かめてある。③古い控えを書き戻すと、`slashLevel.js` にもう無いので
  **骨組みごと赤くなる**(こちらは狙って作った赤ではない)

#### 部品が2か所で使われているときは、**渡さない側**も数える

`SearchBar` は**ゲストの「宿題をさがす」と、トレーナーの「教材をさがす」**の
2か所で使っている。畳めるのは前者だけなので、`collapsible` を
**渡したときだけ**形が変わるようにしてある(渡さなければ1ドットも変わらない)。

**「畳める」だけを見る検証は書かない** —— それだと
**教材の画面まで畳んでも緑のまま**になる。`npm run test:bar` は
両方を描いて、**教材の側が `section.searchbar` のまま・札を使っていない・
行の中に「35 件」がある**ことまで数える(実際に `margin-left: auto` を外し、
画面から `collapsible` を外して、赤くなることを確かめてある)。

#### 畳んだ `<details>` の中は、**`offsetParent` では見分けられない**

「畳んでいるのに検索の欄が見えている」と最初に赤くなったが、
**これは検証の側の誤りだった。** 畳んだ `<details>` の中身は
`content-visibility: hidden` で**描かれないだけ**で、
`offsetParent` は残っている(実測して気づいた)。
**見えているかどうかは `checkVisibility()` で見る。**

### 教材のカードは、3つの画面で同じ形にする

トレーナーの「教材」/ ゲストのカードの「過去の宿題」/ ゲストの「今週の宿題」。
**1画面だけ直さない。** 同じ指定を2度言わせた(2026-09)。

> 教材モードの教材の表示と同じだと言っても伝わらないのですかね?

```
グレーの囲み(押すと開閉。右端に ▸ / ▾)
何が何問あるか
[印刷 / PDFで保存]
[セッションで使う(大きく表示)]        ← ゲストでは「大きく表示する」
```

- **「中身を見る・印刷する」の行は置かない。** 閉じる言葉も置かない。
  開けるのも閉じるのも同じ囲みなので、**同じことをするものを2つ見せない**
- **`.material-detail` に囲みを付けない。** 中に残るのはボタンだけなので、
  枠だけが空っぽで残ると中身が抜けているように見える

### 教材のカードには、中身を出さない(2026-09 利用者の指定)

> 赤で囲った部分は必要ないです。
> 教材のあるところ全てで適用してください。(ゲストエンドでも同じ)

カードを開いても出るのは**印刷 / 大きく表示する**だけ。
**指導ポイント・Quick Response・演習のタブと設問の一覧は出さない。**
中身に取り組むのは「セッションで使う(大きく表示)」、
持ち出すのは「印刷 / PDFで保存」である。

- **描くのはやめない。隠すだけ**(`exercise-view is-closed`)。
  描かないと、紙に**見出しだけ**が出る(2026-08 に一度やった)
- ただし**ゲストの「今週の宿題」は、印刷する一瞬だけ描く。**
  `PassagePractice` が入っており、隠したまま置くと
  **やってもいない練習の時間を数える**
- `TeachingNote` の**見た目は変えていない。** 出す場所だけの話

### 「やった」は申告させない。取り組んだら自動で残す

2026-09 利用者の指定「やった もいらないです / やればトレーナー側で
わかる仕組みにしてください」。

**申告は二重に当てにならない。** 押し忘れれば「まだ」のまま、
中身を見ずに押しても「やった」になる。
いまは**「大きく表示する」で開いたときと、印刷したとき**に
`assignments.learner_done_at` を**1度だけ**書く。

- すでに日付が入っていれば触らない(**最初に手を付けた日**を残す)
- **取り消す道は作らない。** 申告ではなく、起きたことの記録である
- **黙って記録しない。** ゲストには「8/30 に取り組みました」と静かに出す
- トレーナー側には**いつ取り組んだか**も出す。「やった」だけでは
  今日なのか3週間前なのかが分からない
- 入れ物は増やさない。**SQL は要らない**

### 教材の画面は「探す / 条件で絞り込む / 作る」の3つ

2026-09 利用者の指定。

> 教材をさがす、作るの欄で条件を入れると教材が絞り込まれますが、
> UIがわかりにくいです。「探す🔍」「条件で絞り込む」「作る」の3つを
> ベースにしつつ、絞り込み、検索結果内にも「ないので作る」のボタンを
> 配置してください

以前は「教材をさがす・作る」という**1つの箱に条件の欄だけ**が入り、
名前で引く入力欄はその**外**にあった。同じ「さがす」が2か所に分かれ、
「作る」は名前だけあってボタンが無い。**何をする欄なのかが読み取れない。**

```
教材をさがす                    [＋ 教材を作る]   ← ③ いつも見える
🔍 [教材名・見出しでさがす ] [新しい順 ▾] 35 件   ← ① 畳まない
▸ 条件で絞り込む (3)                              ← ② 畳める
```

- **③「作る」は、いつも見えるところに置く。** 名前だけでは押せない
- **②は畳めるが、いくつ条件が付いているかは畳んだままでも出す**
  (`.finder-badge`)。出さないと**なぜ1件しか出ないのか分からない**
- **条件を外す道を、条件が付いているときだけ出す**
  (「絞り込みをぜんぶ外す」)。1つずつ「すべて」に戻して回らせない
- **一覧のいちばん下にも「無いので作る」を置く**(`.finder-more`)。
  無いと分かるのは下まで見たときなので、画面の上まで戻らせない。
  0件のときは、その1枚だけを出して青くする(`.finder-empty`)
- **一覧の途中では、画面の隅に小さく出す**(`.finder-float`・2026-09 実機)。
  36件もあると、上の箱もいちばん下も画面の外にいる時間が長い。
  **その2つのどちらも見えていないあいだだけ**出す
  (`IntersectionObserver`。同じことをするボタンを2つ見せない)。
  **青の塗りつぶしにしない。** 既定の動線は探すことである
- **「条件で絞り込む」は、はじめは閉じておく**(2026-09 利用者の指定)。
  欄が5つ縦に並ぶので、開いたままだと教材が1件も見えない。
  開け閉めは覚える。**鍵の名前を変えた**(`eas.finderFilters`)ので、
  前に「開いている」を覚えた端末も、いったん閉じた状態から始まる
- **条件が付いていれば「この条件で教材を作る」**と書く。
  押したあと何が引き継がれるかが、押す前に分かる

### さがす画面は「見比べる」ための画面。1件を長くしない

行そのものを押すと中身が開く(▸ / ▾)。カードにいつも出しておくのは
**「セッションで使う(大きく表示)」と「ゲストと共有する」の2つだけ。**
すぐしたいのは中身を眺めることではなく、使うか配るかである。
**青の塗りつぶしは「セッションで使う」。** 共有は小さく灰色にする
(2026-08 利用者の指定。さがした直後にするのは、まず開いて見ることである)。

**弱点の札だけを目立たせる**(`mtitle-chip--main`)。何の弱点の教材かは
選ぶときにいちばん見る情報なので、レベルや業界と同じ見た目だと埋もれる。
**どれが弱点かは呼び出し側から渡す**(`weakness`)。札の並び順から当てない。
見出しそのものが弱点のとき(文型ドリル)は、札で繰り返さない。

**日付は右上、その下にカテゴリー名**(2026-08 利用者の指定)。
横に並べると、どちらが日付でどちらが種類なのか読み取りにくい。
日付は `MaterialTitle` にも出せるので、**2か所に出さない**
(さがす画面では `hideDate` を渡す)。

**弱点タグを2回出さない。** 弱点は教材名の中にすでに入っており、
`MaterialTitle` が出す(ドリルは見出しそのもの、記事はグレーの札)。
別に札の行を並べない。手で名前を付けた教材のためには、
`fallbackTags` の先頭に弱点を渡す。
**この決まりは、ゲストの「過去の宿題」にも同じように効かせる。**

### 押せる場所は、押す前から押せると分かるようにする

`:hover` だけで「押せる」を示さない(2026-08 利用者の指定)。

> タップした時にタイトル周りが薄いグレーで囲まれますが、
> タップする前からその仕様にしてください。
> 上部のグレーの部分を押して開くことは押してみるまで分かりません。

**触る端末には「カーソルを載せる」が無い。** `:hover` で色を付けると、
**押したあとに初めて色が付き、しかもそのまま残る。**
押せる場所を探すのに、片っ端から押してみることになる。

- **囲みは、はじめから出しておく**(`.material-open` の `--surface-2`)
- **囲みだけでは足りない。** 押すと何が出るのかは言葉でしか書けないので、
  **「▸ 中身を見る・印刷する」**と書いた行を囲みの中に置く(開くと「閉じる」)
- カーソルでは**色を変えず、輪郭を出す**(`box-shadow: inset`)。
  色をもう一段足すと、明るい側と暗い側で「濃くなる / 薄くなる」が逆になる

さがす画面のカードの中身は **見出し → 札(レベル・業界)→
カテゴリー名(左)と日付(右)→ 中身を見る**の順(2026-08 利用者の指定)。
日付とカテゴリーを右上に縦に積んでいたが、読み取りにくかった。

### 一覧は「1件 = 1枚のカード」。線1本で区切らない

細い線で区切って余白も詰めると、**どこからどこまでが1件か分からない**
(2026-08「宿題と宿題の区切りがパッと見て分からない」)。
枠線 + 角丸 + 地色のカードにし、**カードどうしのあいだを空ける**
(1問ごとのカード `.qa-row` と同じ考え方)。中の要素にも息をつかせる。

### いつも 0 になる数字を、画面に出さない

「学習した日 0 日 / 合計 0 時間」を出し続けていた。ゲストが自分で
入力する仕組みをやめた(0022)ので、**もう誰も入力しない数字**である。
**数えていないものを、数えているように見せない。**
いまは `practice_days` を裏で数えたものを出す(`practiceLine()`)。


中身の抜き書き(記事の1段落目)を並べていたので、**1件で画面ぜんぶを
食っていた**(2026-08「教材の内容の要約のような部分、いらないです」)。
出すのは**何の演習が何問あるか**だけ(`.material-parts`・1行に畳む)。
中身は「中身を見る」で開ける。**一覧に足すものは、1行に収まるかで決める。**

### さがす画面の指定は、作る画面へ「全部」引き継ぐ

`TrainerMaterials`(さがす)→ `MaterialForm`(作る)の `initial` に
**さがす画面にある指定を全部渡す。** 一部だけ渡して2度同じ失敗をした。

1度目は弱点タグ、2度目は種類(ダイアローグを選んだのに文型に戻る)(2026-08)。
どれが残ってどれが消えるのかは利用者には見分けられず、気まぐれに見える。

**絞り込みの項目を足したら、`initial` にも必ず足すこと。**


### すでにある教材に、**足りない演習だけ**を足す(第5.234節)

2026-09-22 利用者の指定。

> この教材を改めて新しい Generate material で作成しなおせませんか

演習を新しく足しても、**すでにある教材には付かない。**
作り直すと**本文が別の文章になる**ので、それは作り直しではない。
カードに「足りない演習を作る」を置いて、**いまの本文から**足す。

- **本文には、絶対に触れない。** 英文が1文字も変わらないので、
  **読み上げ音声も作り直しにならない**(鍵は英文の指紋)
- **本文が無ければ、何も足せない**(設問は本文を渡して作る)。
  文型ドリル・単語帳にはボタンごと出ない。**既定は「できない」側**
- **判断は `materialFill.js` 1か所**(`canFillMaterial()` /
  `fillableSections()`)。カードのボタン・欄・窓口の3か所で数え直さない
- **末尾に足す。** `material_sections` は `unique (material_id, seq)` で、
  並べ直すには在る行の番号もずらすことになる。
  **足りないのは、どの種類でもいちばん後ろの演習である**
- **途中で失敗しても巻き戻さない。** もう一度押せば残りだけが作られる
  (在る種類は `fillableSections()` が外す)。行き止まりを作らない
- **押す前に、問数と金額を出す**(見えない費用は管理できない)。
  **足した英文には、あとで読み上げ音声も作られる**(`audioFrom`)ので、
  そちらも押す前に断る。**本文の作り直しとは別の話**である ——
  書かないと「音声はかからない」と読める

#### 演習を新しく足したら、**既定の構成に入れる**だけでよい

足せるものは `sectionsFor(kind)` から引いている。
**`materialFill.js` にも画面にも、演習の名前を書き足さなくてよい。**
「一覧をフォルダから読む」(第5.233節の配り)と同じ考え方である。

### 教材を先にえらび、ゲストはあとで選ぶ(第5.238節)

2026-09-22 利用者の指定。

> 初めに教材の一覧から教材を選択(複数同時選択可)、
> そしてゲストを選ぶのはその次にしてください。

**順番が逆だった。** カードごとに「共有する」を押し、そのたびに
ゲストを選んでいたので、**同じ相手に3本出すなら同じ25人から3回選ぶ**
ことになる。

- **印は、カードの見出しの行のいちばん左**(`.material-pick`)。
  どのカードのことか、行を見れば分かる
- **ゲストのページの中では出さない**(`!forLearner`)——
  相手がもう決まっているので、まとめてえらぶ意味がない。
  **効かない操作を見せない**(CLAUDE.md)
- **「n 件をえらんでいます」の帯は、一覧の上**(`.pick-bar`)。
  末尾だと、教材が増えるほど下へ流れて見つからない
  (`.claude/rules/common.md`「押すものは、一覧の末尾に置かない」)
- **1件もえらんでいなければ、帯ごと出さない**

#### ゲストを選ぶ欄は1つ(`LearnerPick`)

3つの画面(教材 / ゲストのページ / アサインする)で**同じ部品**を使う。
絞り方は `learnerPick.js` が持っており、画面の中で
`display_name.includes(…)` と書かない。

- **既定は名前で探す。** 一覧は「一覧をひらく(n)」を押したときだけ
- **打ったときは、開いていなくても出す** ——
  打ったのに何も出ないと行き止まりになる
- **選んだ人は、一覧を畳んでも見えている**(`.learner-pick-now`)
- **1人だけ選ぶ欄は丸ぽち、何人でも選べる欄は四角**(形そのもので分かる)
- **`<details>` を使わない**(畳んでも中身が場所を取り続ける)

#### まとめて共有したときの数え方と文は、1か所

まとめて共有する場所が**2つ**になった(教材の画面 / 「アサインする」)。

- 数えるのは `assignMaterials()`(`materials.js`)——
  **1本ずつ順に待ち**、断られたらそこで止めて**通ったぶんを返す**
- 言うのは `assignMany.js`(`manyDoneText` / `manyStoppedText`)
- **画面は `data.text` をそのまま出すだけ。**
  ここで「n 件を n 人と共有しました」と書き写さない

**赤チェックで1本、最初は緑のままだった。**
ファイル全体で「人と共有しました」を探していたので、
**1つの教材を出す道(`doAssign`)の1行に当たっていた。**
`doAssignMany` の中だけを切り出す形に直した
(CLAUDE.md「置き換える前に `grep -n` で数える」)。
