# 型シフト — 同じ内容を、別の型で言い直す

2026-09 利用者の指定。

> 画期的なトレーニングを作りたいです

そのうえで、どこから作るかを選んでもらった ——
**「型シフトの練習画面から」**、お題は
**「仕事の場面から新しく書く」+「66 型の見本を種にする」**。

---

### 何が新しいのか。**測ってから書いた**

`frameMatch.js`(型を決まりで見分ける道具)を、**採点**に使ったのが新しい。

| 入れた文 | 見分けた型 |
|---|---|
| The price worries me. | — (型なし・素の文) |
| It's the price that worries me. | ○ `It is X that / who ~` |
| What worries me is the price. | ○ `What ~ is …` |
| When it comes to the price, we need to talk. | ○ `When it comes to ~` |

**同じ内容を別の型で言い直した文を、機械が判定できる。**
66 型の見本は **66 / 66** 言い当てる。**AI を呼ばないので 0円。**

- **パタプラ・スピフル**は「**語**を入れ替える」。型を入れ替える練習は無い
- **瞬間英作文**は「和文 → 英文」の**1対1**。同じ内容を3通りで言う練習は無い
- そして何より、**言い換えが正しいかを機械で採点できない**。
  採点できないから、その練習が作れない

### **✕ を付けない。** ここがいちばん大事

`frameMatch` は**迷ったら黙る**(取り違えるくらいなら、見落とすほう)。
だから「見分けられなかった」は「**間違っている**」ではない。

実際 `It is not cost but timing.` は正しい英語だが見分けられない
(`but` のうしろが冠詞の無い裸の名詞のため)。
これを ✕ と出したら、**正しく言えた人に嘘をつくことになる。**

返す答えは4つ。**`unsure` は ✕ ではない。**

| 返り | いつ | 見せ方 |
|---|---|---|
| `ok` | 狙った型で言えている | ○ |
| `other` | **別の型**になっている | 型の名前を出して知らせる(✕ではない) |
| `unsure` | 型として見分けられない | 「たしかめられませんでした」+ お手本 |
| `empty` | 何も言っていない | 何も出さない |

`npm run test:shift` が「**4つの道が全部出るか**」を見る。
片方しか見ないと、**どこにも ○ を出さない形・全部 ○ にする形**に
書き換えても緑のままになる(CLAUDE.md「出ると出ないの両方を見る」)。

### **素の文を見せる。** 和文英訳に戻さないため

日本語だけを出すと「和文英訳」になり、**訳せたかどうか**の練習に戻る。
鍛えたいのは**言い換える力**なので、**言える形(素の文)を先に見せて、
そこから型へ移してもらう。**

`npm run test:shift` は「**素の文が、どの型にも当たらない**」ことも見る。
素の文がすでにその型なら、言い直す練習が成り立たないからである。
(37 本すべてが 66 型のどれでもないことを確かめてある)

### **66 型を、最初の版から1つも落とさない**

37 お題 / **66 問**(答え)で、**66 型すべてに少なくとも1問ある。**
あとから問を増やすときに「型の抜け」から直すのは大変だからである
(**一覧を勝手に減らさない**・`.claude/rules/common.md`)。

`test:shift` は**数を書き写さない。**「型の数 = 覆った型の数」という
**関係**で見る —— お題を1つ足した日に期待値も一緒に動いてしまわないように。

### **新しい仕組みを1つも作っていない**

| 何 | どの道を使うか |
|---|---|
| お題 66 問 | `src/data/frameShift.js`(**ファイル・0円**) |
| 採点 | `src/lib/frameMatch.js`(既にあるもの) |
| 判定と出題 | `src/lib/frameShift.js`(**素の node で走る**) |
| 画面 | `src/components/FrameShift.jsx` |

**表も列も RPC も1つも増えていない。** Native Flow(`nativeFlowQr.js`)・
コロケーション(`collocations.js`)とまったく同じ作法である。

### 覚え具合を `qr_reviews` に入れなかった

Native Flow は `qr_reviews`(0040)に溜めている。あちらは
**Quick Response の問そのもの**だからである。
型シフトの答えを同じところへ入れると、**Quick Response 帳に 66 文が混ざる。**
利用者の指定「最終的には単語帳も Quick Response 帳もすっきり見やすく
まとめたい」(2026-09)と正面からぶつかる。

**だからこの段では端末(localStorage)に持つ。**
置き場所を Supabase に移すかどうかは、次の段(型の地図)で確かめる ——
**データの置き場を変えるのは方針の転換**であり、勝手にやらない。

### 絞り込みは畳んでおく(**390px で実測**)

型の組が 14 あるので、開いたままだと**問が画面の外まで押し出される**
(狭い画面で 600px ぶん)。実際に Playwright で描いて気づいた。

畳んだ代わりに、**掛かっている絞り込みは畳んだままでも見せる** ——
**黙って絞らない**(CLAUDE.md)。
「絞っているときだけ札を出す」ので、`test:shift` は
**出る形と出ない形の両方**を見る。

### **畳んだ箱が、本当に畳まれていなかった**(実測して初めて分かった)

`<details>` を `display: grid` にしていたため、
**畳んでいても中身が場所を取り続けていた。**
見た目は畳まれているのに、札が**下の問と重なったまま**である。
`npm run test:bar` のすき間の見張りが

    shift@390px  縦 0.1px  p.fshift-baseen / button.btn.btn--small.btn--ghost

と出して捕まえた。**画面を見ても分からない。**
直し方は「`<details>` そのものに `display` を書かず、
中の入れ物(`.fshift-filterbody`)に持たせる」。
決まりそのものは `.claude/rules/common.md` に書いた
(**どのアプリでも起きる**ため)。

### この段では、音を鳴らしていない

お手本の読み上げは `speak` の窓口を呼ぶので**課金される。**
**見えない費用は管理できない**(CLAUDE.md)ので、
足すかどうかは利用者に確かめてからにする。

### **66 の型を、カテゴリー別のトレーニングにした**(2026-09 利用者の指定)

> これら 66 の型をカテゴリー別に分け、
> それぞれを活用するためのトレーニングを作りたい

**区分けは、ここで作らない。** `sentenceFrames.js` の
**節(3つ)と組(14)**がすでに持っている。別の分け方を作ると、
**分け方が2つ**になり、どちらが本物か分からなくなる。
`src/data/frameTraining.js` が持つのは
**「その組で、何をするか」だけ**である。

#### 14 の組を、8つの「やること」に束ねた

| やること | 組 | 指示 |
|---|---|---|
| モノを主語にする | ①の7組 | モノ・ことを主語にして、言い直す |
| 主語の席に、かたまりを入れる | ② | 主語をひとつのかたまりにして、言い直す |
| 焦点を当てる | ③ 焦点 | 際立たせたいところを前に出して、言い直す |
| 話題を先に置く | ③ 話題 | 何の話かを先に置いて、言い直す |
| 文と文をつなぐ | ③ つなぐ | 2つを1つにつないで、言い直す |
| 1文を後ろへ伸ばす | ③ 伸ばす | うしろに説明を足して、1文に収める |
| やわらげる・提案する | ③ やわらげ | 角が立たない言い方に変える |
| 比べる | ③ 比べる | A と B を比べる形で、言い直す |

- **①の7組は、やることが同じ**(人ではなくモノを主語にする)。
  違うのは動詞の意味あいだけなので、練習の形は1つにした
- **③の6組は、やることがそれぞれ違う。** 焦点は語順を変える練習、
  つなぐは2文を1文にする練習、やわらげるは角を落とす練習で、
  **同じ画面で出しても頭の使い方が別**である

#### 入口を「一覧」にした

はじめは 66 問を1本の列にして「型の組」で絞る形にしていた。
**それだと、何の練習をしているのかが最後まで出てこない。**
いまは**一覧(14 枚のカード)から入る。**
カードには「やること」「その組の型の名前」「問の数」「言えた数」が出る。

- **同じ指示を7回くり返さない。** ①の7枚は指示が同じなので、
  **変わったときだけ**出す。代わりに**その組の型の名前**を出す ——
  組ごとに必ず違うので、これが見分けの手がかりになる
  (**同じことをするものを2つ見せない**・CLAUDE.md)

#### **`what 節` の問が、ちがう組に入り込んでいた**(カードの数で気づいた)

一覧を描いたら、**② が 5 問・③焦点が 6 問**になっていた(本来 6 と 5)。

原因は1つ。組を**読み替え先**(`shiftTargetOf`)から引いていた。
`what 節`(②)は `SAME_SHAPE` で `What ~ is …`(③)として見分けるので、
**③焦点の組に入ってしまう。**

**読み替えは採点のためだけのもの**で、
**どのカテゴリーの練習かとは別**である。組は**書いたほうの型**から引く。

見張りも足した ——
**組ごとの問の数が、その組の型の数と合うか。**
これが、この食い違いを機械で見つける唯一の形である
(問の総数だけ見ていても、どちらの組に入っていても 66 のままで気づけない)。

### **書き込む欄は置かない**(2026-09 実機・利用者の指定)

> 書き込む欄はいらないですね。基本的にタイプするのは面倒なので
> 型シフトトレーニングについては書き込みはなしを共通仕様にしてください

これは**話す練習**である。打たせると、**打つ速さの練習**になってしまう。
**共通仕様**なので、14 のカテゴリーでも名詞句の入れ替えでも同じ。

- 答えるのは**マイクだけ**。「見てもらう」ボタンも要らなくなった
  (話し終わった時点で判定する)
- **聞き取った文は必ず見せる。** 何と聞こえたか分からないと、
  判定に納得できない
- `npm run test:shift` が、この画面に `input` / `textarea` が
  **1つでもあれば赤くする**(`onChange` も見る)

#### マイクが使えない端末を、行き止まりにしない

音声認識に対応していない端末では、**「言えた」を自分で押せる**ようにした。
**打たせないのは同じ**である。あわせて、なぜ機械で確かめられないのかも書く。

**箱ごと出さない。** はじめは「言い直した文」の枠を出したままにしていたが、
マイクが無い端末では一生うまらないうえ、
中の案内が「**マイクを押して**、声に出して言ってください」になっていた ——
**効かない操作を見せない**(CLAUDE.md)の破り方である。描いて気づいた。

### **段1「見分ける」**(2026-09 利用者の指定「どのように量をこなし」)

言い直す練習は1問 **20 秒**かかる。**量をこなす受け皿にならない。**
見分けるだけなら**1問3秒**で、移動中でも押せる。
そして「その型だと気づける」ことは、「その型で言える」ことの**手前**にある。

**素材を1文も書いていない。** すでにある文をそのまま使う ——
66 型の見本・型シフトの答え・名詞句を入れ替えた文。
**答えは機械が知っている**(`frameMatch.js`)ので、正解を書く必要もない。
**素材を足すと、この段の問も自動で増える。**

#### 「約 690 問」は、偏りを数えていなかった

はじめ「素材は約 690 文ある」と書いたが、**数えたら 660 文で、
しかも7つの型に 80 文ずつ偏っていた**(名詞句の入れ替えが、その7つの骨で
作られているため)。そのまま混ぜると**同じ型ばかり出る。**

だから**型ごとに最大3問**にそろえた。いまは **113 問 / 65 型**である。
`npm run test:shift` が「どの型も3問まで」を見張っている ——
上限を外すと赤くなる。

#### まぎらわしい選択肢は「同じ組」から

でたらめな3つを混ぜると、**組さえ分かれば当たる。**
だから同じ組(させる / させない / 要る…)から選ぶ。

**はじめ効いていなかった。** 同じ組・同じ節・全体を1本につないでから
**12 個まとめて混ぜて上から3つ**取っていたので、
同じ組に十分あってもよそから出ていた ——
**113 問中 16 問しか同じ組になっていなかった**(測って気づいた)。
**同じ組を使い切ってから、次の受け皿へ移る**形に直した。

#### 並びは、問の id から決める

毎回 `Math.random()` で混ぜると、**描き直すたびに選択肢が動く**
(押そうとした先が変わる)。問の id から種を作るので、
**同じ問なら必ず同じ並び**になる。

#### **2つの見張りが、互いを吸っていた**

「素材を入れる前に機械で確かめる」と「見分けられない型(`what 節`)を外す」は、
**どちらか片方を外しても、もう片方が拾ってしまう。**
いまの素材はどれも型に当たるので、数え上げの見張りも 0 のまま緑だった。

**赤チェックで赤くならないのは、壊し方が違うという知らせ**(CLAUDE.md)。
**書いてある形そのもの**を数える見張りを足して、
片方を消したら赤くなることを確かめた。

### **型の組を軸に、3段を一本の道にした**(2026-09 利用者の問い)

> どうするのが学習者が一番使いやすく、仕組みを理解しやすいでしょうか

**数えたら、はっきりした。** 足りないのは材料ではなく**道順**だった。

| | 直す前 |
|---|---|
| 最初に見るカード | **16枚** |
| 「させる」を鍛えたい人が回る場所 | **3か所**(①1組で9問 → ④で86問 → ⑤で27問) |
| その3つが同じ型の練習だと分かるか | **画面のどこにも書いていない** |
| やさしい順に並んでいるか | **逆**。いちばんやさしい「見分ける」が最後のカード |

材料はすでに十分あった(見分ける 195 / 入れ替える 1,640 / 言い直す 1,706)。

#### 直した形

```
型シフト ── 鍛えたい型の組をえらぶ(14枚 + まぜて見分ける)
       ↓
1. させる(背中を押す)
やること: モノ・ことを主語にして、言い直す

[ ① 見分ける 27 ] [ ② 入れ替える 86 ] [ ③ 言い直す 9 ]
   ↑ やさしい                      むずかしい ↑
（えらんだ段のドリルが、すぐ下に出る）
```

- **入口が 16 → 15 枚**に減った(「入れ替える」が各組の**段2**に吸収された)
- **同じ型の練習が1か所に集まった**
- **3段はいつも同じ場所・同じ順。** 1つの組でやれば、**残り13組も全部同じ**と分かる
- **開いたら、いちばんやさしい段から**始まる
- 一覧のカードにも**段ごとの問数**が出るので、開く前にどれだけこなせるか分かる

#### 段の一覧と順は `frameTraining.js` が持つ

画面に書き写さない —— 書き写すと、順を変えた日に片方だけ古くなる。
`npm run test:shift` が「段の名前を画面に書き写していないか」も見る。

#### 段1には絞り込みを出さない

組の中の「見分ける」には、絞るものが無い(その組だけを出している)。
**効かない操作を見せない**(CLAUDE.md)。

#### **また見張りが吸われた**

「組を開いたら、いちばんやさしい段から始まる」を
`setStage(FIRST_STAGE)` が在るかで見ていたが、
**`close()` にも同じ行がある**ので、`open()` から外しても緑のままだった。
`const open = (id) => { setPick(id); setStage(FIRST_STAGE)` と
**名指しで**見るように直した。**これで2度目である。**

### **使ってみて、2段に減らした**(2026-09 利用者の指定)

> やってみて決めました。①の見分けるは廃止です。③は残しましょう。
> そして⓶はシンプルな quick response にしましょう。日本語→英語です。

**実際に使った人の判断**なので、そのとおりにした。

| 段 | 前 | 後 |
|---|---|---|
| ① | 見分ける(4択・113問) | **廃止** |
| ② | 入れ替える(骨を見せて、空いた席だけ言う) | **Quick Response**(日本語 → 英語) |
| ③ | 言い直す(66問) | そのまま |

#### 見分けるは、道具ごと消した

`src/lib/frameQuiz.js` と `src/components/FrameQuiz.jsx` を削除し、
骨組み・すき間の見張り・検証からも外した。
「まぜて見分ける」のカードも消えたので、**一覧は 15 → 14 枚**になった。

**消したつもりで残っているのが、いちばん分かりにくい。**
`npm run test:shift` が「画面にも骨組みにも名残が無いか」を機械で見る。

#### Quick Response は、もとの英文を出さない

もとは**骨**(`___ allows you to work from home.`)を見せて、
空いた席だけを言わせていた。いまは**日本語のお題と型の名前だけ**である。

```
この道具のおかげで、家で働ける
この型で英語を言う
  S allows 人 to do
[ 話す ]  [ お手本を見る ]
```

**型は出す。** 出さないと `This tool lets you work from home.` のような
別の言い方でも正しくなってしまい、「ちがう型です」が理不尽になる。
**英文(骨)は出さない** —— 見せると写すだけの練習になる。

### **Quick Response の中の冊にした**(2026-09 利用者の指定)

> 型シフトはサイドバーからなくして、quick response 内に「66の型のQR」として
> その中に「日本語→英語」と「言い換え」をコンテンツとして追加します

**左メニューの行き先を増やさない。** 冊の札で切り替える ——
Native Flow・単語帳の冊とまったく同じ作法である。

```
Quick Response(復習)
[ 自分の Quick Response 帳 ] [ Native Flow ] [ 66 の型 ]
                                              ↓
  1. させる(背中を押す)
  [ ① 日本語 → 英語 86 ]  [ ② 言い換え 9 ]
```

段の名前も、利用者の言葉にそろえた(`Quick Response` → **日本語 → 英語**、
`言い直す` → **言い換え**)。

#### **札が、空の判定より後ろにあった**(描いて気づいた)

冊の札は `rows.length === 0`(まだ1問も溜まっていない)の**中**にあった。
つまり**溜まっていない人には札そのものが見えず、66 の型を開く道が無い。**
札も中身も、読み込みと空の判定より**前**へ出した。

#### **Supabase が無いと、手前で打ち切っていた**

`if (!isSupabaseConfigured) return …` で画面ごと終わっていた。
**66 の型はサーバーを1回も呼ばない**(ファイルに書いてある)ので、
会社のネットワークが塞がった日に**開く道が無くなる**。
札と 66 の型は、設定が無くても出す。

どちらも **行き止まりを作らない**(CLAUDE.md)の破り方だった。
`npm run test:shift` が「札と中身が、空の判定より前にあるか」を見張る。

#### 言い方も直した

- **「言い直した文」と書かない。** 日本語 → 英語の段では言い直していない。
  **どちらの段でも真になる**「言った文」にした
- **見出しで「66 の型」を繰り返さない。** 冊の札がすでに言っている

### 手応えの言葉は「**できた**」(2026-09 利用者の指定)

> 統一感が欲しいのです。

**「言えた」と書いていたのを、全部「できた」にした。**
単語帳・Quick Response の段の札と同じ言葉である
(理由は `docs/notes/18-単語帳とQuickResponseの復習.md`・仕様書 5.168)。

| どこ | もと | いま |
|---|---|---|
| カードの数 | `言えた 3` | `できた 3` |
| 問に付く印 | `言えた` | `できた` |
| 声が使えないときのボタン | `言えた` | `できた` |
| 下の数 | `言えた問: 3 / 12` | `できた: 3 / 12` |

**`npm run test:play` が、「できた」があり「言えた」が無いことを見ている** ——
**「出る」と「出ない」の両方**を見ないと、
どこにも出さない形に書き換えても緑のままになる(CLAUDE.md)。

### **専用の画面を廃止した。Quick Response の冊にそのまま移した**(2026-09 利用者の指定)

> マイクで「話す」の機能は入りません。削除です。全ての型で削除してください。
> そして、型のトレーニングの UI は廃止して、
> quick response の UI にそのままコンテンツを移してください

**実機で使って決まったこと**である。ここまでの経緯(上の節)は残すが、
**画面はもう無い。**

#### 何を消したか

| 消したもの | どこに在ったか |
|---|---|
| **マイク(「話す」)** | `FrameShift.jsx`。66 型ぜんぶで消えた |
| 採点(`judgeShift` / `shiftSay` / 5つの答え) | `lib/frameShift.js`。**マイクのためだけに在った** |
| 14 の組のカード・段の札・骨の札 | `FrameShift.jsx` / `frameTraining.js` |
| 端末の控え(`eas.frameShift.done`) | `lib/frameShift.js` |
| 見た目の指定(`.fshift-*` 64 行) | `styles.css` |

**`frameTraining.js` はファイルごと消えた。** あそこに在ったのは
「どの組で何をするか」「段の名前と順」で、**あの画面の持ちもの**だった。

**残したもの。** 問そのもの(`data/frameShift.js` の 37 お題 /
`phraseSwap.js` の 66 骨 / `swapParts.js` の肉)と、
**出す前に機械で確かめる安全弁**(`swapQuestions()` の中の `frameFormOf`)。
採点は消えたが、**確かめてから出すのは残っている** ——
あれは出す前の話で、採点ではない。

#### どう移したか —— **新しい仕組みを1つも作っていない**

`loadNativeFlowQr()`(Native Flow 690 問)と**1文字も違わない形**にした。

| 何 | どの道を使うか |
|---|---|
| 問 | `phraseSwap.js` / `frameShift.js`(**ファイル・0円**) |
| 覚え具合 | **`qr_reviews`(0040)** —— ふだんの Quick Response と同じ |
| 出題・絞り込み・聞き流し・紙・終わりの1枚 | `QrReview.jsx` そのまま |
| 1問ぶんの見た目 | `QrCard`(まだ / 言える・英語を見る・Listen・もう出さない) |

**行の欄が `nativeFlowRows()` と同じかを、検証が見ている。**
ずれた日に `QrReview.jsx` が書き分けを持つことになる
(**数え方を2通り持たない**・CLAUDE.md)。

**SQL は1行も要らない。** 表も列も RPC も増えていない。

#### 覚え具合の置き場を変えた(**前の決定を上書きしている**)

もとは「Quick Response 帳に 66 文が混ざる」ことを避けて、
端末(localStorage)に持っていた。**冊が別になったので、混ざらない** ——
Native Flow がすでにそうしているのと、まったく同じ形である。

#### コンテンツは2つのまま。**混ぜない**

| 中身 | 問数 |
|---|---|
| **日本語 → 英語** | 1,879 |
| **言い換え** | 66 |

**1つずつ開く**(`FrameParts`・`<select>` は Unit の欄と同じ形)。
混ぜると **1,879 対 66** なので、言い換えが埋もれて出てこない ——
**黙って絞るのと同じことになる。**
「ぜんぶ」は置かない。**もともと別々の練習**で、通しは1度も無かった。

#### 言い換えだけ、**型を問のほうに書いた**

お題1つに型が2つ以上ある(37 お題 → 66 問)。
**型を書かないと、同じ日本語が二度出て、どちらの答えか分からない。**
もとの画面が「この型で言い直す」と出していたものを、そのまま問に入れた。

    数字を見て、方針を変えることにした(「S makes 人 do(原形)」の型で)

**日本語 → 英語のほうには足していない** —— あちらは型が1つに決まっており、
`QrCard` が答えの下に型を出す(`showFrame`)。

検証は**「出る」と「出ない」の両方**を見る。さらに
**「型を書かなければ、本当に日本語が重なるのか」**(66 → 37)も数えている ——
重ならないなら、この見張りは何も守っていないことになる。

#### Supabase を引き連れない形に割った

`loadFrameQr()`(Supabase を呼ぶ)だけを `frameQrLoad.js` へ出し、
問と行を作るところは `frameQr.js` に置いた。
1つにまとめると `import.meta.env` が付いてきて、
**`npm run test:shift` が素の node で1行も走らせられない**
(CLAUDE.md「素の node で走らせられる形に切り出す」)。

#### 検証も、見るものを入れ替えた

採点を見るのをやめ、**問が Quick Response の行になっているか**を見る。
消したものは**「無いこと」で**見張る(画面のファイル・`frameTraining.js`・
`.fshift-` の CSS・マイク・打ち込む欄)。

**逆も見る** —— 書き取り・音読・文ごとの練習のマイクは、
**これまでどおり在る**(`lib/recognition.js` を使う3つの画面を数える)。
**言われた場所だけを直す**(CLAUDE.md)の守り方である。

**赤チェックを5本、1つずつ確かめた。** 行の欄を1つ落とす / 言い換えに型を
書かない / 重なりを落とすのをやめる / 知らない中身で先頭に落とす /
画面にマイクを戻す —— 全部それぞれ赤くなる。

### **型で絞る**と、**ヒント**を足した(2026-09 利用者の指定)

> quick response 内で型のトレーニングをする際に、絞り込めるようにして欲しい。
> また、ヒントボタンをつけて、一度ボタンを押したら問題を跨いでも、
> もう一度ヒントボタンを押すまでヒントが出続けるようにして欲しい。
> ヒントは、「-の形」というのがヒントにしてください。

#### ① 型で絞る —— **中身の欄のとなり**に置いた

「出しかた」の中には入れていない。あちらは**読み込んだ問の中から選ぶ**
絞り込みで、こちらは**出す問そのものが変わる**(Native Flow の Unit と同じ)。
同じ場所に並べると、押したときに起きることが2通りになる。

- **66 本を組ごとにまとめる**(`<optgroup>`)。1列に並べるとどこに何があるか
  分からない。組の名前も並びも `sentenceFrames.js` から引く ——
  **書き写さない**
- **「ぜんぶ」を先に置く。** これまでの通し練習を取り上げない
- **1問も無い型は出さない**(行き止まりを作らない)
- **知らない型では0問。** 黙って「ぜんぶ」に落とさない ——
  落とすと**選んでいないものが出る**、いちばん分かりにくい形になる
- 覚える(`eas.frameQr.form`)。中身を切り替えて向こうに無ければ「ぜんぶ」へ落ちる

#### ② ヒント —— **答えの下に出るのと同じ、青い型の札**。押したら出続ける

**はじめは灰色の別の札**(`ヒント 「◯◯」の形`)にしていたが、
利用者が実機を見て**指定し直した**(2026-09)。

> 青で囲まれた「〜の型」をヒントに

答えを開いたあとに出るあの札(`型 Given (that) ~`)を、
**そのままヒントとしても使う。** 見た目も文言も部品も1つ
(`FrameTag`)にまとめた —— **同じことを2つの見た目で見せない**(CLAUDE.md)。

- **押した状態は `QrReview` が持つ。** `QrCard` は問ごとに描き直されるので、
  あちらに持つと**1問で消える**(「問題を跨いでも」が成り立たない)。
  `RepeatToggle` と同じ作法である
- **端末に覚える**(`eas.qrHint`)。「もう一度押すまで出続ける」ので、
  開き直しても消さない
- 出すのは**型の名前そのもの**(`frameHintOf()`)。文言(`型 ◯◯`)も
  見た目も `QrCard` の `FrameTag` 1つが持つ。**画面に書き写さない**
- **ヒントを持たない問には、ボタンごと出さない** ——
  自分の Quick Response 帳と Native Flow の行は `hint` が `null` である
  (効かない操作を見せない・CLAUDE.md)
- **見分け直さない。** 問がすでに型を持っているので、それをそのまま渡す
  (**もらえる正解を捨てない**・CLAUDE.md)
- 答えの下に出る型と**同じ札**(`FrameTag`)。検証が
  「部品が1つか」「2か所で使っているか」「別の見た目を持っていないか」を見る

#### **「ヒントの表示が崩れている」は、再現できなかった**(2026-09 実機)

> ヒント内の「ヒント」の表示が崩れているのと、
> 囲みの色がグレーなのがイマイチです。元の青っぽい囲みにしてもらえませんか

**灰色の札(`ヒント 「◯◯」の形`)の話**である。青い札に変えた版が
まだ端末に届いていなかった(公開は 17:37、指摘は 17:07 の画面)。
**いまの版には「ヒント」という文字は札の中に無く、囲みも青い。**

**崩れのほうは、こちらで再現できていない。**

- いちばん長い型(30 字)を入れて **320 / 360 / 390px** で測ったが、
  箱も中身も1行のまま・はみ出しも無し
- 前の版(灰色の札)に戻して測っても、390px で 291×28 の1行

**分かっていないことを、分かったように書かない**(CLAUDE.md)。
`.qr-frame-name` に `flex: 0 0 auto`(flex の子は既定で縮む)を
**用心として**足したが、**直した跡ではない**とファイルに書いてある。

##### 赤くならない見張りを、いったん足してしまった

「札が潰れていないか」を `npm run test:bar` に足したが、
**壊しても赤くならなかった**(中の英文が自分で折り返すので、
札は縮まない)。**外した。**

- `test-bar.mjs` の `ok()` は**文字を出すだけ**である
  (`ok(条件, 名前)` ではない)。`ok(条件, …)` と書いたので
  **`✓ true` と出て、失敗しようがなかった**
- 「その1本を外したら赤くなるか」を**実際にやって**、
  初めてこれに気づいた(CLAUDE.md)

**骨組みは足した。** `?screen=qrrev` の `QrCard` に
`hintOn` / `onHint` を本物と同じ渡し方で渡してある ——
これで**すき間の見張りが、ヒントの札も測るようになった。**

#### ③ ついでに直した —— **型を問の日本語に混ぜていた**

前の回、言い換えの問を
`…と伝える(「S makes 人 do(原形)」の型で)` と**日本語の中に**
書いていた。**これを外した。** 理由は2つある。

| どこ | 何が起きるか |
|---|---|
| **聞き流し** | `radioJaOf()` は `ja` を読む。**日本語の声が `S makes 人 do` を読み上げる** |
| **紙 / PDF** | `qrSheetPairs()` も `ja` を使うので、**そのまま刷られる** |

型は `hint` として別に持ち、画面が出す。
**同じ日本語が2度出る**という元の問題は、**ヒントと型の絞り込み**で
解ける(どちらも今回の指定で足したもの)。

検証は「問の日本語に型を混ぜていない」と
「言い換えは、同じ日本語が2度以上出る(だからヒントと絞り込みが要る)」の
**両方**を見る —— 片方だけだと、混ぜ直しても緑になる。

#### 行の欄が1つ増えた(`hint`)

「`nativeFlowRows()` と1文字も違わない」という見張りは、
**「欠かしていない」+「足したのは `hint` だけ」**の2本に分けた。
**黙って増やすと、「同じ形」と言いながら中身が離れていく。**

#### やりかけを捨てる場所が4つになった

冊・Unit・中身・**型**。4か所に書き写すと、1つ足し忘れた日に
**前の冊の問が次の冊で出続ける。** `dropRun()` 1つにまとめ、
検証が「4か所とも通っているか」「書き写していないか」を見る。

#### 赤チェック(6本、1つずつ確かめた)

| 壊した中身 | 赤くなった見張り |
|---|---|
| ヒントの文言を「の形」でなくす | ヒントの文は「◯◯」の形 / どの行にもヒントがある |
| 型が分からなくてもヒントを作る | 型が分からない行には、ヒントを作らない |
| 知らない型を「ぜんぶ」に落とす | 絞ると、その型の数だけ出る ほか7本 |
| 型を問の日本語に戻す | 問の日本語に型を混ぜていない ほか2本 |
| 押した状態をカードに持たせる | カードは、押した状態を自分で持たない |
| やりかけの捨て方を書き写す | 4つとも捨てる / 捨て方は1か所だけ |
| ヒント専用の見た目を別に作り直す | 2か所で使っている / 別に持っていない |
| 札を部品にせず2か所に書き写す | 2か所で使っている / 見た目の指定も1か所だけ |
| ヒントに型以外の文を返す | ヒントに出すのは、型の名前そのもの ほか2本 |

### **型ごとの問数を、大幅に増やした**(2026-09 利用者の指定)

> 日本語→英語 / 言い換え ともに型毎の問題数を大幅に増やしてください

| 中身 | 前 | 後 | 型ごとの中央値 | 型ごとの最小 |
|---|---|---|---|---|
| **日本語 → 英語** | 1,879 | **2,872** | 10 → **26** | 8 → **16** |
| **言い換え** | **66** | **2,585** | **1 → 43** | **1 → 9** |

**言い換えは、型1つにつき1問しか無かった。** そこがいちばん痩せていた。

#### ① 日本語 → 英語 —— **肉を増やした。骨は1本も足していない**

部品を1つ足すと、**その席を持つ骨すべてに効く**(掛け算)。

| 部品 | 前 | 後 |
|---|---|---|
| 主語(良い / 妨げる / 知らせる / 仕事) | 10 / 10 / 8 / 8 | **26 / 26 / 24 / 24** |
| 動詞のかたまり | 24 | **52** |
| 短い文 | 16 | **40** |
| 骨だけの肉(やり方・書きもの・人・比較級・同格・名詞化) | 8〜11 | **16〜22** |

**主語は単数のまま**(`Rising costs requires …` を出さないため)。
**~ing は手で書く**(`cut → cutting` を機械で作らない)。

#### ② 言い換え —— **「束」を作った**(`src/data/frameSay.js`)

言い換えは「**同じ内容を、別の型で言う**」練習である。
だから**内容を1つ書けば、そこに乗る型の数だけ問ができる。**

    内容:「この道具のおかげで、家で働ける」
      S allows 人 to do         … This tool allows you to work from home.
      S enables 人 to do        … This tool enables you to work from home.
      S helps 人 (to) do        … This tool helps you work from home.
      S makes it possible to do … This tool makes it possible to work from home.

そこに**肉(`swapParts.js` の動詞 52・短い文 40 など)を掛ける。**
束1つ = **型の数 × 肉の数**。束は 32 本、うち **24 本が型を2つ以上**持つ。

- **肉は借りる。** 日本語 → 英語とまったく同じ一覧を使うので、
  **部品を1つ足すと、両方の練習が同時に増える**
- **手で書いた 37 のお題は1つも消していない。** あちらは**場面のある
  本物の言い回し**で、束は**数**である。役目が違うので2つとも要る。
  検証が「手で書いたお題の日本語が、1つも落ちていない」を見張る
- **相方の無い型**(`All 人 have to do is do` など)は、**型1つの束**を置いた。
  言い換えとしては痩せているが、**1問のままよりずっと良い**
- **機械で確かめてから出す。** 組み立てた英文が狙いの型に見えないものは
  **はじめから出さない** —— 2,590 通りのうち落ちたのは 57 通りだけ

#### 踏んだところ(3つ)

**`It is not cost but timing.` が出せない。** 正しい英語だが、
`frameMatch` は**`but` のうしろが冠詞の無い裸の名詞だと見分けられない**と
宣言している(もとから書いてあった)。見分けられない文を出すと
**正しく言えた人に「ちがう型です」と言う**ことになるので、
`It is not the cost but the ___.` に直した。

**`___ is important.` が小文字で始まっていた。**
`working from home is important.` である。**型は見分けられてしまうので
検証では捕まらない** —— 実物を読んで初めて分かった。
**肉が文の頭に来る骨では、1文字目を大文字にする。**

**札の数と、出てくる問の数が食い違った。** 数え上げ(`frameQrCounts` /
`frameQrForms`)と行(`frameQrRows`)が**別々に重なりを落としていた**ので、
札には 52 問と出て 51 問しか出てこない状態になった。
**重なりを落とすのは `frameQuestions()` 1か所**にして、
3つともそこを通るようにした(**数え方を2通り持たない**・CLAUDE.md)。
検証が「札の数を足すと、出てくる行の数と合う」を見張る。

#### 開くのが遅くならないように、控えを持つ

5,400 問を `frameFormOf()` に通すので 1 回 80ms ほどかかる。画面は
数え上げ・型の一覧・行の**3か所**から呼ぶので、控えが無いと開くたびに3回走る。
中身はファイルから作るだけで**いつ呼んでも同じ**なので、控えて安全である
(実測: 1回目 89ms → 2回目 1ms)。

#### 赤チェック(5本、1つずつ確かめた)

| 壊した中身 | 赤くなった見張り |
|---|---|
| 安全弁(確かめてから出す)を外す | 束から出した問が、どれも狙った型に当たる |
| 大文字にするのをやめる | 文の形の問は、大文字で始まる |
| 手で書いたお題を落とす | 手で書いたお題の日本語が、1つも落ちていない |
| 数え上げと行を別々に数える | 同じ英文が二度出ない ほか2本 |
| 肉(主語)を 10 に戻す | どの性格の主語も 20 以上ある / swap はどの型にも 16 問以上 |

### 次の段(まだ作っていない)

| 段 | 何をする |
|---|---|
| 1. 型シフト | 素の文 → 指定の型で言い直す ← **いまここ** |
| 2. 名詞句の差し替え | 型は固定のまま、中の名詞句だけ入れ替える |
| 3. 型の地図 | 66 マスで「どの型が言えるか」を見る(`shiftMap()` が土台) |

330 問へ増やすのも、この先である。

### 人に見せる数は 14。「〜系ぜんぶ」で練習できる(第5.177節)

2026-09 利用者の指定。

> 66の型ですが、実際はもっと少ないはずです。
> 写真のように「させる系」で一つと数えた時の数に変えてください。
> そして、選択肢に「〜系全て」を追加してください。

**数え方は2つ。数えているものが違う。**

| 呼び口 | 数 | 何を数えているか |
|---|---|---|
| `frameCount()` | 66 | **巻末に刷る行**(単語帳・トレーナー資料) |
| `frameGroupCount()` | **14** | **系(組)** —— 人に見せる数 |

`S allows 人 to do` と `S enables 人 to do` は、覚える側から見れば
**同じ「させる」の言い方違い**である。どちらも `FRAME_SECTIONS` 1つから
数えているので、型を足せば両方が同時に動く。

- **冊の名前は `FRAME_BOOK_LABEL`(`frameQr.js`)1か所。**
  これまで `'66 の型'` を**3か所に書き写していた**(画面と骨組み2つ)
- **短い呼び名(`kei`)を、組に1つ持たせた。** 組の名前は
  `1. させる(背中を押す)` の形で、**機械で「させる」だけを削り出せない**
- **「ぜんぶ」の言い方をそろえた** —— 欄の頭がもともと「ぜんぶ」なので、
  系のほうも「**〜系ぜんぶ**」(同じ欄に「全て」と混ぜない)
- **絞り方の判断は `frameFormFilter()` 1か所。** 型ひとつも系ぜんぶも
  **同じ1つの値**で、`系:` の印が付いているかだけで分かれる。
  **知らない型・知らない系は0問**(黙って「ぜんぶ」に落とさない)
- **出せるかどうかも `frameFormOk()` に聞く** —— 中身を切り替えたときに
  「ぜんぶ」へ落とす判断を、画面と裏で二度書かない

#### 赤チェック(7本、1つずつ確かめた)

| 壊した中身 | 赤くなった見張り |
|---|---|
| `kei` を1つ外す | どの系にも、短い呼び名がある |
| 系から型を1本落とす | 系をつなぐと、型の一覧とぴったり同じ |
| 知らない系を黙って「ぜんぶ」に落とす | 知らない系では、1問も出さない |
| 系の中の1本だけを出す | その系の型が、ぜんぶ混ざって出る |
| 画面に冊の名前を書き写す | 冊の名前を、画面にも骨組みにも書き写していない |
| 「〜系ぜんぶ」の文言を消す | 欄に「〜系ぜんぶ」が出る |
| 冊の名前を古い数に戻す | 冊の名前に、系の数が入っている ほか1本 |

**`npm run test:bar` でも描いて測る** —— `?screen=shift` を実際に開き、
「〜系ぜんぶ」が **14 個**、**型ひとつの選択肢も残っている**、
**値が1つも重なっていない**ことを数える(**書いてあるかだけでは足りない**)。
