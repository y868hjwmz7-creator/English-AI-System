-- ============================================================================
-- ★ Supabase の SQL Editor に、これを丸ごと貼って Run してください ★
--
-- 【これは何か】
--   0035 の1つだけです。GitHub の
--   supabase/migrations/0035_qa_translation.sql と**まったく同じ**中身です。
--
-- 【前にお渡しした pending_2026-09-02c.sql は、先に貼ってください】
--   あちらは「演習の種類」の一覧に、ディスカッションと誤り訂正を足すものです。
--   まだ貼っていなければ、**先にあちらを貼ってから**こちらを貼ってください。
--   (順番を間違えても壊れませんが、あちらを貼るまで教材を発行できません)
--
-- 【なぜ要るか】(2026-09 のご指定)
--
--   > 内容理解の設問と解答の訳を見れるようにしてください。音も聞けるように。
--   > そして解答や設問も単語の意味を調べて単語帳に追加できるようにしてください。
--
--   内容の理解は**設問も解答も英語**です。日本語が1つも無いため、
--   設問の意味が取れないと**設問そのものが壁**になり、
--   本文を理解できていたのかどうかを確かめられませんでした。
--
-- 【何が起きるか】
--   ・`material_items` という表に、**文字の列が2つ増えるだけ**です
--     (`question_ja` = 設問の訳、`answer_ja` = 解答の訳)
--   ・**いま入っている行は、1つも書き換わりません。** どちらも空のままです
--   ・**表は増えません。** 新しい入れ物は作りません
--
-- 【どこまで影響するか】
--   ・教材・宿題・単語帳・ゲストの情報・取り組みの記録には触れません
--   ・見える範囲の決まり(RLS)も変えません
--   ・これを貼る前に作った教材には、訳が入っていません。
--     **その教材では訳が出ないだけ**で、これまでどおり使えます。
--     訳が要るときは、その教材を作り直してください
--
-- 【成功の目安】
--   `Success. No rows returned` と出れば成功です。
--
-- 【うまくいかないとき】
--   赤い字が出たら、その文章をそのまま貼って教えてください。
-- ============================================================================

-- 設問の訳。**内容の理解で使う。** ほかの演習では空のまま
alter table public.material_items
  add column if not exists question_ja text;

-- 解答の訳。**内容の理解で使う。** ほかの演習では空のまま
alter table public.material_items
  add column if not exists answer_ja text;

comment on column public.material_items.question_ja is
  '設問(question)の日本語訳。内容の理解で使う(0035)';
comment on column public.material_items.answer_ja is
  '解答(answer)の日本語訳。内容の理解で使う(0035)';
